# Задачи

- [x] 1. `_dsh_pool_retry_at_wait_secs` (`scripts/lib/dsh-ci.sh`) — разбор
      `retryAt` из тела `pool_unavailable`, три исхода (близко/далеко/не
      разобрано).
- [x] 2. `dsh_run_with_pool_then_chain` — цикл повтора пула с суммарным
      бюджетом ожидания `DSH_POOL_RETRY_AT_MAX_WAIT_SECS` (по умолчанию
      300с), честные сообщения на все три исхода, `pool_rl_used`
      накапливается по всем попыткам пула (не перезаписывается).
- [x] 3. Поведенческий тест на прод-форме (дословное тело инцидента
      #1288, run 34893177035) — `scripts/lib/test/dsh-anthropic-pool.guard.sh`,
      секции 15-18: близко (повтор), суммарный бюджет по нескольким
      повторам, далеко (откат без ожидания), retryAt отсутствует (откат
      старым путём).
- [x] 4. Проверено на совместимость с #1160/PR #1247
      (`DSH_CHAIN_TOTAL_BUDGET_SECS`) — ожидание retryAt происходит ДО
      входа в цепочку и не обходит её бюджет (см. proposal.md, «Почему не
      задета #1160»).
- [x] 5. Заведены отдельными задачами находки того же прогона вне рамок
      этого change: GLM 7200с как первый провайдер (#1160/PR #1247 —
      целевой случай, новая задача не заведена, см. proposal.md), HTTP_410
      «класс не распознан» (#1289).
