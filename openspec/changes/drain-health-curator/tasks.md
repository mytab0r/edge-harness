# Tasks: drain-health-curator (#869)

## A. Композитный сигнал drain_gate

- [ ] `scheduler.py::drain_gate(repo, now, pulls, merged_pulls, dispatch_allowed, wip_count, conflict_exhausted, contract_failed_count) -> (observations, actions, ok)`
      по образцу wip_gate/conveyor_gate; часы — из max(merged_at) в уже
      полученном all_merged_pulls, без нового сетевого вызова.
- [ ] Порог DRAIN_STALL_HOURS — константа рядом с WIP_GATE_STUCK_HOURS/
      WORKER_FAILURE_PAUSE_AFTER (одно место правды, см. AGENTS.md).
- [ ] Ветка «нечего сливать» — открытых PR-кандидатов к merge_queue нет —
      возвращает ok без тревоги (копия критерия приёмки #194).
- [ ] Текст эскалации перечисляет ВСЕ верные в этом прогоне причины
      (предохранитель / WIP≥лимита / конфликты с исчерпанным бюджетом /
      contract:failed) — тест на «две причины сразу» и «ни одной названной
      причины, хотя дренаж стоит» (честный вывод «причина не установлена»,
      не пусто).
- [ ] Идемпотентность маркера на WATCHDOG_ISSUE (open/close, тот же приём,
      что WIP_GATE_OPEN_MARKER/CLOSE_MARKER).
- [ ] Тест: снятие фикса (закомментировать перечисление причин) красит
      мутационный тест.
- [ ] `docs/agents/LABELS.md` — новые маркеры, если решат вводить метку.

## B. Автодиспетч pm

- [ ] Решить развилку §2.2 design.md (отдельный workflow против расширения
      dispatch_conflict_rework) — записать решение сюда до реализации.
- [ ] Условие запуска: drain_gate тревога дольше PM_DISPATCH_AFTER_HOURS ИЛИ
      wip_gate stuck-маркер держится дольше WIP_GATE_STUCK_HOURS.
- [ ] Идемпотентность — один pm-прогон на эпизод (маркер, тот же приём, что
      AI_REVIEW_EXHAUSTED_MARKER/CONFLICT_BUDGET_RESET_MARKER).
- [ ] Механический потолок закрытий (PM_MAX_CLOSURES_PER_RUN) считается по
      факту вызовов gh за прогон, не по декларации модели — красит шаг при
      превышении.
- [ ] pm не трогает: задачи/PR с исполнителем, `waiting:owner`, `blocked`
      без протухшей ссылки (та же граница, что уже в pm.md — здесь
      механически проверяется постфактум по логу прогона).
- [ ] Тест на мутацию: закрытие сверх лимита — тест красный.

## C. Карантин задачи-отравы

- [ ] Парсер номера задачи из лога worker.yml («Аренда взята: замок
      refs/locks/task-N») — тот же проход логов, что уже делает
      pulse_guard.last_error_log_line.
- [ ] `free_task.py` — фильтр карантина в free_candidates/oldest_free.
- [ ] QUARANTINE_AFTER, backoff (база/потолок) — константы рядом с
      PROBE_BACKOFF_BASE_MINUTES/PROBE_BACKOFF_MAX_MINUTES.
- [ ] Автоматическая проба и автоматическое снятие при успехе; продление
      выдержки при повторном фатальном падении.
- [ ] Верхний потолок продлений → эскалация (число — предмет отдельного
      подбора, см. design.md «Не подтверждено» п.3).
- [ ] Тест: убрать фильтр карантина — задача снова выбирается, тест краснеет.
- [ ] Закрывает критерий 3 issue #794 — сослаться в PR явно.

## D. Инвариант

- [ ] `repo_invariants.py::check_drain_stalled_without_signal` — независимый
      пересчёт (свой обход all_merged_pulls + свой обход комментариев
      WATCHDOG_ISSUE), не читает внутреннее состояние scheduler.py.
- [ ] Наблюдательный на старте (не в CI_GATING) — замерить на живом
      репозитории перед включением, тот же порядок, что у инвариантов 8/9/10.
- [ ] Мутационный тест: снять инвариант — фикстура с нарушением перестаёт
      ловиться.

## E. Документация и увязка с прежними задачами

- [ ] `openspec/specs/journal-tasks-hands.md` — влить дельту при архивации
      (раздел «Конвейер (оркестратор)», рядом с п.16/17).
- [ ] Прокомментировать issue #194 фактом реализации (не закрывать самому —
      закрывает тот, кто подтвердит критерий живым прогоном).
- [ ] `docs/agents/ROADMAP.md`/борда — эпик «Конвейер и оркестрация».
