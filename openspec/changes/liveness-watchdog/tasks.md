# Tasks: liveness-watchdog (#519)

## Backend (pulse_guard/scheduler)

- [x] `pulse_guard.decide_event_wake` — чистое решение применимости/сигнала
      (grace-период, окно устаревания, сравнение с anchor).
- [x] `pulse_guard.event_wake_check` — IO-обвязка: 2 дешёвых `recent_runs`
      всегда, +1 только при применимости; эскалация через `escalate()`,
      дедуп парой маркеров (`EVENT_WAKE_MARKER`/`EVENT_WAKE_RESUMED_MARKER`).
- [x] Вызов из `scheduler.main()` сразу после `heartbeat_check`.
- [x] Тесты + мутация (`test_pulse_guard.py`): применимость/грейс/устаревшее
      окно/мутация «старый тик не засчитывается»/эскалация/дедуп/закрытие
      эпизода/повторное закрытие не плодится.
- [x] Проводка в `test_scheduler.py` (два сквозных теста холостого хода
      получили маршруты `ai-review.yml`/`pr-review.yml` без прогонов; общий
      паттерн стаба `heartbeat_check` в full-main тестах дополнен стабом
      `event_wake_check`).

## Follow-up (НЕ в этом PR — отдельные задачи, см. issue #519)

- [ ] Деплой-на-мерж (#374): признак «PR слит, файлы подпадали под
      `cf-worker/`/`dsh-edge/`, а `deploy-*.yml` НЕ был продиспатчен» —
      текущий `dispatch_deploy_on_merge` либо диспатчит, либо роняет прогон
      исключением (`check=True`, ловит `failure_watch`); тихий логический
      промах без исключения (например неверное вычисление prefix) отдельным
      признаком пока не покрыт.
- [ ] Инбокс (#173): «issue с меткой источника при накопленных директивах» —
      нужен отдельный замер (сколько сообщений реально копится без реакции)
      перед тем, как проектировать порог и применимость.

## Документация

- [x] `openspec/changes/liveness-watchdog/proposal.md` — граница с
      failure_watch, таблица механизмов (в issue #519), образцовый случай.
- [x] Делегировано в issue #519 (не дублируется здесь): полная таблица
      механизмов «признак / период / когда неприменимо / статус».
