Задача: #809.

# Tasks: corrupted-session-cold-load-recovery

- [x] 1. `scripts/lib/dsh-edge-session.sh::dsh_edge_session_begin` — фоллбэк
      на один новый session_id (`<исходный>-r<GITHUB_RUN_ID>`) при отказе
      `session.create` ИЛИ `session.rename` (дополнено #871 — живой прогон
      34471287514/harness-140 показал, что провал бывает и на `session.
      rename`, изначальная формулировка предполагала только `session.
      create`), содержащем `failed validation` в тексте ошибки; любая
      другая причина — без фоллбэка, как раньше. stdout несёт РЕАЛЬНО
      использованный id.
- [x] 2. `scripts/worker/task.sh`, `scripts/hands/dsh_task.sh` — читают
      возврат `dsh_edge_session_begin`, не подставляют исходный `HARNESS_SID`
      вручную.
- [x] 3. Смоук-сценарий `scripts/lib/test/dsh-clients.smoke.sh`
      (`worker-corrupted-session`, `worker-corrupted-session-rename` — #871)
      — прод-форма ошибки дословно из живых прогонов `worker.yml`
      34455120330/34441499974/34471287514; проверяет вызовы `session.create`
      И `session.rename` и что задача доводится до DSH/отчёта в обоих
      случаях.
- [x] 4. Мутационная проверка изолированной логики фоллбэка (сеть vs порча,
      повторная порча фоллбэка, порча на create vs порча на rename — #871) —
      доказано локально прогоном/откатом фикса (репозиторий не даёт CI на
      Windows-машине разработчика, см. design.md).

## Закрывающая проверка (после слияния, живой прогон)

- [ ] Живой ручной прогон `worker.yml` (`workflow_dispatch`, `task: 716` или
      `140`) проходит шаг «Задача через DSH headless» ЗА точку создания
      сессии (видно продолжение — установка DSH/провайдер), предохранитель
      диспатча (#847/#205) снимается тем же прогоном или следующим циклом.
      #871 расширил фоллбэк на `session.rename` — до его слияния этот пункт
      не мог быть выполнен для `harness-140` (проваливался именно на
      rename); проверяй заново ПОСЛЕ #871, не до.
