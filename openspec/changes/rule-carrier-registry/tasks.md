# tasks.md: rule-carrier-registry

Разметка ниже — по замыслу: БОЛЬШИНСТВО пунктов создают свои новые файлы и
не пересекаются с соседним пунктом. Явные исключения: (а) пункты 14 и 15
правят «горячие» файлы `scripts/orchestra/repo_invariants.py` и
`.github/workflows/repo-ci.yml` — делать последними, непосредственно перед
мержем, с ребейзом на свежий `origin/main`; (б) пункты внутри одной цепочки
(например, 7 наполняет мутантами файлы, созданные 6) — последовательные по
смыслу, не параллельные, зависимость объявлена явно в каждом пункте (см.
proposal.md, acceptance criterion 5 — критерий переформулирован честно
после разбора первой версии: «файловая независимость ЛЮБЫХ двух пунктов»
опровергнута самим же tasks.md).

Каждый пункт этого файла — ОТДЕЛЬНАЯ задача пула со своей issue и своей
веткой `agent/<N>-<slug>` (см. п.0) — не одна ветка на весь список, иначе
теряется параллельность, ради которой пункты разведены по файлам.

**Оговорка про orphan-test-guard** (`scripts/lib/orphan_test_guard.py`,
гейтит `repo-ci.yml`): каждый пункт ниже, создающий `test_*.py`, добавляет
в НЕГО ЖЕ (не откладывая на п.15) маркер `# ORPHAN-TEST-OK: подключение в
repo-ci.yml — tasks.md этого change, п.14 (файл занят параллельными
агентами до этой точки)` — легальный газ гвардии с конкретной, проверяемой
причиной, не общая отговорка. Пункт 14 обязан СНЯТЬ каждый такой маркер,
заменив его настоящим шагом `run:` — часть приёмки п.14, не необязательная
уборка.

Зависимости (кратко, детали — в каждом пункте): 2, 3, 4, 4б требуют схему
из 1; 5 требует 1–4б; 6–9 требуют 1 (6 — ещё и 2, чтобы сразу доказывать
`proven`); 10 требует только 1; 11 требует 100% покрытия 40 правил (6–9
слиты) и слияния предшествующей задачи доставки (design.md, «Миграция»);
12 требует 11; 13 не раньше, чем `repo_invariants.py` освободится, по
смыслу полезна после 9; 14 требует 4+4б; 15 требует 2 и освобождение
`repo-ci.yml`/`pulse_guard.py`; 16 полезна после 14; 17 требует 14+15; 18
требует всё предыдущее подтверждённым живым циклом.

## 0. Завести задачу пула

- [ ] Issue с меткой `task` (`scripts/gh/issue-create`, сперва сверить
      дубли — `gh issue list --state open --search "rule carrier
      registry"`), тело — формулировка из proposal.md «Класс проблемы» +
      ссылка на `openspec/changes/rule-carrier-registry/` + номер пункта
      этого файла; аренда `python scripts/lib/claim_task.py claim <N>`
      (ровно один раз — не идемпотентно); ветка
      `scripts/git/task-branch <N>-rule-carrier-registry-<кусок>`.

## 1. Схема реестра, валидатор, два иллюстративных правила

- [ ] `docs/agents/rules/README.md` (формат шапки: id, statement, scope,
      section, `order`, carrier, carrier_status, proof_test, proof — БЕЗ
      `_sections.yml`, design.md отверг общий индекс в разделе B: порядок
      секций — маркеры в `AGENTS.md`, порядок внутри секции — поле
      `order`) и `scripts/lib/rule_registry.py` (загрузчик + резолвер
      `carrier`/`proof_test` по расширению файла — Python через `ast`,
      bash регэкспом на `function name()`, `.mjs`/`.ts` на `export`,
      workflow-шаг через `path.yml#step-id` с обязательным `id:`,
      design.md раздел C).
- [ ] `scripts/lib/test_rule_registry.py` — обязательные ключи включая
      `proof_test`/`order`; `scope` непусто ⊆ `{human, worker, reviewer,
      hands, system}`; `id` = имя файла; согласованность
      `carrier_status`/`carrier`/`proof`/`proof_test` по таблице design.md
      раздела A; `carrier` резолвится в реальный файл/символ;
      `proof_test` резолвится РОВНО в один pytest-узел; конфликт `order`
      внутри `section` — ошибка; `scope_reason` обязателен и непуст при
      собственном подмножестве `{human, worker, reviewer, hands}` (кроме
      `scope: [system]`, design.md раздел G).
      `# ORPHAN-TEST-OK: подключение в repo-ci.yml — tasks.md п.14`.
- [ ] Два иллюстративных файла на уже доказанных носителях —
      `docs/agents/rules/task-ref-single-resolver.md`
      (`scripts/lib/task_ref.py::resolve_pr_task`) и `docs/agents/rules/
      pagination-guard-required.md`
      (`scripts/lib/test_pagination_guard.py::<конкретный узел>`) — оба
      `carrier_status: pending` на этом шаге (`proof` — в п.2).
- [ ] **Доказательство и мутация:** `pytest scripts/lib/
      test_rule_registry.py -q` зелёный на этих двух файлах; снятие
      проверки обязательности `scope` красит тест на пустом `scope`
      (вернуть — зелёный); снятие проверки резолвимости `proof_test`
      красит тест на несуществующем узле (вернуть — зелёный).

## 2. Мутационный харнес + первые два мутанта

Зависит от 1 (использует `rule_registry.py`).

- [ ] `scripts/lib/mutation_harness.py` (design.md раздел C):
      `git worktree add --detach`; health-check `proof_test` на
      немутированном дереве (не `exit=0` → «носитель нездоров», не
      мутация); `git apply <mutant>.diff` (не применился → «мутант
      устарел»); `proof_test` на мутированном дереве с разбором кода
      выхода (`1` + то же число собранных тестов, что на health-check →
      доказано; `0` → мутант выжил; `2/3/4/5` → «харнес не смог
      проверить», отдельная категория); `git worktree remove`. Режимы
      `--rule <id>`, `--changed --base <ref>` (0 совпадений при непустом
      реестре = норма, 0 при пустом реестре = красный), `--all` (0 всегда
      красный, кроме первого коммита миграции до включения гейта),
      `--local` (design.md раздел C — работает прямо в рабочем дереве без
      `git worktree`, для проверки ДО коммита; `git apply -R` в `finally`
      гарантирует откат; CI этот режим не использует); деление
      «лёгкое/тяжёлое» по baseline-времени, порог 5 с (design.md раздел C)
      — `--changed` пропускает тяжёлые с явным сообщением.
- [ ] `scripts/lib/test_mutation_harness.py` — самотест на фикстуре
      (`scripts/lib/testdata/`, не боевое правило): здоровый носитель +
      валидный мутант → доказано; изначально красный `proof_test` →
      «нездоров», не «доказано»; неприменимый мутант → «устарел»;
      `exit=2/4/5` на мутации → «харнес не смог проверить», не proven;
      `--changed`/`--all` при `total_rules_loaded == 0` → красный «реестр
      не загрузился».
      `# ORPHAN-TEST-OK: подключение в repo-ci.yml — tasks.md п.14`.
- [ ] `docs/agents/rules/mutants/task-ref-single-resolver.diff` и
      `.../pagination-guard-required.diff` — реальные мутанты для правил
      из п.1; после этого их `carrier_status` → `proven`, `proof_test` —
      конкретный узел, `proof` — `mutation:docs/agents/rules/mutants/
      <id>.diff` (правка тех же двух файлов из п.1).
- [ ] **Доказательство и мутация:** `mutation_harness.py --rule
      task-ref-single-resolver` и `--rule pagination-guard-required` —
      код 0, текст «доказано»; порча мутанта (no-op) → код ≠0, «мутант
      выжил»; порча `proof_test` (переименование файла) → код ≠0, «харнес
      не смог проверить: pytest exit=4», не «доказано» и не «выжил».

## 3. Генератор проекций (AGENTS.md и три промпта)

Зависит от 1.

- [ ] `scripts/lib/gen_rules_docs.py` (design.md раздел B) — короткая
      (промпты) и полная (`AGENTS.md`) форма по `scope`, группировка по
      `section`, сортировка по `order` (не алфавит, не отдельный список
      id); режимы `--check`/`--write`; секция печатается, только если для
      её `section:` есть маркер в целевом файле, иначе явная находка. На
      этом шаге генератор ещё НЕ подключён к реальным файлам (design.md,
      «Миграция», п.8 — после 100% покрытия и предшествующей задачи
      доставки), работает и тестируется только на фикстурах.
- [ ] `scripts/lib/test_gen_rules_docs.py` (3–4 фикстурных правила):
      `scope: [worker]` попадает в worker-проекцию, не в reviewer;
      `section: incident-invariant` + `scope: [system]` не попадает ни в
      одну человекочитаемую проекцию; конфликт `order` в одной `section`
      — ошибка; ссылка на несуществующую `section` — явная находка.
      `# ORPHAN-TEST-OK: подключение в repo-ci.yml — tasks.md п.14`.
- [ ] **Доказательство и мутация:** тесты зелёные; снятие фильтра по
      `scope` красит тест «reviewer не видит worker-only» (вернуть —
      зелёный); снятие проверки конфликта `order` красит соответствующий
      тест (вернуть — зелёный).

## 4. Храповик долга по правилам без носителя и удалению

Зависит от 1.

- [ ] `scripts/lib/rule_carrier_ratchet.py` (design.md раздел D):
      `unproven_count = pending_count + infeasible_count` (ОБА статуса —
      иначе `pending → infeasible` бесплатно обнуляет долг) и
      `total_count` (файлов реестра) на `HEAD` и `base` (`git show
      <base>:<путь>`, `base` — свежий через `git fetch --depth=1 origin
      <base_ref>`, не `fetch-depth: 0` на весь чекаут); красный при
      `unproven_count(HEAD) > unproven_count(base)` ИЛИ
      `total_count(HEAD) < total_count(base)`, сообщение называет ИМЕНА.
- [ ] `scripts/lib/test_rule_carrier_ratchet.py` — новый `pending` → код
      ≠0 с именем; `pending → infeasible` без нового правила → код 0
      (сумма не изменилась); удалённое `proven`-правило → код ≠0 с
      именем, даже если `pending_count` не выросло.
      `# ORPHAN-TEST-OK: подключение в repo-ci.yml — tasks.md п.14`.
- [ ] **Доказательство и мутация:** тесты зелёные; `>` → `>=` красит тест
      на неизменном долге, `>` → всегда-`False` красит тест на растущем
      долге; удаление проверки `total_count` красит тест на удалении
      правила (вернуть каждую — зелёный).

## 4б. Храповик доставки по каналам (scope)

Зависит от 1. Design.md, раздел G.

- [ ] `scripts/lib/rule_delivery_ratchet.py` — `delivered_count(c)` для
      каждого `c ∈ {human, worker, reviewer, hands}` на `HEAD`/`base`;
      падение числа для любого канала — красный с именем правила.
      `scripts/lib/test_rule_delivery_ratchet.py` — правило, потерявшее
      `worker` из `scope`, красит тест независимо от наличия
      `scope_reason` (причина освобождает от блокировки схемой п.1, не от
      ratchet — design.md раздел G).
      `# ORPHAN-TEST-OK: подключение в repo-ci.yml — tasks.md п.14`.
- [ ] **Доказательство и мутация:** тесты зелёные; снятие сравнения для
      одного канала (например `hands`) красит тест на этом канале
      (вернуть — зелёный).

## 5. Самоприменимость: реестр для 4 механизмов реестра

Зависит от 1–4б. Закрывает находку proposal.md «самоприменимость
провалена» — приёмка собственных гвардий этого change не словесная
инструкция, а запись реестра + мутант, как у любого другого правила.

- [ ] По одной записи `docs/agents/rules/<id>.md` (`scope: [system]`,
      `section: incident-invariant`) + мутант на каждый механизм:
      `rule-registry-schema-validated` (carrier —
      `test_rule_registry.py::<тест обязательности scope>`, мутант снимает
      эту проверку), `mutation-harness-proves-carrier` (carrier —
      `mutation_harness.py::<разбор кода выхода>`, мутант возвращает
      «любой ненулевой» вместо строгого `exit=1`), `rules-generated-not-
      handwritten` (carrier — `gen_rules_docs.py::<сравнение --check>`,
      мутант выключает сравнение одного из четырёх файлов),
      `rule-carrier-debt-ratchet` (carrier —
      `rule_carrier_ratchet.py::<сравнение>`, мутант меняет `>` на `>=`).
- [ ] **Доказательство:** `mutation_harness.py --rule <id>` код 0 для
      каждой из четырёх записей.

## 6. Миграция: 7 уже доказанных правил (кроме двух из п.1)

Зависит от 1+2. Можно делить построчно между агентами — каждое правило
отдельный новый файл.

- [ ] Уточнить актуальный список 5 оставшихся правил из «уже сложилось»
      (proposal.md) — минимум `test_label_registry.py`,
      `test_infra_gh_inventory.py`, плюс до трёх по факту существования
      docstring-подтверждённого носителя с историей инцидента; по одному
      новому `docs/agents/rules/<id>.md` + `.../mutants/<id>.diff`,
      `carrier_status: proven`, `proof_test` — конкретный узел.
- [ ] **Доказательство на каждый файл:** `mutation_harness.py --rule <id>`
      код 0.

## 7. Миграция: 17 правил с носителем без доказательства

Зависит от 1. Дробится по правилам.

- [ ] По одному новому `docs/agents/rules/<id>.md`, `carrier_status:
      pending`, `proof_test: none`, `proof: "none: носитель есть
      (<file::symbol>), мутант не написан"` — честно, без придуманного
      `proven`; написание мутантов — отдельные задачи ПОСЛЕ этого пункта.
- [ ] **Доказательство:** `test_rule_registry.py` проходит на каждом
      новом файле.

## 8. Миграция: 16 правил без носителя

Зависит от 1. Дробится по правилам. Рабочий список — design.md,
«Приложение: 16 правил без носителя» (6 `pending` + 10 `infeasible`,
уточняется по факту при миграции).

- [ ] Каждое правило — новый файл, `carrier_status: pending`
      (механизируемо, не сделано) либо `infeasible` + непустая причина;
      долг = `pending + infeasible` (design.md раздел D). Явно:
      `secrets-not-committed` (`AGENTS.md:219-225`) и
      `relative-markdown-links` (`AGENTS.md:212-213`) — `pending` с
      пометкой «дешёвый следующий шаг: gitleaks/grep на `[[...]]`»; для
      будущего мутанта `secrets-not-committed` записать в теле
      ограничение: мутант обязан ослаблять код детекции, не подсовывать
      fixture-строку, похожую на секрет (design.md раздел C).
- [ ] **Доказательство:** `test_rule_registry.py` проходит;
      `rule_carrier_ratchet.py` (после того, как п.4 слит) не краснеет на
      этом PR относительно предыдущего состояния миграции — на момент
      включения ratchet в `CI_GATING` (п.14) весь накопленный
      `pending`/`infeasible` уже отражён в `main`, храповик защищает от
      роста ПОСЛЕ этой точки.

## 9. Миграция: 7 носителей-сирот (scope: system)

Не зависит от 6–8, можно параллельно.

- [ ] Семь новых файлов (`scope: [system]`, `section:
      incident-invariant`) для `exec_bit_guard`, `orphan_test_guard`,
      `epic_guard`, `do_hotpath_aggregate`, `dsh_edge_pnpm`,
      `branch_protection_watch`, `upstream_drift` — `carrier` на реальный
      `file::symbol` каждого (проверить актуальное имя перед записью);
      `scope_reason` не требуется (design.md раздел G, исключение для
      `system`).
- [ ] **Доказательство:** `test_rule_registry.py` проходит; `carrier`
      каждого резолвится (гвардия схемы делает это автоматически).

## 10. Обратное направление: канарейка дублирования прозы

Зависит только от 1. Design.md, раздел F — узкий носитель, НЕ общий
детектор.

- [ ] Поле `duplicate_signatures: [<regex>, ...]` (уже допустимо схемой
      п.1) и `scripts/lib/test_duplicate_prose_guard.py` — сканирует
      закрытый список файлов (`docs/agents/PROTOCOL.md`,
      `scripts/worker/task.sh`, `scripts/hands/dsh_task.sh`) на совпадение
      с `duplicate_signatures` любого правила; для `pr-only-via-pr-create`
      (когда перенесено, п.6/7) — заполнить сигнатуру фразой
      `Closes/Fixes/Resolves ЗАПРЕЩЕНЫ` из `task.sh:296` (живой пример,
      proposal.md п.5). В начало `PROTOCOL.md` — комментарий «новые
      нормативные требования сюда не добавляются, заводи запись в
      `docs/agents/rules/»` (занятый файл — правка после освобождения).
      `# ORPHAN-TEST-OK: подключение в repo-ci.yml — tasks.md п.14`.
- [ ] **Доказательство и мутация:** тест краснеет на живом `task.sh:296`
      ДО того, как строка заменена на ссылку на сгенерированную короткую
      форму, и зеленеет после (живой случай, не фикстура); удаление одного
      файла из сканируемого списка красит тест на известном нарушении
      (вернуть — зелёный).

## 11. Подключение генератора к AGENTS.md и WORKER-PLAYBOOK.md

Зависит от 100% покрытия 40 правил (6–9 слиты) И от слияния
предшествующей задачи доставки правил (design.md, «Миграция» — конкретная
ссылка на неё подставляется перед стартом этого пункта, по факту её
слияния).

- [ ] `AGENTS.md` — секции оборачиваются маркерами `<!-- RULES:BEGIN
      section=<id> --> ... <!-- RULES:END -->`, содержимое между ними
      генерируется (`gen_rules_docs.py --write`), разделы вне маркеров
      остаются хэндрайтенными; `WORKER-PLAYBOOK.md` получает маркеры
      `scope=worker`. **Без промежуточного «режима дополнения»**
      (design.md, «Миграция», п.8 — тихий ноль отвергнут теми же
      причинами, что в разделе C): до этого пункта `--check` для реальных
      файлов не запускается вовсе.
- [ ] **Доказательство и мутация:** `--check` зелёный сразу после
      `--write`; ручная правка текста внутри маркеров без перегенерации
      красит `--check` (вернуть — зелёный).

## 12. Подключение генератора к ai_prompt.md и hands-каналу

Зависит от 11 (тот же генератор; можно параллельно, если 11 уже открыт
PR-ом).

- [ ] `scripts/review/ai_prompt.md` получает маркеры `scope=reviewer`
      (правил там нет вовсе сегодня) — генератор экранирует каждый `$` в
      тексте правила как `$$` перед вставкой (design.md раздел B,
      «Экранирование `$`» — `ai_review.py` рендерит файл через
      `string.Template.safe_substitute`, буквальный `$` иначе исказится);
      новый `scripts/hands/RULES-PREAMBLE.md` (генерируется целиком, без
      этого экранирования — тот канал не проходит через `Template`);
      `scripts/hands/dsh_task.sh` — одна строка, подмешивающая его перед
      `TASK_TEXT` (по образцу `task.sh:326`).
- [ ] **Доказательство:** `--check` покрывает эти файлы; фикстурное
      правило с `$` в тексте (например, `$100`) рендерится в
      `ai_prompt.md` буквально, не как незаполненный/испорченный
      плейсхолдер; сухой прогон `dsh_task.sh` показывает непустой блок
      правил там, где было ноль байт (proposal.md, п.6).

## 13. Гвардия обратной связности в repo_invariants.py

НЕ раньше, чем `repo_invariants.py` освободится (проверить `git
log`/открытые PR перед стартом). По смыслу полезна после 9.

- [ ] Новая `check_*` (design.md раздел E): сканирует
      `scripts/**/test_*_guard.py` и `check_*` внутри самого файла,
      сверяет с `carrier` из `docs/agents/rules/*.md` (ловит и «носитель
      без правила», и «удаление правила при живом коде носителя»); не
      добавляется в `CI_GATING` этим PR — объявленное условие включения
      (design.md раздел E): 0 находок 3 дня подряд на `main` ПОСЛЕ того,
      как 7 сирот (п.9) размечены.
- [ ] **Доказательство и мутация:** `pytest
      scripts/orchestra/test_repo_invariants.py -q` зелёный; носитель без
      `carrier`-записи, вставленный в фикстуру, красит тест (убрать —
      зелёный).

## 14. CI-гейты в repo-ci.yml

Строго ПОСЛЕДНИМ (кроме 15/16/17/18) и только когда 4+4б слиты и локально
проверены.

- [ ] Три новых шага (design.md разделы B/C/D): генерация-сравнение;
      мутации `--changed` (с предваряющим `git fetch --no-tags --prune
      --depth=1 origin "<base_ref>"`, НЕ правкой `fetch-depth`
      глобального `checkout` — design.md раздел C, оценка цены на 33
      открытых PR); оба ratchet (долг + доставка). Снять КАЖДЫЙ маркер
      `ORPHAN-TEST-OK`, поставленный пп.1–4б и 10, заменив реальным
      `run: pytest <файл> -q` — часть приёмки, не отдельная уборка.
      Делать НЕПОСРЕДСТВЕННО перед мержем с ребейзом на свежий
      `origin/main` (файл горячий — 15 открытых PR на момент постановки).
- [ ] **Доказательство и мутация:** живой прогон `repo-ci.yml` на этом же
      PR — все новые шаги зелёные, ни один `ORPHAN-TEST-OK` не остался
      неснятым; намеренная порча одного файла реестра (временный коммит,
      не мержить) красит соответствующий шаг, revert — зелёный.

## 15. Мутационный прогон по расписанию: `rule-mutation-sweep.yml`

Зависит от 2 и от освобождения `repo-ci.yml`/`pulse_guard.py`.

- [ ] Новый workflow: cron раз в сутки + `workflow_dispatch`,
      `permissions: {contents: read, issues: write}`,
      `actions/checkout@v7` с `fetch-depth: 0` (разовый ежесуточный
      прогон — не размножен на 33 открытых PR), `actions/setup-python@v5`,
      `pip install pytest pyyaml`, `mutation_harness.py --all`. Красный
      результат заводит/комментирует issue через `issue-create`
      (потребитель, иначе «тормоз без газа»). Строка в
      `docs/agents/INFRA-GH.md`; workflow добавлен в `WATCHED_WORKFLOWS`
      (`pulse_guard.py:180-183`, занятый файл — после освобождения).
- [ ] **Доказательство и мутация:** `workflow_dispatch` вручную даёт
      зелёный прогон при здоровом реестре; испорченный мутант красит
      прогон и заводит issue с именем правила.

## 16. Метка-выключатель `rules-registry:skip`

Полезна после 14 (design.md раздел H — газ на случай, когда баг в
generated-скрипте блокирует чужие PR).

- [ ] Запись в `docs/agents/LABELS.md` (занятый файл — после
      освобождения): `rules-registry:skip`, по образцу `orchestra:skip`
      — сама является газом; три шага п.14 читают метки PR и пропускают
      себя при её наличии с явным сообщением.
- [ ] **Доказательство:** PR с меткой и намеренно испорченным файлом
      реестра — все три шага зелёные (пропущены), видно в логе.

## 17. Замер стоимости мутационного прогона на реальном раннере

Зависит от 14+15 (хотя бы один реальный прогон каждого в Actions).

- [ ] Не код — обновление «Риски» в `proposal.md` и раздела C `design.md`
      фактическими числами со ссылкой на `run_id` (`gh run view <id>`),
      заменяя оценочные бюджеты (≤30 с per-PR, ≤15 минут ночной прогон) и
      порог «лёгкое/тяжёлое» (5 с) на измеренные.

## 18. Дельта-спека: перенос в источник правды

ПОСЛЕ того как пп.1–17 слиты и подтверждены живым циклом (правило →
промпт → CI подтвердил связь).

- [ ] `specs/journal-tasks-hands/spec.md` этого change перенесено (не
      скопировано заново, а перенесено) в
      `openspec/specs/journal-tasks-hands.md` — если номера 41/42 этого
      change заняты другим уже заархивированным change, перенумеровать на
      актуальные свободные (proposal.md, «Риски» — коллизия системная,
      решается на архивации); change перемещён в
      `openspec/changes/archive/` по процедуре spec-flow.
