# tasks.md: rule-carrier-registry (ходячий скелет)

Три пункта (считая обязательный «завести задачу пула» и завершающий
«перенести дельту в источник правды» — оба стандартная административная
рамка, не содержание скелета) — это скелет, не миграция. Весь код и ОДИН
файл каталога гвардий — в единственном содержательном пункте 1, не
разделены на
несколько: разделять схему/валидатор и харнес/CI на отдельные пункты для
двух правил в реестре не даёт параллельности (харнес зависит от
загрузчика, загрузчик без харнеса не доказывает ничего), а лишь оставляло
бы окно, где новый `test_*.py` уже в дереве, но ещё не подключён ни к
одному шагу `repo-ci.yml` — ровно та находка разбора («ORPHAN-TEST-OK не
может быть способом отложить подключение»), которую этот план обязан не
воспроизвести.

## 0. Задача и ветка — уже есть

- [x] Транспорт выполнил до этого плана: задача пула — #648, ветка
      `agent/648-rule-carrier-registry-v2` (создана `scripts/git/task-branch`,
      аренда живая). Заводить вторую issue или вторую ветку
      `<N>-rule-carrier-registry-skeleton` НЕ нужно — дубль в пуле;
      если задача когда-нибудь закроется раньше работы — не
      переоткрывать (#369), новая узкая задача по протоколу.

## 1. Реестр правил — ходячий скелет: схема, валидатор, мутация одного правила, подключение к CI

- [ ] `docs/agents/rules/README.md` — формат шапки (`id`, `statement`,
      `carrier`, `proof_test`, `status`) и таблица согласованности по
      `status` (design.md, раздел A) и `scripts/lib/rule_registry.py` —
      загрузчик (`load_rules()`) + `validate(rule)`: обязательность всех
      пяти полей, `id` = имя файла, резолвинг `carrier` для Python через
      `ast.parse` (функция/класс/константа верхнего уровня либо
      `Class.method`), резолвинг `proof_test` через
      `pytest --collect-only -q <node>` (код 0, ровно одна строка узла в
      выводе — design.md, раздел C, «Проверенные факты о pytest»),
      абсолютный пол (`total_rules_loaded() > 0`, иначе `RuntimeError` —
      директория непуста, но распарсено 0 правил, это поломка загрузчика,
      не «нечего проверять»). Оба новых скрипта (`rule_registry.py`,
      `mutation_harness.py`) несут console_utf8-bootstrap в точке входа
      (`scripts/lib/console_utf8.py`, класс #723 — печать кириллицы/эмодзи
      валит redirected stdout на Windows) и явный `encoding="utf-8"` в
      файловом чтении реестра. README каталога требует у ТЕЛА записи
      адрес канонического текста правила (раздел `AGENTS.md`/`PROTOCOL.md`
      либо пункт `openspec/specs`) — без него реестр при миграции
      производит пересказы без источника; требование формата, шапочным
      валидатором не проверяется.
- [ ] `scripts/lib/test_rule_registry.py` — тесты валидатора на
      ИЗОЛИРОВАННЫХ фикстурах (временная директория с игрушечными
      файлами правил, не боевые записи из `docs/agents/rules/`, чтобы
      тест не зависел от их будущих правок): отсутствующее поле → отказ;
      `id` ≠ имя файла → отказ; `status: none` разрешает `carrier`/
      `proof_test` в форме `"none: ..."`/`"none"`; `status: proven` с
      нерезолвящимся `carrier` → отказ; `proof_test`, резолвящийся в 0
      или 2+ узлов → отказ; директория с файлами, но 0 распарсенных
      правил → `RuntimeError`; отсутствующая или пустая директория
      правил → `RuntimeError` тоже (47.3: окна «до первой записи» нет);
      ОДИН битый файл среди валидных → красный С ИМЕНЕМ ФАЙЛА, не
      `total == 2` и не зелёный (47.3: тихий пропуск делает битую
      запись отсутствующей); лишнее поле в шапке (не из пяти) → отказ;
      `status: proven` без `docs/agents/rules/mutants/<id>.diff` →
      отказ (47.1: «proven» без воспроизводимого доказательства);
      резолвинг `proof_test` при сломанной тестовой сборке (ошибка
      conftest → exit 2/3) — категория «измеритель не собирается», не
      «запись отклонена» (47.1: код 4 — вина записи, 2/3 — окружения).
      Один из этих тестов — узел
      `test_validate_rejects_missing_statement` — становится
      `proof_test` записи из следующего пункта.
- [ ] Две боевые записи реестра: `docs/agents/rules/
      rule-registry-requires-core-fields.md` (`status: proven`, `carrier:
      scripts/lib/rule_registry.py::validate`, `proof_test:
      scripts/lib/test_rule_registry.py::test_validate_rejects_missing_statement`)
      и `docs/agents/rules/relative-markdown-links.md` (`status: none`,
      `carrier: "none: гейт не заведён"`, тело со ссылкой на
      AGENTS.md, «Работа с документацией»).
- [ ] `docs/INDEX.md` — три ссылки в раздел «Мультиагентная работа»
      (README каталога правил и обе записи правил): гвардия полноты
      карты документации (`docs_index_guard.py`) рекурсивно обходит
      `docs/agents/` и красит каждый `.md` без ссылки из INDEX.md без
      маркера `DOCS-INDEX-OK` — без этого пункта обязательная проверка
      краснеть на ветке этого же change.
- [ ] `scripts/lib/mutation_harness.py`, режим `--rule <id>` (design.md,
      раздел C): health-check `proof_test` на текущем дереве (не `exit=0`
      → «носитель нездоров», это НЕ мутация); проверка адресата мутанта
      ДО применения (`git apply --numstat`, патч не применяется):
      каждый файл патча — файл `carrier`, файл `proof_test` в патче
      запрещён, иначе «мутант устарел» (design.md, раздел C, шаг 2);
      `git apply docs/agents/rules/mutants/<id>.diff` (не применился →
      «мутант устарел»); повторный `proof_test` с разбором кода выхода
      (`1` → доказано; `0` → «мутант выжил»; `2/3/4/5` → «харнес не смог
      проверить: pytest exit=<N>», отдельная категория, не «доказано» и
      не «выжил»); `git apply -R` — только по флагу «патч был применён»
      (на путях «мутант устарел»/«адресат не тот» откатывать нечего,
      reverse-apply не запускается); отказ отката при применённом патче —
      отдельный громкий ненулевой исход «откат не удался — дерево
      осталось мутированным», не тихий успех. Без `git worktree` —
      мутация и откат работают
      прямо в рабочем дереве (design.md обосновывает упрощение для
      одного мутанта). Неизвестный `--rule <id>` — явная ошибка на
      stderr с перечнем известных id и ненулевой код выхода, не тихий
      `exit 0`; `--rule` для записи НЕ-`proven` (вторая боевая запись,
      `relative-markdown-links`, `proof_test: "none"`) — тоже явная
      ошибка «доказывать нечего», БЕЗ запуска pytest (47.4.5: наивный
      `pytest -q none` дал бы `exit=4` и ложное «носитель нездоров»).
      Режим `--all-proven` (его зовёт файл каталога, не ручной список
      id): перебирает все proven-записи по сортировке id, каждую —
      через шаги выше; не-proven пропускает как фильтр с видимым
      счётчиком «прогнано N proven, пропущено M не-proven» (не тихий
      ноль).
      Пустой/недогруженный реестр (0 распарсенных правил при
      непустой директории) — красный результат ПЕРЕД обработкой любого
      конкретного `--rule`.
      `docs/agents/rules/mutants/rule-registry-requires-core-fields.diff`
      — реальный unified diff, убирающий проверку «`statement` непустой»
      внутри `rule_registry.py::validate` (патчит носитель, не
      `test_rule_registry.py` — design.md, раздел C, «Формат мутанта»).
- [ ] `scripts/lib/test_mutation_harness.py` — самотест на изолированной
      fixture (временный git-репозиторий во `tmp_path`, свои игрушечные
      carrier/test/mutant-файлы — не боевое дерево репозитория, чтобы
      порча мутанта в тесте не рисковала настоящим рабочим деревом):
      здоровый носитель + валидный мутант → «доказано»; изначально
      красный `proof_test` → «нездоров», не «доказано»; неприменимый
      мутант (патч на несуществующую строку) → «мутант устарел»;
      мутант-заглушка (no-op diff, ничего не меняющий по существу) →
      «мутант выжил»; мутант, правящий чужой файл (не файл `carrier`) →
      «мутант устарел»; мутант, правящий сам файл `proof_test` →
      «мутант устарел», не «доказано» (47.4.4); переименование
      `proof_test`-файла между
      health-check и мутацией → «харнес не смог проверить: pytest
      exit=4»; неизвестный `--rule <id>` → явная ошибка, ненулевой код;
      `--rule` на не-`proven` запись (`proof_test: "none"`) → явная
      ошибка «доказывать нечего», pytest не запускается (47.4.5);
      `--all-proven` гоняет каждую proven-запись и печатает счётчик,
      не-proven пропускает; `--all-proven` при ненулевом реестре и нуле
      proven-записей → КРАСНЫЙ «единственное мутационное доказательство
      снято с прогона» (47.5: счётчик в логе — не видимый исход);
      понижение `status: proven → unproven` у единственной proven-записи
      красит `--all-proven` тем же полом;
      неприменимый мутант → reverse-откат НЕ выполняется (нечего
      откатывать); отказ отката при применённом мутанте → громкий
      ненулевой исход «откат не удался», не тихий успех;
      пустой реестр (0 файлов на диске) → красный результат независимо
      от `--rule`.
- [ ] Один файл каталога гвардий: `scripts/ci/guards/
      rule-carrier-registry.sh` — регистрация гвардии это ДАННЫЕ (#749):
      рукописный шаг в repo-ci.yml красит
      `ci_guard_registration_guard.py` (проверено прогоном при правке по
      ревью: «новый рукописный шаг гвардии в repo-ci.yml … — перенеси в
      scripts/ci/guards/<имя>.sh»), а шаг-перебор «Каталог гвардий
      scripts/ci/guards» исполняет каждый файл каталога сам — repo-ci.yml
      ЭТИМ change не правится вовсе. Содержимое — те же три команды:
      ```sh
      pip install --quiet pytest pyyaml
      python -m pytest scripts/lib/test_rule_registry.py scripts/lib/test_mutation_harness.py -q
      python scripts/lib/rule_registry.py --check
      python scripts/lib/mutation_harness.py --all-proven
      ```
      Файл несёт свои зависимости сам (контракт run_guards.sh — каждая
      существующая гвардия каталога предваряет pytest своим pip install)
      и проходит общий lint-цикл репозитория (`bash -n` по
      `scripts/ci/guards/*.sh` в repo-ci.yml). Оба новых test-файла и
      мутация подключены в ЭТОМ ЖЕ пункте, не последним пунктом плана;
      маркер `ORPHAN-TEST-OK` не используется нигде в этой задаче.
      Связь «proven ⇒ гоняется мутацией в CI» живёт режимом
      `--all-proven`, не ручным перечнем id в shell-файле: новая
      proven-запись при миграции красит CI сама, без правки каталога
      (47.5).
- [ ] **Доказательство и мутация:** живой прогон `repo-ci.yml` на PR этой
      задачи — файл каталога отработал зелёным внутри шага-перебора.
      Временная порча `validate()` (снятие
      проверки `statement`, не мержить) красит шаг тем же текстом, что и
      объявленный мутант; revert — снова зелёный. Временное
      переименование `test_rule_registry.py` (не мержить) даёт в выводе
      шага «харнес не смог проверить: pytest exit=4», не «доказано» и не
      «мутант выжил»; revert — снова зелёный.

## 2. Дельта-спека: перенос в источник правды

ПОСЛЕ того, как пункт 1 слит и подтверждён живым прогоном CI на main (не
только на PR — правило репозитория «Verify the visible outcome»,
применённое к этому же гейту).

- [ ] `specs/journal-tasks-hands/spec.md` этого change перенесено (не
      скопировано заново, а перенесено) в
      `openspec/specs/journal-tasks-hands.md`; номер 47 (proposal.md,
      «Риски») — рабочий, если к моменту архивации другой уже
      заархивированный change занял его первым, перенумеровать на
      актуальный свободный (свериться подсчётом `grep -oE '^[0-9]+\.'` по
      всем ОСТАВШИМСЯ открытым `specs/journal-tasks-hands/spec.md`, не
      по памяти); change перемещён в `openspec/changes/archive/` по
      процедуре spec-flow.
