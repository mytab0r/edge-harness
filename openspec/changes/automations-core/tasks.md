# Задачи: automations-core (#116)

Заведено постфактум при закрытии находки ревью PR #241, п.3 — часть работы
уже слита, отмечена сразу.

## Ядро (слито в этом change)

- [x] Модель конфига и валидация — `cf-worker/src/automations.ts`
- [x] Хранилище — таблица `automations` в DO SQLite
- [x] Триггеры schedule/webhook/journal, кулдауны, гвардия от зацикливания
- [x] Задачи digest/hands/pool, отчёт slack/telegram
- [x] API: `GET/PUT/DELETE /api/automations[/:id]`, `POST /api/webhooks/:id`
- [x] Раннер: `scripts/automations/run.sh`
- [x] Journal-триггер срабатывает и на внутренние системные kind'ы, не
      только на батч раннера (находка ревью PR #241, п.1)
- [x] Троттлинг журнала для неаутентифицированного webhook-входа (находка
      ревью PR #241, п.2)
- [x] Дельта-спека заведена (эта находка, п.3)

## Часть 2 — морда (НЕ в этом change)

- [ ] DSH-плагин «Автоматизации» по прецеденту plugin-manager — список
      автоматизаций, `last_run`, включение/выключение, правка конфига без
      отдельных страниц/воркеров. Issue заведён `scripts/review/file_tasks.py
      --pr 241` по итогам того же ревью.
