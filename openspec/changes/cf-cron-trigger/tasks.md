# Tasks: cf-cron-trigger (#693)

## Backend (cf-worker)

- [x] Секция `triggers.crons` в `cf-worker/wrangler.jsonc` (интервал 5 мин,
      обоснование числом в docstring `scheduledTick()`).
- [x] `scheduled()` в `cf-worker/src/index.ts` — RPC-вызов к тому же DO, тот
      же биндинг HARNESS, что и `fetch()`.
- [x] `Harness#scheduledTick()` — перезакладывает будильник
      (`#ensureHeartbeat()`) первой операцией на каждом тике; дёргает
      dispatch (`#dispatchOrchestraTick`, вынесенный общий метод для
      `alarm()` и `scheduledTick()`) только при `pulseStale()`.
- [x] Тесты прод-формы (`cloudflare:test`, `cf-worker/test/harness.spec.ts`):
      (a) перезакладка будильника без секретов dispatch'а; (b) dispatch при
      stale-пульсе; (c) отсутствие дублирования при свежем пульсе; (d)
      неудача dispatch'а попадает в пульс; «возможности нет» — тихий выход
      без секретов.
- [x] Мутационное доказательство (a)/(b)+(d)/(c) — снятие проверки красит
      соответствующий тест, возврат — зеленит.
- [x] Тест на `src/index.ts::scheduled()` напрямую (находка ревью PR #696,
      последнее звено цепочки было не покрыто): RPC-обвязка вызывает тот же
      `scheduledTick()`; падение RPC-вызова ловится try/catch, не роняет
      обработчик — обе ветки доказаны мутацией.

## Документация

- [x] `openspec/changes/cf-cron-trigger/proposal.md` — боль владельца,
      решение, ключевой инвариант, честная граница наблюдаемости, бюджет
      триггеров, граница с #689.
- [x] `openspec/changes/cf-cron-trigger/specs/journal-tasks-hands/spec.md` —
      новый инвариант «такт — не только alarm», страховка дедуплицирует
      dispatch через `pulseStale()`, будильник восстанавливается на каждом
      cron-тике безусловно.
- [x] Правка комментария `cf-worker/wrangler.jsonc` (находка ревью PR #696,
      некритично): «35 минут — ровно длительность инцидента» было неверно —
      реальный инцидент 2026-09-07 был неограниченным по времени (нет
      трафика — alarm не восстанавливается вовсе), 35 минут — верхняя
      граница НОВОЙ, ограниченной сверху версии того же простоя, не
      совпадение с фактической длительностью.
- [x] Та же правка в `docs/research/20-cloudflare-free.md` (находка ревью
      PR #696, второй круг, блокирующая): абзац «Подключено» повторял ту же
      неверную формулировку — файл источника фактов для следующих агентов
      не должен нести опровергнутое самим PR утверждение. Переформулирован
      тем же текстом, что и `wrangler.jsonc`.

## Исправление (issue #713): страховка молчала при dispatch_ok=false/run_confirmed=false

- [x] Новый предикат `pulseNeedsRecoveryDispatch()` в `cf-worker/src/harness.ts`
      — только возраст последнего пульса, без короткого замыкания на
      `dispatch_ok`/`run_confirmed` (`pulseStale()` не тронута, остаётся
      источником правды для бейджа `/api/status`).
- [x] `scheduledTick()` использует `pulseNeedsRecoveryDispatch()` вместо
      `pulseStale()`.
- [x] Тесты прод-формы: `cf-worker/test/pulse.spec.ts` (юнит-предикат, 8
      случаев, включая живые сценарии #713 — `dispatch_ok=false`+давно,
      `run_confirmed=false`+давно) и `cf-worker/test/harness.spec.ts` (тест
      (e) — интеграционный сценарий #713 через `scheduledTick()`, реальный
      dispatch случается).
- [x] Мутационное доказательство: возврат короткого замыкания
      (`dispatch_ok`/`run_confirmed` до проверки возраста) красит оба живых
      теста #713 в `pulse.spec.ts` и интеграционный тест (e) в
      `harness.spec.ts`; восстановление зеленит все три.
- [x] `proposal.md` — пересмотрен п.3 «Ключевой инвариант», добавлен раздел
      «Проверяемый прод-критерий такта».
- [x] `specs/journal-tasks-hands/spec.md` — п.46.2 переписан (было:
      намеренное поведение; стало: пересмотрено и почему), добавлен п.46.5
      (ссылка на прод-критерий).
- [ ] Прод-критерий такта (proposal.md, «Проверяемый прод-критерий такта»)
      проверен живым прогоном после деплоя фикса — задача:
      [issue #717](https://github.com/mytab0r/edge-harness/issues/717).

## Вне рамок (см. proposal.md «Что вне рамок»)

- [ ] Изменение интервала `HEARTBEAT.selfOrchestrationMs` самого `alarm()`.
- [ ] Второй независимый будильник вне Cloudflare.
