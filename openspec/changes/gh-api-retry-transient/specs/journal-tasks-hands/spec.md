# Дельта-спека: журнал (journal-tasks-hands) — повтор транзиентного обрыва gh api (#770)

## ADDED: Гейт ревью (ai-review.yml)

42. Транзиентный обрыв `gh api` (обрыв транспорта — `unexpected end of
    JSON input`, 5xx, 429) внутри `ai_review.py::gh()`/`::run_gh()`
    повторяется с экспоненциальной выдержкой (`scripts/lib/gh_retry.py`,
    потолок `GH_RETRY_MAX_ATTEMPTS`), не поднимается как обычный
    `RuntimeError` с первой попытки. Классификация — по структурному
    факту (код возврата `gh`, разобранный HTTP-статус из stderr), не по
    совпадению текста ошибки.

    42.1. Содержательный отказ (401/403/404/422, код возврата `gh`==4 —
        «требует аутентификации») падает с ПЕРВОЙ попытки, как раньше —
        повторять его бессмысленно и вредно.

    42.2. `cmd_verdict` вычисляет вердикт ДО единого сетевого вызова
        (разбор `--answer` — чистая функция); исчерпание повтора на
        публикации поднимает ошибку, чьё сообщение несёт сам вызов, класс
        отказа, число попыток за время И уже вычисленный вердикт
        (значение + метка) — следующий читатель не обязан звать модель
        заново, чтобы его узнать.

    42.3. Смена метки-вердикта (`ai:*`) публикует НОВУЮ метку ПЕРВОЙ,
        старые снимает ПОСЛЕ — обрыв между вызовами (после исчерпания
        повторов) оставляет PR с двумя ai:*-метками, не с нулём.
