# Design: cloud-orchestrator-convergence

## Модель

```
[локальная сессия]   [Telegram webhook]   [морда dsh-edge: session.prompt в helper-сессию]
        |                     |                          |
        | (уже облако в       | POST /api/messages/      | ⚠️ СПАЙК, транспорт НЕ назван
        | смысле результата:  | webhook (cf-worker,      | (находка ревью PR #262):
        | пишет прямо в       | PR #173)                 | session.prompt доставляет текст
        | GitHub, адаптер     |                          | АГЕНТУ ВНУТРИ dsh-edge — дальше
        | не нужен)           v                          | некому и некуда: агент egress
                       +--------------------------------------+ в GitHub не может (#133), а
                       |  Harness DO: таблица `messages`       | из браузера морды в DO нельзя
                       |  (cf-worker/src/harness.ts, PR #173)  | штатно (ADR 0009, SameSite/
                       +--------------------+-------------------+ CORS, #105). См. ниже раздел
                                            |                     "Канал морды — открытый спайк".
                                            | немедленный workflow_dispatch/
                                            | repository_dispatch (паттерн трижды
                                            | доказан: config.ts:38-75,
                                            | harness.ts:238/1140/1188 (строки сдвинулись после мержа инбокса #173/#20))
                                            v
                       +--------------------------------------+
                       |  orchestrator.yml (GitHub Actions)    |  <- диспетчер = сам DO
                       |  gather -> DSH headless (рассуждение) |     (webhook-triggered) +
                       |  -> verdict/action -> умирает         |     orchestra.yml пульс как
                       +--------------------+-------------------+     подстраховка (см. ниже)
                                            | пишет ТОЛЬКО в существующие места правды
                +---------------------------+---------------------------+
                v                           v                           v
          GitHub Issue              messages.result (DO)        ответ в канал происхождения
          (задача/правка)           статус/след решения         (Telegram chat_id / сессия
                                                                  dsh-edge, кроме входа —
                                                                  см. спайк выше)
```

### Канал морды — открытый спайк, не решённая архитектура (находка ревью PR #262)

Третья стрелка на диаграмме выше не имеет названного механизма перехода
границы origin. `session.prompt` в helper-сессию dsh-edge доставляет текст
DSH-агенту, работающему ВНУТРИ dsh-edge (чужой CF Worker, не в этом
репозитории) — а дальше цепочка обрывается: этот агент egress в
`api.github.com` не может (#133, 403), и прямого пути «браузер владельца
(морда) -> Harness DO» тоже нет штатно (ADR 0009: кука `__Host-*` с
`SameSite=Strict` не летит межсайтово, CORS не открыт под произвольный
источник, #105) — единственный обходной путь, write-proxy на стороне
cf-worker, лежит в НЕСМЁРЖЕННОМ PR #230. Ссылка на «прецедент ADR 0009»
(заказ плагина через `[plugin-order:<id>]`) вводит в заблуждение: тот
маршрут целиком живёт ВНУТРИ dsh-edge (агент читает markdown-файл в своём
`$DSH_HOME`, никуда не отправляет через границу origin) — он не пересекает
ту же границу, которую нужно пересечь здесь.

Из этого не следует, что канал морды невозможен — следует, что его
транспорт сегодня НЕ ПОДТВЕРЖДЁН и не может считаться частью выбранной
архитектуры до измерения. Спайк с условием выхода (`tasks.md` добавляет
предусловие явно): замерить, проходит ли исходящий `fetch` из воркера
dsh-edge на URL `cf-worker` (не `api.github.com`, другой домен/адрес) —
если блокировка #133 специфична именно для GitHub-адресов (гипотеза
«IP/colo-абьюз-фильтр» правки в `proposal.md`, «Не подтверждено»), путь
`dsh-edge -> cf-worker -> GitHub` может оказаться рабочим без write-proxy
из PR #230. Результат замера — фиксируется в `docs/research/` (задача
из беклога этого ревью), а требование 9 спеки (ответ в морду) реализуется
только после этого замера, не раньше.

Ключевое свойство: оркестратор — джоб, а не служба. Между вызовами не помнит
ничего; всё состояние читает заново из GitHub (issues/labels/PR) и DO
(`/api/status`, `/api/messages`). Это не новый принцип, а перенос уже
действующего в `scripts/review/ai_review.py` (`gather`/`DSH`/`verdict`,
доверенность разрезана по шагам, комментарий/метка — единственный
переживающий джоб след) на произвольные сообщения вместо диффа PR.

## Развилка 1: где физически рассуждает оркестратор

| Вариант | Цена | Вердикт |
|---|---|---|
| **A. GitHub Actions job, событийный, без состояния (как `ai_review.py`)** | Бесплатно на публичном репо (`docs/research/21-github-actions.md`); задержка старта не документирована, эмпирика секунды-десятки, изредка часы в очереди (там же); job живёт минуты и умирает | **Выбран** |
| B. Рассуждение внутри Harness DO (агент-цикл в самом DO) | 10 ms CPU/инвокацию на Free — уже отвергнуто (`docs/research/30-rejected-alternatives.md`, п.6; `docs/decisions/0002-loop-in-runner-not-do.md`) | Отвергнут повторно, условие возврата не изменилось (Paid: 30с/5мин CPU) |
| C. Постоянный процесс на VPS/CF Containers | Ломает «бесплатно» (от $5/мес), требует согласования владельца (`docs/research/30-rejected-alternatives.md`, п.4 и п.11) | Отвергнут ДО согласования владельцем; к рассмотрению, если ограничение снято |
| D. Рассуждение внутри самой dsh-edge (её собственный агент-цикл, уже работающий в чате) | Egress в GitHub заблокирован (#133); не Linux/нет git+pytest (`docs/research/11-dsh-edge.md`); архитектурно то же ограничение CPU, что у варианта B, только у чужого приложения | Отвергнут: dsh-edge остаётся КАНАЛОМ (ingress/egress текста), не движком решений |

Условие пересмотра A -> C: не деньги на память — память уже есть бесплатно
(dsh-edge хранит историю сессий в Durable Object с SQLite, `/api/health` ->
`"storage":"durable-object-sqlite-vfs"` — снятое возражение, #133). Пересмотр
нужен, только если владелец захочет ФОНОВОЕ рассуждение без входящего
триггера (проактивность) — вот тогда действительно нужен постоянный процесс,
то есть $5/мес (Workers Paid/Containers); джоб-на-сообщение покрывает «ответить
на входящее», не «думать в фоне».

## Развилка 2: как диспетчер узнаёт о новом сообщении

| Вариант | Цена | Вердикт |
|---|---|---|
| **A. DO сам дёргает `workflow_dispatch`/`repository_dispatch` сразу после записи сообщения** | Один HTTP-запрос из DO — паттерн трижды доказан: `cf-worker/src/harness.ts:238` (пульс orchestra), `:1140` (deploy-dsh-edge), `:1188` (harness-task; строки сдвинулись после мержа инбокса #173/#20, было 562/610/657-667); укладывается в лимит 500 dispatch/час (`docs/research/21`) при обычном темпе чата | **Выбран** — минимальная задержка |
| B. Ждать очередной пульс `orchestra.yml` (15 мин, уже существует) | Ноль нового кода в DO | Отклонён как ЕДИНСТВЕННЫЙ механизм: 15 минут не выглядит как «что-то произошло»; остаётся ПОДСТРАХОВКОЙ (см. ниже) |
| C. GitHub-native webhook (issue/comment) как триггер | Каналы входа — Telegram/dsh-edge/локальная сессия, не GitHub-события | Не рассматривается для входа; используется штатно там, где уже есть (`ai-review.yml` на `workflow_run`) |

Комбинация A+B — не третий механизм, а fallback того же диспетчера:
`orchestra.yml` уже читает `/api/status`; добавление проверки «есть
необработанные сообщения старше N минут» — расширение существующего пульса,
идемпотентное по `messages.status` (new -> processing -> done/failed), не
новый компонент. Повтор пульсом ОГРАНИЧЕН потолком попыток (спека,
требование 4, `MESSAGE_DISPATCH_MAX_ATTEMPTS` — по образцу существующего
в main `AI_REVIEW_MAX_ATTEMPTS`, `scripts/orchestra/pulse_guard.py:83`):
без потолка стабильно
падающий джоб оркестратора штурмовал бы 500/час бюджет каждые 15 минут
бесконечно — тот класс тормоза, за который репозиторий уже платил
(находка ревью PR #262, ссылка на #196/#205).

## Развилка 3: адресат ответа

| Вариант | Цена | Вердикт |
|---|---|---|
| **A. Ответ пишется туда, откуда взято сообщение, по полям, уже сохранённым в `messages`** (`chat_id` для Telegram через параметризованный `send_telegram(chat_id, text)`; сессия dsh-edge через примитив `dsh-edge-session.sh`; локальная сессия — адаптер не нужен, она сама терминал) | Требует расширить `pulse_guard.send_telegram` параметром `chat_id` (сегодня жёстко один `TELEGRAM_CHAT_ID`) — точечная правка существующей функции | **Выбран** |
| B. Единый фиксированный канал ответа (всегда Telegram, независимо от источника) | Дешевле, но нарушает прямое требование владельца («что тут, что в морде, что в ТГ — всё одно и то же») | Отклонён |

Условие пересмотра: не найдено — это прямое требование владельца, отказ от него
меняет саму постановку задачи.

## Единая очередь — расширение существующей таблицы, не новая сущность

`messages` (`cf-worker/src/harness.ts`, PR #173) уже несёт `source`, `chat_id`,
`sender_id`, `text`, `kind`, `status`. Предложение:

- `kind = 'chat'|'doc_edit'` перестают быть тупиковыми: вместо
  `note: "Requires manual review"` — остаются в `status='new'`, читаются
  диспетчером как «нужно суждение».
- `directive` остаётся механическим путём (issue создаётся regex'ом, как
  сегодня) — оркестратор НЕ вмешивается в то, что уже работает; подключается
  только там, где сегодня тупик. Усложнение (декомпозиция сложных директив)
  — предмет контракта спеки B, не этой.
- Локальная сессия и находки наблюдателей (#201/#207/#244) НЕ идут через
  Telegram-специфичную схему `messages` — они уже нативно живут в GitHub
  Issues (`auto-detected` и другие метки). Оркестратору-джобу отдельный вход
  для них не нужен: он читает пул задач напрямую (как уже делает `worker.yml`)
  — это не третий канал, а обычное чтение состояния GitHub.

## Идентичность и аутентификация владельца

- **Telegram.** Сегодня `auth:false`, проверок нет (дыра, найденная
  AI-ревьюером PR #173). Гейт — РОВНО ОДИН слой: секрет `TELEGRAM_WEBHOOK_SECRET`
  в заголовке `X-Telegram-Bot-Api-Secret-Token` (штатный механизм Telegram Bot
  API, сверяется на равенство, ставится один раз при `setWebhook`). Не
  полагается на секретность самого URL вебхука — заголовок это гарантирует
  криптографически, безотносительно того, кто узнал адрес. `chat_id` НЕ
  используется как второй гейт (найдено противоречие ревью PR #262: гейт
  по `chat_id` сделал бы требование 8 — ответ в чат, отличный от дефолтного
  `TELEGRAM_CHAT_ID` эскалаций, — недостижимым, поскольку такое сообщение
  отклонялось бы на входе). Ответ уходит в `chat_id`, СОХРАНЁННЫЙ у уже
  принятого сообщения (требование 8) — авторизация этого чата случилась на
  шаге приёма (секрет заголовка), повторной проверки `chat_id` не требуется.
- **Морда dsh-edge.** Уже аутентифицирована штатно: RPC доступен только с
  кукой владельца (`__Host-dsh_edge_owner`, `docs/research/12-dsh-edge-session-api.md`) —
  сообщение уже прошло фильтр «это точно владелец» на уровне морды.
  Дополнительно ничего не требуется.
- **Локальная сессия.** Терминал владельца; аутентификация вне зоны
  ответственности этой спеки — владелец аутентифицирован тем, что сессия у
  него на машине.

## Связь GitHub<->DO без дублирования

Замер (`proposal.md`, п.3) нашёл ДВЕ однонаправленные текстовые связи.
Предложение:

- При создании Issue из сообщения (`#createIssueForDirective` и его будущее
  расширение оркестратором) тело issue получает МАШИНОЧИТАЕМУЮ строку вида
  `<!-- inbox-message: <id> -->` (HTML-комментарий, не рендерится, парсится
  regex'ом) — рядом с уже существующей человекочитаемой «ID сообщения: N», не
  вместо неё. Это даёт `repo_invariants.py` (#244) и будущим проверкам
  возможность сверять пару `messages.result.issue_number` <-> `issue.body` в
  обе стороны без изменения видимого текста.
- Сессия `harness-<N>` получает при создании (`dsh_edge_session_begin`,
  `scripts/lib/dsh-edge-session.sh`) дополнительный шаг: комментарий в Issue
  `#N` со ссылкой на сессию морды. **Не подтверждено**: отдаёт ли dsh-edge
  постоянный deep-link URL конкретной сессии в UI — нужно проверить до
  реализации; при отсутствии такого URL закрывается частично (упоминание
  session-id текстом, не кликабельной ссылкой).
- Ни то, ни другое не создаёт третьего хранилища состояния — только делает
  существующие односторонние упоминания двусторонними и машиночитаемыми.

## Честный потолок

- **Near-real-time, а не мгновенно.** Задержка старта GitHub Actions job'а
  недокументирована (`docs/research/21-github-actions.md`; эмпирика
  секунды-десятки, изредка часы в очереди) — «написал в Telegram» не
  гарантирует ответ за секунды, только «обычно быстро, формально не позже
  35 дней».
- **Без памяти между сообщениями в рамках одного джоба.** Каждый вызов
  оркестратора видит ТОЛЬКО то, что лежит в GitHub/DO на момент запуска.
  Многоходовый диалог («уточни предыдущий ответ») требует, чтобы предыдущий
  обмен был читаем оркестратором из тех же мест правды (история `messages`,
  история сессии морды, предыдущие issue-комментарии) — это разговор ЧЕРЕЗ
  журнал, не разговор с живым процессом, ровно как обозначено в исходной
  постановке владельца.
- **Аутентификация Telegram — секрет, не физическая невозможность.** Любой,
  кто получит секрет заголовка ИЛИ узнает `chat_id`, управляет проектом от
  имени владельца — то же свойство, что и у уже существующего
  `TELEGRAM_CHAT_ID` (владение секретом = владение каналом), не новая
  слабость, но и не решаемая бесплатно иначе, чем секретом.
- **Память между сообщениями не требует денег — она уже есть.** Ранняя версия
  этого документа ошибочно объявляла «нет памяти между сообщениями» причиной,
  по которой облачный оркестратор невозможен без платного тарифа; это снято
  (#133). Морда dsh-edge хранит историю сессий в Durable Object с SQLite и уже
  сегодня — сохраняющий состояние собеседник в облаке, бесплатно. Настоящий
  потолок — не память, а действие: исходящий запрос из воркера морды в
  `api.github.com` получает 403 (#133), поэтому морда может думать и помнить,
  но не может сама создать issue или задиспетчить job.

  **Доказана только одна нога обхода, не обе (находка ревью PR #262 —
  исправлено).** Обход из двух ног: (1) `cf-worker -> api.github.com`/
  `repository_dispatch` — ДОКАЗАНО, `cf-worker/src/harness.ts:238,1140,1188`,
  живые вызовы в проде. (2) `морда dsh-edge -> cf-worker` — ЭТА нога НЕ
  построена и НЕ измерена: `proposal.md`, замер 1, прямо фиксирует «Нет пути
  "произвольное сообщение владельца в чате морды -> оркестратор"», а ADR
  0009 говорит, что из браузера морды в журнал (DO) ни читать, ни писать
  нельзя штатно (кука `SameSite=Strict`, CORS, #105) — единственный
  существующий обходной путь, write-proxy, живёт в НЕСМЁРЖЕННОМ PR #230.
  Писать «обход уже доказан в проде» для канала «морда» было ошибкой — верно
  только для доказанной ноги (cf-worker -> GitHub); нога «морда -> cf-worker»
  перенесена в «Не подтверждено» (`proposal.md`) с явным предусловием в
  `tasks.md` (замер egress dsh-edge на не-GitHub-адреса) до начала работ по
  требованию 9 (ответ в морду).

  Постоянный, фоново рассуждающий процесс без входящего триггера (варианты
  B/C развилки 1) по-прежнему недостижим бесплатно — но это вопрос
  проактивности, не памяти.
- **Прямой REST от `cf-worker` в `api.github.com` для создания Issue —
  не подтверждён живым прогоном** (см. «Не подтверждено» в `proposal.md`).
  Если окажется заблокирован так же, как egress `dsh-edge` (#133), придётся
  вернуться к dispatch-опосредованному созданию issue (job создаёт Issue, не
  DO напрямую) — лишний шаг в цепочке, но развилка 2 переживает это без
  изменений архитектуры.
