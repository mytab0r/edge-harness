Задача: #1160. PR: #1247.

# Tasks: dsh-chain-total-budget

- [x] 1. `scripts/lib/dsh-ci.sh`: `DSH_CHAIN_TOTAL_BUDGET_SECS` (дефолт
      9000с, обоснован замером 10 свежих успешных попыток GLM — максимум
      6014с), дедлайн в epoch, срез `attempt_timeout_secs` на границе
      провайдера, fail loud при остатке ≤ 0 (`timeout 0` = «без ножа»).
- [x] 2. `dsh_run_with_retry`: `DSH_CHAIN_DEADLINE_EPOCH` — нож на КАЖДУЮ
      попытку и КАЖДЫЙ ретрай, кламп пауз ретрая остатком дедлайна,
      `chain_budget_exhausted` без старта попытки (находка третьего ревью
      PR #1247; мутация «нож ретраев снят» = 18360с симулированных часов
      при бюджете 100с — исполнена, смоук красит).
- [x] 3. `dsh_chain_should_advance`: класс `chain_budget_exhausted` изнутри
      ретраев — advance, каноническое сообщение цепочки не дублируется.
- [x] 4. Потребители контракта `DSH_RUN_FAILURE_REASON`: task.sh и
      hands/dsh_task.sh (release-full + человеческое сообщение),
      ai_review.py (`error_reason` + тест), перечни в ai_dsh.sh и
      ai-review.yml.
- [x] 5. Смоук-гвардия `scripts/lib/test/dsh-chain-budget.smoke.sh` (9
      сценариев: реальное время, дефолт 9000с, граница итерации, обратные
      прогоны обоих инцидентов 34757182001/34801868104 по логам, критерий
      приёмки, ретраи с ценой попытки), регистрация каталогом
      `scripts/ci/guards/dsh-chain-budget-guard.sh` (#749).
- [x] 6. Инвариант 21 (наблюдательный, 200 мин), реестр CheckResult,
      тесты numbering/регистрации; арбитраж нумерации (19 → 21, живая
      коллизия с открытыми PR; после ребейза пин {1..18, 21, 22}).
- [x] 7. Доки: рунбук (параграф #1160 про бюджет и исход), комментарии
      worker.yml/scheduler.py/task.sh — арифметика худшего легитимного
      прогона (~302 мин) одним местом в комментарии инварианта 21.
- [ ] 8. Пост-мерж: замер по критериям 2-3 задачи #1160 (20 прогонов
      worker.yml против потолка) — задача #1291, требует дней живых
      прогонов, вне рамок этого PR.
