# Задачи: ai-rework-age-fairness (#1253)

Работа уже реализована и слита в ветку `agent/1253-ai-rework-queue-age`
(PR #1260) двумя каналами ДО того, как заведён этот change — чекбоксы ниже
отмечают уже проверенный факт, не план на будущее. Единственный пункт,
выполняемый ИМЕННО этим change, — сам каталог `openspec/changes/
ai-rework-age-fairness/` (proposal + дельта-спека), закрывающий блокирующую
находку ai-review PR #1260 (класс «поведение-без-спеки»). Работа, сознательно
НЕ входящая в эту задачу (4 некритичные находки ai-review, issue #1256),
названа прозой в `proposal.md` («Что вне рамок», «Известные расхождения»),
не чекбоксами здесь — иначе незакрытые пункты по чужой, не взятой в работу
находке вечно блокировали бы быстрый путь завершения этого change
(`docs/agents/OPENSPEC-PROTOCOL.md`, инвариант 4).

## 1. Код (сделано ДО этого change, зафиксировано здесь по факту)

- [x] Сортировка очереди `dispatch_ai_review_rework` по
      `ai_changes_labeled_at` вместо сырого порядка `open_pulls()`
      (`scheduler.py:1174-1202`)
- [x] `_label_event_times` — общая часть чтения таймлайна меток вместо трёх
      копий (`scheduler.py:712-726`)
- [x] Развязка решения об эскалации бюджета доводки от гейта занятости
      воркера (`scheduler.py:1231-1249`)
- [x] Признак «исход собственного прогона неизвестен» через терминальность
      статуса (`status != "completed"`), не белый список нетерминальных
      (`scheduler.py:1279-1310`, второй круг фикса — первая версия ещё
      несла белый список, находка ревью PR #1260)
- [x] Инвариант 22 `check_ai_rework_never_dispatched`, наблюдательный
      (не в `CI_GATING`/`ESCALATING_INVARIANTS`), номер присвоен арбитром
      `scripts/lib/invariant_numbering.py` (`repo_invariants.py:314-330`,
      `994-1050`)
- [x] **Доказательство:** `python -m pytest scripts/orchestra/test_scheduler.py
      scripts/orchestra/test_repo_invariants.py -q` зелёная (583 passed по
      описанию PR #1260); 4 мутации исполнены реально (снять фикс → RED →
      вернуть → GREEN) — перечислены в `proposal.md`, раздел «Проверено»

## 2. Openspec-change (предмет ЭТОЙ задачи)

- [x] `proposal.md` — зачем, что делается (A/B/C), инвариант 22, что
      проверено (мутации), известные расхождения кода с описанием, что вне
      рамок
- [x] `specs/journal-tasks-hands/spec.md` — дельта-спека: ADDED
      (сортировка по возрасту), MODIFIED (развязка эскалации), ADDED
      (признак терминальности статуса), ADDED (инвариант 22), со сценариями
      на каждое требование
- [x] Названы file:line ссылки на реализацию для каждого требования —
      сверено чтением кода в этой же сессии, не по описанию PR
