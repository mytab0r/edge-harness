# Задачи: runner-session-outcome-notes

Исполнение — задача #480 (ветка agent/480-session-ingest).

## Реализовано в PR

- [x] `scheduler.py::_morde_ingest` — POST `/api/sessions/:id/ingest` (форма
      REST, не RPC-конверт), та же кука/логин, что `_morde_rpc`.
- [x] `scheduler.py::append_session_notes` — один логин на проход, гвардия
      холостого хода (пустой список — ноль сетевых вызовов), `HTTP 404` —
      норма, недоступность морды — громкий сигнал, не хард-стоп.
- [x] Проводка: `after_merge` (PR слит), `accept_merged_tasks` (ok/docs/fail),
      `unhealthy_pulls` (возврат в пул).
- [x] Тесты `test_scheduler.py`: юнит-тесты `append_session_notes`/
      `_morde_ingest` (форма запроса/ответа, 404-норма, hard failure) +
      тесты проводки на всех трёх точках; все новые тесты доказаны мутацией
      (снятие фикса красит соответствующий тест).
- [x] Дельта-спека `specs/journal-tasks-hands/spec.md` (п.38).

## Доводка #1430 (исход различается по содержанию, не по коду)

- [x] `scheduler.py::SESSION_TERMINAL_FAILURES` +
      `session_failure_is_terminal`/`session_failure_terminal_reason` —
      терминальные маркеры содержания отказа в ОДНОМ месте.
- [x] Оба вызывающих морды идут через классификатор: заметки-итоги
      (терминальный — ⚠️ с номером и причиной, прогон зелёный) и архив
      сессий (терминальный — номер уходит из очереди, без вечного повтора).
- [x] Тесты кормят дословной прод-строкой прогона 35647962282; соседний
      случай («исчерпана квота DO») остаётся громким — защита от починки
      глушилкой по коду ответа. Мутации исполнены (см. тело PR).
- [ ] Системный дом классификации — общий модуль для всех вызовов морды —
      задача #1433: не дублировать, пока та работа не слита и не решена.

## Пост-мерж (владелец/оркестратор)

- [ ] Живая проверка: реальный мерж этой задачи (или следующей, чей PR
      декларирует другую задачу пула) должен дописать в сессию `harness-<N>`
      заметку «PR слит в main», видимую владельцу в морде — тот же порядок,
      что был у #119 («Пост-мерж (владелец/оркестратор)»,
      `runner-sessions-in-dsh-morde/tasks.md`). Живой прод не может быть
      проверен из среды разработки этой задачи: секреты `DSH_EDGE_URL`/
      `DSH_EDGE_ACCESS_KEY` — GitHub Actions secrets, недоступные вне
      прогона оркестратора.

## Доводка #1433 (PR к задаче #1433): исход различается в одном месте

- [x] `scripts/lib/morde_outcome.py` — одно место правды: маркер отказа →
      причина и признак «норма/потеря»; умолчание «сломанная возможность».
- [x] Оба вызывающих (`append_session_notes`, `_archive_with_opener`) идут
      через него; рукописных копий правила в `scheduler.py` не осталось.
- [x] Гвардия `scripts/lib/test_morde_outcome.py` держит обе стороны: копий
      нет И общий классификатор реально зовут минимум из двух мест (первая
      проверка сама по себе зеленеет и когда различение убрали вовсе).
- [x] Мутации исполнены (четыре) НА ФИНАЛЬНОМ ДЕРЕВЕ, все числа в шапке
      `scripts/ci/guards/morde-outcome-one-truth-guard.sh` — из прогона.
      Прошлая редакция шапки заявляла «исполнено», но три числа из четырёх
      дереву не соответствовали; нашло это ai-review PR #1434, перегнав
      мутации. Сама строка «два числа исправлены» была таким же
      неисполненным утверждением и заменена этой.
- [x] Гвардия копий — бесмаркерная (AST: любое сравнение текста ошибки со
      строковым литералом в телах функций, ходящих в морду). Прежняя
      зашивала список известных маркеров и была слепа ровно на тот сценарий,
      которым родились все три исторические копии — каждая со своим новым
      маркером (находка ai-review PR #1434). Мутация 2 в шапке гвардии это
      и доказывает.
- [x] Чеклист ревью PR #1434 (некритичные пункты, оба круга) закрыт.
      Тест «чужой маркер доходит до всех потребителей» поднят на уровень
      ВЫЗЫВАЮЩИХ — `test_new_terminal_marker_flips_both_morde_callers_at_
      once` в `scripts/orchestra/test_scheduler.py` с заглушенными
      opener/login/rpc/ingest прогоняет архив И заметки: одна правка
      `TERMINAL_FAILURES` меняет поведение обоих без правки их кода, и
      ветка ПОТЕРИ прогоняется на ПРОД-ФОРМЕ отказа #1430 (литерал
      UNMIGRATABLE реюзи́тся из test_morde_outcome.py, не пересказ) —
      снос ветки `is_loss` у любого из вызывающих теперь краснеет (тест
      живёт в файле scheduler-тестов, чтобы не ломать сверенные с
      прогонами числа в шапке гвардии). Область гвардии копий выводится
      из исходника AST'ом (двери `_morde_*`, потребители `morde_outcome`
      и упоминания дверей) — 11 функций, а не хардкод двух имён, мимо
      которых прошли бы пять ходоков в морду. Суффикс отчёта архива
      честный по документу исхода: «архивировать нечего» — только на
      норме («сессии нет»), на потере — «повтор не поможет, запись снята
      с очереди», иначе строка врала бы факту (сессия-то есть). В
      дельта-спеке над таблицей исходов оговорка: таблица — про отчёт
      заметок; у архива НОРМА даёт 🗄️-строку, ПОТЕРЯ — ⚠️ с причиной,
      а красит прогон возраст очереди (#1422), не сам исход.
- [x] Блокирующая находка третьего круга ai-review (гвардия молча зеленела
      на копиях в формах `==` и `re.search`, при том что её текст называл
      правило обобщённо): признак расширен — `ast.Eq`/`ast.NotEq` с
      литералом с ЛЮБОЙ стороны, `re.search/match/fullmatch/findall/
      finditer` со строковым литералом-паттерном (и объект-паттерн, и
      модуль `re`). Мутации двух новых форм исполнены на этом дереве,
      дословные выводы — в шапке гвардии (мутации 5 и 6). Ложных
      срабатываний на реальном scheduler.py ноль: `kind == …` в
      `post_entity_snapshot` признак не цепляет (имя не из набора ошибки).
