# Задачи: ai-review-gate

Исполнение — задача #18 (ветка agent/18-ai).

## Реализовано в PR

- [x] `scripts/lib/review_labels.py` — одно место правды: имена меток обоих
      гейтов, `merge_label_gate` (review:ok И ai:ok), `ai_verdicts_to_drop`.
- [x] `scripts/review/ai_review.py` — доверенный транспорт gather/verdict:
      дифф-пак + задача пула + промпт; разбор контракта (последняя строка
      «ВЕРДИКТ: …», неоднозначность = error), маскирование через dsh-ci.sh,
      канонический комментарий (шапка-факты + фенсы задач), метка, проверка
      head перед применением вердикта.
- [x] `scripts/review/ai_prompt.md` — промпт ревьюера (адаптация живого
      паттерна владельца из Harness: контракт вердикта в последней строке,
      блоки ЗАДАЧА/КОНЕЦ ЗАДАЧИ).
- [x] `scripts/review/ai_dsh.sh` — DSH-транспорт без GitHub-токена (пины и
      GLM-патч из общей lib, rc≠0 не роняет шаг — судьбу решает verdict).
- [x] `scripts/review/file_tasks.py` — заведение задач из ревью одной
      командой, идемпотентно по заголовку открытой issue, --dry-run.
- [x] `.github/workflows/ai-review.yml` — триггер labeled:review:ok +
      workflow_dispatch; trust-зона по шагам; concurrency на PR с отменой
      летящего ревью.
- [x] `scripts/review/check_pr.py` — снимает ai:* на каждом запуске
      (вердикт AI привязан к head); метки импортированы из lib.
- [x] `scripts/orchestra/scheduler.py` — гейт слияния через
      review_labels.merge_label_gate (оба гейта); убран задвоенный continue.
- [x] `scripts/review/test_ai_review.py` — 24 теста: контракт вердикта,
      блоки задач, шапка-факты/фенсы, roundtrip комментария, гейт меток
      (обе прод-формы), маскирование. Мутационно доказано: снятие ai-гейта,
      сканирование фактов по всему телу и «вердикт не последний» красят тесты.
- [x] Проводка в repo-ci.yml: pytest-шаг, bash -n для scripts/review/*.sh,
      gh()-гвардия keyword body= расширена на scripts/review/, smoke
      bash-клиентов дополнен клиентом ai_dsh.sh (ответ DSH обязан попасть
      в answer.txt).
- [x] ADR 0007 + дельта-спека + PROTOCOL/PLAYBOOK/INDEX.

## Доработка по ревью второго агента (PR #137, вердикт rework → исправлено)

- [x] [blocker] Trust-зона разрезана по env, но не по состоянию job'а
      (workspace/GITHUB_ENV/PATH общие у шагов — инъекция через дифф могла
      исполнить переписанный код с токеном): verdict вынесен в отдельный job
      со свежей ВМ и чистым чекаутом из GitHub; ответ/head едут outputs'ами
      доверенных шагов; номер PR — из контекста события; права job'а review
      урезаны до чтения.
- [x] [major] issues:read в permissions (gather читает задачи пула) +
      task_section различает 404 («не задача — дальше») от 403/5xx (громкий
      отказ вместо молчаливого «нет задачи»).
- [x] [major] concurrency: cancel-in-progress выключен — скипнутый run
      любой метки не должен гасить летящее ревью без автоперезапуска.
- [x] [major] хвост stderr DSH в логе — через redact (как у соседних
      транспортов).
- [x] [minor] пагинация file_tasks (комментарии desc + листание, задачи
      листаются) — >100 сущностей больше не теряются молча.
- [x] [minor] фенс задачи — 4 бэктика: внутренний ```-фенс в теле не
      обрезает roundtrip (+тест).
- [x] [minor] мёртвый meta.json удалён из мёртвых: head из него теперь
      реально едет в verdict-job; design.md синхронизирован с кодом.

## Круг 3: живое AI-ревью PR #138 (первый настоящий вердикт гейта)

- [x] Dispatch на PR #108 вскрыл: доверенные шаги исполняли скрипты из
      чекаута head PR (gather падал на старой ветке) — класс «PR поставляет
      код под токен». Исправлено: чекаут main для шагов с GH_TOKEN, дерево
      PR (pr-head) — только данные агента; verdict-джобу дерево не нужно.
- [x] Замер: метки GITHUB_TOKEN не создают workflow-событий —
      `on: pull_request[labeled]` не мог работать. Триггер заменён на
      `workflow_run` (завершение pr-review) + проверка метки review:ok
      доверенным шагом; оба факта — research/21.
- [x] Первый живой вердикт: PR #138 получил содержательное ревью
      (3 находки doc-дрейфа + 2 задачи в беклог), метка
      `ai:changes-requested`, слияние заблокировано (см. отчёт orchestra:
      «нет вердикта ai:ok»).
- [x] `file_tasks.py --pr 138` завёл #139/#140 одной командой; поймано
      живым прогоном: идемпотентность по списку issues гоняется с
      eventual-consistency (дубли #141/#142 закрыты) — добавлен маркер
      `filed:` в ревью-комментарий (состояние живёт в PR, паттерн Harness).

## Пост-мерж (проверка живым конвейером)

- [ ] Миграция открытых PR: события `labeled:review:ok` у них уже отгорели, а
      триггер ai-review до мержа файла в main не работает (замер в
      [research/21](../../../docs/research/21-github-actions.md)) — для каждого
      открытого PR с review:ok запустить `gh workflow run ai-review -f pr=<N>`
      (на момент слияния это #108), иначе он зависнет в ожидании ai:ok.
- [ ] Первый PR после мержа получает содержательный ревью-комментарий и
      метку ai:ok/ai:changes-requested (улики — в задаче #18).
- [ ] Оркестратор НЕ сливает PR с review:ok без ai:ok (видно в отчёте
      merge_queue: «нет вердикта ai:ok»).
- [ ] `python scripts/review/file_tasks.py --pr <N>` заводит предложенные
      ревью задачи с меткой task; повтор — без дублей.
- [ ] Живой прогон контракта вердикта на GLM-5 (ADR 0007, «не подтверждено»):
      если модель регулярно ломает последнюю строку — ужесточить промпт.
