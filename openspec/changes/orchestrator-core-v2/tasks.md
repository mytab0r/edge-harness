# Задачи: orchestrator-core-v2

Это ПРОЕКТНЫЙ tasks.md: чекбоксы ниже — не подзадачи одного PR, а ЗАЯВКИ
на отдельные задачи пула (каждая заводится через `scripts/gh/issue-create`
своим номером, когда очередь до неё доходит, со ссылкой на этот change).
Пункты выполняются В ПОРЯДКЕ, каждый — самостоятельный PR со своим
критерием приёмки; следующий этап не начинается, пока предыдущий не прожил
минимум один полный цикл слияний без регресса (design.md, раздел 5.1).
Роль-исполнитель для КАЖДОГО этапа — worker-агент, взявший задачу этапа
из пула (в PROTOCOL.md — «worker-агент»), приёмка — критик по критерию,
названному в самом пункте: детерминированный гейт + AI-ревью второго
гейта на PR этапа + стадия приёмки оркестратора (#227); ни один этап не
меняет `openspec/changes/orchestrator-core-v2/` — он живёт до архивации.
Два НАЗВАННЫХ исключения жизненного цикла, не содержания: Этап 0
(обновление design.md измеренным числом — см. ниже) и Этап 8 (перенос
дельта-спеки и архивация по протоколу), оба прямо названы в самих
этапах.

## Этап 0 — Ходячий скелет (design-in-walking-skeletons)

- [x] Завести задачу «Ходячий скелет снимка: одна пара задача+PR через
      новый путь целиком» — ВЫПОЛНЕНО: задача #1298, PR #1299 слит
      (squash-коммит `1feb02b`): таблицы и маршруты в harness.ts,
      `orchestra.yml:117-118` несёт `HANDS_TOKEN`/`HARNESS_URL`,
      `build_observed_pr`/`build_observed_task` — scheduler.py:438-546,
      замер CPU — design.md раздел 3.6. Открытым остаётся только пункт
      (в) УЛИКИ на проде (реальная запись снимка одной сущности в живой
      DO) — предмет приёмки самого #1298, не этого проектного change. Критерий приёмки: (а) две новые таблицы
      `task_snapshots`/`pr_snapshots` в `cf-worker/src/harness.ts`, два
      новых маршрута `GET /api/tasks-snapshot`/`GET /api/pr-snapshot`
      (Bearer `HANDS_TOKEN`), тесты `cf-worker/test/` на CAS/edge-triggered
      write по образцу `#finishMessage`/`#reclaimStuckMessages`; (б)
      `orchestra.yml` получает `HANDS_TOKEN`/`HARNESS_URL` (те же секреты,
      что `worker.yml`, НЕ новые); (в) `scripts/orchestra/scheduler.py`
      получает функцию `build_observed_pr(repo, pull) -> ObservedPR` и
      реально пишет снимок ОДНОЙ реальной сущности (конкретный PR этой же
      задачи, выбранный по номеру) на реальном проде — не мок, не
      локальный тест; (г) измерена РЕАЛЬНАЯ цена CPU на инвокацию DO для
      этой записи (design.md, раздел 3.6) — числом, через `wrangler tail`
      или аналог, не оценкой. Последовательность приёмки этого пункта
      НАЗВАНА ЯВНО: замер на проде выполняется ПОСЛЕ слияния и деплоя
      `deploy-worker.yml` (деплой делает workflow, не исполнитель) —
      исполнитель этапа проводит его пост-мерж, чтобы улике приёмки было
      на что опереться; локальный замер workerd (design.md раздел 3.6)
      уже выполнен и слит, прод-замер — остающаяся часть критерия (г).
- [x] По итогу этапа — обновить design.md, раздел 3.6 и раздел 10, п.1
      измеренным числом (не оставлять "не измерено" после того, как
      измерено).

## Этап 1 — Read-only точки (риск минимальный)

- [ ] ГАЗ на снятие ходячего скелета (замечание ревью): слитый Этап 0
      оставил в `scheduler.py` константы `WALKING_SKELETON_TASK_NUMBER`/
      `WALKING_SKELETON_PR_NUMBER` (scheduler.py:604-605) и вызов записи
      снимка одной сущности из `main()` — без названного условия они
      остались бы в main навсегда. Этот этап ПЕРЕВОДИТ `build_observed_pr`/
      `build_observed_task` на ВСЕ открытые сущности и СНИМАЕТ хардкод и
      вызов из `main()`; критерий: гвардия/тест фиксирует отсутствие
      `WALKING_SKELETON_*` в исходнике после этапа.
- [ ] Задача: перевести `scripts/gh/queue.py:42-54` и
      `scripts/gh/pr_blockers.py:39-64` на чтение `GET /api/pr-snapshot`
      вместо прямого `gh api pulls/{n}`. Критерий: тест на фикстуре
      прод-формы доказывает совпадение старого и нового вывода на одних и
      тех же входных данных (design.md, раздел 5, преамбула).

## Этап 2 — Consumers без собственной логики (task_ref)

- [ ] Задача: `scripts/orchestra/checklist_tail_labels.py:191-210`
      (`resolve_parent_task`) — заменить прямой вызов `task_ref.resolve_pr_task` на чтение
      `task_number` из снимка (поле ответа `GET /api/pr-snapshot`; выбор
      «колонку не заводим» зафиксирован в design.md, п.3 раздела 5).
      Критерий: `test_task_ref_usage_guard.py` переведён с запрета «всё
      вне `build_observed_pr`» на ЯВНЫЙ allowlist легитимных вызовов вне
      ядра, каждый с обоснованием в докстринге гвардии (находка ревью:
      прежняя формулировка «разрешать вызов ТОЛЬКО внутри
      `build_observed_pr`» невыполнима — легитимные вызовы вне плана
      миграции существуют уже сейчас и design.md их намеренно
      сохраняет): `scripts/review/ai_review.py::task_section` (герой
      инцидента #259, бежит в workflow ai-review без доступа к снимку),
      `scripts/orchestra/reference_closure.py` (исключение own-task и
      проверка чужого PR, #1042), `scheduler.pr_references_issue`
      (намеренно широкая `references_task`, design.md п.13 раздела 5);
      вызов `task_ref.resolve_pr_task` в ЛЮБОМ другом месте — красный
      тест.

## Этап 3 — Наблюдатели, дублирующие scheduler.py

Проводка читателей (замечание ревью): инварианты вызываются и из
`repo-ci.yml`, ребейз — из `conflict-mechanical-rebase.yml`, хвостовой
триаж — из `checklist-tail-triage.yml` (cron "23,53 * * * *"); сегодня
эти workflows не получают `HANDS_TOKEN`/`HARNESS_URL`. Правило выбора
названо профилем job'а (design.md, преамбула раздела 5): cron/push-прогоны
(доверенный код) — провести проводку; pull_request-job'ы — секреты DO не
проводятся никогда, точка остаётся прямым вычислением. Консьюмеры вне
такта писателя дополнительно подчинены контракту свежести (design.md,
раздел 3.8, спека п.48): протухшая/отсутствующая строка — третье
состояние «снимок не смотрел», не ноль и не «нарушений нет»; инварианты,
сигналящие о «прямо сейчас», по умолчанию сохраняют прямой пересчёт в
пуш-прогоне — отступить от умолчания задача этапа вправе, назвав выбор
в своём критерии вместе с freshness-гейтом.

- [ ] Задача: `scripts/orchestra/repo_invariants.py` — перевести
      `check_stuck_review_gate`, `check_wasted_ai_review_runs`,
      `check_worker_false_success_comment` (и другие инварианты, читающие
      `resolve_pr_task`/`status_posted_at`/`list_timeline`/
      `latest_ai_comment` напрямую) на чтение снимка в orchestra-прогоне;
      в пуш-прогоне repo-ci — прямой пересчёт по умолчанию (контракт
      свежести design.md §3.8: инвариант, сигналящий о «прямо сейчас», не
      судит по снимку, который мог заледенеть вместе с тактом; отступить
      от умолчания — только вместе с freshness-гейтом п.48 спеки).
      Критерий: число
      инвариантов, существующих ТОЛЬКО ради стережения расхождения копий
      состояния (по замеру задания от 2026-09-15 — 7 из 18; на дереве этой
      правки `def check_` уже 20, пересчёт «только ради стережения» —
      предмет самого пункта, не этого документа), измерено ЗАНОВО после
      миграции и названо явно в PR — снижение подтверждает структурное
      устранение класса, не косметику.
- [ ] Задача: `scripts/orchestra/contract_check.py:93-97,218-269` —
      профиль запуска назван в design.md (п.7 раздела 5): job `contract`
      бежит на pull_request-событиях, секретов к снимку не имеет, и
      ничто не перезапускает его после ближайшего такта — поэтому гейт в
      этом профиле ОСТАЁТСЯ прямым вычислением (своя линковка +
      `_all_open_pulls` живой пагинацией), проводка `HANDS_TOKEN`/
      `HARNESS_URL` в pull_request-job НЕ делается (решение с
      обоснованием в design.md). Этап ЗАВОДИТ второй барьер записи:
      частичный уникальный индекс ПО ВЫРАЖЕНИЮ
      `json_extract(flags_json, '$.task_number')` с WHERE по стадии из
      того же flags_json (колонок task_number/is_terminal в слитой схеме
      нет — выделение колонки миграцией НЕ выполняется, решение
      зафиксировано в design.md, п.3 раздела 5). Критерий приёмки:
      тест на двух PR одной задачи, появившихся в одном такте, — гейт
      ловит конкурента живым сканом (как сегодня), индекс отвергает
      вторую открытую строку на стороне записи (проверка мутацией:
      снять индекс — тест краснеет).
- [ ] Задача: `scripts/orchestra/mechanical_rebase.py` — перевести
      ДОМЕННЫЕ входы очереди конфликтов на снимок: метку `conflict`
      (conflict_queue, :436) и якорь `sch.conflict_labeled_at` (:440) —
      во флаг conflict снимка с его evidence-полем времени, task-linkage
      (worker_blocks_pr, :529) — в task_number (узкая форма, как точка
      12). Гейт активного воркера (состояние Actions-прогонов) и
      git-работа модуля не мигрируют (design.md, п.4 раздела 5, раздел 6).
      «Прямой запрос ПОСЛЕ попытки» из прежней формулировки — НОВАЯ
      функция, не перенос: перепроверки после пуша в модуле сегодня нет
      (находка ревью head `15f06af`: grep mergeable_state по модулю —
      ноль, строки 112-130 — докстринг); в объём этапа не входит.
- [ ] Задача: `scripts/orchestra/stalled_pr_triage.py:312-423` (`decide`) — величины
      4 и 6 (ADR 0021) из снимка, величины 1/2/3/5 остаются как есть.

## Этап 4 — Флаги и производные scheduler.py

- [ ] Задача: `pr_check_runs`/`bad_check_names`/`pr_bad_checks`
      (scheduler.py:2226,2256,2269) -> флаг `checks_red` снимка.
- [ ] Задача: `pr_needs_rework`/`wip_gate` (scheduler.py:4062,4078) ->
      флаг `needs_rework`; агрегат wip остаётся вычислением НАД снимком.
      Входит в критерий: СВЕДЕНИЕ СОСТАВА флага — слитый Этап 0 кладёт в
      флаг {review:changes-requested, ai:changes-requested}
      (build_observed_pr, scheduler.py:484), решающий слой считает по
      `REWORK_LABELS` = {ai:changes-requested, conflict, contract:failed}
      (scheduler.py:4035); после этой задачи состав флага снимка обязан
      совпадать с `REWORK_LABELS` (тест на равенство двух множеств —
      гвардия расхождения).
- [ ] Задача: `pr_is_unhealthy`/`unhealthy_pulls`
      (scheduler.py:4898,4913) -> узкая форма task-linkage из снимка.
- [ ] Задача (пробел политики, design.md §2.2.1, issue #1296): назвать
      действие decide() для комбинации `stage=ready` + `checks_red=true`
      БЕЗ `needs_rework` (оба вердикта ревью зелёные, обязательный чек
      красный) — сегодня, и после миграции данных ДВУХ пунктов выше как
      сформулированы, эта комбинация не обслуживается НИЧЕМ, кроме
      низкоприоритетного fallback'а `dispatch_worker`, который голодает за
      каналами `dispatch_conflict_rework`/`dispatch_ai_review_rework` (живая
      улика #1296: PR #1263/задача #1261, 2ч42м без прогона worker.yml при
      закрытом WIP-гейте 18≥12). Критерий приёмки: `decide()` называет
      действие для этой комбинации (тот же приоритет, что у conflict/
      needs_rework — раньше wip-гейта; бюджет попыток НА ОТПЕЧАТОК диффа;
      эскалация владельцу при исчерпании — по образцу уже существующих
      `dispatch_conflict_rework`/`dispatch_ai_review_rework`, не пятый
      независимый механизм), тест на фикстуре прод-формы PR #1263
      (`review:ok`+`ai:ok`, check-run `test`=`failure`) доказывает, что
      новый decide() выдаёт это действие независимо от занятости
      conflict/needs_rework-каналов в том же такте.
      **Пересмотрено (design.md раздел 5.2, ответ на вопрос координатора):** формулировка выше — точечная заплатка (пятое условие в разлитом приоритете), не структурная починка. Эта задача СУЖАЕТСЯ до regression-фикстуры (PR #1263) для Этапа 7.5 (см. ниже по файлу), где и появляется единственный decide_pr — критерий приёмки ИМЕННО этого пункта Этапа 4 меняется на «фикстура PR #1263 подготовлена и задокументирована», реализация действия переезжает в Этап 7.5 (после Этапа 7 — ниже по файлу).

## Этап 5 — Исторически расходившиеся двойники (#303/#432)

- [ ] Задача: `last_gate1_labeled_at`/`last_ready_labeled_at`
      (scheduler.py:4478,4994 + парная копия `repo_invariants.py:781`) ->
      единственное вычисление в `build_observed_pr`, ОБА потребителя
      читают один снимок. Критерий приёмки прямо ссылается на #303/#432:
      регресс-тест воспроизводит исторические фикстуры этих issue и
      доказывает, что оба консьюмера теперь структурно не могут разойтись
      (общий источник, не совпадение).
- [ ] Задача: `pr_references_issue` (scheduler.py:754-782) внутри
      `reap_stale` — ОСОЗНАННО не мигрирует в снимок (design.md, п.5.13).
      Задача этого этапа — только регресс-тест на живой инцидент 2026-09-04
      (задача #90/PR #181), чтобы узкая (снимок) и широкая (прямой вызов)
      семантики не перепутались местами при будущих правках.

## Этап 6 — Решающий комбинатор и действие

- [ ] Задача: `pr_is_merge_ready` (scheduler.py:5029-5052) ->
      чтение `ObservedPR.stage == 'ready'` целиком.
- [ ] Задача: `merge_queue` (scheduler.py:2281) — сама точка слияния.
      Критерий приёмки — САМЫЙ строгий из всех этапов: минимум 3 полных
      цикла слияний на реальном конвейере без единого случая расхождения
      между старым (параллельно оставленным на время проверки) и новым
      вычислением, прежде чем старый код удаляется.

## Этап 7 — Эскалация

- [ ] Задача: `scripts/orchestra/pulse_guard.py` — все маркер-based
      трекеры (design.md, раздел 4.1) разом, одним PR (не по одному
      маркеру — риск half-migrated состояния выше цены большого PR для
      ИМЕННО этого модуля, обоснование в design.md, раздел 5, пункт 16).

## Этап 7.5 — Решающий слой: decide_pr и decide_pool (design.md раздел 5.2, точка 17)

Добавлено по итогам самопроверки, запрошенной координатором (issue #1296,
design.md раздел 5.2): Этапы 1-4 и 5-6 переносят только НАБЛЮДЕНИЕ, не
РЕШЕНИЕ. Этот этап — единственное место, где решающий код
(dispatch_conflict_rework, dispatch_ai_review_rework, wip_gate,
dispatch_worker, trigger_ai_review, решающая часть
stuck_large_ok_pulls/unhealthy_pulls/stale_ready_pulls, per-PR
эскалационные решатели pulse_guard.py) реально
перестаёт существовать как N независимых функций. Без этого этапа признак
завершения раздела 9 пункта 1а недостижим, а задача из Этапа 4 (ready
плюс checks_red, issue #1296) остаётся точечной заплаткой, добавляющей
пятое условие в разлитый по функциям приоритет, вместо структурной
починки. Состав = полная область определения числа 1795 из раздела 9
п.3: 1378 + 230 (trigger_ai_review, scheduler.py:4612) + 187 = 1795 —
перечень в design.md (п.17 раздела 5) и здесь совпадает поэлементно
(находка ревью head `15f06af`: функция была в замере, но не в плане).

Выполняется ПОСЛЕ Этапа 6 (merge_queue уже читает snapshot.stage
надёжно) и ПОСЛЕ того, как Этап 7 (pulse_guard.py) перевёл маркеры в поля
снимка — decide_pr не может читать поля, которых ещё нет.

- [ ] Задача: спроектировать и реализовать decide_pr(observed, snapshot)
      как ОДНУ таблицу приоритета "предикат по флагам -> действие",
      поглощающую решающую часть dispatch_conflict_rework и
      dispatch_ai_review_rework (retry-бюджеты и эскалация читаются как
      поля снимка, раздел 4.1 — conflict_rework_attempts,
      ai_retry_count и аналогичные), задачу из Этапа 4 про ready плюс
      checks_red (issue #1296), решение об эскалации из
      stuck_large_ok_pulls/unhealthy_pulls/stale_ready_pulls и
      авто-повтор гейта 2 из trigger_ai_review (scheduler.py:4612, 230
      строк: первый диспетч ai-review для PR с решённым гейтом 1 и
      ретрай по ai:failed — строки той же таблицы поверх
      ai_retry_count/ai_retry_exhausted_at снимка). Критерий
      приёмки: тест исчерпывающим перебором ПАР «стадия x комбинация
      значений флагов» снимка по РЕАЛЬНОМУ произведению осей (стадия —
      7 значений раздела 2.2; conflict — ТРИ значения false/true/unknown;
      gate2_error — ПЯТЬ значений: «нет» либо один из четырёх reason_tag,
      действие между которыми различается; остальные булевы флаги —
      сегодня 4,
      итого 7x3x5x2^4 = 1680 пар, 3360 при седьмом флаге — вычислимо в
      юнит-тесте целиком; находка ревью head `15f06af`: перебор одних
      булевых осей молча позеленеет на conflict=unknown и схлопнет
      reason_tag в один бит; сама #1296 была парой stage=ready +
      checks_red — её ловит именно пара стадия x флаги, не одна ось) доказывает,
      что для каждой пары, где СТАРЫЙ код
      (любой из перечисленных решателей) выдавал действие, decide_pr тоже
      выдаёт действие, и что фикстуры issue #303/#432/#740/#1296 входят в
      этот перебор как именованные случаи, не как отдельный список.
      Старые функции-решатели УДАЛЯЮТСЯ этим PR, не остаются параллельно
      "на всякий случай" (design.md раздел 9, последний абзац).
- [ ] Задача: спроектировать и реализовать decide_pool(снимки всех PR,
      снимки всех задач, wip_limit) отдельно от decide_pr — поглощает
      wip_gate (гейт) и dispatch_worker. Критерий: агрегатное решение
      (сколько PR сейчас needs_rework, стоит ли открывать диспетч новой
      задачи) вычисляется из ЧТЕНИЯ снимков, не из повторного gh api;
      регресс-тест на живой случай issue #1296 (18 PR needs_rework против
      лимита 12, задача #1261 голодала за приоритетом) доказывает, что
      после этой задачи PR с checks_red-only (без needs_rework) получает
      адресный dispatch НЕ ПОЗЖЕ, чем через N тактов после появления
      флага — порог N называется в самой задаче реализации, не здесь.
- [ ] Задача: измерить границу хранилище/решение внутри pulse_guard.py
      построчно (design.md раздел 10, п.6) ДО того, как per-PR
      эскалационные решатели этого файла (конфликт, ai-rework) сворачиваются
      в decide_pr — число нужно, чтобы Этап 8 (снос обвеса) мог отчитаться
      реальной, не оценочной разницей строк.

## Этап 8 — Закрытие класса

- [ ] Задача: удалить старый обвес (design.md, раздел 9, признак
      завершения) — не оставлять «на всякий случай рядом». Критерий:
      измеренная разница числа строк `scripts/orchestra/` до/после
      (ожидание — сопоставимо с 6 261 строкой обвеса из замера задания,
      с оговоркой, что часть кода естественно останется — например,
      величины 1/2/3/5 ADR 0021).
- [ ] Задача (документационная): пополнить
      `docs/research/30-rejected-alternatives.md` записями A/B из
      design.md, раздел 8 (снимок в dsh-edge DO; реконсилятор как
      DSH-плагин) — отдельный PR, чисто документационный.
- [ ] Задача (документационная): перенести ИЗМЕРЕННЫЕ факты о внешних
      системах из этого change в постоянные источники до его архивации:
      цена SQL-операции в DO (0.0107-0.0127 мс, workerd) и метод замера —
      в `docs/research/20-cloudflare-free.md`, рядом с уже живущими там
      замерами rows_written/rows_read (замечание ревью: change-каталог
      Этап 8 уводит в archive/, где факты о внешней системе теряют
      видимость).
- [ ] Задача (гвардия, замечание ревью): `upsertOneSnapshot` в
      `cf-worker/test/snapshot.spec.ts` повторяет SQL `#postSnapshot`
      байт-в-байт — при правке маршрута дубль разойдётся молча и замер
      design.md раздела 3.6 протухнет незамечено. Гвардия — структурная
      сверка SQL-литерала из harness.ts со строкой теста (сверка
      литералов здесь — не антипаттерн #891/#893: проверяется КОНСТАНТА,
      не поведение).
- [ ] Задача: перенести пункты 42-48 дельта-спеки в
      `openspec/specs/journal-tasks-hands.md` (см. заголовок
      `specs/journal-tasks-hands/spec.md` этого change) и заархивировать
      `openspec/changes/orchestrator-core-v2/` по протоколу
      `docs/agents/OPENSPEC-PROTOCOL.md`.

## Явные незавершённости (честно, не молчаливый пропуск)

- Полный граф легальных переходов PR (design.md, раздел 10, п.4) может
  потребовать уточнения по ходу реализации Этапа 6 — уточнение вносится
  правкой design.md ДО того, как реализующий PR меняет поведение, не
  постфактум.

Снято (закрыто): величина реальной частоты переходов (design.md, раздел
10, п.2) измерена по логам GitHub за 14.22 суток — design.md, раздел 3.7
(297-534/сутки в среднем, 712-1408/сутки на пике; бюджет укладывается с
запасом даже на измеренном пике). Оговорки измерения — там же, честно не
всё подтверждено (`checks_red` не измерялся вовсе — недооценка, не
переоценка).
