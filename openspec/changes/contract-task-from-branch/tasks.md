# Задачи: contract-task-from-branch (#394)

## 1. Резолвер: список кандидатов, не только один ответ

- [x] `scripts/lib/task_ref.py::pr_task_candidates` — ветка + декларация тела,
      без схлопывания, без повторов (см. docstring, design.md).
- [x] `scripts/lib/test_task_ref.py` — прод-форма PR #388 (реворк: ветка
      #256 закрыта, тело объявляет #391), PR #253 (согласие ветки/тела),
      dependabot-PR #282 (кандидатов нет), PR без agent-ветки.

## 2. Перевод потребителей

- [x] `scripts/orchestra/contract_check.py` — резолвит через
      `task_ref.pr_task_candidates`, пробует кандидатов по порядку, берёт
      первого пригодного (`task_eligibility_problems`). Прямых вызовов
      `declared_tasks`/`declares_task` для вопроса «какая задача у PR» не
      осталось. Симметричная проверка дубликата PR — тоже через кандидатов.
- [x] `scripts/orchestra/test_contract_check.py` — прод-форма живого случая
      #388 (ветка + прозаическая первая строка), реворк-переориентация
      (закрытая ветка → открытое тело), «оба кандидата непригодны»,
      дубликат по ветке без декларации в теле.
- [x] `scripts/lib/free_task.py::declared_pr_for_task` — переведён на
      `task_ref.pr_task_candidates` (проверка «входит ли», не «совпадает с
      единственным резолвом» — иначе реворк-переориентация не находилась бы).
- [x] `scripts/lib/test_free_task.py` — прод-форма PR #181 (ветка/тело
      согласны) не меняет поведение; добавлен тест реворк-переориентации
      (прод-форма тела PR #388) — задача-преемница находится по номеру,
      старая закрытая находится тоже (безвредно, пул её не спросит).
- [x] `scripts/orchestra/scheduler.py::merged_pr_map` — регистрирует слитый
      PR под КАЖДЫМ кандидатом (не только первым) — см. design.md.
- [x] `scripts/orchestra/scheduler.py::after_merge` — напоминание про
      приёмку сверяется с `pr_task_candidates`, не с `declares_task` по телу
      одному.
- [x] `scripts/orchestra/test_scheduler.py` — тест реворк-регистрации
      `merged_pr_map` под обоими числами; существующие тесты приёмки/
      after_merge не сломаны (проверено прогоном).

## 3. Отказ называет действие

- [x] Сообщение «задача не определена» (нет ни ветки agent/<N>-<slug>, ни
      декларации тела) называет обе конкретные починки.

## 4. Документация

- [x] `AGENTS.md`/`docs/agents/PROTOCOL.md`/`docs/agents/WORKER-PLAYBOOK.md`/
      `.github/PULL_REQUEST_TEMPLATE.md` — ветка названа основным источником
      задачи PR, первая строка тела — запасным (не отменена).
- [x] `openspec/specs/journal-tasks-hands.md` — п.17 уточнён: контракт тоже
      резолвит через `task_ref.pr_task_candidates`, не отдельно от п.17.1.

## 5. Живые данные

- [x] Прогон резолвера по всем 30 открытым PR репозитория на 2026-09-06:
      30/30 по ветке, 0 запасным путём, 0 без ответа, 4 расхождения
      ветка↔тело (реворк-переориентация) — см. proposal.md.

## Явно не задачи этого change

- Ослабление `declared_tasks`/`declares_task` (правило #251/#312) — не
  трогается, речь о том, ЧТО спрашивается, а не как парсится текст.
- `scheduler.py::pr_references_issue`/широкая часть `after_merge` — осознанно
  широкая семантика (#259), не переводится на узкую.
