# Задачи: dsh-edge-plugin-system

Исполнение — через пул Issues. Правила — [PROTOCOL](../../../docs/agents/PROTOCOL.md).
Этот change — дизайн-фаза задачи #78: реализация стартует только после ревью
владельца. Порядок ниже — порядок реализации: PoC руками закрывает самые
рискованные пункты [design](design.md) «Не подтверждено», потом патч-серия и
воркфлоу, потом форж-канал.

## 0. Ревью дизайна (завершено)

- [x] Дизайн-документы созданы: `proposal.md`, `design.md`, `tasks.md`
- [x] Ревью владельца пройдено в PR #79 (merged 2026-08-30)
- [x] PoC (PR #81) подтвердил работоспособность дизайна: hello-world плагин проходит forge → deploy → морда
- [x] Первый настоящий плагин `runner-bridge` собран, выпущен (`plugins-runner-v0.1.1`), задеплоен и работает в живой морде (сессия `canary-17-runner-bridge`, приёмка #17)
- [x] Гвардия класса `smoke-edge-plugins.mjs` ловит нарушение `inject`-контракта на сборке (п.6 задач)

## 1. Входы владельца (один раз, до PoC — получены в ходе PoC)

- [x] Название npm-скоупа плагинов (в дизайне — `@edge-harness`, заглушка-решение)
      и подтверждение источника: только релизы этого репо.
      — Закреплено кодом: `PLUGIN_SCOPE` в `dsh-edge/manifest.mjs`; валидатор
      отвергает чужой скоуп и URL-источники («plugins come only from this
      repo's releases, never URLs»), состав пинуется sha256 из релизов этого
      репо (мутационная проверка — п. «Отрицательный канал» §4).
- [x] Политика пина апстрима: кто и когда двигает `dsh-edge/upstream.json`
      (ручной PR; автоматизации нет — design, «Манифест»).
      — Политика живёт в `note` самого `upstream.json` («Обновление — ручной PR
      с полным прогоном деплоя»); бамп #134 (0.7.1 → 0.8.0) прошёл этим путём.
- [x] Порог роста патч-серии (в дизайне: >6 патчей или любой >100 строк —
      пересмотреть форк против серии) — подтвердить или поменять.
      — Подтверждён без изменений: формулировка дизайна перенесена в
      `dsh-edge/PATCHES.md`; на серии из 4 патчей порог ещё не срабатывал.
- [x] Решение по опциональной идее «каталог моделей из build-time константы»
      (закрыла бы хрупкую хирургию минифицированного бандла в текущем
      `deploy-dsh-edge.yml`) — делать ли в этом слайсе. Номер патча `0004` в
      фактической серии занят другой, уже сделанной фичей
      (`0004-harness-ingest.patch`, #119, вне области #78) — см.
      `dsh-edge/PATCHES.md` и design.md «Форк-серия»/«Девиации» п.4; у этой
      идеи, если её примут, будет следующий свободный номер.
      — Решено против отдельной константы в этом слайсе: каталог моделей
      патчится деплой-шагом на обоих путях (п.4 «Девиаций» design). Нумерация
      серии ушла дальше: `0005` заявлен под #235 (/api/harness-прокси).
- [x] `dsh-edge/plugins.json` в main содержит третий плагин `plugin-manager`
      (client-only, релиз `plugins-manager-v0.1.1`), ни разу не упомянутый в
      proposal/design/tasks этого change. Решить: задокументировать его здесь
      как третий реализованный плагин (аналогично hello/runner-bridge выше)
      или снять из манифеста до отдельного change, если он экспериментальный.
      — Решено: задокументирован собственной задачей #102 (раздел «Плагины»
      в морде: манифест, статусы, заказ), доведён до `plugins-manager-v0.1.4`
      в манифесте. Этот change задним числом не расширяется.

## 1. PoC руками: hello-world через весь канал (до продакшн-воркфлоу)

- [x] Минимальный плагин `plugins/hello/` (сервер: cordis-плагин с тривиальным
      инструментом/сервисом; клиент: `./client` бандл с простейшей
      `dsh.client`-декларацией — design «Не подтверждено» п.8).
      Фактически — `plugins-src/hello-world/` (`@edge-harness/dsh-plugin-hello`,
      сервер — инструмент `plugin_hello` через апстримный `defineTool`).
- [x] Локальный прогон схемы сборки на машине: клон апстрима по SHA → патчи
      0001-0003 руками → `pnpm install --frozen-lockfile` → `pnpm add <tgz>` →
      **проверить, что пять pnpm-патчей пережили add** (п.1 «Не подтверждено»);
      провал → план Б из design. — Пережили все **семь** (п.5 девиаций design).
- [x] Кодогенератор: `plugins.json` → `edge-plugins.generated.ts` +
      `standalone/edge-plugins.json`; пустой манифест деградирует в апстримную
      сборку (доказать оба направления). — Оба направления прогнаны.
- [x] Пробная сборка: `bundle-standalone direct` — gzip-бюджет (замер запаса,
      п.3), `bundle-meta.json` содержит входы плагина, алиас резолвится
      (патч 0001 работает). — Бюджет 775046/921600 (запас ~143 KiB);
      bundle-meta эфемерен (п.2 девиаций design), состав доказан литералами
      в durability-артефакте и гвардией состава.
- [x] `verify-standalone` / `verify-determinism` на сборке со сгенерированным
      файлом (п.2 «Не подтверждено»). — Зелёные; детерминизм воспроизвёл хеш
      байт-в-байт между двумя клонами.
- [ ] Негативный тест изоляции: инсталл, бросающий исключение, → DO жив,
      остальные плагины стоят (п.4); заодно проверить контракт
      `Context.plugin()` при throw. — Пост-мерж (W5 из ревью PR #81):
      нужен живой DO.
- [x] Мутационная проверка гвардии: убрать плагин из бандла (снять
      кодогенерацию) → гвардия состава краснеет; вернуть → зелёная. Снять
      фикс — убедиться, что тест ловит (правило «закрой класс»). — Прогнано
      в обе стороны.
- [x] Развёртывание в личный воркер: морда поднялась, канарейка 200, в чате
      виден эффект hello-плагина, клиент-часть смонтировалась после reload.
      — Закрыто живым прогоном: владелец зафиксировал закрытие PoC в #80
      (source-build деплой зелёный, морда 0.7.1 + hello-world,
      `plugin_status ready` в журнале по `plugin:hello`); свежий живой прогон
      канала — deploy-dsh-edge run 33737092655 (2026-09-03, source-build,
      пин b9a8ddd): plugin_status deploying→ready принят журналом по каждому
      плагину, канарейка морды и ingest-шов #119 зелёные.
- [ ] Замер сквозной задержки forge→deploy→морда (п.7). — Пост-мерж
      (замеряется на живом прогоне деплоя). Нога deploy→морда замерена на
      живом прогоне (run 33737092655): валидный манифест → `plugin_status
      ready` ≈ 79 с (deploying→ready ≈ 13 с), job целиком 1m34s. Нога
      forge→ появится вместе с форжем (#162) — снять пункт после её приёмки.

## 2. Состав в репо (после PoC — с поправленными фактами design)

- [x] `dsh-edge/upstream.json`, `dsh-edge/plugins.json` (+ валидатор схемы в
      кодогенераторе: id, скоуп, sha256, уникальность, релиз существует).
      Валидатор — `manifest.mjs`, общий с гвардией; CLI-режим — первый шаг
      workflow; существование релиза доказывает шаг скачивания до кодогена.
- [x] Патч-серия `dsh-edge/patches/0001-0003` (+ `0004` по решению владельца)
      и `dsh-edge/PATCHES.md`: у каждого патча причина и `removeWhen`-условие
      снятия, порог роста серии. — 0004 в этом слайсе не делается: каталог
      моделей патчится деплой-шагом на обоих путях (п.4 девиаций design).
- [x] `.github/workflows/build-dsh-edge.yml`: схема «Сборка» из design —
      clone@SHA, `git apply` (громко), pnpm-инсталлы, sha256-сверка tgz,
      codegen, бандл, **гвардия состава** (каждый enabled-плагин в
      `bundle-meta.json` и в `dist/plugins`), verify, deploy, канарейка.
      Секреты не печатаются, веерных `set -x` нет. — Фактически единый
      `deploy-dsh-edge.yml` (п.1 девиаций); гвардия по durability-артефактам
      вместо эфемерного bundle-meta (п.2 девиаций).
- [x] Судьба текущего `deploy-dsh-edge.yml` (префаб): либо удалить после
      стабилизации сборки из исходников, либо оставить как аварийный префаб —
      решить владельцу в ревью; два автодеплоя одной морды сосуществовать не
      должны (гонка за воркер). — Решено в #78: один workflow, префаб —
      fallback при пустом манифесте; гонки нет по построению.

## 3. Форж и канал обновления

> Срез статуса на закрытии PoC-канала (#80): deploy-сторона статусов сделана —
> `plugin_status` постится `deploy-dsh-edge.yml` с fail-loud градацией
> (финальный статус обязан доехать, иначе job красный), рендер статусов в
> морде — раздел «Плагины» #102. Сам форж — отдельная задача #162
> (воркфлоу-слайс — #268, «стройка 3» эпика #77); пункты ниже остаются
> открытыми там, не в #80.

- [ ] `.github/workflows/plugin-forge.yml`: workflow_dispatch, вход — путь к
      исходникам плагина; шаги чеклиста совместимости как обязательные
      (запрещённые импорты, нет рантайм-import(), размер вклада в бандл,
      smoke на локальной сборке) — design «Workers-совместимость».
- [ ] Публикация релиза `plugins-vN` (tarball + checksums) из форжа.
- [ ] PR с правкой `plugins.json` (версия, sha256) — ручной или из форжа
      (ветка + PR, не прямой пуш).
- [ ] Статусы: оба воркфлоу постят `plugin_status` в журнал
      (`POST /api/events`, псевдо-задача `plugin:<id>`, seed seq с сервера —
      как hands); отказ поста не валит job молча — финальный статус обязан
      доехать или job краснеет (fail loud: деплой без `ready`/`failed`
      события = silent-wrong).
- [ ] Рендер `plugin_status` в морде журнала — минимальный, как в слайсе
      dsh-streaming.

## 4. Доказательства (критерий #78: дизайн с ревью; реализации — после)

- [x] PoC-улики приложены к задаче: лог сборки с гвардией состава, скрин/лог
      морды с hello-плагином, замеры бюджета и задержки. — Улики: комментарий
      владельца в #80 (source-build зелёный, `plugin_status ready` по
      `plugin:hello`, гвардии проверены мутациями); бюджет — §1 (775046/921600,
      запас ~143 KiB); задержка и свежий живой прогон канала — §1 (run
      33737092655).
- [x] Мутационная улика гвардии: снятый плагин краснит сборку. — Прогнана
      в обе стороны (§1, «Мутационная проверка гвардии»); повтор класса на
      релизах 0.1.0/0.1.1 — design «Девиации» п.9.
- [x] `PATCHES.md` отвечает «почему» и «когда снять» для каждого патча. —
      Проверено: у всех четырёх патчей причина и `removeWhen`, плюс порог
      серии и факт бампа 0.8.0.
- [x] Отрицательный канал: манифест с чужим URL/битым sha256 → красная сборка
      на валидаторе, а не деплой. — Доказано мутациями на
      `dsh-edge/manifest.mjs`: битый sha256 / чужой скоуп / дубликат id —
      exit 1 с точным `::error::` до любых дорогих шагов; несходство sha256 и
      недоступность релиза красят шаг скачивания в деплое (`sha256sum -c` /
      «Релиз недоступен» — exit 1).

## 5. Первый настоящий плагин: runner-bridge (#95, PR без форжа — руками)

Конвейер из пп. 1–2 в первый раз использован по назначению: плагин написан,
собран и опубликован вне форжа (форж из п.3 всё ещё впереди — чеклист
совместимости прогонялся вручную, факты в design «Девиации» п.8).

- [x] `plugins-src/runner-bridge/` (сервер-only): инструменты `runner_task`
      (issue с меткой `task` → dispatch `worker.yml` `{ref:main,
      inputs:{task}}`) и `runner_status` (state issue + метка blocked + PR по
      cross-references таймлайна), оба через апстримный `defineTool`.
      Маршрутизация («умный принцип») — в description `runner_task`:
      серверная работа (сборка/тесты/плагины/git/долгие >5 мин) — звать
      раннер, остальное — чат.
- [x] Токен из env воркера: секрет `GH_RUNNER_TOKEN` синхронизируется
      `deploy-dsh-edge.yml` из секрета репозитория `GH_PIPELINE_PAT` (до задачи #6 — `GH_DISPATCH_TOKEN`)
      (тот же PAT; права issues+workflow dispatch доказаны оркестратором),
      репозиторий пула — переменная воркера `GH_RUNNER_REPO` в конфиге
      деплоя. Чтение — `process.env` воркера (заполнение секретами включено
      с compat date ≥ 2025-04-01 — [research/20](../../docs/research/20-cloudflare-free.md)).
- [x] Таймауты: каждый fetch под `AbortSignal.timeout(15с)` + отмена хода
      агента (`AbortSignal.any`); ошибки инструментов — громкие тексты агенту
      (конфигурация/HTTP-статус/сеть), значение токена в вывод не попадает.
- [x] Релиз `plugins-runner-v0.1.0` с asset'ом `runner-bridge-0.1.0.tgz`;
      sha256 скачанного с релиза asset'а сверён с локальным tarball'ом и
      вписан в `dsh-edge/plugins.json` (`server: true, client: false`).
- [x] Негатив на настоящем dsh-tools 0.1.1-rc.2 (пир из публичного npm):
      нет токена / нет `GH_RUNNER_REPO` → внятный конфигурационный текст;
      битый токен против живого GitHub → `HTTP 401: Bad credentials` текстом
      агенту; утечки токена в выводе нет; скомпилированные схемы параметров
      корректны (`required`: title/body; issue: integer).
- [x] Пост-мерж: деплой по новой манифестной записи прошёл (гвардия состава
      видит `runner-bridge` в обоих бандлах), секрет `GH_RUNNER_TOKEN`
      появился у воркера. — Деплой 2026-08-31 (run 33409297588): гвардия
      состава, дым инсталла, шаг «Секрет раннера морды» — зелёные. Первый
      живой чат-тест (приёмка #17, сессия `canary-17-runner-bridge`): агент
      морды видит тулсет и сам зовёт `runner_task` с дословным ТЗ. Создание
      задачи упёрлось в **403 GitHub API из egress CF** (GET и POST;
      контроль тем же PAT из раннера — 200; замер —
      [research/21](../../../docs/research/21-github-actions.md)) → маршрут
      «морда → GitHub» в [задаче #133](https://github.com/mytab0r/edge-harness/issues/133);
      лёгкий запрос без инструмента — там же, после разблокировки.

## 6. #100: плагинные тулов нет в тулсете агента (фикс класса)

- [x] Исследование первоисточника: точка композиции тулсета найдена — снапшота
      нет, `ToolRuntime` регистрирует у `SystemPrompt` живой провайдер, тулсет
      читается из реестра на каждой сборке хода; гипотеза тайминга опровергнута
      (факты — design «Девиации» п.9).
- [x] Настоящая причина: плагины объявляли `inject: []` и читали `ctx.tools` —
      cordis 4 бросает `cannot get property "tools" without inject` в apply,
      инсталл-цикл изолировал отказ в статус `failed` (зелёная сборка, пустой
      тулсет). Фикс — в плагинах, там где контракт: `inject: ['tools']`
      (plugins-src hello-world и runner-bridge, версия 0.1.1), не в патче 0002.
- [x] Релизы `plugins-hello-v0.1.1` / `plugins-runner-v0.1.1` с asset'ами
      `hello-world-0.1.1.tgz` / `runner-bridge-0.1.1.tgz`; sha256 скачанных с
      релизов asset'ов сверены с локальными и перепинованы в
      `dsh-edge/plugins.json`.
- [x] Тест-гвардия класса: `dsh-edge/smoke-edge-plugins.mjs` — инсталл каждого
      серверного плагина в настоящий cordis + ToolRuntime на сборке; шаг в
      `deploy-dsh-edge.yml` после кодогенерации. Мутационное доказательство:
      против 0.1.0 дым красный с той самой ошибкой apply, против 0.1.1 —
      зелёный («hello» +1 тул, «runner-bridge» +2; тулсет:
      plugin_hello, runner_status, runner_task).
- [x] Полная репетиция CI-пути на свежем клоне: патчи → pnpm install →
      pnpm add tgz (patchedDependencies целы) → кодоген → дым → build →
      гвардия состава → verify-standalone/детерминизм. Все зелёные; в обоих
      воркер-бандлах литералы `runner_task`, `runner_status`, `plugin_hello`,
      состава и `inject:["tools"]` ×2; `node --check` бандлов зелёный.
- [x] Пост-мерж: деплой прошёл; тулсет агента живой сессии содержит тулы
      плагинов — доказано не перечислением, а вызовом: сессия
      `canary-17-runner-bridge` (2026-08-31, приёмка #17) — агент сам позвал
      `runner_task` (и `runner_status` для диагностики), аргументы дошли
      дословно. `plugin_hello` в чате не звался — состав тулсета на сборке
      доказывает дым инсталла (шаг CI). Ключи `dsh-edge-plugins:*` в DO
      storage глазами не смотрели (нет API; косвенно — тулы работают).
      Живой тест issue + dispatch — blocked на 403 egress CF,
      [задача #133](https://github.com/mytab0r/edge-harness/issues/133).

## Границы

- Транспорт «исходник из чата в форж» и UI-поверхность статусов в dsh-edge —
  следующие слайсы эпика #77 (design, «Чего намеренно не решает»).
- Плагины runner-профиля не трогаются.
- Всё, что стоит денег, — вне change.
