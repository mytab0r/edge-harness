# tasks: inbox-auto-revive-configured-token (#304)

- [x] Проверено: архитектура токена (ADR 0011, свой `GH_ISSUES_TOKEN`) уже
      соответствует финальному, слитому решению — код не меняется.
- [x] Секрет `GH_ISSUES_TOKEN` подтверждён отсутствующим (`gh secret list`,
      2026-09-06) — 0 issues с меткой `source:inbox`.
- [x] `alarm()` сам возвращает в `new` failed-сообщения с
      `issues_not_configured`, когда токен появился — тест с мутацией,
      прочие failed не трогает (проверено отдельным сообщением в тесте).
- [x] Дельта-спека + правка базовой спеки (п.34).
