# Tasks: pulse-external-alert (#1103)

## Backend (cf-worker)

- [x] Новая таблица `pulse_alert` (`cf-worker/src/harness.ts`, SCHEMA) — дедуп-флаг
      живости пульса, отдельная от `pulse` (нет прецедента ALTER TABLE на уже
      развёрнутую строку — новая таблица безопасна при любом прод-состоянии).
- [x] `pulseAlertText(lastPulse)` — чистая функция текста алерта: факт
      (конкретный `detail` записанного пульса) + план (что делает
      самовосстановление, порог эскалации на ручную проверку). ПЕРЕСМОТРЕНО
      находкой ревью PR #1104: убрана ветка по `pulseStale()` — недостижима
      при вызове на пульсе, который тик только что сам записал (`ts: now`,
      возраст всегда 0); убран параметр `now`, больше не нужен.
- [x] `#recordPulseAlertFlag`/`#tickPulseAlert` — дедуп переходов
      здоров↔нездоров, переиспользует `storageReadyAlertDecision` (не вторая
      копия решения), доставка через существующий `#telegramApi`.
- [x] Вызов `#tickPulseAlert` из общего `#dispatchOrchestraTick` — общий код
      для `alarm()` и `scheduledTick()` (Cron Trigger), одно место правды.
- [x] `docs/decisions/0018-pulse-alert-lives-in-do.md` — решение и три
      отклонённые альтернативы (внешний cron, GH Actions watchdog на
      `schedule`, ретрай средствами workflow).

## Тесты

- [x] `cf-worker/test/pulse-alert.spec.ts`:
      - серия из восьми подряд падающих тиков (403 — прод-форма живого
        инцидента, включая `/runs`, не только `/dispatches`, находка ревью
        PR #1104) → ровно один incident-алерт с проверкой СОДЕРЖИМОГО
        текста (не только факта отправки), восстановление → ровно один
        recovery-алерт, второй здоровый тик подряд — без повторного recovery;
      - страховка Cron Trigger (`scheduledTick()`, резервный dispatch сам
        проваливается 403) тоже шлёт алерт тем же общим кодом, текст несёт
        причину именно этого тика;
      - `TELEGRAM_CHAT_ID` не задан — тик не падает, не звонит («возможности
        нет»);
      - таблица `pulse_alert` недоступна — тик не падает, не спамит, первый
        удавшийся тик догоняет.
      - Самообновление dsh-edge (`#checkDshEdgeUpdate`, тоже вызывается
        каждым `alarm()`-тиком) замокано по точному URL (тот же приём, что
        `harness.spec.ts`) — без этого тест бил в реальную сеть и падал по
        таймауту, найдено на живом прогоне при доводке PR.
- [x] `cf-worker/test/pulse.spec.ts` — прямой юнит-тест `pulseAlertText`
      (находка ревью PR #1104: чистая функция, несущая весь текст алерта,
      не была покрыта ни одним ассертом на содержимое): называет `detail`;
      `detail: null` — фраза-заглушка, не буквальный `null`; называет план
      (частота повтора `alarm()`, `GH_DISPATCH_TOKEN`, порог «час»); текст
      не зависит от возраста пульса (символьное доказательство недостижимости
      бывшей `pulseStale`-ветки).
- [x] Полный прогон существующего набора (`npx vitest run`) — 176/176, ничего
      не сломано; `npx tsc --noEmit` — чисто; `npm run check`
      (check-frontend-contract.mjs) — OK, контракт фронта не тронут.

### Мутационное доказательство (дословно)

Снята строка `this.#tickPulseAlert(now, {...})` в `#dispatchOrchestraTick`
(закомментирована). `npx vitest run test/pulse-alert.spec.ts test/pulse.spec.ts`:

```
 Test Files  1 failed | 1 passed (2)
      Tests  3 failed | 58 passed (61)
```

(упали ровно три поведенческих сценария из `pulse-alert.spec.ts`: «цепочка
рвётся … один incident-алерт; восстановление …», «alarm подвис — страховка
scheduledTick тоже шлёт алерт», «таблица pulse_alert недоступна …» — все три
ждут `sendMessage`, получают `[]`; юнит-тесты чистой `pulseAlertText` в
`pulse.spec.ts` не затронуты — мутация ровно там, где ожидается: в проводке
вызова, не в самой функции текста).

Строка возвращена, дополнительно снят fallback `?? "причина не записана"` в
`pulseAlertText` — `npx vitest run test/pulse.spec.ts`:

```
 Test Files  1 failed (1)
      Tests  1 failed | 56 passed (57)
```

(упал ровно тест «detail null … — фраза-заглушка, не буквальный null»: текст
содержит буквальный `"null"`). Обе мутации возвращены — `npx vitest run`:

```
 Test Files  5 passed (5)
      Tests  176 passed (176)
```

## Openspec

- [x] `proposal.md`, `specs/journal-tasks-hands/spec.md` этого change'а.

## Вне рамок

- [ ] Честный потолок наблюдаемости (issue #1143, найдено ревью PR #1104):
      простой, закончившийся удавшейся страховкой ДО того, как incident-алерт
      успел записаться, невидим владельцу. Не заводится здесь без решения
      владельца — отдельная, более узкая задача.
