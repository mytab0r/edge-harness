- [x] Замер: сколько гвардий каталога реально читают API (шесть, поимённо) —
      вместо посылки «ни одной», стоявшей в двух местах.
- [x] Одна обёртка `rate_guard.run_guard_main` на всех шести потребителей,
      не шесть копий обработки.
- [x] Классификатор отказа — один на гейт и на обёртку
      (`rate_guard.is_rate_limit_refusal`).
- [x] Обёртка узкая: не-квотная ошибка проходит наверх нетронутой.
- [x] Ложная посылка исправлена во ВСЕХ трёх местах: `run_guards.sh`,
      `repo-ci.yml` и причина `_GUARD_CATALOGUE` в реестре #1437, куда я её
      сам перенёс цитированием вместо замера.
- [x] Класс закрыт проверкой `catalogue_problems`: новый потребитель без
      обёртки красит CI; тест на живом дереве фиксирует и число, и имена.
- [x] Детектор переделан с подстроки на AST (ревью PR #1460): проза
      комментария больше не считается вызовом, транзитивное чтение видно,
      `_gh_api` и `subprocess.run(["gh", …])` не пропускаются. Каждый случай
      закрыт своим тестом, не общим утверждением.
- [x] Замкнутый круг «модуль подключил обёртку → обёртка зондирует бюджет →
      модуль стал потребителем» разорван исключением `rate_guard.py` из
      обхода, с тестом.
- [x] Честная оговорка про верхнюю оценку: статически видно «может дойти до
      API», не «дойдёт». Проверено исполнением обеих сторон — `declared_deps`
      под заглушкой падает (EXIT=1, настоящий потребитель),
      `deploy_workflow_registry_guard` не падает (EXIT=0, завышение).
      Завышение принято: цена обёртки на модуле без вызова — ноль.
