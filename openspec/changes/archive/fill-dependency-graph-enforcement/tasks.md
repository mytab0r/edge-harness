# Задачи: fill-dependency-graph-enforcement

## 1. Разовая миграция существующих зависимостей

- [x] Сканирование тел + комментариев 113 открытых задач пула шестью
      формулировками («после #N», «зависит от #N», «блокируется #N»,
      «блокирует #N», «заблокирован(а) #N», «блокер — #N», «до
      закрытия/слияния/мержа #N») + полнотекстовый поиск GitHub по тем же
      формулировкам (включая комментарии) — ~40 кандидатов.
- [x] Каждый кандидат прочитан целиком (не по регэкспу) — 2 подтверждены
      (`#254←#170`, `#258←#133`), ~38 отвергнуты (см. design.md, развилка а).
- [x] `task_deps.py block` — обе подтверждённые связи проставлены,
      живой запрос `task_deps.py pool` подтверждает: было 0 задач с
      непустым `blocking_open`/`blocked_by_open`, стало 4 (обе стороны
      обеих связей).

## 2. Обязательность на входе — шаблон issue

- [x] `.github/ISSUE_TEMPLATE/task.yml` — обязательные поля «Чем
      блокируется»/«Что блокирует» (обе стороны).
- [x] `.github/ISSUE_TEMPLATE/white-spot.yml` — обязательное поле «Чем
      блокируется» (одна сторона: white-spot почти никогда сам не
      блокирует другие задачи пула).
- [x] `docs/agents/PROTOCOL.md` — раздел «Зависимости» получил
      подраздел «Заведение задачи требует явного ответа», честно
      называющий границу (форма — только человекочитаемый след, перенос
      в граф для этого пути остаётся ручным).

## 3. Обязательность на входе — file_tasks.py (основной автопоставщик)

- [x] `scripts/review/ai_prompt.md` — контракт блока `ЗАДАЧА:` получил
      обязательную последнюю строку `БЛОКИРУЕТСЯ: #N … | ничем` с
      явным указанием не угадывать номер.
- [x] `scripts/review/ai_review.py` — `blocked_by_numbers` (парсинг
      строки), `parse_tasks` отбрасывает блок без неё целиком, громкое
      `::warning::`.
- [x] Тесты (`test_ai_review.py`): существующие блоки без строки
      обновлены под новый контракт, новые тесты — отброс блока без
      строки (с проверкой громкого предупреждения), позиционность строки
      (не последняя — не считается), `blocked_by_numbers` на всех формах
      (`ничем`, один номер, несколько, отсутствие, регистр).
- [x] `scripts/review/file_tasks.py::wire_declared_dependency` —
      перенос заявленного в нативный граф ПРИ создании issue; номер вне
      открытого пула — не линкуется, предупреждение; отсутствие строки
      (старый формат) — деградация без падения.
- [x] `scripts/lib/task_deps.py::fetch_pool` — параметр `include_body`
      (одно поле в том же запросе, не второй проход); тесты на оба
      режима (с телом/без).
- [x] Тесты (`test_file_tasks.py`, новый файл — тестов на file_tasks.py
      не было вовсе): `wire_declared_dependency` на всех случаях
      (нет строки, «ничем», валидный номер, невалидный номер, смешанный
      список), консолидация `open_pool_issues`/`open_task_titles` (один
      проход, не два), интеграционный `main()` — заведение + перенос в
      граф, dry-run без мутаций. Мутационно доказано: снятие проверки
      `n not in open_numbers` красит 2 теста.

## 4. Fail loud на рассинхроне тела и графа

- [x] `scripts/lib/declared_deps.py` (новый) — `declared_candidates`
      (эвристика формулировок, окно `[^#).\n]{0,20}` — не пересекает
      границу скобки/предложения, живой замер показал ложный матч на
      более широком окне `\D{0,20}`), `find_desync` (сравнение с
      `blocked_by_open`, никогда не пишет).
- [x] Тесты (`test_declared_deps.py`) — прод-форма (реальные фрагменты
      тел #258/#243/#111 этого репозитория, не выдуманные), мутация
      (снятие исключения `)`/`.` из окна красит именно 2 теста — те, что
      ловят пересечение границы скобки).
- [x] `.github/workflows/repo-ci.yml` — шаг «Рассинхрон тела задачи и
      графа зависимостей»: `::warning::` на каждую находку, не
      проваливает гейт `test` (обоснование в design.md, развилка в/г —
      цена ложных срабатываний эвристики и квота DO `rows_read`, #320).
- [x] Живой прогон `declared_deps.py check` на этом репозитории после
      миграции (задача 1) — 0 находок.

## 5. Газ — проверка по факту

- [x] Прочитан `free_task.py`: `blocked_by_open` не используется НИ в
      фильтре свободы, НИ в приоритете — `blockedBy` сегодня НЕ тормоз
      для выбора задачи (см. design.md, раздел «Газ»). Единственный
      реально читаемый механизм — `blocking_open`, пересчитывается
      живьём (`_is_open` на каждом запросе, без кэша) — газ уже есть и
      уже доказан мутацией в #361. Физическое снятие ребра `blockedBy`
      при закрытии блокирующей стороны не требуется — состояние issue
      уже единственный источник правды.

## 6. Дельта-спека

- [x] `openspec/changes/fill-dependency-graph-enforcement/proposal.md`,
      `design.md`, `specs/journal-tasks-hands/spec.md`.

## Явно не задачи этого change (см. proposal.md Out)

- Превращение `blockedBy` в EXCLUSION-тормоз (воркер не берёт
  заблокированную задачу) — отдельное решение владельца, #361 оставил
  его сознательно нерешённым.
- Автоматический перенос произвольной прозы ЛЮБОГО issue (не только
  структурного блока `ЗАДАЧА:`) в граф — тот же отказ, что в design.md
  #361.
