# Tasks: priority-impact-weight (#1178)

- [x] `scripts/lib/free_task.py`: `IMPACT_LABEL`, `_is_impact`, новый
      уровень 1 в `issue_priority_key` (5 уровней), `graph_is_empty`
      (учитывает `impact_label`), `priority_reason` (называет метку и
      номер тира). Обратная совместимость: `prioritized_free`/
      `priority_top`/`oldest_free`/CLI `oldest-free`/`why` — та же
      сигнатура для вызывающей стороны (`scheduler.py`, `task.sh`,
      `issue-create`).
- [x] Тесты (`test_free_task.py`): новый уровень обгоняет `area:process`
      при большем номере (живой случай), работает без `area:process`,
      не обгоняет уровень 0, тай-брейк по номеру внутри нового уровня
      сохранён, два теста переименованы под сдвинутые номера уровней
      (`тир 1`→`тир 2`, `тир 2`→`тир 3`).
- [x] Доказательство мутацией: `impact_label` на несуществующую метку —
      возврат к старому порядку и на юнит-фикстуре, и на живом пуле;
      возврат фикса — новый порядок. Вывод обоих прогонов записан в PR.
- [x] Замер на живом пуле (350 свободных задач, 2026-09-14): очередь
      ДО/ПОСЛЕ, ранги всех шести барьерных задач в обе стороны — см.
      proposal.md.
- [x] Документация: `docs/agents/LABELS.md` (новая строка `impact:system`,
      правка строки `area:process`), `docs/agents/PROTOCOL.md` (оба
      места с описанием уровней приоритета), `openspec/specs/
      journal-tasks-hands.md` (п.26, дельта этого change).
- [x] Метка `impact:system` создана на репозитории и проставлена всем
      шести барьерным задачам (#1161, #1172, #1149, #1141, #1160, #1109).
- [x] Регресс потребителей: `python -m pytest scripts/lib -q` (786
      тестов), `python -m pytest scripts/orchestra -q` (1098 тестов) —
      все зелёные без изменений в их коде.
- [x] Задача о принудительном явном ответе про `impact:system` при
      заведении (по образцу `--not-process-ack` для `area:process`,
      #695) заведена ОТДЕЛЬНО (#1181), не расширяет scope этого change.
