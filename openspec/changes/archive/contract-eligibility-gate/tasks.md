# Задачи: contract-eligibility-gate

## 1. Пригодность задачи — одно место правды

- [x] `scripts/orchestra/contract_check.py::task_eligibility_problems` —
      существует как issue (не PR) / открыта / несёт `task` / не несёт
      `blocked`; PR-ветка (`"pull_request" in issue`) — ранний выход, дальше
      нечего проверять.
- [x] `main()` — авто-назначение, сверка занятости и поиск дубликата PR
      выполняются только в ветке `else` после пустого списка пригодности;
      непригодна → причины уходят в `problems`, ни один изменяющий вызов над
      issue_number не выполняется.
- [x] `BLOCKED_LABEL` — новое условие непригодности, отдельная константа (не
      импорт `scheduler.py` ради одной строки).

## 2. Гвардия класса (тест + мутация)

- [x] `scripts/orchestra/test_contract_check.py` — `FakeGh` считает изменяющие
      вызовы (`mutating_calls`); три прод-формы непригодности (закрыта, нет
      метки `task`, `blocked`) доказывают отсутствие `.../assignees` среди
      них; контрольный тест доказывает, что легитимное авто-назначение на
      пригодную свободную задачу по-прежнему происходит.
- [x] Мутация доказана вручную: откат `task_eligibility_problems`/раннего
      `else` к исходной форме (проверка `state`/`labels` рядом с
      авто-назначением без общего условия) красит три теста; фикс возвращён,
      все семь тестов зелёные.

## 3. Соседи того же класса

- [x] `scripts/orchestra/scheduler.py` прочитан целиком — паттерн не найден
      (см. проверку класса в proposal.md); пересечение с #357/PR #358 не
      трогается.
- [x] `scripts/review/check_pr.py`, `scripts/review/ai_review.py`,
      `scripts/lib/free_task.py` — прочитаны, тот же класс не найден.

## 4. Дельта-спека

- [x] `openspec/changes/contract-eligibility-gate/proposal.md` — п.17.2 также
      добавлен прямо в главную спеку `openspec/specs/journal-tasks-hands.md`
      (тот же приём, что `pr-task-resolver`, #292: без этого главная спека не
      знала бы о гейте, хотя код и гвардия его уже проводят).

## Явно не задачи этого change

- Пересмотр семантики авто-назначения автора PR (все агенты — один логин) —
  предложение в отчёте разработчика, решение за владельцем.
- Разбор/переоткрытие #131 и работа PR #359 по существу — отдельная узкая
  задача, заведена отдельно (не входит в этот change).
