Задача: #830.

# Tasks: pm-pool-groom-deterministic

## Этап 1 — детерминированный груминг (этот PR)

- [x] 1.1 `scripts/orchestra/stall_detector.py`: `groom_auto_tasks(repo, now, lines)` —
      (а) закрытие стоп-задачи, чей отпечаток устойчиво не воспроизводится
      (`RESOLVE_QUIET_MINUTES`, переходные маркеры на `WATCHDOG_ISSUE`, симметрично
      `STALL_PERSIST_MINUTES`); (б) закрытие дубля по отпечатку среди открытых
      `auto-detected`, оставляя младший номер открытым. Пропускает задачи с назначенным
      исполнителем.
- [x] 1.2 `scheduler.py`: вставить вызов `groom_auto_tasks` между `detect_and_act` и
      `escalate_stale_auto_tasks` (одна try/except область, тот же `stall_hard_failure`).
      Восемь мест `test_scheduler.py`, стубящих `detect_and_act`/`escalate_stale_auto_tasks`
      на уровне `main()`, дополнены стубом `groom_auto_tasks` (иначе интеграционные тесты
      бьют настоящим `gh api` за новым маршрутом `issues?labels=auto-detected`).
- [x] 1.3 Тесты `scripts/orchestra/test_stall_detector.py` (13 новых, 41 всего в файле):
      закрытие неактуальной стоп-задачи, НЕ-закрытие живой (отпечаток продолжает
      воспроизводиться идентичной уликой 5 пульсов подряд — гвардия отвергнутого
      варианта из proposal.md), маркер «снова жив» при возврате отпечатка, сброс
      устойчивости тишины при повторном появлении отпечатка (мутационная гвардия
      класса #248 — красный тест при мутации подтверждён), закрытие дубликата,
      дубликат/задача с assignee не тронуты, эскалация владельцу проверяется только
      после того, как груминг отработал на том же пульсе (интеграционный тест с
      состоянием, закрытие грумом убирает issue из следующего списка открытых).
      Мутация ключевой развилки (`_silence_episode_start`) и early-exit по `active`
      доказана вручную (снят фикс → тест красный → фикс возвращён). Полный прогон
      `scripts/orchestra/` (768 тестов) и `test_scheduler.py` (304 теста) — зелёный.
- [x] 1.4 Докстринг модуля `stall_detector.py` — предохранитель 6 описывает место
      вызова, порядок и границу с этапом 2 (одно место правды, `docs/agents/
      PROTOCOL.md` про внутренности stall_detector текста не несёт и не правится
      второй копией).

## Этап 2 — pm-судейский груминг (НЕ в этом PR, отдельная задача)

Для того, что детерминированно не решить (устарела ли задача по существу, семантический
дубль, приоритет) — диспатч pm-роли (`~/.claude/agents/pm.md`) вместо немедленной эскалации
владельцу. Требует решения: отдельный узкий канал диспатча или переиспользование
существующего воркера с ролью pm — назвать явно в design.md этапа 2, не плодить второй
воркер без обоснования.
