- [x] `rate_guard` читает применяемый счётчик (заголовки `X-Ratelimit-*`
      реального запроса), а не только `rate_limit`.
- [x] Решение — по меньшему из двух остатков; оба числа печатаются рядом в
      КАЖДОМ прогоне, чтобы следующий 403 дал сравнение в логе.
- [x] Не прочитался применяемый счётчик или неизвестен репозиторий — громкий
      отказ, а не «квота ок».
- [x] Зонд получил 403 «rate limit exceeded» — это ИЗМЕРЕНИЕ, а не отказ
      измерения: обычный `skip=true`, код 0, `::warning::`, job зелёный.
      Иначе в худшем состоянии объекта дефект #1437 воспроизводился бы
      детерминированно (находка ai-review PR #1451, блокирующая).
- [x] Гейт в `repo-ci.yml::test` переехал первым шагом job'а; три шага,
      читавших API выше гейта, встали за его условие.
- [x] Гвардия `api_quota_gate_guard` + поведенческие тесты на настоящих
      YAML-файлах; мёртвая запись реестра краснеет.
- [x] Долг измерен числом (29 шагов в 17 workflow) и разложен по трём
      РАЗНЫМ причинам, а не под одной общей формулировкой.
- [x] `docs/research/21-github-actions.md`: «не подтверждено» замера
      2026-09-09 закрыто, прежний вывод «лимит и правда 5000» снят как
      неверный с указанием, почему.
- [x] Имя шага-гейта объявлено один раз (`QUOTA_GATE_STEP_NAME`) и читается
      тестом оттуда — было две копии строки.
