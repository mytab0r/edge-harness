# Задачи: soft-failure-digest (#1121)

- [x] Модуль `scripts/orchestra/soft_failure_digest.py`: канал A (аннотации
      Checks API) + канал B (условные шаги, Jobs API), дедуп по открытым
      issues `soft-failure`, суточный потолок, гейт цикла.
- [x] Тесты `scripts/orchestra/test_soft_failure_digest.py` — прод-форма
      фикстур (реальные аннотации `NO_ADAPTER`/`UNKNOWN_MODEL`, реальный
      multi-suite check-runs ответ), мутационное доказательство фильтра
      `check_suite_id`.
- [x] Шаг «Дайджест мягких отказов (#1121)» в `.github/workflows/
      orchestra.yml` (continue-on-error, тот же приём, что соседние гвардии
      пульса).
- [x] Метка `soft-failure` в `docs/agents/LABELS.md` (гвардия
      `test_label_registry.py` зелёная).
- [x] Живой прогон на реальных данных — не менее пяти находок из #1121,
      результат приложен в PR.
- [x] Пагинация `fetch_runs` (класс #308) — orchestra.yml/ai-review.yml
      несут >100 прогонов/сутки, однострочный запрос молча терял их целиком
      (найдено первым же живым прогоном, до мержа).
- [x] Ревью PR #1136 (rework, три блокера) — доведено:
      1) восстановлена запись `ALLOWED_SINGLE_PAGE_CALLS` для
         `repo_invariants.check_conveyor_gate_phantom_pause`, утерянная
         неаккуратным `cp` при синхронизации worktree (класс: копировать
         файл вместо git-мержа теряет чужие параллельные правки);
      2) квота-гейт: `if: steps.quota.outputs.skip != 'true'` на шаге
         workflow + `quota_sufficient()`/`SOFT_FAILURE_QUOTA_THRESHOLD`
         внутри скрипта (общий порог orchestra калиброван не под смету
         этого скана);
      3) честный сигнал полного провала чтения (`workflows_ok == 0`) —
         не heartbeat, не «группы не найдены», ненулевой код возврата.
- [x] Классификация класса причины (эвристика, находка координатора
      #1115) — переиспользован `pulse_guard.classify_failure_cause`,
      столбец «класс» в таблице и теле задачи.
- [x] Некритичные находки ревью PR #1136 (первый круг) — доведены:
      дельта-спека описывает канал B двумя фильтрами (as-is, не расходится
      с кодом), честно называет границу «job Jobs API сопоставляется с
      YAML по ключу name, расхождение — молчаливая пустая группа, не
      фикс».
- [x] Критерий 4 (инвариант 19, `check_continue_on_error_readers`) и
      критерий 5 (окно завершённых суток `merge_throughput`/
      `do_rows_read_pct`) — изначально заведены отдельными задачами
      (#1154/#1155, беклог первого круга ревью, `soft-failure` вне
      приоритета пула — #1156), реализованы в этом же PR после того, как
      `repo_invariants.py`/`scheduler.py` освободились от параллельных PR;
      #1154/#1155/#1156 закрыты ссылкой на этот PR — см. `openspec/changes/
      archive/soft-failure-digest/proposal.md`, раздел «Критерии 4 и 5».
- [x] Регрессия ревью (второй круг): восстановлен блок тестов инварианта
      18 (`check_pipeline_status_marker_impersonation`) в
      `test_repo_invariants.py` — потерян при добавлении тестов инварианта
      17 (замена вместо дополнения, тот же класс «cp вместо git-мержа
      теряет чужие правки», что уже был найден на allowlist-записи в
      первом круге).
- [x] Конфликт с main (третий круг, сутки спустя, 27 слияний): номер
      инварианта 17 (`check_continue_on_error_readers`) занят параллельным
      #1076 (`check_frontend_deploy_stale`, слит раньше) — переехал на
      первый свободный, 19 (18 уже занят #1101). Переименовано во всех
      носителях: `repo_invariants.py` (реестр, докстринг, ветка
      `build_report`), `test_repo_invariants.py` (заголовок секции,
      докстринги, ожидание в `test_live_debt_snapshot_known_violations`),
      дельта-спека, `proposal.md`. Восстановленный во втором круге блок
      тестов инварианта 18 оказался ДУБЛЁМ — main независимо восстановил
      те же тесты в правильном месте файла (тем же PR #1101/#1077) —
      дубль-хвост удалён, осталась одна копия. `classify_failure_cause`/
      `INFRA_ERROR_SIGNATURES` (#1177) не тронуты этим PR — контракт
      функции не менялся, только список сигнатур в `pulse_guard.py`
      вырос, читается дайджестом тем же вызовом.
