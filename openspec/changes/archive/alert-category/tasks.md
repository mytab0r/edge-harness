- [x] Реестр категорий объявлен ОДИН раз (`scripts/lib/alert_category.py`);
      четыре категории с описанием «что сюда относится» у каждой.
- [x] Две языковые копии (bash для `task.sh`, TypeScript для воркера) — их
      расхождение с каноном красит CI, тест читает все три исходника.
- [x] `category` — обязательный именованный аргумент отправителя; незнакомая
      категория падает ДО сетевого вызова.
- [x] Все девять точек отправки называют категорию явно, по смыслу каждой.
- [x] Гвардия по исходнику: вызов без категории красит CI. Разбор — AST, а не
      регулярка (многострочный вызов).
- [x] Гвардия проверяет РАЗРЕШИМОСТЬ значения, не только наличие аргумента —
      закрыто по живой ошибке автора в этом же PR.
- [x] Адресация отделена от категоризации: префикс сегодня, тема потом, точки
      отправки не меняются.

Доводка по ai-ревью PR #1462 (4 блокирующие находки, все закрыты):

- [x] Три из четырёх вызовов `telegram_report` в `task.sh` остались со старой
      сигнатурой — отчёты об эскалации и провалах не уходили вовсе
      (`alert_prefix` на тексте возвращает 1, `|| true` глотает): переведены
      на `decision`/`pipeline`.
- [x] В `harness.ts` декорирован только `#tickStorageReadyAlert`;
      `#tickPulseAlert` (инцидент пульса и recovery) шлёт голый текст —
      оба вызова переведены на `decorateAlert("breakage", …)`.
- [x] Гвардия покрывала один отправитель из трёх: добавлены структурные
      проверки bash (`telegram_report`, слово из реестра первым аргументом)
      и TypeScript (`sendMessage` с `decorateAlert` и литералом из реестра);
      граница по `answerCallbackQuery`/`editMessageText` названа в спеке.
- [x] Гвардия принимала любое имя атрибута у `alert_category` без сверки с
      реестром (`PIEPLEINE` проходила CI, падая AttributeError'ом в
      рантайме) — имя константы сверяется, тест с опечаткой добавлен.
- [x] Замер отправителей сведён к одному структурному подсчёту
      (7+1+1 Python, 4 bash, 4 TS sendMessage) во всех файлах PR; ложное
      «сигнатура обратно совместима» в докстринге `send_telegram` вычеркнуто.
- [x] Заглушка смока пишет вызов Telegram построчно — многострочный текст
      отчёта (категория первой строкой) рвал запись, красный прогон repo-ci
      стоял на исправном проде: переносы экранируются в логе заглушки,
      вызов не упрощён; в смок добавлен ассерт на видимую категорию.
