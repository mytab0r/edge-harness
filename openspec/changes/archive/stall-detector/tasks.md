# Задачи: stall-detector

## 1. Детектор устойчивого простоя (#201)

- [x] `scripts/orchestra/stall_detector.py` — отпечаток из структурированных
      строк отчёта пульса, дедупликация по машиночитаемому ключу, порог
      устойчивости с маркером в `WATCHDOG_ISSUE`, суточный потолок, эскалация
      владельцу. Видимый результат: `python -m pytest
      scripts/orchestra/test_stall_detector.py -q` зелёный, мутационное
      доказательство каждой гвардии (см. PR #248).
- [x] Вставка в `scripts/orchestra/scheduler.py::main()` — один вызов
      `detect_and_act(...)` и один `escalate_stale_auto_tasks(...)` после
      сборки отчёта пульса, до `summary(lines)`.
- [x] Реестр меток `docs/agents/LABELS.md` — строка `auto-detected` с
      явно названным газом (дедупликация + суточный потолок + эскалация),
      гвардия `scripts/lib/test_label_registry.py` зелёная.
- [x] Предохранитель устойчивости переживает закрытие прошлой автозадачи по
      тому же отпечатку — маркер первого наблюдения в `WATCHDOG_ISSUE`
      сбрасывается по факту закрытия задачи (`_closed_task_reset_times`),
      иначе блип после решения мгновенно заводит вторую задачу (находка
      AI-ревью PR #248, воспроизведена и закрыта мутационным тестом
      `test_blip_after_task_closed_does_not_bypass_persistence`).
- [x] Шаг `.github/workflows/repo-ci.yml` на `test_stall_detector.py` —
      находка AI-ревью PR #248 (третий раунд): гвардии детектора были
      написаны и никогда не запускались в CI. Видимый результат: имя файла
      в выводе pytest живого прогона job `test`, не только факт добавления
      шага.
- [x] Комментарий-дубликат при живом дубликате отпечатка пишется только на
      ИЗМЕНИВШУЮСЯ улику — находка AI-ревью PR #248 (третий раунд): маркер
      с хэшем улики в теле комментария (тот же приём, что маркер эскалации),
      доказано мутацией
      (`test_detect_and_act_same_evidence_twice_posts_one_comment`,
      `test_detect_and_act_changed_evidence_posts_second_comment`).

## Явно не задачи этого change (см. proposal.md «Что вне рамок»)

- Живой прогон приёмки на проде — юнит-тесты на прод-форме данных с
  мутационным доказательством достаточны для этого уровня.
