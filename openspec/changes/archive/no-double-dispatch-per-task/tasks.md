- [x] Гейт «по этой задаче уже идёт прогон» в доводке по находкам ai-review
      (`dispatch_ai_review_rework`).
- [x] Тот же гейт в расшивке конфликта (`dispatch_conflict_rework`) — задача
      называет именно его, первая редакция фикса его пропустила (находка
      ai-review PR #1449, исполнена на стенде).
- [x] Тот же гейт в адресном прогоне при закрытом WIP-гейте
      (`dispatch_worker`, ветка `inputs[task]=<target>`) — третья дверь,
      найдена прочёсом всех вызовов `worker.yml/dispatches`: их четыре,
      адресных три, гейт стоял в одном.
- [x] Дедуп номеров прогонов (`dict.fromkeys`): маркер git-шага пишется на
      каждый шаг, один прогон оставлял пачку одинаковых номеров и столько же
      GET состояния (замечание ai-review PR #1449 без блокировки).
- [x] Живость — по признаку завершённости (`status != "completed"`), не
      белым списком неконечных статусов: `pending` пропускался (#1260
      закрывал этот класс в соседней ветке того же файла).
- [x] Отказ чтения статуса поднимается наверх, ⚠️-строку ставит вызывающий —
      прежняя редакция глотала его и отдавала тот же None, что «живых нет».
- [x] Проводка закреплена тестами у ВСЕХ ТРЁХ диспатчеров: мутация «снять
      гейт» краснеет адресно, тесты самой функции её отсутствия не замечали.
- [x] Честный потолок назван: окно до первого git-шага и снятие аренды
      перед адресным диспатчем.
