# Дельта-спека: журнал (journal-tasks-hands) — итог запуска воркера по классам отказа (#1286)

Меняет п.29: «иначе → … job красный» разбивается на классы — провайдерный
отказ возвращает задачу в пул и оставляет job зелёным, НАШ отказ и неизвестные
классы остаются громко красными. Живой отказ — прогон worker.yml 34893177035
(задача #1286).

## MODIFIED: Конвейер (итог запуска воркера)

~~29. Итог запуска воркера оценивается по видимому артефакту, а не по коду
    возврата: открытый PR → комментарий в задачу (ссылка + хвост отчёта) и
    Telegram, job зелёный; эскалация по playbook (метка `blocked` — то, что
    есть только у владельца) → комментарий и Telegram, job зелёный; иначе →
    комментарий с хвостом лога и Telegram, job красный. Все наружные тексты
    проходят маскирование секретов до отправки.~~

29. Итог запуска воркера оценивается по видимому артефакту, а не по коду
    возврата: открытый PR → комментарий в задачу (ссылка + хвост отчёта) и
    Telegram, job зелёный; эскалация по playbook (метка `blocked` — то, что
    есть только у владельца) → комментарий и Telegram, job зелёный; отказ
    провайдера (`quota_exhausted`, `rate_limit_retry_budget_exceeded`,
    `all_providers_exhausted` — #422/#727) → задача возвращается в пул сразу
    (`release-full`), комментарий с разбором по классам (#1307) и
    машиночитаемым маркером `[воркер: провайдерный отказ (#1286)]`
    (WORKER_PROVIDER_REFUSAL_MARKER, scripts/worker/task.sh — пишет;
    scripts/orchestra/scheduler.py::worker_run_was_provider_refusal — читает),
    Telegram, job зелёный (#1286: чужая вина не красит job — красные прогоны
    воркера кормят предохранитель диспатча #226 и задерживают восстановление
    после сброса квоты, #205; маркер различает такой прогон в авто-доводке
    оркестратора — dispatch_ai_review_rework ведёт его по инфра-пути, попытка
    не считается честной пробой находок); НАШ отказ (`prompt_too_long`,
    #1315) и любая иная причина → комментарий с хвостом лога и Telegram,
    job красный. Все наружные тексты проходят маскирование секретов до
    отправки. Контракт кода возврата гвардится исполнением настоящего блока
    task.sh: scripts/worker/test/provider-exhaustion-exit-code.smoke.sh.
    Честная граница: серию зелёных прогонов при долгом сбое провайдеров
    предохранитель не видит — автостопа нет, сигнал владельцу — комментарий
    и Telegram на каждую пробу.
