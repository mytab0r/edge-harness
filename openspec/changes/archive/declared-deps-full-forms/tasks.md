# Tasks: declared-deps-full-forms

## Разбор форм записи (scripts/lib/declared_deps.py)

- [x] `_FORM_FIELD_RE` устойчив к уровню заголовка (`#{1,6}`) и к числу
      переводов строки (`\n+`) — фиксирует только текст заголовка.
- [x] `_BLOCKING_FIELD_RE`/`blocking_field_numbers` — обратное поле «Что
      блокирует», та же семантика `None`/`[]`/список.
- [x] `declared_blocked_by` — объединение заголовка «Чем блокируется» и
      инлайн-конвенции «БЛОКИРУЕТСЯ: …» (`ai_review.blocked_by_numbers`).
- [x] Тесты на прод-форму (реальные тела #601/#679/#373), мутация доказана
      (снят фикс — тест покраснел).

## Перенос в граф (auto_wire)

- [x] `auto_wire` использует `declared_blocked_by` для собственного поля
      каждой issue И `blocking_field_numbers` вторым проходом (обратное
      направление) — идемпотентно на пересечении обеих деклараций.
- [x] Тесты на реверс-направление и на отсутствие повторного сетевого
      вызова при двусторонней декларации одной пары.

## Инвариант рассинхрона (repo_invariants.py)

- [x] `check_declared_deps_mismatch` — обе стороны (missing/stale), issue
      без поля не проверяется.
- [x] Вписан в `build_report` (секция 9 — исправлено ревью PR #711, major:
      номер в этом файле изначально коллидировал с существующим инвариантом
      8, `check_wasted_ai_review_runs`), не в CI_GATING (живой долг —
      #679), `GATING_RELEASE_CONDITION[9]` назван.
- [x] Тесты на прод-случай #679 (stale) и на реверс-направление (missing).

## Документация

- [x] `.github/ISSUE_TEMPLATE/task.yml`: описание «Что блокирует» тоже
      говорит про автоперенос.
- [x] Комментарий на #626 (уже разрешена #529/PR#537, эта задача её не
      переоткрывает).

## Проверка

- [x] `PYTHONUTF8=1 python -m pytest scripts/ -q` — зелёный, кроме
      известного `scripts/review/test_ai_review.py::test_redact_*`
      (артефакт Windows, не регрессия).
