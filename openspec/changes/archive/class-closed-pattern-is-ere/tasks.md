- [x] Ловушки PCRE-в-ERE распознаются ДО `git grep`; сообщение называет
      конструкт, его поведение (падает / молчит) и замену.
- [x] Тихие ловушки (`\d`, `\D`, ленивые квантификаторы) закрыты наравне с
      громкими — замер показал, что они не падают вовсе.
- [x] GNU-расширения, которые работают (`\w`, `\s`, `\b`, `{n,m}`,
      POSIX-классы), НЕ отвергаются: газ важнее полноты списка.
- [x] Экранированные метасимволы не дают ложных срабатываний (`\(`, `\*?`,
      `\\d`) — обход посимвольный, по чётности слэшей.
- [x] Носитель — `__post_init__`, а не вызывающий код: claim с ловушкой
      нельзя собрать руками.
- [x] Замер записан в `docs/research/25-git-grep-regex-dialect.md` со
      стендом, дословным выводом и заменами; INDEX обновлён.
- [x] Мутация: снять проверку — тест краснеет адресно.
