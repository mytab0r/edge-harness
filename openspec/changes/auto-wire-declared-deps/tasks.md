# Задачи: auto-wire-declared-deps

## 1. Единый цикл переноса

- [x] `scripts/lib/task_deps.py::wire_dependencies` — цикл «пропустить
      номер вне открытого пула с меткой `task`/ссылку на себя, иначе
      `add_dependency` + лог», переиспользуемый обоими вызывающими.
- [x] `scripts/review/file_tasks.py::wire_declared_dependency` сведён к
      вызову `task_deps.wire_dependencies` (поведение не меняется).
- [x] `scripts/lib/test_task_deps.py` — тесты `wire_dependencies`
      (открытый номер линкуется, вне пула/самоссылка пропускается с
      логом, пустой вход — без вызовов).
- [x] `scripts/review/test_file_tasks.py` — существующие тесты
      `wire_declared_dependency` зелёные без изменений (регресс-гвардия
      на рефакторинг).

## 2. Структурный разбор поля формы

- [x] `scripts/lib/declared_deps.py::form_field_numbers` — `None` (поля
      нет), `[]` (явно «ничем»/пусто), иначе список номеров.
- [x] `scripts/lib/test_declared_deps.py` — тесты на реальном теле issue
      #529 (прод-форма, поле «ничем» + несвязанные номера в другой
      секции тела не попадают в результат), канонический рендер формы,
      регистронезависимость «ничем».
- [x] Мутация (снять структурный разбор, вернуть наивный поиск `#N` по
      всему телу) — доказано красным на реальном теле #529 и трёх других
      тестах, откат мутации возвращает зелёный (см. proposal.md/отчёт).

## 3. Автоперенос и CLI

- [x] `scripts/lib/declared_deps.py::auto_wire` — обход открытого пула,
      перенос недостающих номеров поля через `wire_dependencies`,
      идемпотентно (уже присутствующие в `blocked_by_open` не трогает
      повторно — без лишнего сетевого вызова).
- [x] CLI `declared_deps.py wire <owner/repo> [label]`.
- [x] `scripts/lib/test_declared_deps.py` — тесты `auto_wire` (линкует
      недостающее, идемпотентность без сетевых вызовов, игнорирует
      отсутствие поля и «ничем») и CLI `wire`.

## 4. Проводка в CI

- [x] `.github/workflows/repo-ci.yml` — шаг «Автоперенос поля «Чем
      блокируется» в граф (#529)», только `if: github.event_name ==
      'push'`, после существующего шага детектора рассинхрона (#371).
      Честно (исправлено по находке AI-ревью PR #537; прежнее «не второй
      проход пула» было неверно): это ВТОРОЙ проход пула на тот же push —
      `wire` отдельный процесс со своим `fetch_pool`, общего состояния с
      `check` нет; цена принята, см. design.md развилку «в».
- [x] `permissions` job `test` — точечно `issues: write` (было `read`,
      унаследовано с уровня файла), с обоснованием рядом в комментарии.
- [x] Живая проверка (не только тесты): создана и сразу закрыта временная
      issue #532 с полем «Чем блокируется: #529» — `declared_deps.py
      wire` реально проставил `addBlockedBy` на настоящем репозитории,
      повторный запуск — no-op (идемпотентность), связь и временная
      issue убраны после проверки.

## 5. Честная оценка охвата (не пропустить)

- [x] Замер живым `task_deps.py pool` на день внедрения: из 135 открытых
      задач с меткой `task` поле «Чем блокируется» несёт ровно ОДНА (эта
      же, #529) — остальные 134 заведены до #387 и поля не несут. Прямой
      эффект на день внедрения — 0 новых связей; эффект накопится по мере
      появления НОВЫХ задач через шаблон. Зафиксировано в proposal.md, не
      скрыто.

## 6. Ревью PR #537 (rework)

- [x] Доки↔код (блокирующая находка ревью): design.md (развилка «в»),
      proposal.md (Out) и этот файл (§4) утверждали «не второй проход
      пула», а `wire` — отдельный процесс со своим
      `fetch_pool(include_body=True)`: на каждый push в main пул
      проходится дважды (check + wire). Формулировка во всех трёх местах
      заменена на честную «второй проход на push, не на тик расписания —
      принят сознательно»; в design.md добавлен газ пересмотра при замере.
- [x] `auto_wire` фильтрует закрытые/внепуловые номера и самоссылки ДО
      `wire_dependencies`: `blocked_by_open` содержит только открытых
      блокеров, поэтому после закрытия блокера каждый push снова давал
      `missing` и предупреждение «связь НЕ поставлена» без ограничения
      частоты — хотя связь стоит, а ссылка на закрытую «не влияет ни на
      что» (find_desync тот же случай молчит намеренно). Дельта-спека,
      сценарий 4, уточнён; тесты на реальном сценарии закрытия блокера
      (мутация фильтра доказана красным).
- [x] proposal.md (Scope In): «поведение и тексты предупреждений не
      меняются» → текст предупреждения обобщён на оба пути, названо что
      именно изменилось (старое сообщение несло «БЛОКИРУЕТСЯ: #N»).
- [x] Тест-фикстура `test_cli_wire_prints_linked_numbers` притворялась,
      что закрытый номер линкуется (двойник `wire_dependencies` возвращал
      всё без фильтра) — фикстура приведена к реальному графу.
