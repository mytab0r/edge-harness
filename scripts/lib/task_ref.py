#!/usr/bin/env python3
"""Номер задачи в тексте PR/issue — одно место правды (#187).

Класс проблемы: `f"#{n}" in text` и `line.split("#")` — сравнение подстрокой.
`#18` содержится в `#180`, `#181`, `#5180` — вылезло на живом прогоне orchestra
(33570081734): контракт спутал PR #185 (задача #182) с задачей #18. Число,
на которое ссылается `#N`, обязано иметь границу с обеих сторон: слева не
цифра (иначе `#18` внутри `#5180`), справа не цифра (иначе `#18` внутри
`#180`).

Импорт — importlib по файлу (паттерн claim_task/review_labels): скрипты
запускаются как файлы, не как пакет.

Два разных вопроса, две разные группы функций — не путать (#259, живой
замер: ai_review.py:353 отвечал на вопрос «какая задача упомянута первой в
прозе» там, где нужен ответ на «какая задача у этого PR», и путал #253 с
#120, #248 с #119, #247 с #43, #263 с #4):

  «Какая задача у этого PR» (узкая, для решений вида «этот PR закрывает
  задачу N», «контекст ревью», «занята ли задача») — `resolve_pr_task`,
  единственный источник — имя agent-ветки (`task_from_branch`). Решение
  владельца 2026-09-06 (#394, вторым заходом после `pr_task_candidates`):
  тело PR для этого вопроса не читается вообще — ни первой строкой, ни
  каким-либо иным способом. `None` — «у PR нет задачи из пула» (боты, PR без
  agent-ветки), ожидаемый ответ, не ошибка. PR, чья ветка называет уже
  закрытую задачу (докрытие/реворк), не «переориентируется» неявным чтением
  тела — заводится новая ветка `scripts/git/task-branch <M>-<slug>` на новый
  номер, см. design.md contract-task-from-branch.

  «Упоминает ли текст задачу» (широкая, ЛЮБОЕ вхождение `#N`) —
  `extract_task_refs`/`references_task`. Годится только там, где широта
  осознанна и названа в докстринге места вызова. Фактические потребители на
  сегодня — четыре: три в scheduler.py (`pr_references_issue`/`reap_stale`,
  `after_merge`, `partial_disclaimer`) и один в `repo_invariants.py`
  (`declared_change_task`, инвариант 4, другой вопрос — «какая задача у
  этого change», не «у этого PR», см. её докстринг). `reap_stale`/
  `after_merge`/`partial_disclaimer` — не потерять живой PR, не снять
  чужой замок, не пропустить дисклеймер — ошибиться в сторону «не трогать»
  там дешевле, чем в сторону «занята». `unhealthy_pulls` (#286) сюда больше
  НЕ входит: там та же широта била в другую сторону — не «не тронули
  вовремя», а «сняли аренду ЧУЖОЙ задачи» по случайному упоминанию номера
  в прозе тела чужого PR (живой случай: #90 дважды лишилась аренды из-за
  тела PR #181, 2026-09-04) — сопоставление PR → задача там сузилось до
  `resolve_pr_task` (имя agent-ветки). Новый вызов широкой пары ВНЕ
  scheduler.py/repo_invariants.py — красный тест
  `scripts/lib/test_task_ref_usage_guard.py`, не только докстринг (и то
  только файлово, см. её ALLOWED_WIDE_USAGE — построчного носителя нет).

Функции `declared_tasks`/`declares_task` (декларация — первая непустая
строка тела, правило #251/#312) удалены 2026-09-06 вместе с
`pr_task_candidates`: решение владельца убрало тело PR как источник для
вопроса «какая задача у PR» целиком, а других вызывающих у этой пары не
было. История класса (подстрочное совпадение #187, перенос строки прозы
#251, HTML-комментарий шаблона #312) — в git-истории этого файла и в
openspec/changes/contract-task-from-branch/proposal.md, не потеряна, просто
больше не активный код.
"""

import re

# Слева: не должно быть цифры прямо перед `#`-числом (запрещает `5#18`→`518`
# и `#5` перед `180`, т.е. случай "числовой хвост предыдущего номера слипся").
# Справа: не должно быть цифры сразу после числа (запрещает `#180` матчить `#18`).
_TASK_REF_RE = re.compile(r"(?<!\d)#(\d+)(?!\d)")

# GitHub HTML-комментарии (`<!-- … -->`) из тела PR/issue при рендере НЕ
# вырезает — они просто невидимы человеку (тот же факт, что раньше объяснял
# поведение удалённого `declared_tasks`, #312/#398) — здесь нужен для того же
# вырезания перед поиском директив GitHub ниже. Комментарий может занимать
# несколько строк — вырезаем блоком (DOTALL), а не построчно.
_HTML_COMMENT_RE = re.compile(r"<!--.*?-->", re.DOTALL)

# ── Директива GitHub Closes/Fixes/Resolves #N (#423) ─────────────────────────
#
# Класс проблемы: contract_check.py раньше решал «это директива» по
# ПОЗИЦИИ подстроки (`line.lstrip().lower().startswith(("closes", ...))`),
# а не по тому, распознаёт ли её GitHub. Живой случай — PR #415: тело в
# момент прогона `contract` 02:46:46 (2026-09-06) содержало голое
# `Closes #413 руками не пишу (контракт запрещает `Closes/Fixes/Resolves` в
# теле PR) — issue закрою отдельно после мержа.` — здесь красная проверка
# была ПРАВОМЕРНОЙ (см. ниже), но после правки (перенос `Closes/Fixes/
# Resolves` в inline-код, голый номер вынесен в `#413` отдельно) старый код
# на позиции строки продолжал бы читать любую строку, случайно начавшуюся
# с этого слова, директивой — не проверяя, попало ли слово в код или нет.
#
# Проверено экспериментом (`gh api -X POST markdown -f mode=gfm -f
# context=mytab0r/edge-harness`, официальный рендерер GitHub, 2026-09-06 —
# без побочных эффектов, ничего не создаёт):
#   - `Closes #413 руками не пишу` → `#413` рендерится ссылкой
#     `<a class="issue-link" ...>#413</a>` рядом со словом Closes — реальная
#     директива, GitHub закроет issue при мерже (contract был прав красить
#     именно этот текст);
#   - `Этот PR closes #413 окончательно.` (слово НЕ в начале строки) — та же
#     ссылка: позиция в строке роли не играет, GitHub ищет по всему телу,
#     значит и старая проверка `startswith` пропускала настоящие директивы
#     не в начале строки (ложноотрицательно, второй дефект того же класса);
#   - `` `Closes #413` `` (вся фраза в inline-коде) и то же самое в
#     тройных ``` — рендерится как `<code>`/`<pre>` без единой ссылки —
#     GitHub это директивой не считает;
#   - `<!-- Closes #413 -->` — HTML-комментарий из рендера вырезается целиком
#     (GitHub его не показывает и, соответственно, не парсит как директиву);
#   - `> Closes #413` (блокцитата) — ссылка ПОЯВЛЯЕТСЯ, GitHub цитаты не
#     исключает из разбора директив, поэтому строки с `>` здесь НЕ вырезаются
#     (в отличие от инлайн-кода/код-блока/HTML-комментария);
#   - `Closes\n#413` (мягкий перенос строки, без пустой строки между) —
#     ссылка ПОЯВЛЯЕТСЯ (тот же `<p>`, `\n` рендерится `<br>`) — директива
#     пересекает перенос строки, находка ревью #429: `_CLOSING_KEYWORD_RE`
#     обязан это учитывать, а не только «в пределах строки»;
#   - `` `Closes\n#10` `` (инлайн-код с мягким переносом ВНУТРИ спана) —
#     рендерится ОДНИМ `<code>Closes #10</code>` (перенос строки внутри
#     код-спана становится пробелом, CommonMark), не директива — но пустая
#     строка ВНУТРИ обратных кавычек (`` `Closes\n\n#10` ``) спан рвёт:
#     GitHub закрывает абзац раньше закрывающей кавычки, кавычка остаётся
#     буквальным символом, а #10 снова становится директивой (тот же
#     эксперимент, см. соседние тесты) — `_INLINE_CODE_RE` обязан пересекать
#     ОДИНОЧНЫЙ перенос строки внутри спана, но не пустую строку.
#
# Значит директива = ключевое слово рядом с `#N` (через пробел/перенос
# строки и необязательное `:`) ГДЕ УГОДНО в прозе тела, кроме inline-код-спана,
# fenced-код-блока и HTML-комментария.
# Fenced-блок: строка (после ≤3 пробелов отступа) из 3+ бэктиков/тильд,
# всё до строки с ТЕМ ЖЕ (или длиннее) открывающим забором — вырезаем целиком,
# включая примеры, которые случайно совпали бы с директивой внутри.
_FENCED_CODE_RE = re.compile(r"^ {0,3}(`{3,}|~{3,}).*?^ {0,3}\1", re.DOTALL | re.MULTILINE)
# Inline-код-спан: `code` (в общем виде N бэктиков подряд ... тот же N
# бэктиков). Пересекает ОДИНОЧНЫЙ перенос строки (мягкий перенос внутри
# спана — легитимный CommonMark, проверено рендерером выше), но не пустую
# строку: `\n(?!\n)` матчит перенос, только если следующий символ не тоже
# `\n` — на пустой строке альтернатива проваливается, и спан обязан
# закрыться до неё (или не матчится вовсе, как настоящий CommonMark).
_INLINE_CODE_RE = re.compile(r"(`+)(?:(?!\1)[^\n]|\n(?!\n))+?\1")
# Ключевое слово GitHub + `#N`: не привязано к началу строки (проверено
# экспериментом выше), с необязательным `:` и переносом строки между словом
# и номером (мягкий перенос — та же директива, см. эксперимент выше).
_CLOSING_KEYWORD_RE = re.compile(
    r"(?i)\b(?:close[sd]?|fix(?:e[sd])?|resolve[sd]?)\b\s*:?\s*#\d+"
)


def closing_keyword_refs(text: str) -> list[str]:
    """Директивы `Closes/Fixes/Resolves #N`, которые GitHub реально
    распознаёт как закрывающие issue при мерже — не любое вхождение слова
    (#423). Вырезаются перед поиском: fenced-код-блоки, inline-код-спаны,
    HTML-комментарии (именно в этом порядке — см. ниже) — только там
    директива невидима GitHub (см. докстринг констант выше, факт проверен
    экспериментом с рендерером GitHub). Блокцитаты НЕ вырезаются: GitHub
    директиву в них видит.

    Возвращает список найденных фрагментов (для сообщения об ошибке) с
    внутренними переносами строк, схлопнутыми в пробел (находка ревью #429:
    директива-мягкий-перенос — законный матч, но `\n` внутри фрагмента ломает
    форматирование `::error::`-аннотации CI и комментария на PR — на сам
    поиск директивы схлопывание не влияет, оно только для читаемости
    сообщения); пустой список — директив нет, `contract_check.py` пропускает
    PR."""
    if not text:
        return []
    # Порядок вырезания важен (живой ложноположительный случай — собственное
    # тело PR #429, находка ревью #439): fenced/inline-код-спаны — ПЕРЕД
    # HTML-комментарием, не после. GitHub парсит код-спаны первым проходом —
    # их содержимое непрозрачно для всего остального, включая HTML-комментарий
    # ВНУТРИ спана (`` `<!-- Closes #413 -->` `` — это буквальный текст в
    # примере, а не настоящий комментарий). Старый порядок (HTML-комментарий
    # первым) резал ЛЮБОЕ вхождение `<!-- … -->` до того, как код-спаны
    # успевали защитить свой текст: `` `<!-- Closes #413 -->` `` превращался
    # в пустой код-спан `` `` `` (парная пустая пара бэктиков не матчится
    # _INLINE_CODE_RE вовсе — в нём всегда нужен хотя бы один символ контента),
    # и один бэктик из этой пары оставался бесхозным, ложно закрывая СЛЕДУЮЩИЙ
    # реальный код-спан дальше в тексте — а освобождённая директива внутри
    # него утекала в прозу непрозрачной.
    prose = _FENCED_CODE_RE.sub("", text)
    prose = _INLINE_CODE_RE.sub("", prose)
    prose = _HTML_COMMENT_RE.sub("", prose)
    return [" ".join(match.split()) for match in _CLOSING_KEYWORD_RE.findall(prose)]


def extract_task_refs(text: str) -> list[int]:
    """Все номера задач `#N` из текста, с границей числа с обеих сторон.

    Порядок — как в тексте, дубликаты не схлопываются (вызывающий код решает
    сам, нужна ли уникальность и первый элемент).
    """
    if not text:
        return []
    return [int(match.group(1)) for match in _TASK_REF_RE.finditer(text)]


def references_task(text: str, task_number: int) -> bool:
    """Упоминает ли текст задачу `#task_number` — без ложных срабатываний на
    `#<task_number><ещё цифры>` или `<ещё цифры><task_number>` (класс #187)."""
    return task_number in extract_task_refs(text)


# ── Резолвер «PR → задача» (#259, #394) — одно место правды, один источник ───
#
# Единственный источник — имя ветки `agent/<N>-<slug>`: её создаёт ТОЛЬКО
# `scripts/git/task-branch` (enforced-правило репозитория, гвардия
# .githooks/pre-commit), номер в ней подделать случайно нельзя. Тело PR для
# этого вопроса не читается вовсе — ни первой строкой, ни GitHub-связью
# closes/fixes (closingIssuesReferences), ни любым другим способом.
#
# Раньше здесь стояли ещё два запасных источника — оба сняты решением
# владельца 2026-09-06:
#
#   closingIssuesReferences — формальная связь GitHub PR↔issue. Проверено
#   фактом: живой GraphQL-запрос по последним 8 слитым PR репозитория вернул
#   пустой список у каждого (Closes/Fixes/Resolves запрещены contract_check.py
#   — GitHub нечего связывать формально). Источник никогда не был заполнен и
#   ни разу не вызывался с реальным значением (только в тестах) — снят как
#   мёртвый, а не как нежелательный.
#
#   Декларация первой строкой тела (`declared_tasks`) — держалась ради
#   переориентации в реворке: если ветка называла уже закрытую задачу, а
#   тело первой строкой объявляло новую, резолвер брал её из тела
#   (`pr_task_candidates`, #394, первый заход). Владелец отменил это без
#   исключений: «номер задачи берётся ТОЛЬКО из имени ветки… тело PR не
#   читается вообще». PR, чья ветка называет закрытую задачу, не
#   переориентируется неявно — заводится НОВАЯ ветка `scripts/git/task-branch
#   <M>-<slug>` на новый номер (живые PR на 2026-09-06, требуют новой ветки:
#   #388, #384, #359, #167 — см. proposal.md contract-task-from-branch).
#
# Проза по всему телу (extract_task_refs) как источник НЕ использовалась и не
# используется: это была ошибка ai_review.py:353 (см. докстринг модуля).

_BRANCH_TASK_RE = re.compile(r"^agent/(\d+)-")


def task_from_branch(branch: str) -> int | None:
    """Номер задачи из имени agent-ветки `agent/<N>-<slug>` — единственный
    источник резолвера (см. блок выше). `None`, если ветка не этой формы
    (ручной пуш, боты вроде dependabot — «задачи нет», а не ошибка)."""
    if not branch:
        return None
    match = _BRANCH_TASK_RE.match(branch)
    return int(match.group(1)) if match else None


def resolve_pr_task(pull: dict) -> int | None:
    """Задача, которой принадлежит PR — единственная точка входа для этого
    вопроса (см. докстринг модуля и блок выше). `None` — «у PR нет задачи»,
    ожидаемый ответ для ботов (dependabot) и PR без agent-ветки, не ошибка.

    `pull` — объект PR из `gh api repos/.../pulls/N` (нужен только
    `head.ref`; тело не читается вовсе, см. блок выше)."""
    branch = ((pull.get("head") or {}).get("ref")) or ""
    return task_from_branch(branch)
