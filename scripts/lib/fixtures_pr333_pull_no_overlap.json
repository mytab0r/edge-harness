{
  "before_pull": [
    {
      "filename": ".githooks/pre-commit",
      "patch": "@@ -12,6 +12,46 @@ if [ \"$branch\" = \"main\" ]; then\n   echo \"ПРЕДУПРЕЖДЕНИЕ: коммит прямо в main — в этом репо main меняется только через PR (docs/agents/PROTOCOL.md).\" >&2\n fi\n \n+# Гвардия «дерево == задача» (#332). Класс инцидента: агент по задаче #320\n+# правил файлы в дереве оркестратора (.claude/worktrees/pr-workflow-sessions-8f1ab5)\n+# на чужой ветке agent/258-...; в тот же день другой агент нашёл там же\n+# незакоммиченные диффы чужой ветки #297, а библиотекарь переключил главное\n+# дерево на свою ветку из-под работавшего там агента. Дисциплина «не путай\n+# деревья» не сработала трижды за сутки — здесь она enforced механически, а\n+# не по памяти. Правило: коммит в agent/<N>-<slug> разрешён ТОЛЬКО из\n+# рабочего дерева, чьё имя (basename top-level пути) начинается с того же\n+# числа <N>-. Проверка чисто локальная (без сети) — идёт раньше свежести\n+# base ниже, чтобы не ждать fetch там, где откажем и так.\n+#\n+# Исключение для CI (находка ревью #333): task-branch в режиме\n+# GITHUB_ACTIONS=true намеренно переключает ветку в одноразовом чекауте\n+# воркера, не заводя worktree (scripts/worker/task.sh уже сам изолирован на\n+# один прогон — см. комментарий там же). Без этого исключения гвардия\n+# отклоняет ПЕРВЫЙ же коммит автономного воркера в CI: каталог называется\n+# «edge-harness», а не «<N>-...», и правило падает не на инцидент #332\n+# (спутанные деревья агента/человека локально), а на штатный путь конвейера.\n+# Пропуск по переменной обязан быть ВИДЕН (находка ревью #333): молча\n+# выключенная гвардия — тот silent-wrong, против которого она построена.\n+# Переменная могла утечь в локальное окружение (sourced CI-env, экспорт в\n+# профиле, act) — тогда гвардия выключилась бы без следа в любом месте;\n+# строка в stderr оставляет след в любом логе, откуда бы переменная ни взялась.\n+if [[ \"$branch\" =~ ^agent/([0-9]+)(-|$) ]]; then\n+  if [ \"${GITHUB_ACTIONS:-}\" = \"true\" ]; then\n+    echo \"ПРЕДУПРЕЖДЕНИЕ: гвардия «дерево == задача» отключена: GITHUB_ACTIONS=true — коммит в $branch не проверяется на своё рабочее дерево (штатно только в одноразовом чекауте CI-воркера).\" >&2\n+  else\n+    task_num=\"${BASH_REMATCH[1]}\"\n+    worktree_name=$(basename \"$(git rev-parse --show-toplevel)\")\n+    if [[ ! \"$worktree_name\" =~ ^${task_num}(-|$) ]]; then\n+      echo \"ОШИБКА: ветка $branch — рабочее дерево этой задачи не здесь.\" >&2\n+      echo \"  Текущее дерево: «$worktree_name» (имя должно начинаться с «$task_num-»).\" >&2\n+      echo \"  Не путай чужую ветку со своим деревом (класс #332).\" >&2\n+      echo \"  Заведи/найди дерево этой задачи (команда печатает путь, коммить оттуда):\" >&2\n+      echo \"    scripts/git/task-branch ${branch#agent/}\" >&2\n+      exit 1\n+    fi\n+  fi\n+fi\n+\n if ! git rev-parse --verify -q refs/remotes/origin/main >/dev/null; then\n   echo \"ОШИБКА: нет refs/remotes/origin/main — сделай 'git fetch origin' и повтори.\" >&2\n   exit 1",
      "sha": "cfddf8f1c0673e1a37d3ea9a3bd37360e6fa564a",
      "status": "modified"
    },
    {
      "filename": ".github/workflows/repo-ci.yml",
      "patch": "@@ -167,6 +167,12 @@ jobs:\n       - name: Провайдер/модель LLM — одно место правды (vars), без зашитых дефолтов\n         run: bash scripts/lib/test/provider-default.guard.sh\n \n+      # Гвардия «дерево == задача» (#332): .githooks/pre-commit отклоняет коммит\n+      # в agent/<N>-* не из своего рабочего дерева — прогоняет РЕАЛЬНЫЙ файл\n+      # гвардии на синтетических git-worktree, не пересказ поведения.\n+      - name: pre-commit отклоняет коммит в agent/<N>-* не из своего дерева\n+        run: bash scripts/git/test/worktree-guard.test.sh\n+\n       # Кампания замера хвоста dispatch (задача #4): чистая логика — метрики,\n       # критерий покрытия, сводка, вставка в доку — кормится прод-формой данных.\n       - name: Тесты кампании dispatch-tail\n@@ -675,16 +681,19 @@ jobs:\n       - name: Bash-скрипты валидны\n         run: |\n           shopt -s nullglob\n-          for f in scripts/*.sh scripts/hands/*.sh scripts/worker/*.sh scripts/worker/test/*.sh scripts/lib/*.sh scripts/lib/test/*.sh scripts/plugins/*.sh scripts/plugins/test/*.sh scripts/review/*.sh scripts/git/task-branch scripts/git/pr-create scripts/git/test/*.sh; do\n+          for f in scripts/*.sh scripts/hands/*.sh scripts/worker/*.sh scripts/worker/test/*.sh scripts/lib/*.sh scripts/lib/test/*.sh scripts/plugins/*.sh scripts/plugins/test/*.sh scripts/review/*.sh scripts/git/task-branch scripts/git/pr-create scripts/git/test/*.sh .githooks/*; do\n             [ -f \"$f\" ] || continue\n             bash -n \"$f\"\n           done\n           echo \"bash ok\"\n \n       # Проверка на входе в task-branch (не гвардия постфактум): задача обязана\n       # существовать/быть открытой/нести task/не нести blocked до создания\n-      # ветки — прогон на реальном git с фейковым gh, сеть не нужна.\n-      - name: Smoke task-branch — проверка задачи на входе\n+      # ветки — прогон на реальном git с фейковым gh, сеть не нужна. Тот же\n+      # файл проверяет и рабочее дерево (#332): вне CI заводит/переиспользует\n+      # .claude/worktrees/<task>, в CI (GITHUB_ACTIONS=true) старое поведение\n+      # не меняется.\n+      - name: Smoke task-branch — проверка на входе и рабочее дерево\n         run: bash scripts/git/test/task-branch.test.sh\n \n       # Проверка на входе в scripts/git/pr-create (не гвардия постфактум,",
      "sha": "e203272bcb62217217c9b3bf91facf4aba638f0e",
      "status": "modified"
    },
    {
      "filename": ".gitignore",
      "patch": "@@ -18,3 +18,9 @@ __pycache__/\n \n # Локальный кеш npm при установке зависимостей на раннере (не артефакт репо).\n .npm-cache/\n+\n+# Рабочие деревья агентов (#332/#333) — заводятся scripts/git/task-branch\n+# локально на диске, в историю репозитория не попадают (находка ревью #333:\n+# без этой строки `git add -A` в главном дереве затягивает их как embedded\n+# gitlink).\n+.claude/worktrees/",
      "sha": "f61a924697e97cd21de2aa3e04d039d5243f9546",
      "status": "modified"
    },
    {
      "filename": "AGENTS.md",
      "patch": "@@ -20,20 +20,28 @@\n   `python3 scripts/lib/claim_task.py claim N` (замок + назначение, #121): отказ\n   «задача занята» = её уже делает другой канал, бери следующую. Для НОВОЙ задачи\n   аренду берёт сам `task-branch` (пункт ниже) — руками claim вызывать не нужно;\n-  вручную он нужен только при доводке уже открытого PR (чекаут существующей\n-  ветки, `task-branch` там не вызывается): отказ «занята» при доводке может\n+  вручную он нужен только при доводке уже открытого PR (в CI `task-branch` на\n+  существующую ветку откажет ДО аренды, вне CI сам найдёт/заведёт рабочее\n+  дерево, но аренду не берёт — возьми сам): отказ «занята» при доводке может\n   означать твою же аренду — если держатель ты сам (транспорт пробросил\n   `LEASE_ALREADY_CLAIMED`), это не чужой канал, работай дальше.\n - **Ветка задачи создаётся только скриптом** `scripts/git/task-branch <N>-<slug>`.\n   Он же берёт аренду задачи (#121) перед созданием ветки (#356) — отказ аренды\n   («занята») останавливает создание ветки, назвав, где искать держателя; аренда\n-  под уже существующую ветку не берётся вовсе (доводка существующей — чекаут,\n-  не повторный запуск скрипта). Голый `git switch -c … origin/main` запрещён:\n+  под уже существующую ветку не берётся вовсе: в CI доводка существующей —\n+  чекаут (повторный запуск откажет ДО аренды), вне CI повторный запуск сам\n+  найдёт/заведёт рабочее дерево и напечатает путь, аренду для доводки возьми\n+  руками. Голый `git switch -c … origin/main` запрещён:\n   оркестратор сливает PR на сервере, и локальный\n   main протухает молча — так появляются ветки от старой базы. Скрипт доказывает свежесть\n   через `git ls-remote` и ставит гвардию `.githooks/pre-commit` (`core.hooksPath`), которая\n   перед каждым коммитом сверяется с сервером и блокирует коммит от протухшей базы\n-  (лечится `git rebase origin/main`).\n+  (лечится `git rebase origin/main`). Вне CI тот же скрипт заводит или\n+  переиспользует рабочее дерево задачи `.claude/worktrees/<N>-<slug>` и печатает\n+  путь: коммит в `agent/<N>-*` разрешён только из дерева, чьё имя начинается с\n+  `<N>-`, — ту же гвардию это проверяет перед каждым коммитом (класс #332),\n+  отказ лечится вызовом `scripts/git/task-branch <N>-<slug>` и работой из\n+  напечатанного пути.\n - **Прямой пуш в main** не проходит обязательные проверки. Только ветка `agent/<N>-<slug>` и PR.\n - **Слияния делает оркестратор** (workflow `orchestra`), по одному PR за проход и до\n   нескольких за запуск (цикл слияний, #297).",
      "sha": "700425089e410d08a145ff531313cf7fa2952fab",
      "status": "modified"
    },
    {
      "filename": "docs/agents/PROTOCOL.md",
      "patch": "@@ -142,13 +142,26 @@ escalate`, второй канал не заводится): комментар\n    ещё не взял транспорт) — отказ аренды останавливает создание ветки, называя\n    где искать держателя (assignee и комментарий 🔒 issue), что делать (взять\n    другую задачу) и выход, если замок твой же с прошлого умершего прогона\n-   (`claim_task.py release N`); ветка, которая уже существует (локально или на\n-   origin), отклоняется ДО аренды — доводка существующей идёт чекаутом, не\n-   повторным запуском скрипта. Поломка пути аренды (`claim_task.py` rc=2) —\n+   (`claim_task.py release N`); в CI (GITHUB_ACTIONS=true) ветка, которая уже\n+   существует (локально или на origin), отклоняется ДО аренды — доводка\n+   существующей идёт чекаутом, не повторным запуском скрипта. Вне CI\n+   существующая ветка отказом не считается: до аренды она получает своё\n+   рабочее дерево (переиспользование или усыновление, абзац ниже) — повторный\n+   запуск не валился бы о собственный замок и не вешал осиротевший\n+   (класс #360, интеграция #332). Поломка пути аренды (`claim_task.py` rc=2) —\n    предупреждение, ветка создаётся без аренды; полный офлайн/`gh` недоступен\n    громко отказывает раньше — гвардия эпика (#376), офлайн-режима у входа в\n    задачу нет. Голый `git checkout -b … origin/main` запрещён.\n    Локальный main — только зеркало origin (`git pull --ff-only`), коммитить в него нельзя.\n+   Вне CI `task-branch` сам заводит или переиспользует рабочее дерево\n+   `.claude/worktrees/<N>-<slug>` и печатает путь — работай оттуда, не в общем\n+   чекауте. Правило одной фразой (класс #332: три чужих коммита в чужом дереве\n+   за сутки — #320 в дереве оркестратора на чужой ветке, незакоммиченный дифф\n+   #297 там же, главное дерево переключено из-под работавшего агента):\n+   **коммит в `agent/<N>-*` разрешён только из дерева, чьё имя начинается с\n+   `<N>-`** — `.githooks/pre-commit` проверяет это перед каждым коммитом и при\n+   несовпадении отказывает, называя ветку, текущее дерево и команду\n+   `scripts/git/task-branch <N>-<slug>`, которая заведёт/найдёт правильное.\n 4. Прямые пуши в main отклоняются защитой **молча под `push -q`** — проверяй пуши без\n    `-q`. На конфликтном PR проверки не запускаются вовсе («Checks awaiting conflict\n    resolution») — сигнал перебазироваться, а не поломка CI. main ушёл вперёд, пока ты",
      "sha": "2ae54d26d21fbe8bc47d214c578916f643312ac2",
      "status": "modified"
    },
    {
      "filename": "docs/agents/WORKER-PLAYBOOK.md",
      "patch": "@@ -61,15 +61,22 @@\n    (`python3 scripts/lib/claim_task.py release N`) и повтори `task-branch` —\n    чужой живой замок так снимать нельзя, это уход к другому каналу, бери\n    другую задачу. Ветка `agent/N-slug` уже существует (повторный запуск,\n-   чужой незаброшенный прогон) — `task-branch` откажет ДО аренды: доводка\n-   существующей ветки — чекаут (`git fetch origin <ветка> && git checkout -B\n-   <ветка> origin/<ветка>`), не повторное создание. Если на задачу уже\n+   чужой незаброшенный прогон) — повторного создания не бывает: в CI\n+   `task-branch` откажет ДО аренды, вне CI тот же вызов\n+   `scripts/git/task-branch N-slug` сам найдёт/заведёт рабочее дерево задачи\n+   `.claude/worktrees/<N>-<slug>` (#332) и напечатает путь — доводка ведётся\n+   оттуда, потому что гвардия «дерево == задача» (`.githooks/pre-commit`)\n+   коммит в `agent/<N>-*` из главного чекаута отклонит. Ответил «локальная\n+   ветка разошлась с origin/<ветка>» — синхронизируйся по напечатанной команде\n+   (`git fetch origin <ветка>`, затем `git branch -f` или `rebase`) и повтори.\n+   Если на задачу уже\n    открыт PR без\n    исполнителя (`task_ref.resolve_pr_task` — единственный источник это имя\n    agent-ветки, тело PR не читается вовсе, решение владельца 2026-09-06,\n-   `scripts/lib/task_ref.py`, #394) — чекаут его ветки, новый PR не открывать;\n-   этот путь `task-branch` не вызывает — аренду для него возьми сам\n-   (`claim_task.py claim N`) до чекаута (см. исключение в пункте 2 выше).\n+   `scripts/lib/task_ref.py`, #394) — доводи его ветку из дерева задачи\n+   (предыдущий абзац: `task-branch` его найдёт/заведёт), новый PR не\n+   открывать; этот путь `task-branch` не вызывает — аренду для него возьми сам\n+   (`claim_task.py claim N`) до работы (см. исключение в пункте 2 выше).\n    Гвардия свежести (та же, что у `task-branch`) — при «main уехал»\n    `git rebase origin/main` и повтори.\n 4. Перед кодом: `docs/INDEX.md` → релевантные research/спеки; архитектурное",
      "sha": "3b569e007aba93c05efc1143ab21f0c2e750d5c5",
      "status": "modified"
    },
    {
      "filename": "openspec/changes/worktree-branch-guard/proposal.md",
      "patch": "@@ -0,0 +1,57 @@\n+# worktree-branch-guard: рабочее дерево задачи — инвариант, а не дисциплина\n+\n+Задача: #332. PR: #333.\n+Дельта-спека: `specs/journal-tasks-hands/spec.md` — дополнение п.28 «Захват\n+задачи воркером» инвариантом рабочего дерева.\n+\n+## Класс закрываемой ошибки\n+\n+За сутки три инцидента одного класса (контекст #332): агент по задаче #320\n+правил файлы в дереве оркестратора\n+(`.claude/worktrees/pr-workflow-sessions-8f1ab5`) на чужой ветке\n+`agent/258-...`; там же найдены незакоммиченные диффы чужой ветки #297;\n+главное дерево переключено на чужую ветку из-под работавшего агента.\n+Дисциплина «не путай деревья» держалась на памяти трижды за сутки — теперь\n+она enforced механически.\n+\n+## Решение\n+\n+1. `.githooks/pre-commit` — гвардия «дерево == задача»: коммит в\n+   `agent/<N>-*` отклоняется, если имя рабочего дерева (basename top-level\n+   пути) не начинается с `N-`. Отказ называет ветку, текущее дерево и\n+   лечащую команду. Пропуск для CI (`GITHUB_ACTIONS=true`, одноразовый\n+   чекаут воркера называется `edge-harness`) печатает ПРЕДУПРЕЖДЕНИЕ в\n+   stderr — тихого обхода гвардии нет.\n+2. `scripts/git/task-branch` вне CI заводит или переиспользует дерево\n+   `<корень репозитория>/.claude/worktrees/<N>-<slug>` и печатает путь\n+   агенту — легаси-разнобой имён (`246-lease-claim` на ветке `agent/121-...`,\n+   `pr-workflow-sessions-8f1ab5` на `agent/323-...`) перестаёт\n+   воспроизводиться. Повторный вызов на существующую ветку, усыновление\n+   осиротевшей и ветки, существующей только на origin (серверная голова, не\n+   тихая локальная копия от origin/main), стоят ДО аренды (класс #360: ничего\n+   не захватив, ничего не оставляет осиротевшим; вместо аренды — напоминание\n+   взять её вручную) и отказывают с командой синхронизации,\n+   если локальная голова разошлась с `origin/<ветка>` — PR мог подвинуть\n+   другой канал.\n+3. Существующие деревья не удаляются и не переименовываются: единственный\n+   `git worktree prune` вычищает только админ-запись, чей каталог уже стёрт\n+   с диска, и кричит об этом.\n+\n+## Доказательство\n+\n+- `scripts/git/test/worktree-guard.test.sh` — РЕАЛЬНЫЙ файл хука на живых\n+  git-репозиториях и worktree: совпадение проходит, чужое дерево отклоняется\n+  с внятным сообщением, вне `agent/*` правило неприменимо. Мутация: снять\n+  блок гвардии — случай 2 краснеет.\n+- `scripts/git/test/task-branch.test.sh`, случаи 8–14: заведение дерева,\n+  переиспользование без дубля, CI-режим, стык «CI-переключение → коммит под\n+  настоящим хуком», отказ при расхождении с `origin/<ветка>` на путях\n+  переиспользования и усыновления, свои незапушенные коммиты не отсекаются.\n+  Мутация: снять `branch_diverged_from_origin` — случаи 12/13 краснеют.\n+\n+## Газ\n+\n+Гвардия останавливает только коммит не из своего дерева; возврат движения —\n+одна команда `scripts/git/task-branch <N>-<slug>`, напечатанная в самом\n+отказе. Автоматического возврата не нужно: отказ сам называет единственное\n+лечащее действие, и оно не требует чужих рук.",
      "sha": "5fc974a1997b31d4a00c0d85b24992fa31c0a3d4",
      "status": "added"
    },
    {
      "filename": "openspec/changes/worktree-branch-guard/specs/journal-tasks-hands/spec.md",
      "patch": "@@ -0,0 +1,54 @@\n+# Дельта-спека: журнал (journal-tasks-hands) — рабочее дерево задачи (#332)\n+\n+Дополняет п.28 (захват задачи воркером, #121) — не отменяет его, добавляет\n+инвариант рабочего дерева, недостающий в описанном там поведении.\n+\n+## MODIFIED: Автономный воркер (конвейер пула)\n+\n+28. Захват задачи воркером — атомарная аренда (ADR 0006, #121):\n+    `scripts/lib/claim_task.py claim` серверно создаёт замок\n+    `refs/locks/task-N` и тем же вызовом ставит назначение и след в задаче.\n+    Назначение — видимость, а не защита; защита от гонки каналов — замок,\n+    проигравший гонку получает отказ. Работа идёт в ветке `agent/N-slug` от\n+    свежего origin/main (вход — `scripts/git/task-branch`); если на задачу\n+    уже открыт PR без исполнителя, воркер доводит его ветку, второй PR не\n+    открывает (#245). `task-branch` перед созданием ветки отказывает, если N —\n+    эпик (`is_epic_issue`, тот же признак, что использует приёмка #335):\n+    критерий готовности эпика распределён по нескольким стройкам и недостижим\n+    в одном PR, отказ называет газ — взять узкую задачу конкретной стройки\n+    (#376, класс воспроизведён: PR #162, 44 раунда ревью на ветку, привязанную\n+    к эпику #77). Исполняет задачу DSH headless; транспорт\n+    (scripts/worker/task.sh) не принимает за агента содержательных решений.\n+    Пул кандидатов (до фильтра свободы и аренды) собирается через\n+    `scripts/lib/task_deps.py` (GraphQL) — REST-листинг не отдаёт\n+    `blockedBy`/`blocking`, одним пагинированным запросом, не двумя\n+    расходящимися (REST для списка + GraphQL для графа).\n+\n+    **Инвариант рабочего дерева (#332):** коммит в `agent/<N>-*` разрешён\n+    только из рабочего дерева, чьё имя (basename top-level пути) начинается\n+    с того же числа `N-`. Проверяет `.githooks/pre-commit` перед каждым\n+    коммитом, чисто локально и до гвардии свежести base; отказ называет\n+    ветку, текущее дерево и лечащую команду `scripts/git/task-branch\n+    <N>-<slug>` — она вне CI заводит или переиспользует\n+    `<корень репозитория>/.claude/worktrees/<N>-<slug>` и печатает путь\n+    агенту, так что легаси-разнобой имён деревьев (`246-lease-claim` на\n+    ветке `agent/121-...`, `pr-workflow-sessions-8f1ab5` на\n+    `agent/323-...`) больше не воспроизводится. В CI\n+    (`GITHUB_ACTIONS=true`) гвардия отключается с ПРЕДУПРЕЖДЕНИЕМ в stderr:\n+    одноразовый чекаут воркера называется `edge-harness`, и без исключения\n+    отказ падал бы на штатный путь конвейера, а не на инцидент #332\n+    (спутанные деревья агента/человека локально). Повторный вызов\n+    `task-branch` на существующую ветку переиспользует или усыновляет\n+    дерево до аренды (класс #360: ничего не захватив, ничего не оставляет\n+    осиротевшим) и отказывает с командой синхронизации, если локальная\n+    голова разошлась с `origin/<ветка>` — PR мог подвинуть другой канал\n+    (оркестратор снимает исполнителя с конфликтного PR и диспатчит нового\n+    воркера); свои незапушенные коммиты (origin — предок локальной головы)\n+    расхождением не считаются. Ветка, существующая только на origin (чужой\n+    PR, локальное дерево потеряно), усыновляется СЕРВЕРНОЙ головой — молча\n+    заводить одноимённую локальную ветку от origin/main значило бы тихо\n+    подменить чужую работу; аренда на путях переиспользования и усыновления\n+    не берётся (класс #360), взамен печатается напоминание взять её вручную.\n+    Существующие деревья скрипт не удаляет и не\n+    переименовывает: единственный `git worktree prune` вычищает только\n+    админ-запись, чей каталог уже стёрт с диска.",
      "sha": "14f7418e67c17de270c7d0109d0e36235286c7bd",
      "status": "added"
    },
    {
      "filename": "scripts/git/task-branch",
      "patch": "@@ -8,7 +8,17 @@\n #   1) fetch + авторитетная сверка ls-remote — тихо упавший fetch base не выдаёт;\n #   2) самовосстановление core.hooksPath — гвардия свежести обязательна всегда.\n #\n-# Использование: scripts/git/task-branch <номер>-<slug>\n+# Рабочее дерево (#332): агенты заводили `.claude/worktrees/<...>` вручную кто\n+# как — разнобой имён стал причиной трижды за сутки (агент правил чужую ветку\n+# в чужом дереве, дифф чужой задачи затирался). Здесь дерево заводится/\n+# переиспользуется по одному правилу (`.githooks/pre-commit` его же и\n+# проверяет): `<корень репозитория>/.claude/worktrees/<номер>-<slug>`. В CI\n+# (GITHUB_ACTIONS=true) поведение старое — переключение ветки в текущем\n+# каталоге: воркер (scripts/worker/task.sh) уже работает в одноразовом\n+# изолированном чекауте на один прогон, заводить там ещё и worktree незачем\n+# и меняло бы каталог работы у уже написанного пайплайна.\n+#\n+# Использование: scripts/git/task-branch <номер>-<slug>   (без изменений)\n set -euo pipefail\n \n task=\"${1:-}\"\n@@ -114,18 +124,196 @@ if [ -n \"$epic_task_num\" ]; then\n   fi\n fi\n \n-# Ветка уже существует — повторно не заводим и аренду под неё НЕ берём (находка\n-# ревью PR #360): иначе claim проходит, а `git switch -c` ниже падает на\n-# существующем имени — замок остаётся висеть до TTL-сборщика (24 ч), ровно тот\n-# осиротевший замок, от которого этот скрипт защищает. Проверка ДО claim'а и\n-# ДО флага транспорта: существующая ветка — это доводка, у неё другой вход\n-# (чекаут, WORKER-PLAYBOOK.md «Ветка»), и аренда там берётся вручную до чекаута.\n-if git show-ref --verify --quiet \"refs/heads/agent/$task\" \\\n-   || git ls-remote --heads origin \"refs/heads/agent/$task\" | grep -q .; then\n-  echo \"ОШИБКА: ветка agent/$task уже существует — task-branch заводит только НОВЫЕ ветки.\" >&2\n-  echo \"  Доводи существующую: git fetch origin agent/$task && git checkout -B agent/$task origin/agent/$task,\" >&2\n-  echo \"  аренду для доводки возьми вручную (python3 scripts/lib/claim_task.py claim N) ДО чекаута.\" >&2\n-  exit 1\n+branch=\"agent/$task\"\n+\n+# Печать ключевых граблей инфраструктуры в момент старта задачи, а не когда-нибудь\n+# потом «если вспомнить прочитать» (задача #323): task-branch — один из ДВУХ\n+# входов в agent-ветку (второй — доводка уже открытого PR в scripts/worker/task.sh,\n+# он тоже зовёт print_infra_digest напрямую, минуя этот файл, — см. #326 находка 1).\n+# Текст — одно место правды в scripts/gh/infra_digest.sh, не копия здесь.\n+# Функция, а не копии: стартов задачи три (усыновление осиротевшей ветки,\n+# CI-переключение, новое дерево) — путь печатается последним в каждом, грабли\n+# идут перед ними. На переиспользовании (повторный вызов держателем) дайджест\n+# не повторяется: агент его уже видел при старте, ему нужен путь.\n+print_digest() {\n+  source \"$(dirname \"${BASH_SOURCE[0]}\")/../gh/infra_digest.sh\"\n+  print_infra_digest\n+}\n+\n+# Сверка локальной ветки с origin/<branch> (находка ревью #333): локальная\n+# голова могла отстать или разойтись с серверной — PR мог подвинуть другой\n+# канал (оркестратор снимает исполнителя с конфликтного PR и диспатчит нового\n+# воркера). Гвардия свежести в .githooks/pre-commit сверяет только origin/main\n+# и этот стык не видит — коммит поверх чужой головы валился бы уже только на\n+# пуше. Отказ — до работы, с командой синхронизации. Свои незапушенные коммиты\n+# (origin — предок локальной головы) расхождением НЕ считаются: это штатная\n+# середина работы держателя.\n+branch_diverged_from_origin() {\n+  git show-ref --verify -q \"refs/remotes/origin/$branch\" || return 1\n+  local local_sha remote_sha\n+  local_sha=$(git rev-parse \"refs/heads/$branch\")\n+  remote_sha=$(git rev-parse \"refs/remotes/origin/$branch\")\n+  [ \"$local_sha\" = \"$remote_sha\" ] && return 1\n+  git merge-base --is-ancestor \"$remote_sha\" \"$local_sha\" && return 1\n+  return 0\n+}\n+\n+if [ \"${GITHUB_ACTIONS:-}\" = \"true\" ]; then\n+  # CI: одноразовый изолированный чекаут на прогон — worktree не нужен,\n+  # старое поведение (переключение в текущем каталоге) сохранено как есть.\n+  # Отказ «ветка уже существует» (#360) живёт и здесь: иначе claim (ниже)\n+  # проходит, а `git switch -c` (ниже) падает на существующем имени — замок\n+  # остаётся висеть до TTL-сборщика (24 ч), ровно тот осиротевший замок,\n+  # от которого отказ защищает. Проверка ДО claim'а и ДО флага транспорта:\n+  # существующая ветка — это доводка, у неё другой вход.\n+  if git show-ref --verify --quiet \"refs/heads/$branch\" \\\n+     || git ls-remote --heads origin \"refs/heads/$branch\" | grep -q .; then\n+    echo \"ОШИБКА: ветка $branch уже существует — task-branch заводит только НОВЫЕ ветки.\" >&2\n+    echo \"  Доводи существующую: git fetch origin $branch && git checkout -B $branch origin/$branch,\" >&2\n+    echo \"  аренду для доводки возьми вручную (python3 scripts/lib/claim_task.py claim $task_number) ДО чекаута.\" >&2\n+    exit 1\n+  fi\n+else\n+  # ── Рабочее дерево (#332) ──────────────────────────────────────────────────\n+  # Разрешение дерева стоит ДО аренды: повторный запуск на той же задаче\n+  # обязан напечатать путь, а не отвалиться о собственный замок (claim не\n+  # идемпотентен по актору, #356), и не оставить осиротевший замок — оба\n+  # выхода ниже стоят до claim'а и ничего не захватывают (класс #360).\n+  main_root=$(dirname \"$(git rev-parse --path-format=absolute --git-common-dir)\")\n+  wt_dir=\"$main_root/.claude/worktrees/$task\"\n+\n+  # Уже есть worktree на эту ветку (повторный запуск на той же задаче) —\n+  # переиспользуем его, а не пытаемся завести дубль (git worktree add откажет\n+  # сам, но сообщение должно указывать путь, а не сырой git-эксепшн).\n+  #\n+  # Только под .claude/worktrees/ (находка ревью #333): главная запись\n+  # `worktree list` (корень репозитория) не годится — это и есть инцидент\n+  # #332 (ветка задачи зачекаучена в главном дереве). Легализовать его тут\n+  # значило бы тут же противоречить .githooks/pre-commit, которая коммит из\n+  # главного дерева на agent/<N>-* отклонит. Ветка вне .claude/worktrees/ —\n+  # заводим отдельное дерево по правилу, а не переиспользуем чужое место.\n+  existing_path=\"\"\n+  while IFS= read -r line; do\n+    case \"$line\" in\n+      worktree\\ *) cur_path=\"${line#worktree }\" ;;\n+      branch\\ refs/heads/\"$branch\")\n+        case \"$cur_path\" in\n+          \"$main_root\"/.claude/worktrees/*) existing_path=\"$cur_path\" ;;\n+        esac\n+        ;;\n+    esac\n+  done < <(git worktree list --porcelain)\n+\n+  if [ -n \"$existing_path\" ]; then\n+    if [ -d \"$existing_path\" ]; then\n+      # Имя дерева обязано отвечать тому же правилу <N>-, что проверяет\n+      # гвардия (находка ревью #333, раунд 4): легаси-дерево (например\n+      # «246-lease-claim» на agent/121-...) иначе замыкает цикл — лечащая\n+      # команда из отказа хука вечно печатает то же дерево, хук вечно\n+      # отклоняет коммит. Путь не выдаём: отказ с переносом дерева; сам\n+      # перенос делает владелец — скрипт существующие деревья не двигает и\n+      # не удаляет (критерий #332).\n+      wt_name=$(basename \"$existing_path\")\n+      if [[ ! \"$wt_name\" =~ ^${task_number}(-|$) ]]; then\n+        echo \"ОШИБКА: дерево $existing_path закреплено за веткой $branch, но имя дерева не начинается с «$task_number-» — гвардия (#332) коммит там отклонит.\" >&2\n+        echo \"  Перенеси дерево на каноническое имя и повтори scripts/git/task-branch $task:\" >&2\n+        echo \"    git worktree move \\\"$existing_path\\\" \\\"$wt_dir\\\"\" >&2\n+        exit 1\n+      fi\n+      if branch_diverged_from_origin; then\n+        echo \"ОШИБКА: локальная ветка $branch ($(git rev-parse --short \"refs/heads/$branch\")) разошлась с origin/$branch ($(git rev-parse --short \"refs/remotes/origin/$branch\")).\" >&2\n+        echo \"  Её мог подвинуть другой канал — коммит поверх чужой головы не выдаю (класс #332/#333).\" >&2\n+        echo \"  Синхронизируй дерево и повтори scripts/git/task-branch $task:\" >&2\n+        echo \"    git -C \\\"$existing_path\\\" rebase origin/$branch\" >&2\n+        exit 1\n+      fi\n+      echo \"Ветка $branch уже заведена — переиспользую рабочее дерево:\"\n+      echo \"Напоминание: аренду здесь task-branch не берёт — доводка берёт её вручную (python3 scripts/lib/claim_task.py claim $task_number), если ещё не держишь.\" >&2\n+      echo \"$existing_path\"\n+      exit 0\n+    fi\n+    # Протухшая запись (находка ревью #333): каталог стёрт мимо\n+    # `git worktree remove` — не молчать и не выдавать путь в никуда, а\n+    # вычистить запись и завести дерево заново.\n+    echo \"Рабочее дерево $existing_path пропало с диска — чищу протухшую запись и завожу заново.\" >&2\n+    git worktree prune\n+  fi\n+\n+  if git rev-parse --verify -q \"refs/heads/$branch\" >/dev/null; then\n+    # Ветка существует, но нужного дерева под .claude/worktrees/ нет. Если она\n+    # при этом зачекаучена ГДЕ-ТО ещё (например прямо в главном дереве —\n+    # инцидент #332) — не легализуем это молча (находка ревью #333):\n+    # `git worktree add` на такую ветку упадёт сырым git-фаталом, здесь —\n+    # понятная ошибка с указанием, где ветка застряла.\n+    checked_out_elsewhere=\"\"\n+    while IFS= read -r line; do\n+      case \"$line\" in\n+        worktree\\ *) cur_path=\"${line#worktree }\" ;;\n+        branch\\ refs/heads/\"$branch\") checked_out_elsewhere=\"$cur_path\" ;;\n+      esac\n+    done < <(git worktree list --porcelain)\n+    if [ -n \"$checked_out_elsewhere\" ]; then\n+      echo \"ОШИБКА: ветка $branch уже зачекаучена вне .claude/worktrees/:\" >&2\n+      echo \"  $checked_out_elsewhere\" >&2\n+      echo \"Не переиспользую чужое место (класс #332) — перенеси ветку туда самостоятельно:\" >&2\n+      echo \"  git -C \\\"$checked_out_elsewhere\\\" switch - (вернуть исходную ветку)\" >&2\n+      echo \"  git worktree add \\\"$wt_dir\\\" $branch\" >&2\n+      exit 1\n+    fi\n+    if branch_diverged_from_origin; then\n+      echo \"ОШИБКА: локальная ветка $branch ($(git rev-parse --short \"refs/heads/$branch\")) разошлась с origin/$branch ($(git rev-parse --short \"refs/remotes/origin/$branch\")).\" >&2\n+      echo \"  Её мог подвинуть другой канал — под дерево ставлю только свежую ветку (класс #332/#333).\" >&2\n+      echo \"  Синхронизируйся и повтори scripts/git/task-branch $task:\" >&2\n+      echo \"    git branch -f $branch origin/$branch   # своих коммитов на ветке нет/не нужны\" >&2\n+      echo \"    git worktree add \\\"$wt_dir\\\" $branch && git -C \\\"$wt_dir\\\" rebase origin/$branch   # нужны — усынови сам и перенеси\" >&2\n+      exit 1\n+    fi\n+    # Осиротела (нигде не зачекаучена) — заводим дерево на неё, не создавая\n+    # вторую ветку. Аренду task-branch на путях переиспользования/усыновления\n+    # не берёт (выходы стоят до claim'а, класс #360) — доводка берёт её\n+    # вручную (WORKER-PLAYBOOK, п. 2–3), поэтому напоминание в каждом выводе.\n+    git worktree add \"$wt_dir\" \"$branch\"\n+    git config core.hooksPath .githooks\n+    echo \"Напоминание: аренду здесь task-branch не берёт — доводка берёт её вручную (python3 scripts/lib/claim_task.py claim $task_number), если ещё не держишь.\" >&2\n+    echo \"Рабочее дерево (иди туда, коммить оттуда — .githooks/pre-commit это проверяет, #332):\"\n+    echo \"$wt_dir\"\n+    print_digest\n+    exit 0\n+  fi\n+\n+  # Ветки локально нет, но на origin она есть (чужой PR или работа прежнего\n+  # прогона, дерево потеряно): молча заводить одноимённую ветку от origin/main\n+  # нельзя — `git worktree add -b` создал бы локальную голову поверх чужой\n+  # базы, и коммиты агента тихо подменили бы работу другого канала (класс\n+  # #332; живое репро — находка ревью #333). Усыновляем СЕРВЕРНУЮ голову —\n+  # «найдёт/заведёт» из WORKER-PLAYBOOK становится правдой.\n+  if git show-ref --verify -q \"refs/remotes/origin/$branch\"; then\n+    mkdir -p \"$(dirname \"$wt_dir\")\"\n+    git worktree add -b \"$branch\" \"$wt_dir\" \"refs/remotes/origin/$branch\"\n+    git config core.hooksPath .githooks\n+    echo \"Ветка $branch существует на origin — усыновляю серверную голову ($(git rev-parse --short \"refs/remotes/origin/$branch\")).\"\n+    echo \"Напоминание: аренду здесь task-branch не берёт — доводка берёт её вручную (python3 scripts/lib/claim_task.py claim $task_number), если ещё не держишь.\" >&2\n+    echo \"Рабочее дерево (иди туда, коммить оттуда — .githooks/pre-commit это проверяет, #332):\"\n+    echo \"$wt_dir\"\n+    print_digest\n+    exit 0\n+  fi\n+  # Путь дерева занят (зарегистрированное дерево на другой ветке — легаси\n+  # имена с числовым префиксом делают коллизию вероятной, — или просто\n+  # посторонняя папка): падать сквозь claim на сыром `git worktree add`\n+  # нельзя — аренда уже взята и остаётся висеть до TTL-сборщика (класс\n+  # #360), а ветка успевает создаться (находка ревью #333, раунд 4, живое\n+  # репро). Отказ ДО claim'а: называем занятие и лечение; разбор пути —\n+  # действие владельца дерева, скрипт существующие деревья не трогает.\n+  if [ -e \"$wt_dir\" ]; then\n+    echo \"ОШИБКА: путь $wt_dir занят (не пуст) — завести туда дерево задачи нельзя.\" >&2\n+    echo \"  Разберись с путём вручную, затем повтори scripts/git/task-branch $task:\" >&2\n+    echo \"    git worktree list   # найти, какое дерево занимает путь\" >&2\n+    echo \"    git worktree remove \\\"\\$путь\\\"   # если это дерево задачи и оно больше не нужно\" >&2\n+    exit 1\n+  fi\n+  # Ветки нет ни локально, ни в дереве, ни на origin — падаем сквозь: аренда\n+  # ниже, затем создание ветки СРАЗУ в дереве (хвост файла).\n fi\n \n # Аренда задачи (#121, ADR 0006) — до сих пор её брал только облачный воркер\n@@ -220,17 +408,22 @@ else\n   fi\n fi\n \n-git switch -c \"agent/$task\" refs/remotes/origin/main\n+if [ \"${GITHUB_ACTIONS:-}\" = \"true\" ]; then\n+  git switch -c \"$branch\" refs/remotes/origin/main\n+  git config core.hooksPath .githooks\n+  echo \"Ветка $branch от origin/main $(git rev-parse --short origin/main) ($(git log -1 --format=%cd --date=iso origin/main))\"\n+  print_digest\n+  exit 0\n+fi\n \n-# Гвардия свежести ставится каждым запуском: пересозданный клон защищён сразу.\n+# Гвардия свежести ставится в общем конфиге репозитория (git config без\n+# --worktree пишет в <git-common-dir>/config) — общий для ВСЕХ worktree,\n+# ставить заново в каждом дереве не нужно, но лишним не будет.\n git config core.hooksPath .githooks\n \n-echo \"Ветка agent/$task от origin/main $(git rev-parse --short origin/main) ($(git log -1 --format=%cd --date=iso origin/main))\"\n-\n-# Печать ключевых граблей инфраструктуры в момент старта задачи, а не когда-нибудь\n-# потом «если вспомнить прочитать» (задача #323): task-branch — один из ДВУХ\n-# входов в agent-ветку (второй — доводка уже открытого PR в scripts/worker/task.sh,\n-# он тоже зовёт print_infra_digest напрямую, минуя этот файл, — см. #326 находка 1).\n-# Текст — одно место правды в scripts/gh/infra_digest.sh, не копия здесь.\n-source \"$(dirname \"${BASH_SOURCE[0]}\")/../gh/infra_digest.sh\"\n-print_infra_digest\n+mkdir -p \"$(dirname \"$wt_dir\")\"\n+git worktree add -b \"$branch\" \"$wt_dir\" refs/remotes/origin/main\n+echo \"Ветка $branch от origin/main $(git rev-parse --short origin/main) ($(git log -1 --format=%cd --date=iso origin/main))\"\n+echo \"Рабочее дерево (иди туда, коммить оттуда — .githooks/pre-commit это проверяет, #332):\"\n+echo \"$wt_dir\"\n+print_digest",
      "sha": "26774421204302c7bb1a51f7efbec0b0ad43fca4",
      "status": "modified"
    },
    {
      "filename": "scripts/git/test/task-branch.test.sh",
      "patch": "@@ -11,10 +11,36 @@\n # в scripts/git/task-branch (от `task_number=` до строки перед `git fetch\n # origin --prune`) — случай 2 (закрытая задача) перестаёт отклоняться, тест\n # краснеет. Верни блок — тест снова зелёный.\n+#\n+# Случаи 8-10 — рабочее дерево (#332): task-branch заводит/переиспользует\n+# рабочее дерево, не только ветку:\n+#   8) вне CI (без GITHUB_ACTIONS) — заводит .claude/worktrees/<task>,\n+#      печатает путь, текущий каталог остаётся на своей ветке;\n+#   9) повторный вызов на ту же задачу — переиспользует то же дерево, не\n+#      падает и не плодит второе;\n+#   10) в CI (GITHUB_ACTIONS=true) — старое поведение: переключает ветку\n+#       прямо в текущем каталоге, worktree не заводит (обратная совместимость\n+#       с scripts/worker/task.sh, который сам работает в одноразовом чекауте);\n+#   11) стык с гвардией (находка ревью #333): после переключения ветки в\n+#       CI-режиме коммит настоящим .githooks/pre-commit обязан проходить —\n+#       без этого случая случай 10 (CI-режим task-branch) и\n+#       worktree-guard.test.sh (гвардия вне CI) проверяют половины отдельно,\n+#       а стык — коммит после CI-переключения под настоящим хуком — не\n+#       покрывает никто;\n+#  12-14) сверка локальной ветки с origin/<branch> (находка ревью #333):\n+#      12) ветку подвинул другой канал — переиспользование отказывает\n+#          с командой синхронизации;\n+#      13) то же на пути усыновления (дерево пропало с диска);\n+#      14) свои незапушенные коммиты расхождением не считаются —\n+#          переиспользование работает;\n+#     15) ветка существует только на origin (чужой PR, дерево потеряно) —\n+#          усыновляется СЕРВЕРНАЯ голова, а не тихо заводится одноимённая\n+#          локальная от origin/main (класс #332, репро ревью #333).\n set -euo pipefail\n \n REPO_ROOT=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/../../..\" && pwd)\"\n SCRIPT_SRC=\"$REPO_ROOT/scripts/git/task-branch\"\n+HOOK_SRC=\"$REPO_ROOT/.githooks/pre-commit\"\n \n WORK=\"$(mktemp -d)\"\n cleanup() { rm -rf \"$WORK\"; }\n@@ -115,18 +141,46 @@ run_task_branch() {\n   local dirname=\"$1\" task=\"$2\"\n   (\n     cd \"$WORK/$dirname\"\n-    PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" \"$task\"\n+    # -u GITHUB_ACTIONS: случаи 1-7 симулируют локальный не-CI запуск\n+    # (worktree, не переключение ветки на месте, #332) — если сам этот тест\n+    # выполняется внутри настоящего GitHub Actions job, переменная\n+    # GITHUB_ACTIONS=true уже стоит в окружении и наследовалась бы сюда,\n+    # заставляя task-branch взять CI-ветку поведения по ошибочной причине.\n+    env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" \"$task\"\n   )\n }\n \n+# Вне CI (#332) успешный запуск заводит ОТДЕЛЬНЫЙ worktree на agent/<task>,\n+# не переключает ветку в текущем каталоге. `git worktree list` перечисляет и\n+# сам исходный чекаут (первой строкой) — если бы скрипт вместо worktree\n+# сделал `git switch -c` на месте, исходный каталог сам оказался бы\n+# «совпадением» и маскировал регресс; поэтому путь ещё сверяется с исходным\n+# каталогом — совпадение с ним не считается заведённым worktree.\n+worktree_for_branch() {\n+  local dirname=\"$1\" branch=\"$2\"\n+  # Оба пути — из самого git (не из shell pwd): на Windows/Cygwin `git worktree\n+  # list` печатает диск-стиль (C:/Users/...), а builtin pwd — POSIX-стиль\n+  # (/c/Users/...) для ОДНОГО и того же каталога — прямое сравнение строк\n+  # дало бы ложное «это отдельный worktree» даже когда скрипт остался на\n+  # месте (`git switch -c`), маскируя ровно тот регресс, который тест обязан\n+  # ловить.\n+  local origin_dir path\n+  origin_dir=$(git -C \"$WORK/$dirname\" worktree list --porcelain | awk 'NR==1{print $2}')\n+  path=$(git -C \"$WORK/$dirname\" worktree list --porcelain \\\n+    | awk -v want=\"refs/heads/$branch\" '/^worktree /{p=$2} $0==\"branch \"want{print p}')\n+  [ -n \"$path\" ] || return 0\n+  [ \"$path\" = \"$origin_dir\" ] && return 0\n+  printf '%s\\n' \"$path\"\n+}\n+\n # ── случай 1: открытая задача с меткой task — ветка заводится ────────────────\n make_tree \"case1\"\n if out=$(run_task_branch \"case1\" \"61-good\" 2>\"$WORK/case1.stderr\"); then\n-  branch=$(git -C \"$WORK/case1\" branch --show-current)\n-  if [ \"$branch\" = \"agent/61-good\" ]; then\n-    note \"случай 1 (открыта, метка task): ветка заведена — ОК\"\n+  wt=$(worktree_for_branch \"case1\" \"agent/61-good\")\n+  if [ -n \"$wt\" ] && [ -d \"$wt\" ]; then\n+    note \"случай 1 (открыта, метка task): worktree на agent/61-good заведён — ОК\"\n   else\n-    note \"случай 1: ветка не та ($branch) — ОШИБКА\"\n+    note \"случай 1: worktree на agent/61-good НЕ найден — ОШИБКА\"\n     fail=1\n   fi\n else\n@@ -267,6 +321,7 @@ done\n make_tree \"case7\"\n if (\n   cd \"$WORK/case7\"\n+  unset GITHUB_ACTIONS\n   PATH=\"$safe_path:$shim\" \"$bash_bin\" \"$SCRIPT_SRC\" \"67-no-gh\"\n ) 2>\"$WORK/case7.stderr\"; then\n   note \"случай 7 (gh не найден): скрипт создал ветку — ОШИБКА, ожидался отказ гвардии эпика (офлайн-режима нет)\"\n@@ -281,7 +336,305 @@ else\n   fi\n fi\n \n+# ── рабочее дерево (#332): отдельные синтетические origin ───────────────────\n+# origin — локальный путь, не github-вид URL, поэтому проверка задачи в\n+# task-branch не резолвит настоящий owner/repo и всегда уходит в ветку\n+# «предупреждение, не отказ» — НО только если сам gh не сможет ответить.\n+# Фейковый gh из $WORK/bin (сделан для случаев 1-7) переиспользуется здесь:\n+# для номеров 99/55 он не знает маршрута и вернёт «неизвестный маршрут»\n+# (не HTTP 404) — предохраняет тест от НАСТОЯЩЕГО gh на PATH хоста, который\n+# иначе бы дошёл до api.github.com и мог вернуть подлинный 404 на несуществующий\n+# путь, ложно провалив случай (плавающий тест, воспроизведено локально).\n+new_origin() {\n+  local name=\"$1\"\n+  git init -q --bare \"$WORK/$name-origin.git\"\n+  git init -q \"$WORK/$name-seed\"\n+  (\n+    cd \"$WORK/$name-seed\"\n+    git config user.email test@example.com\n+    git config user.name test\n+    git commit -q --allow-empty -m init\n+    git remote add origin \"$WORK/$name-origin.git\"\n+    git push -q origin HEAD:refs/heads/main\n+  )\n+}\n+\n+# ── случай 8+9: вне CI — заводит и переиспользует worktree ──────────────────\n+# Скрипт зовётся по $SCRIPT_SRC (не копией): после #386 он тянет соседей\n+# (../lib/lease.sh, ../lib/epic_guard.py, ../gh/infra_digest.sh) относительно\n+# СЕБЯ — копия в синтетическом клоне осталась бы без них. Git-операции при\n+# этом идут в синтетический клон (cwd) и через insteadOf — в локальный bare;\n+# номера задач (67) взяты из маршрутов фейкового gh: epic guard (#376) на\n+# неизвестном маршруте отказывает громко, и случай 8 падал бы не по делу.\n+new_origin \"a\"\n+git clone -q --branch main \"$WORK/a-origin.git\" \"$WORK/a-main\" 2>/dev/null\n+\n+(cd \"$WORK/a-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-demo-slug) \\\n+  >\"$WORK/a-run1.out\" 2>&1 || { fail=1; note \"run1 упал:\"; cat \"$WORK/a-run1.out\"; }\n+\n+# Путь берём из самого git (worktree list), а не собираем строкой сами: на\n+# Windows/Cygwin git печатает диск-стиль (C:/Users/...), а $WORK — POSIX-стиль\n+# (/tmp/...) — один и тот же каталог, разные строки одного пути.\n+expected_wt=$(\n+  git -C \"$WORK/a-main\" worktree list --porcelain \\\n+    | awk '/^worktree /{p=$2} /^branch refs\\/heads\\/agent\\/67-demo-slug$/{print p}'\n+)\n+if [ -n \"$expected_wt\" ] && [ -d \"$expected_wt\" ]; then\n+  note \"случай 8 (вне CI, новая задача): worktree заведён на agent/67-demo-slug — ОК\"\n+else\n+  note \"случай 8 (вне CI, новая задача): worktree НЕ заведён — ОШИБКА\"\n+  fail=1\n+fi\n+if [ -n \"$expected_wt\" ] && grep -qF \"$expected_wt\" \"$WORK/a-run1.out\"; then\n+  note \"  путь напечатан агенту — ОК\"\n+else\n+  note \"  путь НЕ напечатан — ОШИБКА (агент не узнает, куда идти)\"\n+  fail=1\n+fi\n+main_branch_after=$(git -C \"$WORK/a-main\" rev-parse --abbrev-ref HEAD)\n+if [ \"$main_branch_after\" = \"master\" ] || [ \"$main_branch_after\" = \"main\" ]; then\n+  note \"  исходный каталог остался на своей ветке ($main_branch_after) — ОК\"\n+else\n+  note \"  исходный каталог переключился на $main_branch_after — ОШИБКА (не должен трогаться)\"\n+  fail=1\n+fi\n+\n+before_count=$(git -C \"$WORK/a-main\" worktree list --porcelain | grep -c '^worktree ')\n+(cd \"$WORK/a-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-demo-slug) \\\n+  >\"$WORK/a-run2.out\" 2>&1 || { fail=1; note \"run2 (повтор) упал:\"; cat \"$WORK/a-run2.out\"; }\n+after_count=$(git -C \"$WORK/a-main\" worktree list --porcelain | grep -c '^worktree ')\n+if [ \"$before_count\" = \"$after_count\" ] && grep -qF \"$expected_wt\" \"$WORK/a-run2.out\"; then\n+  note \"случай 9 (повтор на ту же задачу): дерево переиспользовано, дубля нет — ОК\"\n+else\n+  note \"случай 9 (повтор на ту же задачу): дубль дерева или путь не сообщён — ОШИБКА\"\n+  fail=1\n+fi\n+\n+# ── случай 10: CI — старое поведение (переключение в текущем каталоге) ──────\n+# LEASE_ALREADY_CLAIMED=61 — транспорт арендует задачу сам и пробрасывает номер\n+# (#356): здесь этим же путём проверяется, что claim под флагом не повторяется.\n+new_origin \"b\"\n+git clone -q --branch main \"$WORK/b-origin.git\" \"$WORK/b-main\" 2>/dev/null\n+(cd \"$WORK/b-main\" && GITHUB_ACTIONS=true LEASE_ALREADY_CLAIMED=61 PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 61-ci-demo) \\\n+  >\"$WORK/b-run.out\" 2>&1 || { fail=1; note \"CI-прогон упал:\"; cat \"$WORK/b-run.out\"; }\n+ci_branch=$(git -C \"$WORK/b-main\" rev-parse --abbrev-ref HEAD)\n+if [ \"$ci_branch\" = \"agent/61-ci-demo\" ] && [ ! -d \"$WORK/b-main/.claude/worktrees\" ]; then\n+  note \"случай 10 (CI): ветка переключена в текущем каталоге, worktree не заведён — ОК\"\n+else\n+  note \"случай 10 (CI): ожидалось старое поведение (ветка $ci_branch, worktree отсутствует) — ОШИБКА\"\n+  fail=1\n+fi\n+\n+# ── случай 11: после CI-переключения коммит настоящим хуком обязан пройти ───\n+# Скрипт зовётся по $SCRIPT_SRC (не копией): эпик-гвардия (#376) и lease.sh\n+# тянут соседей относительно СЕБЯ; сам ХУК при этом копируется в c-main —\n+# смысл случая в том, что коммит идёт под НАСТОЯЩИМ файлом гвардии. Номер 61 —\n+# из маршрутов фейкового gh: на неизвестном маршруте эпик-гвардия отказала бы\n+# громко ещё до переключения ветки.\n+new_origin \"c\"\n+git clone -q --branch main \"$WORK/c-origin.git\" \"$WORK/c-main\" 2>/dev/null\n+mkdir -p \"$WORK/c-main/.githooks\"\n+cp \"$HOOK_SRC\" \"$WORK/c-main/.githooks/pre-commit\"\n+chmod +x \"$WORK/c-main/.githooks/pre-commit\"\n+(cd \"$WORK/c-main\" && GITHUB_ACTIONS=true PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 61-ci-commit) \\\n+  >\"$WORK/c-run.out\" 2>&1 || { fail=1; note \"случай 11: CI-переключение упало:\"; cat \"$WORK/c-run.out\"; }\n+(\n+  cd \"$WORK/c-main\"\n+  git config user.email test@example.com\n+  git config user.name test\n+  echo \"change\" >>note.txt\n+  git add note.txt\n+  GITHUB_ACTIONS=true git commit -q -m \"ci commit after task-branch\"\n+) >\"$WORK/c-commit.out\" 2>\"$WORK/c-commit.stderr\"\n+if [ -f \"$WORK/c-commit.out\" ] && grep -qF \"ci commit after task-branch\" <(git -C \"$WORK/c-main\" log -1 --format=%s 2>/dev/null); then\n+  note \"случай 11 (коммит после CI-переключения настоящим хуком): прошёл — ОК\"\n+else\n+  note \"случай 11 (коммит после CI-переключения настоящим хуком): ОТКЛОНЁН — ОШИБКА\"\n+  cat \"$WORK/c-commit.stderr\" >&2\n+  fail=1\n+fi\n+\n+# ── сверка с origin/<branch> (находка ревью #333): ветку мог подвинуть другой ──\n+# канал — оркестратор снимает исполнителя с конфликтного PR и диспатчит нового\n+# воркера, локальная голова отстаёт от серверной. Гвардия свежести сверяет\n+# только origin/main и этот стык не видит, коммит поверх чужой головы валился\n+# бы уже на пуше — отказ обязан прозвучать ДО работы в дереве.\n+# Мутация: сними сверку branch_diverged_from_origin в scripts/git/task-branch\n+# (пути переиспользования и усыновления) — случаи 12/13 перестают отклоняться,\n+# тест красный. Верни — снова зелёный. Случай 14 страхует обратное: свои\n+# незапушенные коммиты (origin — предок локальной головы) отсекаться не должны.\n+foreign_move() {\n+  local clone=\"$1\" branch=\"$2\"\n+  git clone -q \"$WORK/$3\" \"$clone\" 2>/dev/null\n+  (\n+    cd \"$clone\"\n+    git config user.email rival@example.com\n+    git config user.name rival\n+    git checkout -q -b \"$branch\" \"origin/$branch\"\n+    echo \"rival move\" >rival.txt\n+    git add rival.txt\n+    git commit -q -m \"rival move\"\n+    git push -q origin \"$branch\"\n+  )\n+}\n+\n+# ── случай 12: подвинутая ветка — переиспользование отказывает ──────────────\n+new_origin \"d\"\n+git clone -q --branch main \"$WORK/d-origin.git\" \"$WORK/d-main\" 2>/dev/null\n+(cd \"$WORK/d-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-sync-reuse) \\\n+  >\"$WORK/d-run1.out\" 2>&1 || { fail=1; note \"случай 12: run1 упал:\"; cat \"$WORK/d-run1.out\"; }\n+git -C \"$WORK/d-main\" push -q origin agent/67-sync-reuse\n+foreign_move \"$WORK/d-rival\" agent/67-sync-reuse \"d-origin.git\"\n+git -C \"$WORK/d-main\" fetch -q origin\n+if (cd \"$WORK/d-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-sync-reuse) \\\n+  >\"$WORK/d-run2.out\" 2>\"$WORK/d-run2.stderr\"; then\n+  note \"случай 12 (ветка подвинута другим каналом): переиспользование прошло — ОШИБКА, ожидался отказ\"\n+  fail=1\n+else\n+  case \"$(cat \"$WORK/d-run2.stderr\")\" in\n+    *\"разошлась\"*) note \"случай 12 (ветка подвинута другим каналом): отказ с командой синхронизации — ОК\" ;;\n+    *) note \"случай 12: отказ без внятной причины — ОШИБКА ($(cat \"$WORK/d-run2.stderr\"))\"; fail=1 ;;\n+  esac\n+fi\n+\n+# ── случай 13: та же сверка на пути усыновления (дерево пропало с диска) ────\n+new_origin \"e\"\n+git clone -q --branch main \"$WORK/e-origin.git\" \"$WORK/e-main\" 2>/dev/null\n+(cd \"$WORK/e-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-sync-adopt) \\\n+  >\"$WORK/e-run1.out\" 2>&1 || { fail=1; note \"случай 13: run1 упал:\"; cat \"$WORK/e-run1.out\"; }\n+git -C \"$WORK/e-main\" push -q origin agent/67-sync-adopt\n+foreign_move \"$WORK/e-rival\" agent/67-sync-adopt \"e-origin.git\"\n+# Дерево стёрто мимо `git worktree remove`: админ-запись чистится prune'ом\n+# внутри скрипта, ветка остаётся — дальше путь усыновления.\n+rm -rf \"$WORK/e-main/.claude/worktrees/67-sync-adopt\"\n+git -C \"$WORK/e-main\" worktree prune\n+git -C \"$WORK/e-main\" fetch -q origin\n+if (cd \"$WORK/e-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-sync-adopt) \\\n+  >\"$WORK/e-run2.out\" 2>\"$WORK/e-run2.stderr\"; then\n+  note \"случай 13 (усыновление подвинутой ветки): прошло — ОШИБКА, ожидался отказ\"\n+  fail=1\n+else\n+  case \"$(cat \"$WORK/e-run2.stderr\")\" in\n+    *\"разошлась\"*) note \"случай 13 (усыновление подвинутой ветки): отказ с командой синхронизации — ОК\" ;;\n+    *) note \"случай 13: отказ без внятной причины — ОШИБКА ($(cat \"$WORK/e-run2.stderr\"))\"; fail=1 ;;\n+  esac\n+fi\n+\n+# ── случай 14: свои незапушенные коммиты — переиспользование работает ───────\n+new_origin \"f\"\n+git clone -q --branch main \"$WORK/f-origin.git\" \"$WORK/f-main\" 2>/dev/null\n+(cd \"$WORK/f-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-ahead) \\\n+  >\"$WORK/f-run1.out\" 2>&1 || { fail=1; note \"случай 14: run1 упал:\"; cat \"$WORK/f-run1.out\"; }\n+ahead_wt=$(git -C \"$WORK/f-main\" worktree list --porcelain \\\n+  | awk '/^worktree /{p=$2} /^branch refs\\/heads\\/agent\\/67-ahead$/{print p}')\n+(\n+  cd \"$ahead_wt\"\n+  git config user.email test@example.com\n+  git config user.name test\n+  echo \"own work\" >own.txt\n+  git add own.txt\n+  env -u GITHUB_ACTIONS git commit -q -m \"own unpushed work\"\n+)\n+if (cd \"$WORK/f-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-ahead) \\\n+  >\"$WORK/f-run2.out\" 2>&1 && grep -qF \"$ahead_wt\" \"$WORK/f-run2.out\"; then\n+  note \"случай 14 (свои незапушенные коммиты): дерево переиспользовано, путь напечатан — ОК\"\n+else\n+  note \"случай 14 (свои незапушенные коммиты): переиспользование отказало или путь потерян — ОШИБКА\"\n+  cat \"$WORK/f-run2.out\" 2>/dev/null\n+  fail=1\n+fi\n+\n+# ── случай 15: ветка только на origin — усыновление серверной головы ────────\n+# Чужой PR уже запушен, локальной ветки и дерева нет: молча завести\n+# одноимённую локальную ветку от origin/main значило бы тихо подменить чужую\n+# работу (находка ревью #333, живое репро). task-branch обязан усыновить\n+# серверную голову и напомнить про ручную арену доводки.\n+# Мутация: сними проверку `refs/remotes/origin/$branch` в scripts/git/task-branch\n+# (не-CI fall-through) — случай 15 перестаёт падать на сверке голов, тест красный.\n+new_origin \"g\"\n+git clone -q --branch main \"$WORK/g-origin.git\" \"$WORK/g-main\" 2>/dev/null\n+git clone -q \"$WORK/g-origin.git\" \"$WORK/g-first\" 2>/dev/null\n+(\n+  cd \"$WORK/g-first\"\n+  git config user.email rival@example.com\n+  git config user.name rival\n+  git checkout -q -b agent/67-origin-adopt\n+  echo \"pushed work\" >pushed.txt\n+  git add pushed.txt\n+  git commit -q -m \"pushed work\"\n+  git push -q origin agent/67-origin-adopt\n+)\n+remote_head=$(git -C \"$WORK/g-main\" ls-remote origin refs/heads/agent/67-origin-adopt | cut -f1)\n+if (cd \"$WORK/g-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-origin-adopt) \\\n+  >\"$WORK/g-run.out\" 2>\"$WORK/g-run.stderr\"; then\n+  adopt_wt=$(git -C \"$WORK/g-main\" worktree list --porcelain \\\n+    | awk '/^worktree /{p=$2} /^branch refs\\/heads\\/agent\\/67-origin-adopt$/{print p}')\n+  adopt_head=$(git -C \"$WORK/g-main\" rev-parse refs/heads/agent/67-origin-adopt 2>/dev/null || true)\n+  if [ -n \"$adopt_wt\" ] && [ \"$adopt_head\" = \"$remote_head\" ] \\\n+     && git -C \"$adopt_wt\" ls-files --error-unmatch pushed.txt >/dev/null 2>&1 \\\n+     && grep -qF \"$adopt_wt\" \"$WORK/g-run.out\"; then\n+    note \"случай 15 (ветка только на origin): серверная голова усыновлена, путь напечатан — ОК\"\n+  else\n+    note \"случай 15: дерево не от серверной головы, чужой файл потерян или путь не напечатан — ОШИБКА\"\n+    fail=1\n+  fi\n+  grep -q \"аренду здесь task-branch не берёт\" \"$WORK/g-run.stderr\" \\\n+    || { note \"случай 15: нет напоминания про ручную аренду доводки — ОШИБКА\"; fail=1; }\n+else\n+  note \"случай 15 (ветка только на origin): task-branch отказал — ОШИБКА, ожидалось усыновление\"\n+  cat \"$WORK/g-run.stderr\" >&2\n+  fail=1\n+fi\n+\n+# ── случай 16: легаси-имя дерева — отказ с переносом, не вечный цикл ────────\n+# Дерево «246-legacy» закреплено за веткой agent/61-x: путь переиспользования,\n+# не сверяющий имя с правилом <N>-, замыкает цикл «гвардия шлёт в task-branch,\n+# task-branch печатает то же дерево, гвардия снова отклоняет» (находка ревью\n+# #333, раунд 4, репро на состоянии из задачи: 246-lease-claim / agent/121).\n+# Мутация: сними сверку имени в пути переиспользования — случай 16 перестаёт\n+# отклоняться, тест красный.\n+new_origin \"h\"\n+git clone -q --branch main \"$WORK/h-origin.git\" \"$WORK/h-main\" 2>/dev/null\n+mkdir -p \"$WORK/h-main/.claude/worktrees\"\n+git -C \"$WORK/h-main\" worktree add -q -b agent/61-x \"$WORK/h-main/.claude/worktrees/246-legacy\" origin/main\n+if (cd \"$WORK/h-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 61-x) \\\n+  >\"$WORK/h-run.out\" 2>\"$WORK/h-run.stderr\"; then\n+  note \"случай 16 (легаси-имя дерева): путь напечатан — ОШИБКА, ожидался отказ с командой переноса\"\n+  fail=1\n+else\n+  case \"$(cat \"$WORK/h-run.stderr\")\" in\n+    *\"не начинается с «61-»\"*\"worktree move\"*) note \"случай 16 (легаси-имя дерева): отказ с рабочей командой git worktree move — ОК\" ;;\n+    *) note \"случай 16: отказ без внятной причины или без команды переноса — ОШИБКА ($(cat \"$WORK/h-run.stderr\"))\"; fail=1 ;;\n+  esac\n+fi\n+\n+# ── случай 17: путь дерева занят — отказ ДО claim'а, без осиротевшей ветки ──\n+# Канонический путь .claude/worktrees/61-occupied занят деревом на чужой\n+# ветке (легаси-имя с числовым префиксом делает такую коллизию вероятной),\n+# ветки agent/61-occupied нет нигде: падение сквозь claim на сыром\n+# `git worktree add` захватывало аренду и оставляло висеть свежесозданную\n+# ветку (класс #360; находка ревью #333, раунд 4, живое репро). Мутация:\n+# сними проверку [ -e \"$wt_dir\" ] — ветка agent/61-occupied появляется,\n+# тест красный.\n+new_origin \"i\"\n+git clone -q --branch main \"$WORK/i-origin.git\" \"$WORK/i-main\" 2>/dev/null\n+mkdir -p \"$WORK/i-main/.claude/worktrees\"\n+git -C \"$WORK/i-main\" worktree add -q -b agent/121-other \"$WORK/i-main/.claude/worktrees/61-occupied\" origin/main\n+(cd \"$WORK/i-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 61-occupied) \\\n+  >\"$WORK/i-run.out\" 2>\"$WORK/i-run.stderr\" \\\n+  && { note \"случай 17 (путь занят): скрипт прошёл — ОШИБКА, ожидался отказ\"; fail=1; }\n+case \"$(cat \"$WORK/i-run.stderr\")\" in\n+  *\"занят (не пуст)\"*) note \"случай 17 (путь занят): отказ с объяснением и лечением — ОК\" ;;\n+  *) note \"случай 17: отказ без внятной причины — ОШИБКА ($(cat \"$WORK/i-run.stderr\"))\"; fail=1 ;;\n+esac\n+if git -C \"$WORK/i-main\" show-ref --verify -q refs/heads/agent/61-occupied; then\n+  note \"случай 17: ветка всё же создана (осиротевшая, класс #360) — ОШИБКА\"\n+  fail=1\n+else\n+  note \"случай 17: ветка не создавалась — осиротевших не осталось — ОК\"\n+fi\n+\n if [ \"$fail\" = 0 ]; then\n-  echo \"task-branch: все случаи входной проверки прошли как ожидалось\"\n+  echo \"task-branch: все случаи входной проверки, работы с worktree, стыка с гвардией и сверки с origin прошли как ожидалось\"\n fi\n exit \"$fail\"",
      "sha": "417a18db6738e0cd2b6b2510459f77de5bffc7c7",
      "status": "modified"
    },
    {
      "filename": "scripts/git/test/worktree-guard.test.sh",
      "patch": "@@ -0,0 +1,120 @@\n+#!/usr/bin/env bash\n+# Гвардия «дерево == задача» (#332): доказывает поведение РЕАЛЬНОГО\n+# .githooks/pre-commit на живых git-репозиториях и worktree — не пересказ,\n+# а прогон настоящего файла гвардии (копируется как есть, не переписывается\n+# здесь заново). Три случая:\n+#   1) коммит в agent/<N>-* из дерева «<N>-slug»           — проходит;\n+#   2) коммит в agent/<N>-* из дерева «<M>-slug» (M != N)  — отклонён;\n+#   3) коммит в main (не agent/*) из дерева с любым именем — гвардия неприменима, проходит.\n+#\n+# Мутация, которой доказана гвардия (#332): временно закомментируй блок\n+# «Гвардия «дерево == задача»» в .githooks/pre-commit (от `if [[ \"$branch\"`\n+# до соответствующего `fi`) — случай (2) перестаёт отклоняться, тест красный.\n+# Верни блок — тест снова зелёный.\n+set -euo pipefail\n+\n+REPO_ROOT=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/../../..\" && pwd)\"\n+HOOK_SRC=\"$REPO_ROOT/.githooks/pre-commit\"\n+\n+WORK=\"$(mktemp -d)\"\n+cleanup() { rm -rf \"$WORK\"; }\n+trap cleanup EXIT\n+\n+fail=0\n+note() { echo \"$@\"; }\n+\n+# ── подготовка: bare origin + локальный клон с main, гвардия из прод-файла ──\n+# .githooks/pre-commit коммитится в main ДО создания worktree — иначе каждое\n+# новое дерево (свой checkout ветки) окажется без файла гвардии вовсе, и тест\n+# ничего не докажет (реальный репозиторий коммитит .githooks вместе с кодом).\n+git init -q --bare \"$WORK/origin.git\"\n+\n+git init -q \"$WORK/seed\"\n+(\n+  cd \"$WORK/seed\"\n+  git config user.email test@example.com\n+  git config user.name test\n+  mkdir -p .githooks\n+  cp \"$HOOK_SRC\" .githooks/pre-commit\n+  chmod +x .githooks/pre-commit\n+  git add .githooks\n+  git commit -q -m init\n+  git remote add origin \"$WORK/origin.git\"\n+  git push -q origin HEAD:refs/heads/main\n+)\n+\n+git clone -q \"$WORK/origin.git\" \"$WORK/main-tree\"\n+(\n+  cd \"$WORK/main-tree\"\n+  git config user.email test@example.com\n+  git config user.name test\n+  git config core.hooksPath .githooks\n+)\n+\n+make_worktree() {\n+  local dirname=\"$1\" branch=\"$2\"\n+  git -C \"$WORK/main-tree\" worktree add -q -b \"$branch\" \"$WORK/$dirname\" origin/main\n+}\n+\n+try_commit() {\n+  # echo \"ok\" >&2 через git commit — возвращает код возврата коммита.\n+  # -u GITHUB_ACTIONS: гвардия несёт исключение «в CI (GITHUB_ACTIONS=true)\n+  # task-branch не заводит worktree» (#333) — этот тест симулирует ИМЕННО\n+  # локальный не-CI коммит агента, но если сам ЭТОТ тест-раннер запущен\n+  # внутри настоящего GitHub Actions job, переменная GITHUB_ACTIONS=true уже\n+  # стоит в окружении и наследуется сюда, отключая гвардию по ошибочной\n+  # причине — случай 2 молча переставал ловить регресс на CI (не локально,\n+  # где GITHUB_ACTIONS обычно не задана).\n+  local dir=\"$1\"\n+  (\n+    cd \"$WORK/$dir\"\n+    git config user.email test@example.com\n+    git config user.name test\n+    echo \"change in $dir at $(date +%s%N)\" >>note.txt\n+    git add note.txt\n+    env -u GITHUB_ACTIONS git commit -q -m \"test commit\" 2>\"$WORK/$dir.stderr\"\n+  )\n+}\n+\n+# ── случай 1: дерево «42-good-slug», ветка agent/42-good-slug — должен пройти ──\n+make_worktree \"42-good-slug\" \"agent/42-good-slug\"\n+if try_commit \"42-good-slug\"; then\n+  note \"случай 1 (дерево совпадает с задачей): коммит прошёл — ОК\"\n+else\n+  note \"случай 1 (дерево совпадает с задачей): коммит ОТКЛОНЁН — ОШИБКА, ожидался успех\"\n+  cat \"$WORK/42-good-slug.stderr\" >&2\n+  fail=1\n+fi\n+\n+# ── случай 2: дерево «99-other-task», ветка agent/42-mismatch — должен отклониться ──\n+make_worktree \"99-other-task\" \"agent/42-mismatch\"\n+if try_commit \"99-other-task\"; then\n+  note \"случай 2 (дерево НЕ совпадает с задачей): коммит прошёл — ОШИБКА, ожидался отказ\"\n+  fail=1\n+else\n+  msg=\"$(cat \"$WORK/99-other-task.stderr\")\"\n+  note \"случай 2 (дерево НЕ совпадает с задачей): коммит отклонён — ОК\"\n+  note \"  сообщение гвардии: $(printf '%s' \"$msg\" | head -1)\"\n+  case \"$msg\" in\n+    *\"рабочее дерево этой задачи не здесь\"*) ;;\n+    *)\n+      note \"  ОШИБКА: сообщение гвардии не объясняет причину отказа\"\n+      fail=1\n+      ;;\n+  esac\n+fi\n+\n+# ── случай 3: не agent/*-ветка (main) — гвардия неприменима вне зависимости от имени дерева ──\n+make_worktree \"unrelated-dir-name\" \"local/no-task-branch\"\n+if try_commit \"unrelated-dir-name\"; then\n+  note \"случай 3 (ветка не agent/*): коммит прошёл — ОК (правило вне области)\"\n+else\n+  note \"случай 3 (ветка не agent/*): коммит ОТКЛОНЁН — ОШИБКА, правило не должно применяться\"\n+  cat \"$WORK/unrelated-dir-name.stderr\" >&2\n+  fail=1\n+fi\n+\n+if [ \"$fail\" = 0 ]; then\n+  echo \"worktree-guard: все три случая прошли как ожидалось\"\n+fi\n+exit \"$fail\"",
      "sha": "1a371776aa57dfd8b48f2fe2ba164f500409ea33",
      "status": "added"
    }
  ],
  "after_pull": [
    {
      "filename": ".githooks/pre-commit",
      "patch": "@@ -12,6 +12,46 @@ if [ \"$branch\" = \"main\" ]; then\n   echo \"ПРЕДУПРЕЖДЕНИЕ: коммит прямо в main — в этом репо main меняется только через PR (docs/agents/PROTOCOL.md).\" >&2\n fi\n \n+# Гвардия «дерево == задача» (#332). Класс инцидента: агент по задаче #320\n+# правил файлы в дереве оркестратора (.claude/worktrees/pr-workflow-sessions-8f1ab5)\n+# на чужой ветке agent/258-...; в тот же день другой агент нашёл там же\n+# незакоммиченные диффы чужой ветки #297, а библиотекарь переключил главное\n+# дерево на свою ветку из-под работавшего там агента. Дисциплина «не путай\n+# деревья» не сработала трижды за сутки — здесь она enforced механически, а\n+# не по памяти. Правило: коммит в agent/<N>-<slug> разрешён ТОЛЬКО из\n+# рабочего дерева, чьё имя (basename top-level пути) начинается с того же\n+# числа <N>-. Проверка чисто локальная (без сети) — идёт раньше свежести\n+# base ниже, чтобы не ждать fetch там, где откажем и так.\n+#\n+# Исключение для CI (находка ревью #333): task-branch в режиме\n+# GITHUB_ACTIONS=true намеренно переключает ветку в одноразовом чекауте\n+# воркера, не заводя worktree (scripts/worker/task.sh уже сам изолирован на\n+# один прогон — см. комментарий там же). Без этого исключения гвардия\n+# отклоняет ПЕРВЫЙ же коммит автономного воркера в CI: каталог называется\n+# «edge-harness», а не «<N>-...», и правило падает не на инцидент #332\n+# (спутанные деревья агента/человека локально), а на штатный путь конвейера.\n+# Пропуск по переменной обязан быть ВИДЕН (находка ревью #333): молча\n+# выключенная гвардия — тот silent-wrong, против которого она построена.\n+# Переменная могла утечь в локальное окружение (sourced CI-env, экспорт в\n+# профиле, act) — тогда гвардия выключилась бы без следа в любом месте;\n+# строка в stderr оставляет след в любом логе, откуда бы переменная ни взялась.\n+if [[ \"$branch\" =~ ^agent/([0-9]+)(-|$) ]]; then\n+  if [ \"${GITHUB_ACTIONS:-}\" = \"true\" ]; then\n+    echo \"ПРЕДУПРЕЖДЕНИЕ: гвардия «дерево == задача» отключена: GITHUB_ACTIONS=true — коммит в $branch не проверяется на своё рабочее дерево (штатно только в одноразовом чекауте CI-воркера).\" >&2\n+  else\n+    task_num=\"${BASH_REMATCH[1]}\"\n+    worktree_name=$(basename \"$(git rev-parse --show-toplevel)\")\n+    if [[ ! \"$worktree_name\" =~ ^${task_num}(-|$) ]]; then\n+      echo \"ОШИБКА: ветка $branch — рабочее дерево этой задачи не здесь.\" >&2\n+      echo \"  Текущее дерево: «$worktree_name» (имя должно начинаться с «$task_num-»).\" >&2\n+      echo \"  Не путай чужую ветку со своим деревом (класс #332).\" >&2\n+      echo \"  Заведи/найди дерево этой задачи (команда печатает путь, коммить оттуда):\" >&2\n+      echo \"    scripts/git/task-branch ${branch#agent/}\" >&2\n+      exit 1\n+    fi\n+  fi\n+fi\n+\n if ! git rev-parse --verify -q refs/remotes/origin/main >/dev/null; then\n   echo \"ОШИБКА: нет refs/remotes/origin/main — сделай 'git fetch origin' и повтори.\" >&2\n   exit 1",
      "sha": "cfddf8f1c0673e1a37d3ea9a3bd37360e6fa564a",
      "status": "modified"
    },
    {
      "filename": ".github/workflows/repo-ci.yml",
      "patch": "@@ -167,6 +167,12 @@ jobs:\n       - name: Провайдер/модель LLM — одно место правды (vars), без зашитых дефолтов\n         run: bash scripts/lib/test/provider-default.guard.sh\n \n+      # Гвардия «дерево == задача» (#332): .githooks/pre-commit отклоняет коммит\n+      # в agent/<N>-* не из своего рабочего дерева — прогоняет РЕАЛЬНЫЙ файл\n+      # гвардии на синтетических git-worktree, не пересказ поведения.\n+      - name: pre-commit отклоняет коммит в agent/<N>-* не из своего дерева\n+        run: bash scripts/git/test/worktree-guard.test.sh\n+\n       # Кампания замера хвоста dispatch (задача #4): чистая логика — метрики,\n       # критерий покрытия, сводка, вставка в доку — кормится прод-формой данных.\n       - name: Тесты кампании dispatch-tail\n@@ -675,16 +681,19 @@ jobs:\n       - name: Bash-скрипты валидны\n         run: |\n           shopt -s nullglob\n-          for f in scripts/*.sh scripts/hands/*.sh scripts/worker/*.sh scripts/worker/test/*.sh scripts/lib/*.sh scripts/lib/test/*.sh scripts/plugins/*.sh scripts/plugins/test/*.sh scripts/review/*.sh scripts/git/task-branch scripts/git/pr-create scripts/git/test/*.sh; do\n+          for f in scripts/*.sh scripts/hands/*.sh scripts/worker/*.sh scripts/worker/test/*.sh scripts/lib/*.sh scripts/lib/test/*.sh scripts/plugins/*.sh scripts/plugins/test/*.sh scripts/review/*.sh scripts/git/task-branch scripts/git/pr-create scripts/git/test/*.sh .githooks/*; do\n             [ -f \"$f\" ] || continue\n             bash -n \"$f\"\n           done\n           echo \"bash ok\"\n \n       # Проверка на входе в task-branch (не гвардия постфактум): задача обязана\n       # существовать/быть открытой/нести task/не нести blocked до создания\n-      # ветки — прогон на реальном git с фейковым gh, сеть не нужна.\n-      - name: Smoke task-branch — проверка задачи на входе\n+      # ветки — прогон на реальном git с фейковым gh, сеть не нужна. Тот же\n+      # файл проверяет и рабочее дерево (#332): вне CI заводит/переиспользует\n+      # .claude/worktrees/<task>, в CI (GITHUB_ACTIONS=true) старое поведение\n+      # не меняется.\n+      - name: Smoke task-branch — проверка на входе и рабочее дерево\n         run: bash scripts/git/test/task-branch.test.sh\n \n       # Проверка на входе в scripts/git/pr-create (не гвардия постфактум,",
      "sha": "f37f270556b59a8eb5d04f8fcf9cb3fc5ee0256b",
      "status": "modified"
    },
    {
      "filename": ".gitignore",
      "patch": "@@ -18,3 +18,9 @@ __pycache__/\n \n # Локальный кеш npm при установке зависимостей на раннере (не артефакт репо).\n .npm-cache/\n+\n+# Рабочие деревья агентов (#332/#333) — заводятся scripts/git/task-branch\n+# локально на диске, в историю репозитория не попадают (находка ревью #333:\n+# без этой строки `git add -A` в главном дереве затягивает их как embedded\n+# gitlink).\n+.claude/worktrees/",
      "sha": "f61a924697e97cd21de2aa3e04d039d5243f9546",
      "status": "modified"
    },
    {
      "filename": "AGENTS.md",
      "patch": "@@ -20,20 +20,28 @@\n   `python3 scripts/lib/claim_task.py claim N` (замок + назначение, #121): отказ\n   «задача занята» = её уже делает другой канал, бери следующую. Для НОВОЙ задачи\n   аренду берёт сам `task-branch` (пункт ниже) — руками claim вызывать не нужно;\n-  вручную он нужен только при доводке уже открытого PR (чекаут существующей\n-  ветки, `task-branch` там не вызывается): отказ «занята» при доводке может\n+  вручную он нужен только при доводке уже открытого PR (в CI `task-branch` на\n+  существующую ветку откажет ДО аренды, вне CI сам найдёт/заведёт рабочее\n+  дерево, но аренду не берёт — возьми сам): отказ «занята» при доводке может\n   означать твою же аренду — если держатель ты сам (транспорт пробросил\n   `LEASE_ALREADY_CLAIMED`), это не чужой канал, работай дальше.\n - **Ветка задачи создаётся только скриптом** `scripts/git/task-branch <N>-<slug>`.\n   Он же берёт аренду задачи (#121) перед созданием ветки (#356) — отказ аренды\n   («занята») останавливает создание ветки, назвав, где искать держателя; аренда\n-  под уже существующую ветку не берётся вовсе (доводка существующей — чекаут,\n-  не повторный запуск скрипта). Голый `git switch -c … origin/main` запрещён:\n+  под уже существующую ветку не берётся вовсе: в CI доводка существующей —\n+  чекаут (повторный запуск откажет ДО аренды), вне CI повторный запуск сам\n+  найдёт/заведёт рабочее дерево и напечатает путь, аренду для доводки возьми\n+  руками. Голый `git switch -c … origin/main` запрещён:\n   оркестратор сливает PR на сервере, и локальный\n   main протухает молча — так появляются ветки от старой базы. Скрипт доказывает свежесть\n   через `git ls-remote` и ставит гвардию `.githooks/pre-commit` (`core.hooksPath`), которая\n   перед каждым коммитом сверяется с сервером и блокирует коммит от протухшей базы\n-  (лечится `git rebase origin/main`).\n+  (лечится `git rebase origin/main`). Вне CI тот же скрипт заводит или\n+  переиспользует рабочее дерево задачи `.claude/worktrees/<N>-<slug>` и печатает\n+  путь: коммит в `agent/<N>-*` разрешён только из дерева, чьё имя начинается с\n+  `<N>-`, — ту же гвардию это проверяет перед каждым коммитом (класс #332),\n+  отказ лечится вызовом `scripts/git/task-branch <N>-<slug>` и работой из\n+  напечатанного пути.\n - **Прямой пуш в main** не проходит обязательные проверки. Только ветка `agent/<N>-<slug>` и PR.\n - **Слияния делает оркестратор** (workflow `orchestra`), по одному PR за проход и до\n   нескольких за запуск (цикл слияний, #297).",
      "sha": "700425089e410d08a145ff531313cf7fa2952fab",
      "status": "modified"
    },
    {
      "filename": "docs/agents/PROTOCOL.md",
      "patch": "@@ -142,13 +142,26 @@ escalate`, второй канал не заводится): комментар\n    ещё не взял транспорт) — отказ аренды останавливает создание ветки, называя\n    где искать держателя (assignee и комментарий 🔒 issue), что делать (взять\n    другую задачу) и выход, если замок твой же с прошлого умершего прогона\n-   (`claim_task.py release N`); ветка, которая уже существует (локально или на\n-   origin), отклоняется ДО аренды — доводка существующей идёт чекаутом, не\n-   повторным запуском скрипта. Поломка пути аренды (`claim_task.py` rc=2) —\n+   (`claim_task.py release N`); в CI (GITHUB_ACTIONS=true) ветка, которая уже\n+   существует (локально или на origin), отклоняется ДО аренды — доводка\n+   существующей идёт чекаутом, не повторным запуском скрипта. Вне CI\n+   существующая ветка отказом не считается: до аренды она получает своё\n+   рабочее дерево (переиспользование или усыновление, абзац ниже) — повторный\n+   запуск не валился бы о собственный замок и не вешал осиротевший\n+   (класс #360, интеграция #332). Поломка пути аренды (`claim_task.py` rc=2) —\n    предупреждение, ветка создаётся без аренды; полный офлайн/`gh` недоступен\n    громко отказывает раньше — гвардия эпика (#376), офлайн-режима у входа в\n    задачу нет. Голый `git checkout -b … origin/main` запрещён.\n    Локальный main — только зеркало origin (`git pull --ff-only`), коммитить в него нельзя.\n+   Вне CI `task-branch` сам заводит или переиспользует рабочее дерево\n+   `.claude/worktrees/<N>-<slug>` и печатает путь — работай оттуда, не в общем\n+   чекауте. Правило одной фразой (класс #332: три чужих коммита в чужом дереве\n+   за сутки — #320 в дереве оркестратора на чужой ветке, незакоммиченный дифф\n+   #297 там же, главное дерево переключено из-под работавшего агента):\n+   **коммит в `agent/<N>-*` разрешён только из дерева, чьё имя начинается с\n+   `<N>-`** — `.githooks/pre-commit` проверяет это перед каждым коммитом и при\n+   несовпадении отказывает, называя ветку, текущее дерево и команду\n+   `scripts/git/task-branch <N>-<slug>`, которая заведёт/найдёт правильное.\n 4. Прямые пуши в main отклоняются защитой **молча под `push -q`** — проверяй пуши без\n    `-q`. На конфликтном PR проверки не запускаются вовсе («Checks awaiting conflict\n    resolution») — сигнал перебазироваться, а не поломка CI. main ушёл вперёд, пока ты",
      "sha": "2ae54d26d21fbe8bc47d214c578916f643312ac2",
      "status": "modified"
    },
    {
      "filename": "docs/agents/WORKER-PLAYBOOK.md",
      "patch": "@@ -61,15 +61,22 @@\n    (`python3 scripts/lib/claim_task.py release N`) и повтори `task-branch` —\n    чужой живой замок так снимать нельзя, это уход к другому каналу, бери\n    другую задачу. Ветка `agent/N-slug` уже существует (повторный запуск,\n-   чужой незаброшенный прогон) — `task-branch` откажет ДО аренды: доводка\n-   существующей ветки — чекаут (`git fetch origin <ветка> && git checkout -B\n-   <ветка> origin/<ветка>`), не повторное создание. Если на задачу уже\n+   чужой незаброшенный прогон) — повторного создания не бывает: в CI\n+   `task-branch` откажет ДО аренды, вне CI тот же вызов\n+   `scripts/git/task-branch N-slug` сам найдёт/заведёт рабочее дерево задачи\n+   `.claude/worktrees/<N>-<slug>` (#332) и напечатает путь — доводка ведётся\n+   оттуда, потому что гвардия «дерево == задача» (`.githooks/pre-commit`)\n+   коммит в `agent/<N>-*` из главного чекаута отклонит. Ответил «локальная\n+   ветка разошлась с origin/<ветка>» — синхронизируйся по напечатанной команде\n+   (`git fetch origin <ветка>`, затем `git branch -f` или `rebase`) и повтори.\n+   Если на задачу уже\n    открыт PR без\n    исполнителя (`task_ref.resolve_pr_task` — единственный источник это имя\n    agent-ветки, тело PR не читается вовсе, решение владельца 2026-09-06,\n-   `scripts/lib/task_ref.py`, #394) — чекаут его ветки, новый PR не открывать;\n-   этот путь `task-branch` не вызывает — аренду для него возьми сам\n-   (`claim_task.py claim N`) до чекаута (см. исключение в пункте 2 выше).\n+   `scripts/lib/task_ref.py`, #394) — доводи его ветку из дерева задачи\n+   (предыдущий абзац: `task-branch` его найдёт/заведёт), новый PR не\n+   открывать; этот путь `task-branch` не вызывает — аренду для него возьми сам\n+   (`claim_task.py claim N`) до работы (см. исключение в пункте 2 выше).\n    Гвардия свежести (та же, что у `task-branch`) — при «main уехал»\n    `git rebase origin/main` и повтори.\n 4. Перед кодом: `docs/INDEX.md` → релевантные research/спеки; архитектурное",
      "sha": "3b569e007aba93c05efc1143ab21f0c2e750d5c5",
      "status": "modified"
    },
    {
      "filename": "openspec/changes/worktree-branch-guard/proposal.md",
      "patch": "@@ -0,0 +1,57 @@\n+# worktree-branch-guard: рабочее дерево задачи — инвариант, а не дисциплина\n+\n+Задача: #332. PR: #333.\n+Дельта-спека: `specs/journal-tasks-hands/spec.md` — дополнение п.28 «Захват\n+задачи воркером» инвариантом рабочего дерева.\n+\n+## Класс закрываемой ошибки\n+\n+За сутки три инцидента одного класса (контекст #332): агент по задаче #320\n+правил файлы в дереве оркестратора\n+(`.claude/worktrees/pr-workflow-sessions-8f1ab5`) на чужой ветке\n+`agent/258-...`; там же найдены незакоммиченные диффы чужой ветки #297;\n+главное дерево переключено на чужую ветку из-под работавшего агента.\n+Дисциплина «не путай деревья» держалась на памяти трижды за сутки — теперь\n+она enforced механически.\n+\n+## Решение\n+\n+1. `.githooks/pre-commit` — гвардия «дерево == задача»: коммит в\n+   `agent/<N>-*` отклоняется, если имя рабочего дерева (basename top-level\n+   пути) не начинается с `N-`. Отказ называет ветку, текущее дерево и\n+   лечащую команду. Пропуск для CI (`GITHUB_ACTIONS=true`, одноразовый\n+   чекаут воркера называется `edge-harness`) печатает ПРЕДУПРЕЖДЕНИЕ в\n+   stderr — тихого обхода гвардии нет.\n+2. `scripts/git/task-branch` вне CI заводит или переиспользует дерево\n+   `<корень репозитория>/.claude/worktrees/<N>-<slug>` и печатает путь\n+   агенту — легаси-разнобой имён (`246-lease-claim` на ветке `agent/121-...`,\n+   `pr-workflow-sessions-8f1ab5` на `agent/323-...`) перестаёт\n+   воспроизводиться. Повторный вызов на существующую ветку, усыновление\n+   осиротевшей и ветки, существующей только на origin (серверная голова, не\n+   тихая локальная копия от origin/main), стоят ДО аренды (класс #360: ничего\n+   не захватив, ничего не оставляет осиротевшим; вместо аренды — напоминание\n+   взять её вручную) и отказывают с командой синхронизации,\n+   если локальная голова разошлась с `origin/<ветка>` — PR мог подвинуть\n+   другой канал.\n+3. Существующие деревья не удаляются и не переименовываются: единственный\n+   `git worktree prune` вычищает только админ-запись, чей каталог уже стёрт\n+   с диска, и кричит об этом.\n+\n+## Доказательство\n+\n+- `scripts/git/test/worktree-guard.test.sh` — РЕАЛЬНЫЙ файл хука на живых\n+  git-репозиториях и worktree: совпадение проходит, чужое дерево отклоняется\n+  с внятным сообщением, вне `agent/*` правило неприменимо. Мутация: снять\n+  блок гвардии — случай 2 краснеет.\n+- `scripts/git/test/task-branch.test.sh`, случаи 8–14: заведение дерева,\n+  переиспользование без дубля, CI-режим, стык «CI-переключение → коммит под\n+  настоящим хуком», отказ при расхождении с `origin/<ветка>` на путях\n+  переиспользования и усыновления, свои незапушенные коммиты не отсекаются.\n+  Мутация: снять `branch_diverged_from_origin` — случаи 12/13 краснеют.\n+\n+## Газ\n+\n+Гвардия останавливает только коммит не из своего дерева; возврат движения —\n+одна команда `scripts/git/task-branch <N>-<slug>`, напечатанная в самом\n+отказе. Автоматического возврата не нужно: отказ сам называет единственное\n+лечащее действие, и оно не требует чужих рук.",
      "sha": "5fc974a1997b31d4a00c0d85b24992fa31c0a3d4",
      "status": "added"
    },
    {
      "filename": "openspec/changes/worktree-branch-guard/specs/journal-tasks-hands/spec.md",
      "patch": "@@ -0,0 +1,54 @@\n+# Дельта-спека: журнал (journal-tasks-hands) — рабочее дерево задачи (#332)\n+\n+Дополняет п.28 (захват задачи воркером, #121) — не отменяет его, добавляет\n+инвариант рабочего дерева, недостающий в описанном там поведении.\n+\n+## MODIFIED: Автономный воркер (конвейер пула)\n+\n+28. Захват задачи воркером — атомарная аренда (ADR 0006, #121):\n+    `scripts/lib/claim_task.py claim` серверно создаёт замок\n+    `refs/locks/task-N` и тем же вызовом ставит назначение и след в задаче.\n+    Назначение — видимость, а не защита; защита от гонки каналов — замок,\n+    проигравший гонку получает отказ. Работа идёт в ветке `agent/N-slug` от\n+    свежего origin/main (вход — `scripts/git/task-branch`); если на задачу\n+    уже открыт PR без исполнителя, воркер доводит его ветку, второй PR не\n+    открывает (#245). `task-branch` перед созданием ветки отказывает, если N —\n+    эпик (`is_epic_issue`, тот же признак, что использует приёмка #335):\n+    критерий готовности эпика распределён по нескольким стройкам и недостижим\n+    в одном PR, отказ называет газ — взять узкую задачу конкретной стройки\n+    (#376, класс воспроизведён: PR #162, 44 раунда ревью на ветку, привязанную\n+    к эпику #77). Исполняет задачу DSH headless; транспорт\n+    (scripts/worker/task.sh) не принимает за агента содержательных решений.\n+    Пул кандидатов (до фильтра свободы и аренды) собирается через\n+    `scripts/lib/task_deps.py` (GraphQL) — REST-листинг не отдаёт\n+    `blockedBy`/`blocking`, одним пагинированным запросом, не двумя\n+    расходящимися (REST для списка + GraphQL для графа).\n+\n+    **Инвариант рабочего дерева (#332):** коммит в `agent/<N>-*` разрешён\n+    только из рабочего дерева, чьё имя (basename top-level пути) начинается\n+    с того же числа `N-`. Проверяет `.githooks/pre-commit` перед каждым\n+    коммитом, чисто локально и до гвардии свежести base; отказ называет\n+    ветку, текущее дерево и лечащую команду `scripts/git/task-branch\n+    <N>-<slug>` — она вне CI заводит или переиспользует\n+    `<корень репозитория>/.claude/worktrees/<N>-<slug>` и печатает путь\n+    агенту, так что легаси-разнобой имён деревьев (`246-lease-claim` на\n+    ветке `agent/121-...`, `pr-workflow-sessions-8f1ab5` на\n+    `agent/323-...`) больше не воспроизводится. В CI\n+    (`GITHUB_ACTIONS=true`) гвардия отключается с ПРЕДУПРЕЖДЕНИЕМ в stderr:\n+    одноразовый чекаут воркера называется `edge-harness`, и без исключения\n+    отказ падал бы на штатный путь конвейера, а не на инцидент #332\n+    (спутанные деревья агента/человека локально). Повторный вызов\n+    `task-branch` на существующую ветку переиспользует или усыновляет\n+    дерево до аренды (класс #360: ничего не захватив, ничего не оставляет\n+    осиротевшим) и отказывает с командой синхронизации, если локальная\n+    голова разошлась с `origin/<ветка>` — PR мог подвинуть другой канал\n+    (оркестратор снимает исполнителя с конфликтного PR и диспатчит нового\n+    воркера); свои незапушенные коммиты (origin — предок локальной головы)\n+    расхождением не считаются. Ветка, существующая только на origin (чужой\n+    PR, локальное дерево потеряно), усыновляется СЕРВЕРНОЙ головой — молча\n+    заводить одноимённую локальную ветку от origin/main значило бы тихо\n+    подменить чужую работу; аренда на путях переиспользования и усыновления\n+    не берётся (класс #360), взамен печатается напоминание взять её вручную.\n+    Существующие деревья скрипт не удаляет и не\n+    переименовывает: единственный `git worktree prune` вычищает только\n+    админ-запись, чей каталог уже стёрт с диска.",
      "sha": "14f7418e67c17de270c7d0109d0e36235286c7bd",
      "status": "added"
    },
    {
      "filename": "scripts/git/task-branch",
      "patch": "@@ -8,7 +8,17 @@\n #   1) fetch + авторитетная сверка ls-remote — тихо упавший fetch base не выдаёт;\n #   2) самовосстановление core.hooksPath — гвардия свежести обязательна всегда.\n #\n-# Использование: scripts/git/task-branch <номер>-<slug>\n+# Рабочее дерево (#332): агенты заводили `.claude/worktrees/<...>` вручную кто\n+# как — разнобой имён стал причиной трижды за сутки (агент правил чужую ветку\n+# в чужом дереве, дифф чужой задачи затирался). Здесь дерево заводится/\n+# переиспользуется по одному правилу (`.githooks/pre-commit` его же и\n+# проверяет): `<корень репозитория>/.claude/worktrees/<номер>-<slug>`. В CI\n+# (GITHUB_ACTIONS=true) поведение старое — переключение ветки в текущем\n+# каталоге: воркер (scripts/worker/task.sh) уже работает в одноразовом\n+# изолированном чекауте на один прогон, заводить там ещё и worktree незачем\n+# и меняло бы каталог работы у уже написанного пайплайна.\n+#\n+# Использование: scripts/git/task-branch <номер>-<slug>   (без изменений)\n set -euo pipefail\n \n task=\"${1:-}\"\n@@ -114,18 +124,196 @@ if [ -n \"$epic_task_num\" ]; then\n   fi\n fi\n \n-# Ветка уже существует — повторно не заводим и аренду под неё НЕ берём (находка\n-# ревью PR #360): иначе claim проходит, а `git switch -c` ниже падает на\n-# существующем имени — замок остаётся висеть до TTL-сборщика (24 ч), ровно тот\n-# осиротевший замок, от которого этот скрипт защищает. Проверка ДО claim'а и\n-# ДО флага транспорта: существующая ветка — это доводка, у неё другой вход\n-# (чекаут, WORKER-PLAYBOOK.md «Ветка»), и аренда там берётся вручную до чекаута.\n-if git show-ref --verify --quiet \"refs/heads/agent/$task\" \\\n-   || git ls-remote --heads origin \"refs/heads/agent/$task\" | grep -q .; then\n-  echo \"ОШИБКА: ветка agent/$task уже существует — task-branch заводит только НОВЫЕ ветки.\" >&2\n-  echo \"  Доводи существующую: git fetch origin agent/$task && git checkout -B agent/$task origin/agent/$task,\" >&2\n-  echo \"  аренду для доводки возьми вручную (python3 scripts/lib/claim_task.py claim N) ДО чекаута.\" >&2\n-  exit 1\n+branch=\"agent/$task\"\n+\n+# Печать ключевых граблей инфраструктуры в момент старта задачи, а не когда-нибудь\n+# потом «если вспомнить прочитать» (задача #323): task-branch — один из ДВУХ\n+# входов в agent-ветку (второй — доводка уже открытого PR в scripts/worker/task.sh,\n+# он тоже зовёт print_infra_digest напрямую, минуя этот файл, — см. #326 находка 1).\n+# Текст — одно место правды в scripts/gh/infra_digest.sh, не копия здесь.\n+# Функция, а не копии: стартов задачи три (усыновление осиротевшей ветки,\n+# CI-переключение, новое дерево) — путь печатается последним в каждом, грабли\n+# идут перед ними. На переиспользовании (повторный вызов держателем) дайджест\n+# не повторяется: агент его уже видел при старте, ему нужен путь.\n+print_digest() {\n+  source \"$(dirname \"${BASH_SOURCE[0]}\")/../gh/infra_digest.sh\"\n+  print_infra_digest\n+}\n+\n+# Сверка локальной ветки с origin/<branch> (находка ревью #333): локальная\n+# голова могла отстать или разойтись с серверной — PR мог подвинуть другой\n+# канал (оркестратор снимает исполнителя с конфликтного PR и диспатчит нового\n+# воркера). Гвардия свежести в .githooks/pre-commit сверяет только origin/main\n+# и этот стык не видит — коммит поверх чужой головы валился бы уже только на\n+# пуше. Отказ — до работы, с командой синхронизации. Свои незапушенные коммиты\n+# (origin — предок локальной головы) расхождением НЕ считаются: это штатная\n+# середина работы держателя.\n+branch_diverged_from_origin() {\n+  git show-ref --verify -q \"refs/remotes/origin/$branch\" || return 1\n+  local local_sha remote_sha\n+  local_sha=$(git rev-parse \"refs/heads/$branch\")\n+  remote_sha=$(git rev-parse \"refs/remotes/origin/$branch\")\n+  [ \"$local_sha\" = \"$remote_sha\" ] && return 1\n+  git merge-base --is-ancestor \"$remote_sha\" \"$local_sha\" && return 1\n+  return 0\n+}\n+\n+if [ \"${GITHUB_ACTIONS:-}\" = \"true\" ]; then\n+  # CI: одноразовый изолированный чекаут на прогон — worktree не нужен,\n+  # старое поведение (переключение в текущем каталоге) сохранено как есть.\n+  # Отказ «ветка уже существует» (#360) живёт и здесь: иначе claim (ниже)\n+  # проходит, а `git switch -c` (ниже) падает на существующем имени — замок\n+  # остаётся висеть до TTL-сборщика (24 ч), ровно тот осиротевший замок,\n+  # от которого отказ защищает. Проверка ДО claim'а и ДО флага транспорта:\n+  # существующая ветка — это доводка, у неё другой вход.\n+  if git show-ref --verify --quiet \"refs/heads/$branch\" \\\n+     || git ls-remote --heads origin \"refs/heads/$branch\" | grep -q .; then\n+    echo \"ОШИБКА: ветка $branch уже существует — task-branch заводит только НОВЫЕ ветки.\" >&2\n+    echo \"  Доводи существующую: git fetch origin $branch && git checkout -B $branch origin/$branch,\" >&2\n+    echo \"  аренду для доводки возьми вручную (python3 scripts/lib/claim_task.py claim $task_number) ДО чекаута.\" >&2\n+    exit 1\n+  fi\n+else\n+  # ── Рабочее дерево (#332) ──────────────────────────────────────────────────\n+  # Разрешение дерева стоит ДО аренды: повторный запуск на той же задаче\n+  # обязан напечатать путь, а не отвалиться о собственный замок (claim не\n+  # идемпотентен по актору, #356), и не оставить осиротевший замок — оба\n+  # выхода ниже стоят до claim'а и ничего не захватывают (класс #360).\n+  main_root=$(dirname \"$(git rev-parse --path-format=absolute --git-common-dir)\")\n+  wt_dir=\"$main_root/.claude/worktrees/$task\"\n+\n+  # Уже есть worktree на эту ветку (повторный запуск на той же задаче) —\n+  # переиспользуем его, а не пытаемся завести дубль (git worktree add откажет\n+  # сам, но сообщение должно указывать путь, а не сырой git-эксепшн).\n+  #\n+  # Только под .claude/worktrees/ (находка ревью #333): главная запись\n+  # `worktree list` (корень репозитория) не годится — это и есть инцидент\n+  # #332 (ветка задачи зачекаучена в главном дереве). Легализовать его тут\n+  # значило бы тут же противоречить .githooks/pre-commit, которая коммит из\n+  # главного дерева на agent/<N>-* отклонит. Ветка вне .claude/worktrees/ —\n+  # заводим отдельное дерево по правилу, а не переиспользуем чужое место.\n+  existing_path=\"\"\n+  while IFS= read -r line; do\n+    case \"$line\" in\n+      worktree\\ *) cur_path=\"${line#worktree }\" ;;\n+      branch\\ refs/heads/\"$branch\")\n+        case \"$cur_path\" in\n+          \"$main_root\"/.claude/worktrees/*) existing_path=\"$cur_path\" ;;\n+        esac\n+        ;;\n+    esac\n+  done < <(git worktree list --porcelain)\n+\n+  if [ -n \"$existing_path\" ]; then\n+    if [ -d \"$existing_path\" ]; then\n+      # Имя дерева обязано отвечать тому же правилу <N>-, что проверяет\n+      # гвардия (находка ревью #333, раунд 4): легаси-дерево (например\n+      # «246-lease-claim» на agent/121-...) иначе замыкает цикл — лечащая\n+      # команда из отказа хука вечно печатает то же дерево, хук вечно\n+      # отклоняет коммит. Путь не выдаём: отказ с переносом дерева; сам\n+      # перенос делает владелец — скрипт существующие деревья не двигает и\n+      # не удаляет (критерий #332).\n+      wt_name=$(basename \"$existing_path\")\n+      if [[ ! \"$wt_name\" =~ ^${task_number}(-|$) ]]; then\n+        echo \"ОШИБКА: дерево $existing_path закреплено за веткой $branch, но имя дерева не начинается с «$task_number-» — гвардия (#332) коммит там отклонит.\" >&2\n+        echo \"  Перенеси дерево на каноническое имя и повтори scripts/git/task-branch $task:\" >&2\n+        echo \"    git worktree move \\\"$existing_path\\\" \\\"$wt_dir\\\"\" >&2\n+        exit 1\n+      fi\n+      if branch_diverged_from_origin; then\n+        echo \"ОШИБКА: локальная ветка $branch ($(git rev-parse --short \"refs/heads/$branch\")) разошлась с origin/$branch ($(git rev-parse --short \"refs/remotes/origin/$branch\")).\" >&2\n+        echo \"  Её мог подвинуть другой канал — коммит поверх чужой головы не выдаю (класс #332/#333).\" >&2\n+        echo \"  Синхронизируй дерево и повтори scripts/git/task-branch $task:\" >&2\n+        echo \"    git -C \\\"$existing_path\\\" rebase origin/$branch\" >&2\n+        exit 1\n+      fi\n+      echo \"Ветка $branch уже заведена — переиспользую рабочее дерево:\"\n+      echo \"Напоминание: аренду здесь task-branch не берёт — доводка берёт её вручную (python3 scripts/lib/claim_task.py claim $task_number), если ещё не держишь.\" >&2\n+      echo \"$existing_path\"\n+      exit 0\n+    fi\n+    # Протухшая запись (находка ревью #333): каталог стёрт мимо\n+    # `git worktree remove` — не молчать и не выдавать путь в никуда, а\n+    # вычистить запись и завести дерево заново.\n+    echo \"Рабочее дерево $existing_path пропало с диска — чищу протухшую запись и завожу заново.\" >&2\n+    git worktree prune\n+  fi\n+\n+  if git rev-parse --verify -q \"refs/heads/$branch\" >/dev/null; then\n+    # Ветка существует, но нужного дерева под .claude/worktrees/ нет. Если она\n+    # при этом зачекаучена ГДЕ-ТО ещё (например прямо в главном дереве —\n+    # инцидент #332) — не легализуем это молча (находка ревью #333):\n+    # `git worktree add` на такую ветку упадёт сырым git-фаталом, здесь —\n+    # понятная ошибка с указанием, где ветка застряла.\n+    checked_out_elsewhere=\"\"\n+    while IFS= read -r line; do\n+      case \"$line\" in\n+        worktree\\ *) cur_path=\"${line#worktree }\" ;;\n+        branch\\ refs/heads/\"$branch\") checked_out_elsewhere=\"$cur_path\" ;;\n+      esac\n+    done < <(git worktree list --porcelain)\n+    if [ -n \"$checked_out_elsewhere\" ]; then\n+      echo \"ОШИБКА: ветка $branch уже зачекаучена вне .claude/worktrees/:\" >&2\n+      echo \"  $checked_out_elsewhere\" >&2\n+      echo \"Не переиспользую чужое место (класс #332) — перенеси ветку туда самостоятельно:\" >&2\n+      echo \"  git -C \\\"$checked_out_elsewhere\\\" switch - (вернуть исходную ветку)\" >&2\n+      echo \"  git worktree add \\\"$wt_dir\\\" $branch\" >&2\n+      exit 1\n+    fi\n+    if branch_diverged_from_origin; then\n+      echo \"ОШИБКА: локальная ветка $branch ($(git rev-parse --short \"refs/heads/$branch\")) разошлась с origin/$branch ($(git rev-parse --short \"refs/remotes/origin/$branch\")).\" >&2\n+      echo \"  Её мог подвинуть другой канал — под дерево ставлю только свежую ветку (класс #332/#333).\" >&2\n+      echo \"  Синхронизируйся и повтори scripts/git/task-branch $task:\" >&2\n+      echo \"    git branch -f $branch origin/$branch   # своих коммитов на ветке нет/не нужны\" >&2\n+      echo \"    git worktree add \\\"$wt_dir\\\" $branch && git -C \\\"$wt_dir\\\" rebase origin/$branch   # нужны — усынови сам и перенеси\" >&2\n+      exit 1\n+    fi\n+    # Осиротела (нигде не зачекаучена) — заводим дерево на неё, не создавая\n+    # вторую ветку. Аренду task-branch на путях переиспользования/усыновления\n+    # не берёт (выходы стоят до claim'а, класс #360) — доводка берёт её\n+    # вручную (WORKER-PLAYBOOK, п. 2–3), поэтому напоминание в каждом выводе.\n+    git worktree add \"$wt_dir\" \"$branch\"\n+    git config core.hooksPath .githooks\n+    echo \"Напоминание: аренду здесь task-branch не берёт — доводка берёт её вручную (python3 scripts/lib/claim_task.py claim $task_number), если ещё не держишь.\" >&2\n+    echo \"Рабочее дерево (иди туда, коммить оттуда — .githooks/pre-commit это проверяет, #332):\"\n+    echo \"$wt_dir\"\n+    print_digest\n+    exit 0\n+  fi\n+\n+  # Ветки локально нет, но на origin она есть (чужой PR или работа прежнего\n+  # прогона, дерево потеряно): молча заводить одноимённую ветку от origin/main\n+  # нельзя — `git worktree add -b` создал бы локальную голову поверх чужой\n+  # базы, и коммиты агента тихо подменили бы работу другого канала (класс\n+  # #332; живое репро — находка ревью #333). Усыновляем СЕРВЕРНУЮ голову —\n+  # «найдёт/заведёт» из WORKER-PLAYBOOK становится правдой.\n+  if git show-ref --verify -q \"refs/remotes/origin/$branch\"; then\n+    mkdir -p \"$(dirname \"$wt_dir\")\"\n+    git worktree add -b \"$branch\" \"$wt_dir\" \"refs/remotes/origin/$branch\"\n+    git config core.hooksPath .githooks\n+    echo \"Ветка $branch существует на origin — усыновляю серверную голову ($(git rev-parse --short \"refs/remotes/origin/$branch\")).\"\n+    echo \"Напоминание: аренду здесь task-branch не берёт — доводка берёт её вручную (python3 scripts/lib/claim_task.py claim $task_number), если ещё не держишь.\" >&2\n+    echo \"Рабочее дерево (иди туда, коммить оттуда — .githooks/pre-commit это проверяет, #332):\"\n+    echo \"$wt_dir\"\n+    print_digest\n+    exit 0\n+  fi\n+  # Путь дерева занят (зарегистрированное дерево на другой ветке — легаси\n+  # имена с числовым префиксом делают коллизию вероятной, — или просто\n+  # посторонняя папка): падать сквозь claim на сыром `git worktree add`\n+  # нельзя — аренда уже взята и остаётся висеть до TTL-сборщика (класс\n+  # #360), а ветка успевает создаться (находка ревью #333, раунд 4, живое\n+  # репро). Отказ ДО claim'а: называем занятие и лечение; разбор пути —\n+  # действие владельца дерева, скрипт существующие деревья не трогает.\n+  if [ -e \"$wt_dir\" ]; then\n+    echo \"ОШИБКА: путь $wt_dir занят (не пуст) — завести туда дерево задачи нельзя.\" >&2\n+    echo \"  Разберись с путём вручную, затем повтори scripts/git/task-branch $task:\" >&2\n+    echo \"    git worktree list   # найти, какое дерево занимает путь\" >&2\n+    echo \"    git worktree remove \\\"\\$путь\\\"   # если это дерево задачи и оно больше не нужно\" >&2\n+    exit 1\n+  fi\n+  # Ветки нет ни локально, ни в дереве, ни на origin — падаем сквозь: аренда\n+  # ниже, затем создание ветки СРАЗУ в дереве (хвост файла).\n fi\n \n # Аренда задачи (#121, ADR 0006) — до сих пор её брал только облачный воркер\n@@ -220,17 +408,22 @@ else\n   fi\n fi\n \n-git switch -c \"agent/$task\" refs/remotes/origin/main\n+if [ \"${GITHUB_ACTIONS:-}\" = \"true\" ]; then\n+  git switch -c \"$branch\" refs/remotes/origin/main\n+  git config core.hooksPath .githooks\n+  echo \"Ветка $branch от origin/main $(git rev-parse --short origin/main) ($(git log -1 --format=%cd --date=iso origin/main))\"\n+  print_digest\n+  exit 0\n+fi\n \n-# Гвардия свежести ставится каждым запуском: пересозданный клон защищён сразу.\n+# Гвардия свежести ставится в общем конфиге репозитория (git config без\n+# --worktree пишет в <git-common-dir>/config) — общий для ВСЕХ worktree,\n+# ставить заново в каждом дереве не нужно, но лишним не будет.\n git config core.hooksPath .githooks\n \n-echo \"Ветка agent/$task от origin/main $(git rev-parse --short origin/main) ($(git log -1 --format=%cd --date=iso origin/main))\"\n-\n-# Печать ключевых граблей инфраструктуры в момент старта задачи, а не когда-нибудь\n-# потом «если вспомнить прочитать» (задача #323): task-branch — один из ДВУХ\n-# входов в agent-ветку (второй — доводка уже открытого PR в scripts/worker/task.sh,\n-# он тоже зовёт print_infra_digest напрямую, минуя этот файл, — см. #326 находка 1).\n-# Текст — одно место правды в scripts/gh/infra_digest.sh, не копия здесь.\n-source \"$(dirname \"${BASH_SOURCE[0]}\")/../gh/infra_digest.sh\"\n-print_infra_digest\n+mkdir -p \"$(dirname \"$wt_dir\")\"\n+git worktree add -b \"$branch\" \"$wt_dir\" refs/remotes/origin/main\n+echo \"Ветка $branch от origin/main $(git rev-parse --short origin/main) ($(git log -1 --format=%cd --date=iso origin/main))\"\n+echo \"Рабочее дерево (иди туда, коммить оттуда — .githooks/pre-commit это проверяет, #332):\"\n+echo \"$wt_dir\"\n+print_digest",
      "sha": "26774421204302c7bb1a51f7efbec0b0ad43fca4",
      "status": "modified"
    },
    {
      "filename": "scripts/git/test/task-branch.test.sh",
      "patch": "@@ -11,10 +11,36 @@\n # в scripts/git/task-branch (от `task_number=` до строки перед `git fetch\n # origin --prune`) — случай 2 (закрытая задача) перестаёт отклоняться, тест\n # краснеет. Верни блок — тест снова зелёный.\n+#\n+# Случаи 8-10 — рабочее дерево (#332): task-branch заводит/переиспользует\n+# рабочее дерево, не только ветку:\n+#   8) вне CI (без GITHUB_ACTIONS) — заводит .claude/worktrees/<task>,\n+#      печатает путь, текущий каталог остаётся на своей ветке;\n+#   9) повторный вызов на ту же задачу — переиспользует то же дерево, не\n+#      падает и не плодит второе;\n+#   10) в CI (GITHUB_ACTIONS=true) — старое поведение: переключает ветку\n+#       прямо в текущем каталоге, worktree не заводит (обратная совместимость\n+#       с scripts/worker/task.sh, который сам работает в одноразовом чекауте);\n+#   11) стык с гвардией (находка ревью #333): после переключения ветки в\n+#       CI-режиме коммит настоящим .githooks/pre-commit обязан проходить —\n+#       без этого случая случай 10 (CI-режим task-branch) и\n+#       worktree-guard.test.sh (гвардия вне CI) проверяют половины отдельно,\n+#       а стык — коммит после CI-переключения под настоящим хуком — не\n+#       покрывает никто;\n+#  12-14) сверка локальной ветки с origin/<branch> (находка ревью #333):\n+#      12) ветку подвинул другой канал — переиспользование отказывает\n+#          с командой синхронизации;\n+#      13) то же на пути усыновления (дерево пропало с диска);\n+#      14) свои незапушенные коммиты расхождением не считаются —\n+#          переиспользование работает;\n+#     15) ветка существует только на origin (чужой PR, дерево потеряно) —\n+#          усыновляется СЕРВЕРНАЯ голова, а не тихо заводится одноимённая\n+#          локальная от origin/main (класс #332, репро ревью #333).\n set -euo pipefail\n \n REPO_ROOT=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/../../..\" && pwd)\"\n SCRIPT_SRC=\"$REPO_ROOT/scripts/git/task-branch\"\n+HOOK_SRC=\"$REPO_ROOT/.githooks/pre-commit\"\n \n WORK=\"$(mktemp -d)\"\n cleanup() { rm -rf \"$WORK\"; }\n@@ -115,18 +141,46 @@ run_task_branch() {\n   local dirname=\"$1\" task=\"$2\"\n   (\n     cd \"$WORK/$dirname\"\n-    PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" \"$task\"\n+    # -u GITHUB_ACTIONS: случаи 1-7 симулируют локальный не-CI запуск\n+    # (worktree, не переключение ветки на месте, #332) — если сам этот тест\n+    # выполняется внутри настоящего GitHub Actions job, переменная\n+    # GITHUB_ACTIONS=true уже стоит в окружении и наследовалась бы сюда,\n+    # заставляя task-branch взять CI-ветку поведения по ошибочной причине.\n+    env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" \"$task\"\n   )\n }\n \n+# Вне CI (#332) успешный запуск заводит ОТДЕЛЬНЫЙ worktree на agent/<task>,\n+# не переключает ветку в текущем каталоге. `git worktree list` перечисляет и\n+# сам исходный чекаут (первой строкой) — если бы скрипт вместо worktree\n+# сделал `git switch -c` на месте, исходный каталог сам оказался бы\n+# «совпадением» и маскировал регресс; поэтому путь ещё сверяется с исходным\n+# каталогом — совпадение с ним не считается заведённым worktree.\n+worktree_for_branch() {\n+  local dirname=\"$1\" branch=\"$2\"\n+  # Оба пути — из самого git (не из shell pwd): на Windows/Cygwin `git worktree\n+  # list` печатает диск-стиль (C:/Users/...), а builtin pwd — POSIX-стиль\n+  # (/c/Users/...) для ОДНОГО и того же каталога — прямое сравнение строк\n+  # дало бы ложное «это отдельный worktree» даже когда скрипт остался на\n+  # месте (`git switch -c`), маскируя ровно тот регресс, который тест обязан\n+  # ловить.\n+  local origin_dir path\n+  origin_dir=$(git -C \"$WORK/$dirname\" worktree list --porcelain | awk 'NR==1{print $2}')\n+  path=$(git -C \"$WORK/$dirname\" worktree list --porcelain \\\n+    | awk -v want=\"refs/heads/$branch\" '/^worktree /{p=$2} $0==\"branch \"want{print p}')\n+  [ -n \"$path\" ] || return 0\n+  [ \"$path\" = \"$origin_dir\" ] && return 0\n+  printf '%s\\n' \"$path\"\n+}\n+\n # ── случай 1: открытая задача с меткой task — ветка заводится ────────────────\n make_tree \"case1\"\n if out=$(run_task_branch \"case1\" \"61-good\" 2>\"$WORK/case1.stderr\"); then\n-  branch=$(git -C \"$WORK/case1\" branch --show-current)\n-  if [ \"$branch\" = \"agent/61-good\" ]; then\n-    note \"случай 1 (открыта, метка task): ветка заведена — ОК\"\n+  wt=$(worktree_for_branch \"case1\" \"agent/61-good\")\n+  if [ -n \"$wt\" ] && [ -d \"$wt\" ]; then\n+    note \"случай 1 (открыта, метка task): worktree на agent/61-good заведён — ОК\"\n   else\n-    note \"случай 1: ветка не та ($branch) — ОШИБКА\"\n+    note \"случай 1: worktree на agent/61-good НЕ найден — ОШИБКА\"\n     fail=1\n   fi\n else\n@@ -267,6 +321,7 @@ done\n make_tree \"case7\"\n if (\n   cd \"$WORK/case7\"\n+  unset GITHUB_ACTIONS\n   PATH=\"$safe_path:$shim\" \"$bash_bin\" \"$SCRIPT_SRC\" \"67-no-gh\"\n ) 2>\"$WORK/case7.stderr\"; then\n   note \"случай 7 (gh не найден): скрипт создал ветку — ОШИБКА, ожидался отказ гвардии эпика (офлайн-режима нет)\"\n@@ -281,7 +336,305 @@ else\n   fi\n fi\n \n+# ── рабочее дерево (#332): отдельные синтетические origin ───────────────────\n+# origin — локальный путь, не github-вид URL, поэтому проверка задачи в\n+# task-branch не резолвит настоящий owner/repo и всегда уходит в ветку\n+# «предупреждение, не отказ» — НО только если сам gh не сможет ответить.\n+# Фейковый gh из $WORK/bin (сделан для случаев 1-7) переиспользуется здесь:\n+# для номеров 99/55 он не знает маршрута и вернёт «неизвестный маршрут»\n+# (не HTTP 404) — предохраняет тест от НАСТОЯЩЕГО gh на PATH хоста, который\n+# иначе бы дошёл до api.github.com и мог вернуть подлинный 404 на несуществующий\n+# путь, ложно провалив случай (плавающий тест, воспроизведено локально).\n+new_origin() {\n+  local name=\"$1\"\n+  git init -q --bare \"$WORK/$name-origin.git\"\n+  git init -q \"$WORK/$name-seed\"\n+  (\n+    cd \"$WORK/$name-seed\"\n+    git config user.email test@example.com\n+    git config user.name test\n+    git commit -q --allow-empty -m init\n+    git remote add origin \"$WORK/$name-origin.git\"\n+    git push -q origin HEAD:refs/heads/main\n+  )\n+}\n+\n+# ── случай 8+9: вне CI — заводит и переиспользует worktree ──────────────────\n+# Скрипт зовётся по $SCRIPT_SRC (не копией): после #386 он тянет соседей\n+# (../lib/lease.sh, ../lib/epic_guard.py, ../gh/infra_digest.sh) относительно\n+# СЕБЯ — копия в синтетическом клоне осталась бы без них. Git-операции при\n+# этом идут в синтетический клон (cwd) и через insteadOf — в локальный bare;\n+# номера задач (67) взяты из маршрутов фейкового gh: epic guard (#376) на\n+# неизвестном маршруте отказывает громко, и случай 8 падал бы не по делу.\n+new_origin \"a\"\n+git clone -q --branch main \"$WORK/a-origin.git\" \"$WORK/a-main\" 2>/dev/null\n+\n+(cd \"$WORK/a-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-demo-slug) \\\n+  >\"$WORK/a-run1.out\" 2>&1 || { fail=1; note \"run1 упал:\"; cat \"$WORK/a-run1.out\"; }\n+\n+# Путь берём из самого git (worktree list), а не собираем строкой сами: на\n+# Windows/Cygwin git печатает диск-стиль (C:/Users/...), а $WORK — POSIX-стиль\n+# (/tmp/...) — один и тот же каталог, разные строки одного пути.\n+expected_wt=$(\n+  git -C \"$WORK/a-main\" worktree list --porcelain \\\n+    | awk '/^worktree /{p=$2} /^branch refs\\/heads\\/agent\\/67-demo-slug$/{print p}'\n+)\n+if [ -n \"$expected_wt\" ] && [ -d \"$expected_wt\" ]; then\n+  note \"случай 8 (вне CI, новая задача): worktree заведён на agent/67-demo-slug — ОК\"\n+else\n+  note \"случай 8 (вне CI, новая задача): worktree НЕ заведён — ОШИБКА\"\n+  fail=1\n+fi\n+if [ -n \"$expected_wt\" ] && grep -qF \"$expected_wt\" \"$WORK/a-run1.out\"; then\n+  note \"  путь напечатан агенту — ОК\"\n+else\n+  note \"  путь НЕ напечатан — ОШИБКА (агент не узнает, куда идти)\"\n+  fail=1\n+fi\n+main_branch_after=$(git -C \"$WORK/a-main\" rev-parse --abbrev-ref HEAD)\n+if [ \"$main_branch_after\" = \"master\" ] || [ \"$main_branch_after\" = \"main\" ]; then\n+  note \"  исходный каталог остался на своей ветке ($main_branch_after) — ОК\"\n+else\n+  note \"  исходный каталог переключился на $main_branch_after — ОШИБКА (не должен трогаться)\"\n+  fail=1\n+fi\n+\n+before_count=$(git -C \"$WORK/a-main\" worktree list --porcelain | grep -c '^worktree ')\n+(cd \"$WORK/a-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-demo-slug) \\\n+  >\"$WORK/a-run2.out\" 2>&1 || { fail=1; note \"run2 (повтор) упал:\"; cat \"$WORK/a-run2.out\"; }\n+after_count=$(git -C \"$WORK/a-main\" worktree list --porcelain | grep -c '^worktree ')\n+if [ \"$before_count\" = \"$after_count\" ] && grep -qF \"$expected_wt\" \"$WORK/a-run2.out\"; then\n+  note \"случай 9 (повтор на ту же задачу): дерево переиспользовано, дубля нет — ОК\"\n+else\n+  note \"случай 9 (повтор на ту же задачу): дубль дерева или путь не сообщён — ОШИБКА\"\n+  fail=1\n+fi\n+\n+# ── случай 10: CI — старое поведение (переключение в текущем каталоге) ──────\n+# LEASE_ALREADY_CLAIMED=61 — транспорт арендует задачу сам и пробрасывает номер\n+# (#356): здесь этим же путём проверяется, что claim под флагом не повторяется.\n+new_origin \"b\"\n+git clone -q --branch main \"$WORK/b-origin.git\" \"$WORK/b-main\" 2>/dev/null\n+(cd \"$WORK/b-main\" && GITHUB_ACTIONS=true LEASE_ALREADY_CLAIMED=61 PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 61-ci-demo) \\\n+  >\"$WORK/b-run.out\" 2>&1 || { fail=1; note \"CI-прогон упал:\"; cat \"$WORK/b-run.out\"; }\n+ci_branch=$(git -C \"$WORK/b-main\" rev-parse --abbrev-ref HEAD)\n+if [ \"$ci_branch\" = \"agent/61-ci-demo\" ] && [ ! -d \"$WORK/b-main/.claude/worktrees\" ]; then\n+  note \"случай 10 (CI): ветка переключена в текущем каталоге, worktree не заведён — ОК\"\n+else\n+  note \"случай 10 (CI): ожидалось старое поведение (ветка $ci_branch, worktree отсутствует) — ОШИБКА\"\n+  fail=1\n+fi\n+\n+# ── случай 11: после CI-переключения коммит настоящим хуком обязан пройти ───\n+# Скрипт зовётся по $SCRIPT_SRC (не копией): эпик-гвардия (#376) и lease.sh\n+# тянут соседей относительно СЕБЯ; сам ХУК при этом копируется в c-main —\n+# смысл случая в том, что коммит идёт под НАСТОЯЩИМ файлом гвардии. Номер 61 —\n+# из маршрутов фейкового gh: на неизвестном маршруте эпик-гвардия отказала бы\n+# громко ещё до переключения ветки.\n+new_origin \"c\"\n+git clone -q --branch main \"$WORK/c-origin.git\" \"$WORK/c-main\" 2>/dev/null\n+mkdir -p \"$WORK/c-main/.githooks\"\n+cp \"$HOOK_SRC\" \"$WORK/c-main/.githooks/pre-commit\"\n+chmod +x \"$WORK/c-main/.githooks/pre-commit\"\n+(cd \"$WORK/c-main\" && GITHUB_ACTIONS=true PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 61-ci-commit) \\\n+  >\"$WORK/c-run.out\" 2>&1 || { fail=1; note \"случай 11: CI-переключение упало:\"; cat \"$WORK/c-run.out\"; }\n+(\n+  cd \"$WORK/c-main\"\n+  git config user.email test@example.com\n+  git config user.name test\n+  echo \"change\" >>note.txt\n+  git add note.txt\n+  GITHUB_ACTIONS=true git commit -q -m \"ci commit after task-branch\"\n+) >\"$WORK/c-commit.out\" 2>\"$WORK/c-commit.stderr\"\n+if [ -f \"$WORK/c-commit.out\" ] && grep -qF \"ci commit after task-branch\" <(git -C \"$WORK/c-main\" log -1 --format=%s 2>/dev/null); then\n+  note \"случай 11 (коммит после CI-переключения настоящим хуком): прошёл — ОК\"\n+else\n+  note \"случай 11 (коммит после CI-переключения настоящим хуком): ОТКЛОНЁН — ОШИБКА\"\n+  cat \"$WORK/c-commit.stderr\" >&2\n+  fail=1\n+fi\n+\n+# ── сверка с origin/<branch> (находка ревью #333): ветку мог подвинуть другой ──\n+# канал — оркестратор снимает исполнителя с конфликтного PR и диспатчит нового\n+# воркера, локальная голова отстаёт от серверной. Гвардия свежести сверяет\n+# только origin/main и этот стык не видит, коммит поверх чужой головы валился\n+# бы уже на пуше — отказ обязан прозвучать ДО работы в дереве.\n+# Мутация: сними сверку branch_diverged_from_origin в scripts/git/task-branch\n+# (пути переиспользования и усыновления) — случаи 12/13 перестают отклоняться,\n+# тест красный. Верни — снова зелёный. Случай 14 страхует обратное: свои\n+# незапушенные коммиты (origin — предок локальной головы) отсекаться не должны.\n+foreign_move() {\n+  local clone=\"$1\" branch=\"$2\"\n+  git clone -q \"$WORK/$3\" \"$clone\" 2>/dev/null\n+  (\n+    cd \"$clone\"\n+    git config user.email rival@example.com\n+    git config user.name rival\n+    git checkout -q -b \"$branch\" \"origin/$branch\"\n+    echo \"rival move\" >rival.txt\n+    git add rival.txt\n+    git commit -q -m \"rival move\"\n+    git push -q origin \"$branch\"\n+  )\n+}\n+\n+# ── случай 12: подвинутая ветка — переиспользование отказывает ──────────────\n+new_origin \"d\"\n+git clone -q --branch main \"$WORK/d-origin.git\" \"$WORK/d-main\" 2>/dev/null\n+(cd \"$WORK/d-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-sync-reuse) \\\n+  >\"$WORK/d-run1.out\" 2>&1 || { fail=1; note \"случай 12: run1 упал:\"; cat \"$WORK/d-run1.out\"; }\n+git -C \"$WORK/d-main\" push -q origin agent/67-sync-reuse\n+foreign_move \"$WORK/d-rival\" agent/67-sync-reuse \"d-origin.git\"\n+git -C \"$WORK/d-main\" fetch -q origin\n+if (cd \"$WORK/d-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-sync-reuse) \\\n+  >\"$WORK/d-run2.out\" 2>\"$WORK/d-run2.stderr\"; then\n+  note \"случай 12 (ветка подвинута другим каналом): переиспользование прошло — ОШИБКА, ожидался отказ\"\n+  fail=1\n+else\n+  case \"$(cat \"$WORK/d-run2.stderr\")\" in\n+    *\"разошлась\"*) note \"случай 12 (ветка подвинута другим каналом): отказ с командой синхронизации — ОК\" ;;\n+    *) note \"случай 12: отказ без внятной причины — ОШИБКА ($(cat \"$WORK/d-run2.stderr\"))\"; fail=1 ;;\n+  esac\n+fi\n+\n+# ── случай 13: та же сверка на пути усыновления (дерево пропало с диска) ────\n+new_origin \"e\"\n+git clone -q --branch main \"$WORK/e-origin.git\" \"$WORK/e-main\" 2>/dev/null\n+(cd \"$WORK/e-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-sync-adopt) \\\n+  >\"$WORK/e-run1.out\" 2>&1 || { fail=1; note \"случай 13: run1 упал:\"; cat \"$WORK/e-run1.out\"; }\n+git -C \"$WORK/e-main\" push -q origin agent/67-sync-adopt\n+foreign_move \"$WORK/e-rival\" agent/67-sync-adopt \"e-origin.git\"\n+# Дерево стёрто мимо `git worktree remove`: админ-запись чистится prune'ом\n+# внутри скрипта, ветка остаётся — дальше путь усыновления.\n+rm -rf \"$WORK/e-main/.claude/worktrees/67-sync-adopt\"\n+git -C \"$WORK/e-main\" worktree prune\n+git -C \"$WORK/e-main\" fetch -q origin\n+if (cd \"$WORK/e-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-sync-adopt) \\\n+  >\"$WORK/e-run2.out\" 2>\"$WORK/e-run2.stderr\"; then\n+  note \"случай 13 (усыновление подвинутой ветки): прошло — ОШИБКА, ожидался отказ\"\n+  fail=1\n+else\n+  case \"$(cat \"$WORK/e-run2.stderr\")\" in\n+    *\"разошлась\"*) note \"случай 13 (усыновление подвинутой ветки): отказ с командой синхронизации — ОК\" ;;\n+    *) note \"случай 13: отказ без внятной причины — ОШИБКА ($(cat \"$WORK/e-run2.stderr\"))\"; fail=1 ;;\n+  esac\n+fi\n+\n+# ── случай 14: свои незапушенные коммиты — переиспользование работает ───────\n+new_origin \"f\"\n+git clone -q --branch main \"$WORK/f-origin.git\" \"$WORK/f-main\" 2>/dev/null\n+(cd \"$WORK/f-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-ahead) \\\n+  >\"$WORK/f-run1.out\" 2>&1 || { fail=1; note \"случай 14: run1 упал:\"; cat \"$WORK/f-run1.out\"; }\n+ahead_wt=$(git -C \"$WORK/f-main\" worktree list --porcelain \\\n+  | awk '/^worktree /{p=$2} /^branch refs\\/heads\\/agent\\/67-ahead$/{print p}')\n+(\n+  cd \"$ahead_wt\"\n+  git config user.email test@example.com\n+  git config user.name test\n+  echo \"own work\" >own.txt\n+  git add own.txt\n+  env -u GITHUB_ACTIONS git commit -q -m \"own unpushed work\"\n+)\n+if (cd \"$WORK/f-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-ahead) \\\n+  >\"$WORK/f-run2.out\" 2>&1 && grep -qF \"$ahead_wt\" \"$WORK/f-run2.out\"; then\n+  note \"случай 14 (свои незапушенные коммиты): дерево переиспользовано, путь напечатан — ОК\"\n+else\n+  note \"случай 14 (свои незапушенные коммиты): переиспользование отказало или путь потерян — ОШИБКА\"\n+  cat \"$WORK/f-run2.out\" 2>/dev/null\n+  fail=1\n+fi\n+\n+# ── случай 15: ветка только на origin — усыновление серверной головы ────────\n+# Чужой PR уже запушен, локальной ветки и дерева нет: молча завести\n+# одноимённую локальную ветку от origin/main значило бы тихо подменить чужую\n+# работу (находка ревью #333, живое репро). task-branch обязан усыновить\n+# серверную голову и напомнить про ручную арену доводки.\n+# Мутация: сними проверку `refs/remotes/origin/$branch` в scripts/git/task-branch\n+# (не-CI fall-through) — случай 15 перестаёт падать на сверке голов, тест красный.\n+new_origin \"g\"\n+git clone -q --branch main \"$WORK/g-origin.git\" \"$WORK/g-main\" 2>/dev/null\n+git clone -q \"$WORK/g-origin.git\" \"$WORK/g-first\" 2>/dev/null\n+(\n+  cd \"$WORK/g-first\"\n+  git config user.email rival@example.com\n+  git config user.name rival\n+  git checkout -q -b agent/67-origin-adopt\n+  echo \"pushed work\" >pushed.txt\n+  git add pushed.txt\n+  git commit -q -m \"pushed work\"\n+  git push -q origin agent/67-origin-adopt\n+)\n+remote_head=$(git -C \"$WORK/g-main\" ls-remote origin refs/heads/agent/67-origin-adopt | cut -f1)\n+if (cd \"$WORK/g-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 67-origin-adopt) \\\n+  >\"$WORK/g-run.out\" 2>\"$WORK/g-run.stderr\"; then\n+  adopt_wt=$(git -C \"$WORK/g-main\" worktree list --porcelain \\\n+    | awk '/^worktree /{p=$2} /^branch refs\\/heads\\/agent\\/67-origin-adopt$/{print p}')\n+  adopt_head=$(git -C \"$WORK/g-main\" rev-parse refs/heads/agent/67-origin-adopt 2>/dev/null || true)\n+  if [ -n \"$adopt_wt\" ] && [ \"$adopt_head\" = \"$remote_head\" ] \\\n+     && git -C \"$adopt_wt\" ls-files --error-unmatch pushed.txt >/dev/null 2>&1 \\\n+     && grep -qF \"$adopt_wt\" \"$WORK/g-run.out\"; then\n+    note \"случай 15 (ветка только на origin): серверная голова усыновлена, путь напечатан — ОК\"\n+  else\n+    note \"случай 15: дерево не от серверной головы, чужой файл потерян или путь не напечатан — ОШИБКА\"\n+    fail=1\n+  fi\n+  grep -q \"аренду здесь task-branch не берёт\" \"$WORK/g-run.stderr\" \\\n+    || { note \"случай 15: нет напоминания про ручную аренду доводки — ОШИБКА\"; fail=1; }\n+else\n+  note \"случай 15 (ветка только на origin): task-branch отказал — ОШИБКА, ожидалось усыновление\"\n+  cat \"$WORK/g-run.stderr\" >&2\n+  fail=1\n+fi\n+\n+# ── случай 16: легаси-имя дерева — отказ с переносом, не вечный цикл ────────\n+# Дерево «246-legacy» закреплено за веткой agent/61-x: путь переиспользования,\n+# не сверяющий имя с правилом <N>-, замыкает цикл «гвардия шлёт в task-branch,\n+# task-branch печатает то же дерево, гвардия снова отклоняет» (находка ревью\n+# #333, раунд 4, репро на состоянии из задачи: 246-lease-claim / agent/121).\n+# Мутация: сними сверку имени в пути переиспользования — случай 16 перестаёт\n+# отклоняться, тест красный.\n+new_origin \"h\"\n+git clone -q --branch main \"$WORK/h-origin.git\" \"$WORK/h-main\" 2>/dev/null\n+mkdir -p \"$WORK/h-main/.claude/worktrees\"\n+git -C \"$WORK/h-main\" worktree add -q -b agent/61-x \"$WORK/h-main/.claude/worktrees/246-legacy\" origin/main\n+if (cd \"$WORK/h-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 61-x) \\\n+  >\"$WORK/h-run.out\" 2>\"$WORK/h-run.stderr\"; then\n+  note \"случай 16 (легаси-имя дерева): путь напечатан — ОШИБКА, ожидался отказ с командой переноса\"\n+  fail=1\n+else\n+  case \"$(cat \"$WORK/h-run.stderr\")\" in\n+    *\"не начинается с «61-»\"*\"worktree move\"*) note \"случай 16 (легаси-имя дерева): отказ с рабочей командой git worktree move — ОК\" ;;\n+    *) note \"случай 16: отказ без внятной причины или без команды переноса — ОШИБКА ($(cat \"$WORK/h-run.stderr\"))\"; fail=1 ;;\n+  esac\n+fi\n+\n+# ── случай 17: путь дерева занят — отказ ДО claim'а, без осиротевшей ветки ──\n+# Канонический путь .claude/worktrees/61-occupied занят деревом на чужой\n+# ветке (легаси-имя с числовым префиксом делает такую коллизию вероятной),\n+# ветки agent/61-occupied нет нигде: падение сквозь claim на сыром\n+# `git worktree add` захватывало аренду и оставляло висеть свежесозданную\n+# ветку (класс #360; находка ревью #333, раунд 4, живое репро). Мутация:\n+# сними проверку [ -e \"$wt_dir\" ] — ветка agent/61-occupied появляется,\n+# тест красный.\n+new_origin \"i\"\n+git clone -q --branch main \"$WORK/i-origin.git\" \"$WORK/i-main\" 2>/dev/null\n+mkdir -p \"$WORK/i-main/.claude/worktrees\"\n+git -C \"$WORK/i-main\" worktree add -q -b agent/121-other \"$WORK/i-main/.claude/worktrees/61-occupied\" origin/main\n+(cd \"$WORK/i-main\" && env -u GITHUB_ACTIONS -u LEASE_ALREADY_CLAIMED PATH=\"$WORK/bin:$PATH\" bash \"$SCRIPT_SRC\" 61-occupied) \\\n+  >\"$WORK/i-run.out\" 2>\"$WORK/i-run.stderr\" \\\n+  && { note \"случай 17 (путь занят): скрипт прошёл — ОШИБКА, ожидался отказ\"; fail=1; }\n+case \"$(cat \"$WORK/i-run.stderr\")\" in\n+  *\"занят (не пуст)\"*) note \"случай 17 (путь занят): отказ с объяснением и лечением — ОК\" ;;\n+  *) note \"случай 17: отказ без внятной причины — ОШИБКА ($(cat \"$WORK/i-run.stderr\"))\"; fail=1 ;;\n+esac\n+if git -C \"$WORK/i-main\" show-ref --verify -q refs/heads/agent/61-occupied; then\n+  note \"случай 17: ветка всё же создана (осиротевшая, класс #360) — ОШИБКА\"\n+  fail=1\n+else\n+  note \"случай 17: ветка не создавалась — осиротевших не осталось — ОК\"\n+fi\n+\n if [ \"$fail\" = 0 ]; then\n-  echo \"task-branch: все случаи входной проверки прошли как ожидалось\"\n+  echo \"task-branch: все случаи входной проверки, работы с worktree, стыка с гвардией и сверки с origin прошли как ожидалось\"\n fi\n exit \"$fail\"",
      "sha": "417a18db6738e0cd2b6b2510459f77de5bffc7c7",
      "status": "modified"
    },
    {
      "filename": "scripts/git/test/worktree-guard.test.sh",
      "patch": "@@ -0,0 +1,120 @@\n+#!/usr/bin/env bash\n+# Гвардия «дерево == задача» (#332): доказывает поведение РЕАЛЬНОГО\n+# .githooks/pre-commit на живых git-репозиториях и worktree — не пересказ,\n+# а прогон настоящего файла гвардии (копируется как есть, не переписывается\n+# здесь заново). Три случая:\n+#   1) коммит в agent/<N>-* из дерева «<N>-slug»           — проходит;\n+#   2) коммит в agent/<N>-* из дерева «<M>-slug» (M != N)  — отклонён;\n+#   3) коммит в main (не agent/*) из дерева с любым именем — гвардия неприменима, проходит.\n+#\n+# Мутация, которой доказана гвардия (#332): временно закомментируй блок\n+# «Гвардия «дерево == задача»» в .githooks/pre-commit (от `if [[ \"$branch\"`\n+# до соответствующего `fi`) — случай (2) перестаёт отклоняться, тест красный.\n+# Верни блок — тест снова зелёный.\n+set -euo pipefail\n+\n+REPO_ROOT=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/../../..\" && pwd)\"\n+HOOK_SRC=\"$REPO_ROOT/.githooks/pre-commit\"\n+\n+WORK=\"$(mktemp -d)\"\n+cleanup() { rm -rf \"$WORK\"; }\n+trap cleanup EXIT\n+\n+fail=0\n+note() { echo \"$@\"; }\n+\n+# ── подготовка: bare origin + локальный клон с main, гвардия из прод-файла ──\n+# .githooks/pre-commit коммитится в main ДО создания worktree — иначе каждое\n+# новое дерево (свой checkout ветки) окажется без файла гвардии вовсе, и тест\n+# ничего не докажет (реальный репозиторий коммитит .githooks вместе с кодом).\n+git init -q --bare \"$WORK/origin.git\"\n+\n+git init -q \"$WORK/seed\"\n+(\n+  cd \"$WORK/seed\"\n+  git config user.email test@example.com\n+  git config user.name test\n+  mkdir -p .githooks\n+  cp \"$HOOK_SRC\" .githooks/pre-commit\n+  chmod +x .githooks/pre-commit\n+  git add .githooks\n+  git commit -q -m init\n+  git remote add origin \"$WORK/origin.git\"\n+  git push -q origin HEAD:refs/heads/main\n+)\n+\n+git clone -q \"$WORK/origin.git\" \"$WORK/main-tree\"\n+(\n+  cd \"$WORK/main-tree\"\n+  git config user.email test@example.com\n+  git config user.name test\n+  git config core.hooksPath .githooks\n+)\n+\n+make_worktree() {\n+  local dirname=\"$1\" branch=\"$2\"\n+  git -C \"$WORK/main-tree\" worktree add -q -b \"$branch\" \"$WORK/$dirname\" origin/main\n+}\n+\n+try_commit() {\n+  # echo \"ok\" >&2 через git commit — возвращает код возврата коммита.\n+  # -u GITHUB_ACTIONS: гвардия несёт исключение «в CI (GITHUB_ACTIONS=true)\n+  # task-branch не заводит worktree» (#333) — этот тест симулирует ИМЕННО\n+  # локальный не-CI коммит агента, но если сам ЭТОТ тест-раннер запущен\n+  # внутри настоящего GitHub Actions job, переменная GITHUB_ACTIONS=true уже\n+  # стоит в окружении и наследуется сюда, отключая гвардию по ошибочной\n+  # причине — случай 2 молча переставал ловить регресс на CI (не локально,\n+  # где GITHUB_ACTIONS обычно не задана).\n+  local dir=\"$1\"\n+  (\n+    cd \"$WORK/$dir\"\n+    git config user.email test@example.com\n+    git config user.name test\n+    echo \"change in $dir at $(date +%s%N)\" >>note.txt\n+    git add note.txt\n+    env -u GITHUB_ACTIONS git commit -q -m \"test commit\" 2>\"$WORK/$dir.stderr\"\n+  )\n+}\n+\n+# ── случай 1: дерево «42-good-slug», ветка agent/42-good-slug — должен пройти ──\n+make_worktree \"42-good-slug\" \"agent/42-good-slug\"\n+if try_commit \"42-good-slug\"; then\n+  note \"случай 1 (дерево совпадает с задачей): коммит прошёл — ОК\"\n+else\n+  note \"случай 1 (дерево совпадает с задачей): коммит ОТКЛОНЁН — ОШИБКА, ожидался успех\"\n+  cat \"$WORK/42-good-slug.stderr\" >&2\n+  fail=1\n+fi\n+\n+# ── случай 2: дерево «99-other-task», ветка agent/42-mismatch — должен отклониться ──\n+make_worktree \"99-other-task\" \"agent/42-mismatch\"\n+if try_commit \"99-other-task\"; then\n+  note \"случай 2 (дерево НЕ совпадает с задачей): коммит прошёл — ОШИБКА, ожидался отказ\"\n+  fail=1\n+else\n+  msg=\"$(cat \"$WORK/99-other-task.stderr\")\"\n+  note \"случай 2 (дерево НЕ совпадает с задачей): коммит отклонён — ОК\"\n+  note \"  сообщение гвардии: $(printf '%s' \"$msg\" | head -1)\"\n+  case \"$msg\" in\n+    *\"рабочее дерево этой задачи не здесь\"*) ;;\n+    *)\n+      note \"  ОШИБКА: сообщение гвардии не объясняет причину отказа\"\n+      fail=1\n+      ;;\n+  esac\n+fi\n+\n+# ── случай 3: не agent/*-ветка (main) — гвардия неприменима вне зависимости от имени дерева ──\n+make_worktree \"unrelated-dir-name\" \"local/no-task-branch\"\n+if try_commit \"unrelated-dir-name\"; then\n+  note \"случай 3 (ветка не agent/*): коммит прошёл — ОК (правило вне области)\"\n+else\n+  note \"случай 3 (ветка не agent/*): коммит ОТКЛОНЁН — ОШИБКА, правило не должно применяться\"\n+  cat \"$WORK/unrelated-dir-name.stderr\" >&2\n+  fail=1\n+fi\n+\n+if [ \"$fail\" = 0 ]; then\n+  echo \"worktree-guard: все три случая прошли как ожидалось\"\n+fi\n+exit \"$fail\"",
      "sha": "1a371776aa57dfd8b48f2fe2ba164f500409ea33",
      "status": "added"
    }
  ]
}
