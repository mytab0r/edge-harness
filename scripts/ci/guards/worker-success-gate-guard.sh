#!/usr/bin/env bash
# Гвардия класса #876 (живой инцидент — прогон worker.yml 34498185823,
# задача #140): успех воркера обязан требовать rc dsh этого прогона,
# непустого WORKER_CHAIN_PROVIDER и нового коммита в ветке, не только
# факта «PR по ветке существует» (предсуществующий PR — не результат этого
# прогона). Доказано мутацией — см. заголовок файла теста.
set -euo pipefail
bash scripts/lib/test/dsh-worker-success-gate.smoke.sh
