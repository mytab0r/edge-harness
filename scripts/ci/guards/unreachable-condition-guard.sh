#!/usr/bin/env bash
# Гвардия issue #1172 («последний барьер автономности»): механизм, который
# не может сработать никогда, потому что раньше срабатывает конкурирующее
# условие на той же величине (подпись 1) или потому что распознаваемая
# строка не встречается ни в одном реальном логе (подпись 3) — ai-review
# нашёл три живых случая за 2026-09-13 (PR #1089/#1104/#1114), сама
# недостижимость тестами не ловится (условие подаётся напрямую, а не через
# реальный путь кода). Логика и исторические фикстуры —
# scripts/orchestra/reachability_guard.py + test_reachability_guard.py
# (мутация: снятие ядра арифметики красит 4 из 17 тестов, см. отчёт PR).
set -euo pipefail
pip install --quiet pytest
python -m pytest scripts/orchestra/test_reachability_guard.py -q
