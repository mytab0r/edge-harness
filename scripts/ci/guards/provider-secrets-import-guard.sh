#!/usr/bin/env bash
# Мигрировано из .github/workflows/repo-ci.yml в каталог гвардий (#749):
# шаг добавлен PR #734 (задача #733, массовая установка секретов/переменной
# combo-router из файла-экспорта роутера учёток владельца) уже ПОСЛЕ того,
# как ALLOWLIST scripts/lib/ci_guard_registration_guard.py для PR #771 был
# заморожен — гвардия каталога справедливо покраснела на «новый рукописный
# шаг», это перенос, а не поднятие потолка.
#
# Синтетическая фикстура (без настоящих кредов) — разбор контракта имён из
# dsh-ci.sh, разбор формата экспорта, отказ при файле внутри рабочего
# дерева, отсутствие значений в выводе, выбор слотов, идемпотентность,
# argv без значений секрета.
set -euo pipefail
pip install --quiet pytest
python -m pytest scripts/lib/test_provider_secrets_import.py -q
