#!/usr/bin/env bash
# Перенесено из .github/workflows/repo-ci.yml транслятором рукописных
# шагов гвардий (issue #897, продолжение #749/#771/#762/#764): исходный
# шаг «Мутационный тест гвардии литерала namespace» перенесён сюда автоматически, при механическом
# ребейзе, без содержательной правки run: — исходный комментарий шага
# (если был) приведён ниже дословно.
#
# Гвардия литерала llm-pi-ai (#378) доказана мутацией: зелёная на живом
# литерале, красная с ожидаемым сообщением на переименованном (это и
# есть симуляция апстримного релиза, убившего кнопку создания).
set -euo pipefail
node --test dsh-edge/verify-provider-namespace.test.mjs
