#!/usr/bin/env bash
# Перенесено из .github/workflows/repo-ci.yml транслятором рукописных
# шагов гвардий (issue #897, продолжение #749/#771/#762/#764): исходный
# шаг «Гвардия паритета маскирования секретов (bash ↔ TS)» перенесён сюда автоматически, при механическом
# ребейзе, без содержательной правки run: — исходный комментарий шага
# (если был) приведён ниже дословно.
#
# Паритет маскирования (ревью PR #173): инбокс морды пишет фриформ-текст
# владельца в публичный issue — паттерны секретов cf-worker/src/redact.ts
# обязаны совпадать один к одному с dsh-ci.sh::redact. Две мутации
# краснят: форма добавлена только в bash; число форм разъехалось.
set -euo pipefail
pip install --quiet pytest pyyaml
python -m pytest scripts/lib/test_redact_parity.py -q
