#!/usr/bin/env bash
# Вторая гвардия каталога, добавленная тем же PR как доказательство
# критерия приёмки #749: этот файл — единственное, что нужно для
# регистрации новой гвардии; .github/workflows/repo-ci.yml не тронут ради
# него ни строкой (проверь диффом PR #749: файл отсутствует среди
# изменённых для коммита, добавившего именно этот guard-файл).
#
# Гвардия механизма #749 самого себя: попытка дописать шаг гвардии
# рукописно в repo-ci.yml (вместо файла здесь) обязана краснеть CI —
# иначе класс «рукописный шаг вместо каталога» вернётся первым же PR,
# который забудет про новый механизм.
set -euo pipefail
pip install --quiet pytest pyyaml
python -m pytest scripts/lib/test_ci_guard_registration_guard.py -q
python scripts/lib/ci_guard_registration_guard.py
