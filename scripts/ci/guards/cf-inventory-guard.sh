#!/usr/bin/env bash
# Гвардия класса «инцидент 2026-09-05» (#322, ревью PR #328 находка 3):
# bash -n не исполняет тело функций scripts/cf/lib.sh — регресс jq-фильтра
# на `.result` целиком вернул бы утечку чужих id/значений bindings в
# публичный лог молча. cf_get заглушен консервным прод-JSON, проверка
# живая: чужие id/значения не печатаются, счётчики верны, отказ по
# умолчанию в api.sh держится. Зарегистрирована файлом каталога (#749),
# не рукописным шагом repo-ci.yml — рукописный шаг, приехавший в ветку
# #322 до появления каталога на main, краснит
# scripts/lib/test_ci_guard_registration_guard.py (ребейз #322, 2026-09-11).
set -euo pipefail
bash scripts/lib/test/cf-inventory.guard.sh
