#!/usr/bin/env bash
# Заявления агента о собственной работе — машинная проверка (#968). Живые
# случаи, поймавшие дыру ВРУЧНУЮ: PR #893 («доказательство мутацией» —
# гвардия проверяла ИМЕНА функций, не поведение), #905/#906 («выводы
# перенесены» — половина осталась в комментариях), #964 (заявленная
# регрессия не воспроизвелась на чистом main).
#
# Два шага:
#   1. Тесты самого механизма (scripts/lib/test_mutation_claim.py и
#      scripts/lib/test_pr_mutation_claim_check.py) — поведенческие, реальный
#      tmp git-репозиторий; включают фикстуру 508e6899 (первый коммит PR #893,
#      ДО переделки на поведенческие тесты) как живое доказательство того, что
#      механизм ловит именно тот класс, ради которого заведён. Мутация самого
#      механизма (снять проверку verdict в run_mutation_proof) доказана тем же
#      файлом.
#   2. Живая проверка ТЕКУЩЕГО PR (scripts/lib/pr_mutation_claim_check.py):
#      непроверяемые заявления без пометки (только предупреждение); «класс
#      закрыт» (grep заявленного паттерна); «мутация доказана» — проверяется
#      всегда, когда блок есть в теле PR, а ОБЯЗАТЕЛЬНА только для PR,
#      меняющих scripts/ci/guards/*.sh (находка ai-review PR #1028: иначе
#      мотивирующий случай #893 проходит мимо механизма молча; цена — см.
#      докстринг pr_mutation_claim_check.py).
#      GH_TOKEN нужен только этому шагу — уже в env перебора каталога
#      (repo-ci.yml, шаг «Каталог гвардий … — перебор (#749)»), без правки
#      workflow ради этого файла.
set -euo pipefail
pip install --quiet pytest
python -m pytest scripts/lib/test_mutation_claim.py scripts/lib/test_pr_mutation_claim_check.py -q
python scripts/lib/pr_mutation_claim_check.py
