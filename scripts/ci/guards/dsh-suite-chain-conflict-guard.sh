#!/usr/bin/env bash
# Перенесено из .github/workflows/repo-ci.yml транслятором рукописных
# шагов гвардий (issue #897, продолжение #749/#771/#762/#764): исходный
# шаг «Гвардия стыка suite и цепочки провайдеров (#215/#727)» перенесён сюда автоматически, при механическом
# ребейзе, без содержательной правки run: — исходный комментарий шага
# (если был) приведён ниже дословно.
#
# Гвардия стыка suite ротации учёток (#215) и цепочки провайдеров
# (#727/#737) — design.md dsh-in-job «Стык suite и цепочки
# провайдеров»: обе переменные разом — fail loud, не молчаливый
# приоритет; внутри цикла цепочки suite всегда глушится плоским патчем.
set -euo pipefail
bash scripts/lib/test/dsh-suite-chain-conflict.guard.sh
