#!/usr/bin/env bash
# Гвардия механизма перебора (#749): scripts/ci/run_guards.sh — прод-код,
# который исполняет этот же каталог прямо сейчас, поэтому его smoke-тест
# лежит здесь же, а не рукописным шагом repo-ci.yml (догфудинг: третье
# доказательство того, что регистрация — данные, а не правка общего файла).
set -euo pipefail
bash scripts/ci/test/run-guards.smoke.sh
