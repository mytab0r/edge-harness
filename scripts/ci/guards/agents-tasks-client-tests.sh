#!/usr/bin/env bash
# Дашборд «Агенты и задачи» в морде (#407, продолжение #111): сборка
# build.mjs (гвардия записи в каталоге dsh-edge/plugins.json, seed-require,
# обёртка бандла) + монтаж в оба слота (потеря sidebar.section красит набор),
# поллинг с паузой при 403 GitHub и рендер секции на прод-форме ответа
# журнала. Критерий #407 — «клиентские тесты зелёные в repo-ci».
#
# Зарегистрирована файлом каталога (#749), не рукописным шагом repo-ci.yml:
# класс «тест есть, шага в CI нет» (#510) и класс «рукописный шаг вместо
# каталога» закрыты одной формой — этот файл и есть регистрация.
set -euo pipefail
node --test plugins-src/agents-tasks/test/client.test.mjs
