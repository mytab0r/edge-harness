#!/usr/bin/env bash
# Каталожная обёртка: авторизация git стоит ДО писателя data-ветки (#1486).
#
# Класс одной фразой: шаг, пишущий на data-ветку, идёт в job'е раньше, чем
# `gh auth setup-git`, — push отказывает 403, и отказ виден не красным
# прогоном, а последствием через час. Оплачено дважды: #882 (молчание
# конвейера под видом дедупликации гонки) и #1486 (карта тем не сохраняется,
# и каждый сигнал заводит ещё одну тему той же категории).
#
# Список писателей НЕ пишется руками: он выводится транзитивно из исходников
# по зависимости от data_branch_writer, поэтому новый писатель попадает под
# гвардию сам. Проверка идёт по разобранному YAML (порядок шагов), а не по
# тексту: перестановка шагов местами обязана краснеть.
set -euo pipefail
python -m pytest scripts/lib/test_git_auth_order_guard.py -q
