#!/usr/bin/env bash
# Гвардия засева реестра провайдеров морды (#1330).
#
# Носитель — ПОВЕДЕНЧЕСКИЙ тест (правило #891/#893): поднимает настоящий HTTP
# сервер в прод-форме RPC-конверта морды и запускает скрипт отдельным процессом,
# а не проверяет наличие имён в исходнике. Держит три вещи, каждая из которых
# мутационно доказана:
#   - идемпотентность (повтор не делает НИ ОДНОЙ записи — иначе каждый прогон
#     переписывал бы прод-настройки владельца без нужды);
#   - значение ключа не попадает ни в stdout, ни в stderr (репозиторий
#     публичный, маскирование GitHub ловит лишь точное совпадение);
#   - несмонтированный namespace `llm-pi-ai` — громкий отказ, а не запись
#     вслепую в морду, где реестра нет.
set -euo pipefail
pip install --quiet pytest
python -m pytest scripts/dsh_edge/test_seed_provider_registry.py -q
