#!/usr/bin/env bash
# Перенесено из .github/workflows/repo-ci.yml транслятором рукописных
# шагов гвардий (issue #897, продолжение #749/#771/#762/#764): исходный
# шаг «Провайдер/модель LLM — одно место правды (vars), без зашитых дефолтов» перенесён сюда автоматически, при механическом
# ребейзе, без содержательной правки run: — исходный комментарий шага
# (если был) приведён ниже дословно.
#
# Гвардия класса #153: провайдер/модель LLM — ровно одно место правды
# (vars.DEEPSEEK_BASE_URL/DEEPSEEK_MODEL репозитория). Вынесена в
# scripts/lib/test/provider-default.guard.sh (находка 9): два паттерна
# (свойство + литерал), исключение только файла гвардии — теперь и
# repo-ci.yml сам под проверкой.
set -euo pipefail
bash scripts/lib/test/provider-default.guard.sh
