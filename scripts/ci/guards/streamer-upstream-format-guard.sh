#!/usr/bin/env bash
# Гвардия «харнес пишет только то, что апстримный формат принимает» (issue #1404).
#
# Класс: коллизии полей проверялись со СЛОВАРЁМ событий (SessionEventMap) и не
# были найдены — с ВАЛИДАТОРОМ формата не сверялся никто. Схема v0 у `data`
# закрытая, любой лишний член даёт `data has unexpected member`, и не мигрирует
# вся сессия целиком. Живой дефект: scripts/dsh-hands-streamer/lib/core.js
# дописывал `truncated`/`original_size`, объявляя усечение.
#
# Цена измерена механизмом #1379, а не предположена: первый же прогон (деплой
# 35571833914, 2026-09-21T07:13Z) показал 165 карантинных сессий прода, из них
# 127 с причиной `unexpected member "truncated"` — 77% карантина породили две
# строки одного файла.
#
# Гвардия ПОВЕДЕНЧЕСКАЯ и кормится прод-формой: настоящий projectEventData
# нашего кода, настоящий апстримный assertReleasedEventPayload того самого
# пакета, который отказывал в проде. Ни пересказа схемы, ни своей копии правил.
# Недоступность пакета — ПАДЕНИЕ, не пропуск: «не смогли проверить» и
# «проверили, всё хорошо» лечатся по-разному (класс #1096/#1395).
#
# Доказано мутациями — ИСПОЛНЕНО, не пересказано. База — «7 passed».
#   1) вернуть `out.truncated`/`out.original_size` в data (тот самый дефект) —
#      «4 failed, 3 passed»: краснеют все три типа события с усечением плюс
#      тест «лишних членов в data нет»;
#   2) резать молча (снять пометку внутри текста) — «1 failed, 6 passed»,
#      краснеет test_the_fact_of_truncation_is_not_lost_it_moved_into_the_text:
#      лечение класса #1404 не имеет права свестись к тому, что журнал
#      перестал врать ЦЕНОЙ молчания.
#
# Регистрируется каталогом (#749), не рукописным шагом repo-ci.yml.
set -euo pipefail
pip install --quiet pytest
python -m pytest scripts/lib/test_streamer_upstream_format.py -q
