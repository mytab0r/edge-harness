#!/usr/bin/env bash
# Гвардия #1500: «класс отказа не распознан» не выдаётся за «повтор поможет».
# Кормится ПРОД-ФОРМОЙ stderr (строки скопированы из живых прогонов, каждая с
# адресом замера) и гоняет настоящие функции dsh-ci.sh — классификацию отказа
# и сборку сводки. Структурная проверка по исходнику осталась бы зелёной при
# вырезанном теле ветки.
set -euo pipefail
python -m pytest scripts/lib/test_chain_outcome_honesty_guard.py -q
