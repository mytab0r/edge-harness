#!/usr/bin/env bash
# Тесты пересылающего рекордера (#1525): стенд поднимает НАСТОЯЩИЙ сервер в
# роли провайдера и зовёт рекордер настоящим HTTP-клиентом. Заглушка здесь
# бессмысленна вдвойне — сама пересылка и есть то, что проверяется, а
# «недоступность» воспроизводится закрытым портом, а не подменой функции
# (AGENTS.md, класс #1373).
set -euo pipefail
python -m pytest scripts/measure/test_dsh_request_capture.py -q
