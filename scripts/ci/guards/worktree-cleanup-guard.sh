#!/usr/bin/env bash
# Мигрировано из .github/workflows/repo-ci.yml в каталог гвардий (#749,
# находка ревью PR #893, второй раунд: рукописный шаг красил job `test`
# ci_guard_registration_guard.py — «новый рукописный шаг гвардии»). Гвардия
# проверяет ПОВЕДЕНИЕ уборки мёртвых worktree'ов (#891) на настоящем
# временном git-репозитории (tempfile + git init + реальный git worktree
# add), не текст исходника — переименование/вырезание тела проверки красит
# её красным, не просто отсутствие имени функции.
#
# GH_TOKEN не объявляется здесь: дымовому тесту CLI (--dry-run на этом же
# репозитории) нужен живой gh для пакетного gh pr list (#891); переменная
# приходит от шага-перебора «Каталог гвардий scripts/ci/guards — перебор
# (#749)» в repo-ci.yml (env: GH_TOKEN на уровне ЭТОГО шага), дочерний
# процесс каждого файла каталога наследует её без per-файловой проводки —
# то же обоснование, что уже несёт сам run_guards.sh. Без токена шаг всё
# равно не упадёт (get_pr_status уйдёт в "не удалось определить" и честно
# ничего не снимет), но с токеном проверяется настоящий путь.
set -euo pipefail
pip install --quiet pytest
python -m pytest scripts/lib/test_worktree_cleanup_guard.py -q
