#!/usr/bin/env bash
# Гвардия «подтверждение issue-created не теряет правду об ответе» (issue #1381).
#
# Два дефекта одного шага `.github/workflows/inbox-issue.yml`, оба про потерю
# правды:
#   1) причина отказа выбрасывалась флагом `-f` — DO отвечает
#      `{"error":{"code":"need_claimed_ts"}}`, а в лог уходило «curl: (22) The
#      requested URL returned error» (тот же класс, что #1371/#1373);
#   2) отказ при 2xx не замечался ВОВСЕ: устаревший claimed_ts DO отвечает
#      200 + `{"accepted": false}` (ватчдог увёл сообщение другой проходке),
#      `curl -f` такой ответ пропускал, шаг выходил нулём — issue создана, DO
#      о ней не знает, дубль всплывает позже и связать его с этим прогоном
#      нечем. Silent-wrong: «принято» и «отброшено» были неотличимы.
#
# Второй дефект найден ПРИ починке первого. Оставить его, увидев, значило бы
# сознательно отгрузить дефект — поэтому закрыт здесь же и назван вслух в теле
# PR, а не подшит молча.
#
# Гвардия ПОВЕДЕНЧЕСКАЯ: исполняет РЕАЛЬНЫЙ bash-скрипт шага, извлечённый из
# workflow, против настоящего HTTP-сервера (приём #1170). Прод-формы ответов
# взяты из тестов самого DO — cf-worker/test/harness.spec.ts, — не выдуманы.
#
# Доказано мутациями — ИСПОЛНЕНО, не пересказано (каждая по отдельности, с
# откатом workflow между ними):
#   1) вернуть `curl -fsS` вместо canary_http — «3 failed, 2 passed»: краснеют
#      test_refusal_reason_reaches_the_log, test_server_error_reaches_the_log_
#      too и структурный test_step_sources_library_before_first_use. Проверка
#      accepted при этом ОСТАЁТСЯ и продолжает ловить устаревший claim — два
#      дефекта независимы, и мутация это показывает;
#   2) убрать проверку `accepted` — «1 failed, 4 passed», краснеет
#      test_stale_claim_is_not_silently_accepted: ровно тот silent-wrong;
#   3) переставить `source` ПОСЛЕ первого вызова — «5 failed»: в прогоне шаг
#      падает на «command not found», то есть не проверяет ничего из того, что
#      должен (собственная ошибка первой итерации PR #1372).
# База до мутаций и после отката — «5 passed».
#
# Регистрируется каталогом (#749), не рукописным шагом repo-ci.yml.
set -euo pipefail
pip install --quiet pytest pyyaml
python -m pytest scripts/lib/test_inbox_issue_callback_guard.py -q
