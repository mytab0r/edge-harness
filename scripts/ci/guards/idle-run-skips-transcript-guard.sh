#!/usr/bin/env bash
# Гвардия класса #1510: холостой прогон воркера (отказ ДО работы агента) не
# льёт транскрипт в морду — записывать ход работы, которой не было, нечего, а
# приём стоит АККАУНТНОЙ квоты rows_read Durable Objects, общей с самой мордой.
# Живая цена (#1513): 2026-09-23 один объект dsh-edge выбрал 5 700 194 строки
# (114% суточного лимита), морда легла в 1101, кнопка решения владельца в
# Telegram умерла вместе с ней — при том что сама морда прочитала 198 строк.
# Носитель тела — scripts/lib/test_idle_run_transcript_guard.py: поднимает
# НАСТОЯЩИЙ http.server вместо морды и зовёт НАСТОЯЩИЙ клиент
# (scripts/lib/dsh-edge-session.sh) настоящим bash, проверяя видимый результат
# — дошёл ли до сервера POST. Доказано мутацией (тело PR).
# Удалишь обёртку — канарейка осиротевших тестов красит.
set -euo pipefail
python -m pytest scripts/lib/test_idle_run_transcript_guard.py -q
