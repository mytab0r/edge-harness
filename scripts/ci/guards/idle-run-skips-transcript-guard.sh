#!/usr/bin/env bash
# Гвардия класса #1510: холостой прогон воркера (отказ ДО работы агента) не
# льёт транскрипт в морду — записывать ход работы, которой не было, нечего, а
# приём стоит АККАУНТНОЙ квоты rows_read Durable Objects, общей с самой мордой.
# Живая цена: 2026-09-23 холостые прогоны выбрали 5 700 194 строки (114%
# суточного лимита), морда легла в 1101, кнопка решения владельца в Telegram
# умерла вместе с ней.
# Носитель тела — scripts/worker/test/idle-run-skips-transcript.smoke.sh:
# поднимает НАСТОЯЩИЙ http.server вместо морды и зовёт НАСТОЯЩИЙ клиент
# (scripts/lib/dsh-edge-session.sh), проверяя видимый результат — дошёл ли до
# сервера POST. Доказано мутацией (см. заголовок теста и тело PR).
# Удалишь обёртку — канарейка осиротевших тестов красит.
set -euo pipefail
bash scripts/worker/test/idle-run-skips-transcript.smoke.sh
