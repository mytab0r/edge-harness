#!/usr/bin/env bash
# Каталожная обёртка слоя категорий и тем Telegram (#1461 + #1465).
#
# Класс одной фразой: все сигналы владельцу — решение, поломка, шум конвейера —
# валятся в один поток, и решение тонет в шуме.
#
# Две стороны, и обе проверяются исполнением, а не чтением исходника:
#   - карта `категория → message_thread_id` живёт на data-ветке и читается/
#     пишется настоящими git и HTTP (живой `http.server`, `git init --bare`);
#   - оба отправителя (Python `send_telegram`, bash `telegram_report`) обязаны
#     назвать категорию из реестра — Python-сторона разбирается AST'ом, bash-
#     сторона исполняется по-настоящему: вырезанное тело функции прошло бы
#     текстовый разбор молча (AGENTS.md, #891/#893).
set -euo pipefail
python -m pytest scripts/lib/test_telegram_topics.py -q
