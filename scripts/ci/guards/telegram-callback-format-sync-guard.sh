#!/usr/bin/env bash
# Перенесено из .github/workflows/repo-ci.yml транслятором рукописных
# шагов гвардий (issue #897, продолжение #749/#771/#762/#764): исходный
# шаг «Гвардия синхронности формата кнопки Telegram (TS ↔ Python)» перенесён сюда автоматически, при механическом
# ребейзе, без содержательной правки run: — исходный комментарий шага
# (если был) приведён ниже дословно.
#
# Синхронность формата кнопки между TS и Python, и внутри самого
# Python (#254, PR #486, находка того же ревью): без этого шага
# гвардия ловит рассинхрон так же плохо, как ловили его комментарии
# без гвардии — тестом, который никто не запускает.
set -euo pipefail
pip install --quiet pytest pyyaml
python -m pytest scripts/lib/test_telegram_callback_format_sync.py -q
