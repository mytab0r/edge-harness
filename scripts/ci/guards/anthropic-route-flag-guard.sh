#!/usr/bin/env bash
# Гвардия #1524: запись цепочки без Anthropic-маршрута не тратит попытку
# прогона, и её пропуск звучит вслух. Проверяется НАСТОЯЩАЯ функция загрузки
# на настоящих манифестах — структурная проверка осталась бы зелёной и при
# вырезанном теле фильтра, и при потерянном сообщении о пропуске.
set -euo pipefail
python -m pytest scripts/lib/test_anthropic_route_flag_guard.py -q
