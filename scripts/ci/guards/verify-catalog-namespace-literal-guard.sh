#!/usr/bin/env bash
# Перенесено из .github/workflows/repo-ci.yml транслятором рукописных
# шагов гвардий (issue #897, продолжение #749/#771/#762/#764): исходный
# шаг «Гвардия «каталог плагинов не отравляет литерал namespace»» перенесён сюда автоматически, при механическом
# ребейзе, без содержательной правки run: — исходный комментарий шага
# (если был) приведён ниже дословно.
#
# Находка ревью PR #453, п.2: dsh-edge/plugins.json и
# dsh-edge/plugins-catalog.json рендерятся в клиентский бандл плагином
# plugin-manager (client:true) — брифы каталога попадают в dist
# буквально. Случайное совпадение с приватным литералом NS$1 апстрима
# молча отравляет гвардию выше (живой прецедент — plugin-manager 0.1.4,
# бамп на 0.1.5 в этом же PR). Проверяет и сам каталог репозитория, и
# мутацию (просочившийся литерал красит гвардию).
set -euo pipefail
node --test dsh-edge/verify-catalog-namespace-literal.test.mjs
