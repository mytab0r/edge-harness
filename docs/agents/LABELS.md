# Реестр меток — тормоз и его газ

Один файл, единственное место правды для GitHub-меток, которыми оперирует
конвейер. Заведён задачей #207: пять из шести собранных тормозов конвейера
оказались без объявленного способа их снять (#205, #204, #196) — не потому,
что газа не было никогда, а потому что его никто не назвал явно и рядом.

**Правило ведения**: строка обязательна для каждой метки, встречающейся в
коде; колонка «Газ» не может быть пустой — «вручную: роль, по сигналу X» это
осознанное объявление, а не пропуск. Значения меток-вердиктов ревью
(`review:*`, `ai:*`) читаются из существующего места правды
[`scripts/lib/review_labels.py`](../../scripts/lib/review_labels.py) — здесь
не дублируются строками.

Список меток на этой странице **не хардкожен**: его печатает
[`scripts/lib/collect_labels.py`](../../scripts/lib/collect_labels.py)
сканированием `scripts/`, `.github/workflows/`, `.github/ISSUE_TEMPLATE/`.
Гвардия [`scripts/lib/test_label_registry.py`](../../scripts/lib/test_label_registry.py)
сверяет вывод сборщика со строками этой таблицы на каждый CI-прогон
(`.github/workflows/repo-ci.yml`).

## Честный потолок

Гвардия покрывает только тормоза, **выраженные меткой**. Тормоз, зашитый
условием в коде — `return False`, `exit 1`, пауза диспатча без метки
(предохранитель конвейера, `scripts/orchestra/pulse_guard.py:282-314`) —
механически не проверяется: метки на нём нет, сканировать нечего. Такие
тормоза закрываются правилом «Тормоз без газа не принимается» в
[`AGENTS.md`](../../AGENTS.md) и вопросом в шаблоне PR, то есть дисциплиной
автора и ревьюера, а не автоматической проверкой. Не путать частичное
покрытие с полным.

## Реестр

| Метка | Кто ставит (file:line) | Когда | Что блокирует | Газ: кто снимает (file:line) | Порог/условие |
|---|---|---|---|---|---|
| `task` | [`.github/ISSUE_TEMPLATE/task.yml:3`](../../.github/ISSUE_TEMPLATE/task.yml), [`scripts/review/file_tasks.py:78`](../../scripts/review/file_tasks.py) | Заведение задачи в пул (шаблоном или ревью-агентом) | Пометка, не блокировка: маркирует issue как «задача из пула», без неё контракт (`scripts/orchestra/contract_check.py:123-124`) не даст назначить её PR | Не снимается — живёт до закрытия issue (закрывает исполнитель после пост-мерж проверки, `docs/agents/PROTOCOL.md:53-55`) | Условия нет — метка не тормоз конвейера, а условие входа в него |
| `white-spot` | [`.github/ISSUE_TEMPLATE/white-spot.yml:3`](../../.github/ISSUE_TEMPLATE/white-spot.yml) | Найдено противоречие/белое пятно, оформлено issue-шаблоном | Пометка, не блокировка: не входит ни в один автоматический гейт | Вручную: владелец или агент, разбирающий issue, закрывает её после решения — сигнал: противоречие устранено или факт подтверждён | Осознанно ручной: смысл шаблона — открытое обсуждение, а не автогейт |
| `blocked` | [`scripts/worker/task.sh:219,375`](../../scripts/worker/task.sh) (эскалация playbook: `gh issue edit $number --add-label blocked`) | Воркер упёрся в то, что есть только у владельца (секрет, доступ, деньги, необратимое действие) — законный исход запуска, PR не открывается | Блокирует автоматический повтор: `scripts/worker/task.sh:375-399` читает метку, снимает аренду задачи (#121) и завершает job зелёным без попытки взять задачу заново | Вручную: владелец, по сигналу — комментарий воркера в issue с описанием, что именно нужно; владелец снимает метку и даёт запрошенное | Осознанно ручной (задача #207, замер: неизвестно, сколько это висит — не измерялось до сплошного прочёса) |
| `orchestra:skip` | Человек, на PR — ручная постановка автором, обходящим контракт пула для мелочи | PR не связан с задачей пула (правка вне Issues) | Не тормоз, а обход тормоза: `scripts/orchestra/contract_check.py:76-78` пропускает проверку «PR ↔ задача» целиком | Сам является газом — не требует отдельного снятия; злоупотребление им видно в истории PR (`docs/agents/PROTOCOL.md:96-97`) | Осознанно ручной по построению |
| `conflict` | [`scripts/orchestra/scheduler.py:141-161`](../../scripts/orchestra/scheduler.py) (`mark_conflicts`, ставится когда `mergeable_state == "dirty"`) | PR перестал сливаться в main без ручного разрешения | Пометка, не отдельная блокировка: реальный тормоз слияния — `mergeable_state` (`scheduler.py:193-195`, `merge_queue` пропускает PR не в `clean/unstable/has_hooks`), метка — видимый сигнал автору | Автоматический по факту (ребейз автором меняет `mergeable_state` на `clean`, слияние снова доступно), но саму МЕТКУ код нигде не снимает — DELETE-вызова нет ни в одном файле (проверено #207) | Требует ребейза на свежий main (`docs/agents/PROTOCOL.md:39-40,67-68`); висящая метка после ребейза — известный нюанс этого реестра, не отдельный тормоз |
| `contract:failed` | [`scripts/orchestra/contract_check.py:52-56`](../../scripts/orchestra/contract_check.py) (`fail()`) | Контракт «PR ↔ задача из пула» нарушен (нет ссылки на задачу, задача чужая/закрыта, второй PR на ту же задачу) | Метка сигнальная — сама проверка контракта является required-статусом защиты ветки, блокирует слияние своим состоянием, не меткой | Автоматический: [`contract_check.py:157-160`](../../scripts/orchestra/contract_check.py) снимает метку в начале каждого следующего успешного прогона (новый пуш переисполняет контракт) | Условие: следующий прогон контракта на том же PR проходит без нарушений |
| `review:ok` | [`scripts/review/check_pr.py:151`](../../scripts/review/check_pr.py) через `scripts/lib/review_labels.py::REVIEW_OK` | Детерминированное ревью не нашло замечаний и дифф не крупный (или крупный, но принят `review:large-ok`) | Часть гейта слияния: `scheduler.py:210` (`merge_label_gate`) требует `review:ok` И `ai:ok` одновременно | Автоматический: следующий пуш перезапускает `check_pr.py`, который снимает и переставляет вердикт (`check_pr.py:143-146`) | Условие: новый прогон ревью проходит без находок |
| `review:changes-requested` | [`scripts/review/check_pr.py:151`](../../scripts/review/check_pr.py) через `REVIEW_CHANGES` | Ревью нашло замечания (секрет в диффе, конфликт-маркер, файл секретов, не компилируется) | Не входит в `merge_label_gate` напрямую — блокирует по факту отсутствия `review:ok` на этом PR | Автоматический: новый пуш переисполняет ревью и переставляет вердикт | Условие: находки устранены, следующий прогон чист |
| `review:large` | [`scripts/review/check_pr.py:151`](../../scripts/review/check_pr.py) через `REVIEW_LARGE` | Дифф больше `LARGE_DIFF_LINES` (800 строк) и `review:large-ok` ещё не выставлена | Блокирует слияние тем же путём, что `review:changes-requested` — `review:ok` не выставляется, пока размер не принят | См. `review:large-ok` ниже — это единственный путь снятия | Порог: `LARGE_DIFF_LINES = 800` строк добавлений (`check_pr.py:33`) |
| `review:large-ok` | Не автоматика — ставит человек/ревью-агент вручную на PR | Размер дифф-гейта принят осознанно (взгляд человека делегирован ревью-агентам, `check_pr.py:113-117`) | Обходит `review:large`: `check_pr.py:122` (`is_large = size_overflow and LARGE_OK not in current`) — при наличии метки verdict становится `review:ok` | Вручную: владелец или назначенный ревьюер, по сигналу — явный просмотр крупного диффа; сама метка и есть газ, снимать нечего | Осознанно ручной (задача #207, замер: #167 и #173 висели больше суток без него — закрывается #204) |
| `ai:ok` | [`scripts/review/ai_review.py:378-380`](../../scripts/review/ai_review.py) через `review_labels.AI_OK` | AI-ревью вернуло вердикт `approve` на актуальный head | Часть гейта слияния — `merge_label_gate` требует его вместе с `review:ok` | Автоматический: новый пуш → `check_pr.py` снимает протухший `ai:*` (`review_labels.ai_verdicts_to_drop`, `check_pr.py:148-149`) → workflow `ai-review` перезапускается по метке `review:ok` (`ai-review.yml:31,50`) и ставит свежий вердикт | Условие: новое AI-ревью на новом head отвечает `approve` |
| `ai:changes-requested` | [`scripts/review/ai_review.py:378-380`](../../scripts/review/ai_review.py) через `review_labels.AI_CHANGES` | AI-ревью вернуло вердикт `rework` | Блокирует по факту отсутствия `ai:ok` | Тот же путь, что `ai:ok`: новый пуш снимает метку, новое ревью переставляет вердикт | Условие: замечания устранены, новое ревью на новом head |
| `ai:failed` | [`scripts/review/ai_review.py:378-380,386-391`](../../scripts/review/ai_review.py) через `review_labels.AI_FAILED` | Ответ модели не соответствует контракту вердикта (нет строки `ВЕРДИКТ:`, их больше одной, транспорт DSH упал) | Блокирует по факту отсутствия `ai:ok`; замер задачи #207: 84% прогонов этого гейта — ручной повторный запуск, закрывается #196 | Тот же путь: новый пуш снимает метку, ревью перезапускается | Условие: то же — новый прогон отвечает по контракту. До #196 автоповтора без нового пуша нет — это и есть находка задачи #196, не #207 |

## Связанные документы

- [`AGENTS.md`](../../AGENTS.md) — правило «Тормоз без газа не принимается».
- [`docs/agents/PROTOCOL.md`](PROTOCOL.md) — роли, пул задач, контракт PR.
- [`scripts/lib/review_labels.py`](../../scripts/lib/review_labels.py) — место
  правды значений `review:*`/`ai:*` и формулировка гейта слияния.
