# Реестр меток — тормоз и его газ

Один файл, единственное место правды для GitHub-меток, которыми оперирует
конвейер. Заведён задачей #207: пять из шести собранных тормозов конвейера
оказались без объявленного способа их снять (#205, #204, #196) — не потому,
что газа не было никогда, а потому что его никто не назвал явно и рядом.

**Правило ведения**: строка обязательна для каждой метки, встречающейся в
коде; колонка «Газ» не может быть пустой — «вручную: роль, по сигналу X» это
осознанное объявление, а не пропуск. Значения меток-вердиктов ревью
(`review:*`, `ai:*`) читаются из существующего места правды
[`scripts/lib/review_labels.py`](../../scripts/lib/review_labels.py) — здесь
не дублируются строками.

Список меток на этой странице **не хардкожен**: его печатает
[`scripts/lib/collect_labels.py`](../../scripts/lib/collect_labels.py)
сканированием `scripts/`, `.github/workflows/`, `.github/ISSUE_TEMPLATE/`.
Гвардия [`scripts/lib/test_label_registry.py`](../../scripts/lib/test_label_registry.py)
сверяет вывод сборщика со строками этой таблицы на каждый CI-прогон
(`.github/workflows/repo-ci.yml`).

## Честный потолок

Гвардия покрывает только тормоза, **выраженные меткой**. Тормоз, зашитый
условием в коде — `return False`, `exit 1`, пауза диспатча без метки
(предохранитель конвейера, `scripts/orchestra/pulse_guard.py:282-314`) —
механически не проверяется: метки на нём нет, сканировать нечего. Такие
тормоза закрываются правилом «Тормоз без газа не принимается» в
[`AGENTS.md`](../../AGENTS.md) и вопросом в шаблоне PR, то есть дисциплиной
автора и ревьюера, а не автоматической проверкой. Не путать частичное
покрытие с полным.

Второй, более узкий потолок (найден 2026-09-06 разбором `review:large`,
см. реестр ниже): `test_label_registry.py::test_every_row_has_gas` проверяет
только то, что колонка «Газ» непуста — форму записи, а не то, что описанный
в ней механизм реально доходит до своего триггера. Строка реестра для
`review:large-ok` описывала рабочую автоматику (`apply_large_ok`, #204) и
проходила гвардию зелёным, пока сам механизм полгода был недостижим:
условие в другом файле (`ai-review.yml`, шаг `facts`) закрывало путь к нему
целиком. Текстовая проверка «газ назван» не эквивалентна «газ работает» —
вторую может доказать только исполнение прод-кода (см. живой пример —
[`scripts/review/test_ai_review_gate.py`](../../scripts/review/test_ai_review_gate.py),
которая гоняет реальный bash шага `facts`, а не описание в markdown).

## Второй канал вердикта: Commit Status API (#345)

Метки `review:*`/`ai:*` ниже с #345 дублируются вторым каналом — GitHub
Commit Status API (`harness/review`/`harness/ai-review`,
[`scripts/lib/review_labels.py::post_commit_status`](../../scripts/lib/review_labels.py)),
публикуемым тем же кодом в тот же момент, что и метка. Это НЕ второй
источник истины: состояние статуса — та же переменная вердикта, что и у
метки. Цель — нативный `allow_auto_merge` (включён на репозитории), который
читает required status checks, а метки — нет. Гейт слияния
(`merge_label_gate`, `scheduler.py::merge_queue`) пока читает только метки —
эта таблица «тормоз/газ» описывает их, статусы гейт не меняют.

## Реестр

| Метка | Кто ставит (file:line) | Когда | Что блокирует | Газ: кто снимает (file:line) | Порог/условие |
|---|---|---|---|---|---|
| `task` | [`.github/ISSUE_TEMPLATE/task.yml:3`](../../.github/ISSUE_TEMPLATE/task.yml), [`.github/ISSUE_TEMPLATE/white-spot.yml:5`](../../.github/ISSUE_TEMPLATE/white-spot.yml); программно — единая точка [`scripts/lib/pool_issue.py::create_pool_issue`](../../scripts/lib/pool_issue.py) (#526, физически не даёт создать issue без `task`), которую зовут [`scripts/review/file_tasks.py::file_task`](../../scripts/review/file_tasks.py), [`scripts/orchestra/scheduler.py::after_merge`](../../scripts/orchestra/scheduler.py) (хвост незакрытых пунктов чеклиста ревью, #462, одна задача на PR), [`scripts/orchestra/stall_detector.py::create_task`](../../scripts/orchestra/stall_detector.py) и [`scripts/orchestra/upstream_drift.py::create_bump_issue`](../../scripts/orchestra/upstream_drift.py); из терминала/CLI (в т.ч. агентом) — [`scripts/gh/issue-create`](../../scripts/gh/issue-create) (тот же инвариант на bash, #526); из cf-worker (Cloudflare Workers, свой рантайм — не может звать ни Python, ни bash) — [`cf-worker/src/harness.ts`](../../cf-worker/src/harness.ts) (инбокс: директива владельца → issue), гвардия — ассерт `labels` в `cf-worker/test/harness.spec.ts` | Заведение задачи в пул (шаблоном task или white-spot, ревью-агентом — только для находок AI-ревью с `МАСШТАБ: отдельно`, #426: `МАСШТАБ: хвост` остаётся «доделай в этом PR» и issue не заводит, критерий различения в `scripts/review/ai_prompt.md`, — оркестратором для незакрытого чеклиста при слиянии, детектором простоя #201, сигналом дрейфа пина #134, или инбоксом морды из директивы владельца, #20) | Пометка, не блокировка: маркирует issue как «задача из пула», без неё контракт (`scripts/orchestra/contract_check.py:123-124`) не даст назначить её PR; пул воркера (`scripts/orchestra/scheduler.py::open_task_issues`) читает только `labels=task` — без метки issue физически не видна пулу (класс #179, живые случаи #523/#425) | Не снимается — живёт до закрытия issue (закрывает стадия приёмки оркестратора по проверяемой улике, #227: `scripts/orchestra/scheduler.py:accept_merged_tasks`, `docs/agents/PROTOCOL.md:99-107`) | Условия нет — метка не тормоз конвейера, а условие входа в него. Честный потолок (#526): channel агентских промптов («заведи issue» вне committed-кода репозитория, прямой ручной `gh issue create`) не покрыт никаким механизмом — только соглашением (`docs/agents/PROTOCOL.md`: `scripts/gh/issue-create` — единственный документированный путь); статический скан «сырой `gh issue create` вне обёртки» рассмотрен и сознательно не заведён по той же причине, что и класс #496 (см. `repo-ci.yml`), — подстрока легитимно встречается в комментариях/доках |
| `area:process` | Ручной `gh issue edit --add-label area:process` (конвенция агентов, `docs/agents/PROTOCOL.md`, раздел «Зависимости»); дропдаун «Площадь» ([`.github/ISSUE_TEMPLATE/task.yml:32`](../../.github/ISSUE_TEMPLATE/task.yml)) только пишет выбор в ТЕЛО issue прозой (`### Площадь\n\n<выбор>`) — issue forms не конвертируют значения полей в labels сами (находка AI-ревью PR #387; см. `scripts/lib/collect_labels.py` докстринг) | Проставляется на задачу, которая **про сам процесс работы**, а не про продуктовую фичу харнеса. Проверяемый критерий (не «на глаз», задача #361): задача меняет правила/механику совместной работы — пул задач, приоритет, аренда/claim, ветка/task-branch, контракт «PR ↔ задача», ревью-гейты (`review:*`/`ai:*`), приёмка после мержа, CI required checks, протокол (`docs/agents/*`, `AGENTS.md`), реестр меток. НЕ мета: продуктовые фичи харнеса (морда, плагины, провайдеры, автоматизации-как-конечный-функционал), даже если код физически лежит в `scripts/orchestra/` (пример: механика слияния PR — мета; фича «инбокс владельца» — не мета, хоть и в том же каталоге). Не исключает другие `area:*` — задача может быть одновременно `area:orchestra` (площадь кода) и `area:process` (природа задачи) | Пометка классификации: `scripts/lib/free_task.py::issue_priority_key` читает её как уровень 1 приоритета выбора свободной задачи (#361) — задачи с этой меткой выбираются раньше остальных независимо от графа блокировок | Не снимается — живёт до закрытия issue; переклассификация (ошибочно проставлена/пропущена) — вручную, тем, кто это заметил | Условия нет — метка классификации/приоритета, не тормоз конвейера |
| `area:worker` | Ручной `gh issue edit --add-label area:worker` (та же конвенция, что `area:process` выше); дропдаун «Площадь» ([`.github/ISSUE_TEMPLATE/task.yml:28`](../../.github/ISSUE_TEMPLATE/task.yml)) только пишет выбор в тело прозой, метку не ставит; [`scripts/orchestra/upstream_drift.py::create_bump_issue`](../../scripts/orchestra/upstream_drift.py) (#505: авто-бамп пина апстрима — отдельный от формы путь, ставит метку кодом напрямую при заведении issue, дефект формы его не касается) | Задача по продуктовой площади «автономный воркер» — классификация зоны кода, не природы (см. `area:process` выше — ортогональное измерение, не взаимоисключающее) | Пометка классификации — ни один гейт слияния/приоритета её не читает | Не снимается — живёт до закрытия issue | Условия нет — метка классификации, не тормоз конвейера |
| `area:hands` | Ручной `gh issue edit --add-label area:hands` (та же конвенция); дропдаун «Площадь» ([`.github/ISSUE_TEMPLATE/task.yml:29`](../../.github/ISSUE_TEMPLATE/task.yml)) только пишет выбор в тело прозой, метку не ставит | Задача по продуктовой площади «руки/раннер» — классификация зоны кода, не природы | Пометка классификации — ни один гейт слияния/приоритета её не читает | Не снимается — живёт до закрытия issue | Условия нет — метка классификации, не тормоз конвейера |
| `area:docs` | Ручной `gh issue edit --add-label area:docs` (та же конвенция); дропдаун «Площадь» ([`.github/ISSUE_TEMPLATE/task.yml:30`](../../.github/ISSUE_TEMPLATE/task.yml)) только пишет выбор в тело прозой, метку не ставит | Задача по документации — классификация зоны, не природы | Пометка классификации — ни один гейт слияния/приоритета её не читает | Не снимается — живёт до закрытия issue | Условия нет — метка классификации, не тормоз конвейера |
| `area:orchestra` | Ручной `gh issue edit --add-label area:orchestra` (та же конвенция); дропдаун «Площадь» ([`.github/ISSUE_TEMPLATE/task.yml:31`](../../.github/ISSUE_TEMPLATE/task.yml)) только пишет выбор в тело прозой, метку не ставит | Задача по продуктовой площади «оркестратор» — классификация зоны кода; не путать с `area:process` (природа задачи — про сам процесс работы, а не про зону кода: одна и та же задача может нести обе метки) | Пометка классификации — ни один гейт слияния/приоритета её не читает | Не снимается — живёт до закрытия issue | Условия нет — метка классификации, не тормоз конвейера |
| `white-spot` | [`.github/ISSUE_TEMPLATE/white-spot.yml:3`](../../.github/ISSUE_TEMPLATE/white-spot.yml) | Найдено противоречие/белое пятно, оформлено issue-шаблоном (шаблон с задачи #179 сразу ставит и `task`, чтобы issue попал в пул воркера) | Частичный гейт: `repo-ci.yml` (`test`, шаг «Белые пятна без метки task», класс #179) красит обязательную проверку, если открытый white-spot остался без `task` — сирота вручную помечен без `task`, либо `task` сняли, не тронув `white-spot` | Газ гейта: `gh issue edit <N> --add-label task` (или закрытие issue) — следующий прогон `repo-ci` перечитывает список сирот сам; отдельно — вручную владелец/агент закрывает issue после решения самого противоречия | Гейт: 0 открытых white-spot без task; закрытие issue — осознанно ручное, автогейта на это нет |
| `blocked` | [`scripts/worker/task.sh:250`](../../scripts/worker/task.sh) (эскалация playbook: `gh issue edit $number --add-label blocked`) | Воркер упёрся в то, что есть только у владельца (секрет, доступ, деньги, необратимое действие) — законный исход запуска, PR не открывается | Блокирует автоматический повтор тремя путями: `scripts/worker/task.sh` (находка ревью PR #367: адрес дрейфует при каждой правке task.sh — искать по якорю `select(.name == "blocked")`, не по номеру строки) читает метку, снимает аренду задачи (#121) и завершает job зелёным без попытки взять задачу заново; `scripts/orchestra/scheduler.py::reap_stale`/`unhealthy_pulls` (`BLOCKED_LABEL`, #245) пропускают такую задачу по назначению (замок #121 уже снят, но assignee остаётся) — иначе снятие исполнителя по таймеру возвращало бы её в пул и `oldest_free` выбирал бы ту же самую задачу как старейшую, вечный цикл без газа | Вручную: владелец, по сигналу — комментарий воркера в issue с описанием, что именно нужно; владелец снимает метку и даёт запрошенное. Сигнал усилен #334/#336 (`scripts/orchestra/stale_blocked_guard.py`): если эскалация называет причину маркером «Блокирована: #N» и #N уже закрыт, гвардия сама эскалирует находку тем же каналом, что пульс (`pulse_guard.escalate`) — комментарий прямо в этот issue + Telegram, один раз на эпизод (маркер «[протухшая блокировка: #N]», повтор не шлёт); шаг оркестратора остаётся видимым красным при `continue-on-error` тоже — снятие метки всё равно ручное, гвардия только доводит сигнал и делает молчание видимым | Осознанно ручной (задача #207, замер: неизвестно, сколько это висит — не измерялось до сплошного прочёса; живой пример протухания — #268, #334) |
| `waiting:owner` | Человек, заводящий/разбирающий задачу (`gh issue edit N --add-label waiting:owner`) — вручную; либо [`scripts/orchestra/waiting_owner_guard.py:find_candidates_for_auto_label`](../../scripts/orchestra/waiting_owner_guard.py) — автоматически, если тело несёт машиночитаемый блок «## Варианты владельца» (формат — [`docs/agents/PROTOCOL.md`](PROTOCOL.md#решение-владельца-как-артефакт-254470)) | Критерий задачи требует явного выбора владельца между названными вариантами, не молчаливого дефолта (живой случай #370, слайс #254, задача #470) | Тормоз для воркера — два места, оба обязательны (находка AI-ревью PR #471): [`scripts/lib/claim_task.py::claim`](../../scripts/lib/claim_task.py) отказывает в аренде тем же путём, что и `blocked`; [`scripts/lib/free_task.py::free_candidates`](../../scripts/lib/free_task.py) исключает такую задачу из «свободных» ДО claim — без этого фильтра задача без исполнителя, но с `waiting:owner`, выбиралась бы `oldest_free` как старейшая свободная на каждом 15-минутном пульсе, получала отказ claim и зелёный no-op, а задачи ЗА ней в очереди не брались бы вовсе (класс #255, тормоз одной задачи стопорил бы весь диспатч) | Автоматический: `waiting_owner_guard.py` снимает метку и оставляет подтверждение, как только находит в теле/комментариях задачи маркер «РЕШЕНИЕ: N» первой строкой — тот же приём, что у `blocked`+#334 (`current_stale_marker_target`), только маркер решает, а не протухания. Эскалация до ответа — тем же каналом, что `blocked`/`upstream_drift` (`pulse_guard.escalate`): комментарий в саму задачу + Telegram, один раз на эпизод (маркер «[решение владельца: запрошено]») | Повтор эскалации не раньше 24 ч без ответа — тот же порог, что `STALE_HOURS` (#427), второй порог правды не заводится; кнопки Telegram (#254, полный протокол) РЕАЛИЗОВАНЫ (PR #486): вебхук с авторизацией `callback_query` подключён, машиночитаемый блок вариантов с подряд идущей нумерацией — эскалация уходит с инлайн-кнопками (`variant_option_labels` → `escalate(options=...)`), иначе обычный текстовый алерт, как раньше |
| `stale-unclaimed` | [`scripts/orchestra/scheduler.py:mark_stale_unclaimed`](../../scripts/orchestra/scheduler.py) (`STALE_UNCLAIMED_LABEL`, #427) | Задача пула без исполнителя старше `STALE_HOURS` (24ч — тот же порог, что у `reap_stale`, второй порог правды не заводится); возраст считается от `created_at` issue, используя `pool`, уже прочитанный этим же прогоном `main()` — новый обход Issues не заводится | Не тормоз: ничего не закрывается и не переназначается автоматически — метка только видимость факта «никто не взял» (замер 2026-09-06: 84 из 109 открытых без исполнителя) | Автоматический по одной стороне: `mark_stale_unclaimed` сама снимает метку следующим прогоном, как только у задачи появляется исполнитель (`issue["assignees"]` непуст). Само решение «взять / переформулировать / закрыть как отпавшую» — ручное: владелец или агент, наткнувшийся на метку; у машины нет признака «нужное или отпавшее», это не изображается автоматикой | Условие снятия: назначение появилось. Условие постановки — возраст ≥ STALE_HOURS без исполнителя и без `blocked` (уже сигнализирует владельцу отдельно, дублировать не нужно) |
| `orchestra:skip` | Человек, на PR — ручная постановка автором, обходящим контракт пула для мелочи | PR не связан с задачей пула (правка вне Issues) | Не тормоз, а обход тормоза: `scripts/orchestra/contract_check.py:76-78` пропускает проверку «PR ↔ задача» целиком | Сам является газом — не требует отдельного снятия; злоупотребление им видно в истории PR (`docs/agents/PROTOCOL.md:96-97`) | Осознанно ручной по построению |
| `conflict` | [`scripts/orchestra/scheduler.py:213-260`](../../scripts/orchestra/scheduler.py) (`mark_conflicts`/`_set_conflict_label`, ставится когда `mergeable_state == "dirty"`) | PR перестал сливаться в main без ручного разрешения | Пометка, не отдельная блокировка: реальный тормоз слияния — `mergeable_state` (`scheduler.py:421`, `merge_queue` пропускает PR не в `clean/unstable/has_hooks`), метка — видимый сигнал автору; сама метка вдобавок газ ко второму тормозу (#252) — делает PR кандидатом на серверное подтягивание ветки (`review_labels.should_update_branch`) | Автоматический: `mark_conflicts` (#270) сама же снимает метку следующим прогоном, когда `mergeable_state` явно подтверждён неконфликтным — один из `review_labels.CONFLICT_CLEAR_STATES` (`clean`/`unstable`/`has_hooks`/`behind`/`blocked`). `None`/`unknown` (вычисление ещё не завершилось) метку не снимают — снятие по ним прятало бы реальный конфликт. Снятие/постановка идут через `_set_conflict_label` — единственное место, которое сразу правит и сервер, и `pull["labels"]` в объекте, который main() передаёт дальше по прогону (класс «устаревшая метка в памяти», #252, второй заход). **С #474 сам ребейз тоже не ручной**: `dispatch_conflict_rework` снимает assignee+замок задачи и запускает `worker.yml` адресно (вход `task`) на авто-ребейз — одна попытка на PR (лифтайм-бюджет), не сошлось — эскалация владельцу с файлами-кандидатами (`pulse_guard.escalate`) | Авто-ребейз воркером (или, до #474/если бюджет исчерпан, — вручную автором) меняет `mergeable_state` на одно из `CONFLICT_CLEAR_STATES` (обычно `clean`) — следующий прогон `mark_conflicts` снимает метку без дополнительного ручного вмешательства |
| `contract:failed` | [`scripts/orchestra/contract_check.py:52-56`](../../scripts/orchestra/contract_check.py) (`fail()`) | Контракт «PR ↔ задача из пула» нарушен (нет ссылки на задачу, задача чужая/закрыта, второй PR на ту же задачу) | Метка сигнальная — сама проверка контракта является required-статусом защиты ветки, блокирует слияние своим состоянием, не меткой | Автоматический: [`contract_check.py:157-160`](../../scripts/orchestra/contract_check.py) снимает метку в начале каждого следующего успешного прогона (новый пуш переисполняет контракт) | Условие: следующий прогон контракта на том же PR проходит без нарушений |
| `review:ok` | [`scripts/review/check_pr.py::verdict_for`](../../scripts/review/check_pr.py) через `scripts/lib/review_labels.py::REVIEW_OK` | Детерминированное ревью не нашло замечаний и дифф не крупный (или крупный, но принят `review:large-ok`) | Часть гейта слияния: `scheduler.py:437` (`merge_label_gate`) требует `review:ok` И `ai:ok` одновременно | Автоматический: следующий пуш перезапускает `check_pr.py`, который снимает и переставляет вердикт (блок вердикта `check_pr.py::main`) | Условие: новый прогон ревью проходит без находок |
| `review:changes-requested` | [`scripts/review/check_pr.py::verdict_for`](../../scripts/review/check_pr.py) через `REVIEW_CHANGES` | Ревью нашло замечания (секрет в диффе, конфликт-маркер, файл секретов, не компилируется, удаляет запись прод-манифеста или файл патч-серии без метки `revert-ok` — #217) | Не входит в `merge_label_gate` напрямую — блокирует по факту отсутствия `review:ok` на этом PR | Автоматический: новый пуш переисполняет ревью и переставляет вердикт | Условие: находки устранены, следующий прогон чист |
| `review:large` | [`scripts/review/check_pr.py::verdict_for`](../../scripts/review/check_pr.py) через `REVIEW_LARGE` | Дифф больше `LARGE_DIFF_LINES` (800 строк) и `review:large-ok` ещё не выставлена (`check_pr.py::size_gate`) | Блокирует слияние тем же путём, что `review:changes-requested` — `review:ok` не выставляется, пока размер не принят | См. `review:large-ok` ниже — это единственный путь снятия | Порог: `LARGE_DIFF_LINES = 800` строк добавлений (`check_pr.py::LARGE_DIFF_LINES`) |
| `review:large-ok` | Автоматика: [`scripts/review/ai_review.py::apply_large_ok`](../../scripts/review/ai_review.py) ставит метку после вердикта `approve` на том же head (#204); вручную — владельцем для диффов за `LARGE_DIFF_HUGE_LINES` | Размер дифф-гейта принят осознанно: взгляд делегирован AI-ревью, решение — `ai_review.py::large_ok_decision` | Обходит `review:large`: `check_pr.py::size_gate` — при наличии метки verdict становится `review:ok` | Автоматический: новое AI-ревью на актуальном head повторяет вердикт и подтверждает размер; 2000+ строк (`LARGE_DIFF_HUGE_LINES`) автоматика не подтверждает — эскалация владельцу (`ai_review.py::huge_diff_escalation_text`); сама метка и есть газ, снимать нечего. **Найдено 2026-09-06 (#204, второй заход)**: до этой даты автоматика была недостижима — шаг `facts` в [`ai-review.yml:145`](../../.github/workflows/ai-review.yml) открывал дорогой прогон (`go=true`) только по метке `review:ok`, а `review:large` эту метку по построению исключает (`check_pr.py::verdict_for` ставит либо `review:ok`, либо `review:large`, никогда обе) — AI-ревью НИКОГДА не запускалось на PR с `review:large`, и `apply_large_ok` не имел ни единого триггера с момента своего мержа. Живой замер: PR #388/#387/#384/#367/#262 простояли с `review:large` без единого `ai:*`-события в таймлайне; `review:large-ok` на них появилась вручную, в обход мёртвой автоматики. Починено тем же файлом: гейт `facts` теперь открывается по `review:ok` ИЛИ `review:large`; гвардия — [`scripts/review/test_ai_review_gate.py`](../../scripts/review/test_ai_review_gate.py), исполняет реальный bash шага `facts`, не пересказ | Условие: вердикт AI `approve` на том же head, где висит `review:large` (#204; до #204 замер задачи #207 — #167 и #173 висели больше суток с ручным путём) |
| `revert-ok` | Исполнитель PR осознанно: `gh pr edit <N> --add-label revert-ok`; значение — единственное место правды [`scripts/lib/review_labels.py:63`](../../scripts/lib/review_labels.py) (`REVERT_OK`), читает [`scripts/review/check_pr.py:285`](../../scripts/review/check_pr.py) (`revert_ok_gas`) | Дифф PR удаляет запись прод-манифеста (`dsh-edge/plugins.json`, `dsh-edge/integrations.json` — список в `check_pr.py::PROD_MANIFESTS`) или файл патч-серии (`dsh-edge/patches/`), и это удаление входит в ЗАМЫСЕЛ PR, а не является молчаливым откатом main (#217, урок PR #164) | Без метки гвардия отката в `check_pr.py::revert_guard` даёт находку — вердикт `review:changes-requested`, красный статус `harness/review`, гейт слияния закрыт; находка называет конкретную удалённую запись | Сам является газом гвардии отката: ставится исполнителем ВМЕСТЕ с объяснением в теле PR — обе части проверяет `revert_ok_gas` (метка без упоминания revert-ok в теле находку не снимает); снимать нечего — следующий дифф без удаляющих правок находки не даёт, принятый обход гейт публикует комментарием в PR | Удаление принято, только когда метка стоит И тело PR упоминает revert-ok; честный потолок: общий случай «значение откачено к базе» (класс #100 в #164: `inject: ['tools']` → `[]`) гвардией НЕ покрыт — нужен merge-base, которого у доверенного чекаута main нет (названо в #217) |
| `ai:ok` | [`scripts/review/ai_review.py::cmd_verdict`](../../scripts/review/ai_review.py) через `review_labels.AI_OK` | AI-ревью вернуло вердикт `approve` на head, где ревью реально проведено, ИЛИ метка сохранена на более позднем head, чей дифф относительно base не изменился (`review_labels.py::diff_unchanged`) — метка может дожить до head, который сам никто не ревьюил (чистый merge-коммит подтягивания main, #252/#294) | Часть гейта слияния — `merge_label_gate` требует его вместе с `review:ok` | Автоматический, условие уточнено #252 и #294: триггер `ai-review.yml` — не метка, а `workflow_run` от завершения `pr-review` (события `GITHUB_TOKEN` меток не создают, антирекурсия GitHub), поэтому сохранение метки в `check_pr.py` само по себе прогон не останавливает. Отпечаток диффа PR (`review_labels.diff_fingerprint`) сверяется ДВАЖДЫ, но одним и тем же местом правды: `check_pr.py::ai_verdict_keep` решает, снимать ли метку, `ai_review.py::cmd_should_run` (шаг `fingerprint` в `ai-review.yml`, читает `review_labels.should_run_ai_review`) решает, идти ли в дорогой прогон вовсе. Дифф ИЗМЕНИЛСЯ → метка снята, `go=true`, свежий вердикт. Дифф НЕ изменился (подтягивание main без конфликтов меняет только head) → метка сохраняется И `go=false` — job `review` останавливается до чекаута/gather/DSH | Условие: новое AI-ревью на новом head с ИЗМЕНИВШИМСЯ диффом отвечает `approve`; при неизменном диффе прежний `approve` остаётся в силе, дорогой прогон не запускается |
| `ai:changes-requested` | [`scripts/review/ai_review.py::cmd_verdict`](../../scripts/review/ai_review.py) через `review_labels.AI_CHANGES` | AI-ревью вернуло вердикт `rework` | Блокирует по факту отсутствия `ai:ok` | Тот же путь, что `ai:ok` (#252, #294): пуш с ИЗМЕНИВШИМСЯ диффом снимает метку и запускает дорогой прогон (`go=true`); неизменный дифф (подтягивание main) метку сохраняет И не запускает прогон (`go=false`, `review_labels.should_run_ai_review`) | Условие: замечания устранены, новое ревью на новом head с изменившимся диффом |
| `ai:failed` | [`scripts/review/ai_review.py::cmd_verdict`](../../scripts/review/ai_review.py) через `review_labels.AI_FAILED` | Ответ модели не соответствует контракту вердикта (нет строки `ВЕРДИКТ:`, их больше одной, транспорт DSH упал) | Блокирует по факту отсутствия `ai:ok`; замер задачи #207: 84% прогонов этого гейта — ручной повторный запуск, закрывается #196 | Пуш с изменившимся диффом снимает метку и перезапускает ревью как обычно (#252). Пуш БЕЗ изменения диффа (подтягивание main) ТОЖЕ перезапускает прогон: `review_labels.should_run_ai_review` пропускает дорогой прогон по совпавшему отпечатку только для `ai:ok`/`ai:changes-requested` — `ai:failed` из этого пропуска исключён по построению и НИКОГДА не отвечает `false`, поэтому новый пуш всегда запускает `ai-review.yml` заново, даже если дифф не менялся. Таймер #196 ([`scripts/orchestra/scheduler.py::trigger_ai_review`](../../scripts/orchestra/scheduler.py)), который перезапускает `ai-review.yml` ПО ТАЙМЕРУ (`AI_REVIEW_RETRY_AFTER_MINUTES` с момента последней перестановки `review:ok`) — ОТДЕЛЬНЫЙ, независимый путь автоповтора, а не компенсация за пропуск по неизменному диффу: такого пропуска для `ai:failed` не существует вовсе, оба пути (новый пуш и таймер) запускают прогон | Условие: новый прогон на изменившемся диффе отвечает по контракту, ИЛИ таймер #196 запускает ручной `workflow_dispatch` повтор на том же диффе. До #196 автоповтора без нового пуша не было — это и есть находка задачи #196, не #207 |
| `update-available` | [`scripts/orchestra/upstream_drift.py`](../../scripts/orchestra/upstream_drift.py) (`_set_drift_label`, ставится вместе с маркер-комментарием `[дрейф пина: …]` в #134) | Релиз апстрима `pawaca/dsh-edge` семверно новее пина в `dsh-edge/upstream.json` (или пин вообще не на теге релиза) — морда отстаёт, нужен бамп пина (#134) | Пометка, не блокировка: ни один гейт слияния её не читает; это сигнал владельцу и агентам, что пора делать бамп (ручной PR с полным прогоном деплоя, разбор чейнджлога — #161) | Автоматический: тот же прогон пульса снимает метку (`_drop_drift_label`), как только пин снова стоит на новейшем стабильном релизе — бамп смержен, метка снята без ручного действия. Известный размен: метку, снятую вручную ВО ВРЕМЯ активного дрейфа, вернёт только новый сигнал (следующий релиз) — мутирующий вызов на каждый пульс ради этого не делается | Условие: сверка `decide_drift` отвечает `ok` — пин на новейшем стабильном теге апстрима |
| `source:inbox` | [`cf-worker/src/harness.ts`](../../cf-worker/src/harness.ts) (`#createIssue`, инбокс владельца #20) | Директива владельца из инбокса превращена в задачу пула автоматически | Пометка, не блокировка: происхождение задачи (пульт владельца), ни один гейт её не читает | Не снимается — живёт до закрытия issue (закрывает исполнитель после пост-мерж проверки, `docs/agents/PROTOCOL.md:53-55`) | Условия нет — метка происхождения, не тормоз |
| `auto-detected` | [`scripts/orchestra/stall_detector.py`](../../scripts/orchestra/stall_detector.py) (`create_task`); [`scripts/orchestra/scheduler.py`](../../scripts/orchestra/scheduler.py) (`replace_closed_task_prs`/`_create_task_replacement`, #543 — второй, независимый сеятель той же метки) | Отпечаток причины простоя (красный обязательный чек, пауза предохранителя, просроченная задача и т.п.) продержался устойчиво дольше `STALL_PERSIST_MINUTES` и не совпал ни с одной уже открытой автозадачей (#201); либо (второй сеятель, #543) открытый PR, чья ветка резолвится (`task_ref.resolve_pr_task`) на уже ЗАКРЫТУЮ задачу, и Search API не нашёл готовой замены по конвенции `Related: #<старая>` | Пометка происхождения, сама метка не блокирует; тормоз — не метка, а неограниченное автозаведение задач. У `stall_detector.py` — дедупликация `find_open_task` (совпадение по отпечатку) и суточный потолок `auto_tasks_created_since`/`STALL_DAILY_CAP` (превышение не тонет молча — строка отчёта «потолок исчерпан», нужен человек). У `replace_closed_task_prs` — СВОЙ, не общий с потолком выше механизм: дедупликация на ДВУХ уровнях — маркер `TASK_REPLACEMENT_MARKER` в уже прочитанном теле PR (без сети) и поиск существующей замены по `Related: #<старая>` через Search API (ловит и ручные замены владельца) — потолка на число заведений в сутки у этого сеятеля нет, ограничитель естественный: одна замена на один открытый PR, число открытых PR ограничено конвейером (WIP-лимит, п.15 докстринга `scheduler.py`) | Не снимается — живёт до закрытия issue; закрывает воркер/владелец после устранения причины. Автозадача, зависшая дольше `ESCALATE_AFTER_HOURS`, эскалируется владельцу автоматически ровно один раз — `escalate_stale_auto_tasks` (issue-комментарий + Telegram, тот же канал, что `pulse_guard.escalate`) — актуально только для сеятеля `stall_detector.py`, у `replace_closed_task_prs` своей эскалации по возрасту нет (задача-замена в остальном обычная задача пула — её видит `mark_stale_unclaimed`/#427, если её никто не взял) | `STALL_PERSIST_MINUTES = 30` мин до заведения, `STALL_DAILY_CAP = 5` задач/сутки, `ESCALATE_AFTER_HOURS = 48` ч до эскалации владельцу (константы объявлены в начале `stall_detector.py` — голые имена, номера строк здесь намеренно не указаны: реестр уже дважды протухал именно на них, #207/находка ревью PR #248). У `replace_closed_task_prs` порогов нет — условие заведения бинарное (ветка называет закрытую задачу И замены ещё не существует) |
| `ci-failure` | [`scripts/orchestra/pulse_guard.py::failure_watch`](../../scripts/orchestra/pulse_guard.py) (#477, ставится вместе с `task` при заведении задачи по классифицированному дефекту CI) | Провал ключевого workflow (`worker.yml`/`hands.yml`/`orchestra.yml`/`deploy-*.yml`) классифицирован как наш дефект (не инфраструктура/не устаревшая база) и этого класса причины (отпечаток workflow+job+нормализованная строка ошибки) ещё нет среди открытых issues с этой меткой | Пометка, не блокировка: используется только для дедупа (`open_ci_failure_fingerprints`) — второй тормоз конвейера не заводит, ни один гейт слияния её не читает. **С #552** та же метка — критерий выбора для [`scripts/orchestra/scheduler.py::dispatch_defect_rework`](../../scripts/orchestra/scheduler.py): открытая и без исполнителя (и без `blocked`) задача с этой меткой получает адресный `workflow_dispatch worker.yml` немедленно, минуя очередь пула и WIP-лимит (#464) целиком | Не снимается — живёт до закрытия issue как обычной задачи пула (стадия приёмки оркестратора или исполнитель вручную); отдельного снятия нет — метка нужна только чтобы не завести вторую задачу того же класса. Газ дефект-диспетча (#552): ровно одна авто-попытка (маркер-комментарий на задаче) — не сошлось, задача всё ещё открыта и без исполнителя — эскалация владельцу (#120 + Telegram), дальше решение ручное | Условия нет для самой метки (классификация/дедуп, не тормоз конвейера); для дефект-диспетча — `DEFECT_DISPATCH_MAX_ATTEMPTS` (1) попытка на задачу, `pulse_guard.py` |

## Связанные документы

- [`AGENTS.md`](../../AGENTS.md) — правило «Тормоз без газа не принимается».
- [`docs/agents/PROTOCOL.md`](PROTOCOL.md) — роли, пул задач, контракт PR.
- [`scripts/lib/review_labels.py`](../../scripts/lib/review_labels.py) — место
  правды значений `review:*`/`ai:*` и формулировка гейта слияния.
